---
phase: 21-tileset-ldtk-pipeline
plan: 02
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - src/level/map.py
autonomous: false
requirements: [LDTK-02, LDTK-03]

must_haves:
  truths:
    - "map.py uses TILE_SIZE (16) instead of hardcoded 8 for all grid math"
    - "map.py uses 16 tiles per row for tile ID decomposition instead of 32"
    - "autoLayerTiles load and render at correct positions with no gaps"
  artifacts:
    - path: "src/level/map.py"
      provides: "Tile loading with 16px grid math"
      contains: "grid_size = TILE_SIZE"
  key_links:
    - from: "src/level/map.py"
      to: "src/core/constants.py"
      via: "TILE_SIZE import"
      pattern: "from.*constants.*import.*TILE_SIZE"
    - from: "src/level/map.py"
      to: "assets/output.ldtk"
      via: "load_autotiles_from_ldtk reads migrated JSON"
      pattern: "grid_size = TILE_SIZE"
---

<objective>
Update map.py hardcoded 8px values to use TILE_SIZE constant (16), fix tile ID decomposition for 16 tiles per row, and visually verify the game renders correctly with the migrated LDtk data.

Purpose: The migration script (Plan 01) converted all data files to 16px, but the game code still has hardcoded `8` and `32` values. Without these code fixes, tiles load at wrong positions.
Output: Working tile rendering with no visual gaps or misalignment.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-tileset-ldtk-pipeline/21-CONTEXT.md
@.planning/phases/21-tileset-ldtk-pipeline/21-01-SUMMARY.md

<interfaces>
<!-- Key constants used by map.py -->

From src/core/constants.py:
```python
TILE_SIZE = 16
TILE_EMPTY = (15, 15)
VIEWPORT_W = 320
VIEWPORT_H = 176
```

From src/level/map.py current hardcoded values to replace:
```python
# Line 71: origin_x = (min_wx // 8) * 8
# Line 72: origin_y = (min_wy // 8) * 8
# Line 86: base_tx, base_ty = world_x // 8, world_y // 8
# Line 147: (v % 32, v // 32)  -- tile ID to (col, row) with 32 tiles/row
# Line 181: grid_size = 8
# Line 465: u = real_id % 32
# Line 466: v = real_id // 32
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update map.py hardcoded grid values to TILE_SIZE</name>
  <files>src/level/map.py</files>
  <read_first>
    - src/level/map.py (full file -- verify all hardcoded 8 and 32 locations)
    - src/core/constants.py (confirm TILE_SIZE=16 is importable)
    - .planning/phases/21-tileset-ldtk-pipeline/21-RESEARCH.md (lines 117-127: hardcoded values table)
  </read_first>
  <action>
**Ensure TILE_SIZE is imported** at the top of map.py. Check if it's already imported from `src.core.constants`. If not, add it to the existing import.

**Replace hardcoded values** (per D-15, D-16):

1. **Line 71**: `origin_x = (min_wx // 8) * 8` -> `origin_x = (min_wx // TILE_SIZE) * TILE_SIZE`
2. **Line 72**: `origin_y = (min_wy // 8) * 8` -> `origin_y = (min_wy // TILE_SIZE) * TILE_SIZE`
3. **Line 86**: `base_tx, base_ty = world_x // 8, world_y // 8` -> `base_tx, base_ty = world_x // TILE_SIZE, world_y // TILE_SIZE`
4. **Line 147**: `(v % 32, v // 32)` -> `(v % TILES_PER_ROW, v // TILES_PER_ROW)` where `TILES_PER_ROW = 256 // TILE_SIZE` (= 16). Define `TILES_PER_ROW` as a module-level constant: `TILES_PER_ROW = 256 // TILE_SIZE  # tileset image is 256px wide`. Add a comment: `# 256px tileset / 16px tiles = 16 tiles per row`
5. **Line 181**: `grid_size = 8` -> `grid_size = TILE_SIZE`
6. **Line 465**: `u = real_id % 32` -> `u = real_id % TILES_PER_ROW`
7. **Line 466**: `v = real_id // 32` -> `v = real_id // TILES_PER_ROW`

Also update the origin_x/origin_y calculation in `load_autotiles_from_ldtk` (around lines 186-187):
- `origin_x = (min_wx // grid_size) * grid_size` -- this already uses `grid_size` variable, so it will be correct once line 181 is fixed.

Scan the entire file for any other hardcoded `// 8`, `* 8`, `% 32`, `// 32` that relate to tile grid math. Update any found.

Do NOT change `TILE_SIZE` itself or any other file -- only `src/level/map.py`.
  </action>
  <verify>
    <automated>python -c "
import ast, sys
with open('src/level/map.py') as f: src = f.read()
# No hardcoded grid_size = 8
assert 'grid_size = 8' not in src, 'Still has grid_size = 8'
# No hardcoded // 8 for tile math (allow in comments)
lines = src.split('\n')
for i, line in enumerate(lines):
    stripped = line.split('#')[0]  # ignore comments
    if '// 8' in stripped and 'TILE_SIZE' not in stripped:
        print(f'WARNING line {i+1}: {line.strip()}')
# No hardcoded % 32 or // 32
for i, line in enumerate(lines):
    stripped = line.split('#')[0]
    if ('% 32' in stripped or '// 32' in stripped):
        print(f'WARNING line {i+1}: {line.strip()}')
# Has TILE_SIZE usage
assert 'TILE_SIZE' in src, 'TILE_SIZE not found in map.py'
assert 'grid_size = TILE_SIZE' in src, 'grid_size not set to TILE_SIZE'
print('OK: map.py hardcoded values updated')
" && python -m pytest tests/test_tilemap.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - src/level/map.py contains "grid_size = TILE_SIZE" (not "grid_size = 8")
    - src/level/map.py contains "TILES_PER_ROW" constant definition
    - src/level/map.py does NOT contain "// 8" in non-comment code for tile math
    - src/level/map.py does NOT contain "% 32" or "// 32" in non-comment code
    - src/level/map.py imports TILE_SIZE from constants
    - python -m pytest tests/test_tilemap.py -x -q exits 0
  </acceptance_criteria>
  <done>All hardcoded 8px and 32-col values in map.py replaced with TILE_SIZE and TILES_PER_ROW constants. Tilemap tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of tile rendering</name>
  <what-built>
    Migration script converted output.ldtk and simplified exports from 8px to 16px grid. map.py updated to use TILE_SIZE=16 for all grid math. Tileset consolidated at tilesets/cavern.png.
  </what-built>
  <how-to-verify>
    1. Run the game: `python main.py`
    2. Verify the cave tilemap renders correctly:
       - No gaps between tiles (no black lines or missing tiles)
       - Tile edges/corners look correct (auto-tile variants aligned)
       - Player spawns at correct position on the map
    3. Walk through a few rooms to verify:
       - Collision aligns with visible tiles (player doesn't float or sink)
       - Door transitions work between rooms
       - No visual glitches at room boundaries
    4. If using Pyxel MCP: run `inspect_tilemap` on tilemap 0 to verify tile data is populated
    5. If using Pyxel MCP: run `inspect_screen` or `run_and_capture` to check rendered frame
  </how-to-verify>
  <resume-signal>Type "approved" if tiles render correctly, or describe any visual issues (gaps, misalignment, wrong tiles)</resume-signal>
</task>

</tasks>

<verification>
- `python -m pytest tests/ -x -q` -- full test suite green
- Visual inspection confirms tiles render at correct positions with no gaps
- Player collision aligns with visible tile boundaries
</verification>

<success_criteria>
- map.py uses TILE_SIZE constant for all grid math (no hardcoded 8 or 32)
- autoLayerTiles render at correct positions in-game
- No visual gaps or misalignment in the tilemap
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/21-tileset-ldtk-pipeline/21-02-SUMMARY.md`
</output>
