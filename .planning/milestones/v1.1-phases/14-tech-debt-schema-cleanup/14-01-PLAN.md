---
phase: 14-tech-debt-schema-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/entity-schema.json
  - src/entities/map_entities.py
  - src/level/map.py
  - main.py
autonomous: true
requirements: [MAP-02]

must_haves:
  truths:
    - "Door entity supports action='event' with an event_id field"
    - "Boss defeat sets event_flags['boss_defeated']=True instead of directly opening gates"
    - "Event-gated doors open on room entry when their event flag is set"
    - "close_gates legacy scan uses VIEWPORT_W/VIEWPORT_H-derived tile counts, not hardcoded 16"
    - "Switch-triggered gates still work (close_gates/open_gates methods intact)"
  artifacts:
    - path: "assets/entity-schema.json"
      provides: "Door entity with action='event' enum value and event_id field"
      contains: "event_id"
    - path: "src/entities/map_entities.py"
      provides: "Door class with action, event_id params and check_event_open method"
      exports: ["Door"]
    - path: "src/level/map.py"
      provides: "Fixed close_gates legacy scan using tiles_w/tiles_h"
      contains: "tiles_w"
    - path: "main.py"
      provides: "event_flags dict, boss sets flag on death, doors check on room entry"
      contains: "event_flags"
    - path: "tests/test_event_doors.py"
      provides: "Tests for event-gated door open/close behavior"
  key_links:
    - from: "main.py"
      to: "src/entities/map_entities.py"
      via: "Door.check_event_open(self.event_flags)"
      pattern: "check_event_open"
    - from: "main.py"
      to: "event_flags dict"
      via: "self.event_flags['boss_defeated'] = True on Mole death"
      pattern: "event_flags.*boss_defeated"
    - from: "assets/entity-schema.json"
      to: "src/entities/map_entities.py"
      via: "Door schema defines action enum + event_id field"
      pattern: "event"
---

<objective>
Implement the event-gated door system (D-01 through D-06) and fix the map.py legacy gate scan (D-18).

Purpose: Replace hardcoded tile ID 4 boss gates with a flexible event-flag system. Doors with action="event" check a game_state_flags dict for their event_id on room entry. This satisfies MAP-02 (room layout with event-gated doors).

Output: Updated entity schema, Door class with event support, main.py event_flags integration, fixed close_gates scan, tests for event doors.
</objective>

<execution_context>
@C:\Users\garva\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\garva\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-tech-debt-schema-cleanup/14-CONTEXT.md
@.planning/phases/14-tech-debt-schema-cleanup/14-RESEARCH.md

<interfaces>
<!-- Current Door class (map_entities.py) - being extended -->
class Door:
    def __init__(self, x, y, target_level_id=None, direction="right"):
    def open(self):
    def close(self):
    def update(self):
    def check_collision(self, x, y, w, h):
    def check_kick_hit(self, kick_x, kick_y, kick_w=8, kick_h=8):
    def check_projectile_hit(self, proj_x, proj_y, proj_w, proj_h):
    def draw(self):

<!-- main.py boss fight integration (lines 212-214, 378-381) -->
# Boss spawn: self.level_map.close_gates(self.cam_x, self.cam_y)
# Boss death: self.level_map.open_gates(self.cam_x, self.cam_y); self.game_state = "WON"
# IMPORTANT: self.game_state is a string "PLAYING"/"WON" -- do NOT reuse for event flags

<!-- map.py close_gates legacy scan (lines 196-201) -->
# Lines 197-198: hardcoded `+ 16` should be `+ tiles_w` and `+ tiles_h`
# tiles_w, tiles_h already computed on line 187
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Entity schema update + Door class event support + event door tests</name>
  <files>assets/entity-schema.json, src/entities/map_entities.py, tests/test_event_doors.py</files>
  <read_first>
    - assets/entity-schema.json (current schema v0.2.0, Door entity custom_fields)
    - src/entities/map_entities.py (current Door class, 80 lines)
  </read_first>
  <behavior>
    - Test 1: Door with action="event", event_id="boss_defeated" starts closed (is_open=False)
    - Test 2: Door.check_event_open({"boss_defeated": True}) opens the door (is_open=True)
    - Test 3: Door.check_event_open({"boss_defeated": False}) keeps door closed
    - Test 4: Door.check_event_open({}) keeps door closed (missing key = not triggered)
    - Test 5: Door with action="spit" ignores check_event_open (non-event doors unaffected)
    - Test 6: Door with action=None ignores check_event_open
  </behavior>
  <action>
    1. In assets/entity-schema.json Door.custom_fields.action.enum, add "event" to the end of the array:
       Change: ["spit", "kick", "drill_dive", "slime_ram", "slime_boost"]
       To:     ["spit", "kick", "drill_dive", "slime_ram", "slime_boost", "event"]
       Also update action.description to: "Ability required to open the door. 'event' checks event_flags for event_id."

    2. In assets/entity-schema.json Door.custom_fields, add new field after "hp":
       "event_id": {
         "type": "string",
         "required": false,
         "default": null,
         "description": "For action='event': the event_flags key to check (e.g. 'boss_defeated')."
       }

    3. In src/entities/map_entities.py Door.__init__, add action=None and event_id=None parameters:
       def __init__(self, x, y, target_level_id=None, direction="right", action=None, event_id=None):
       Add: self.action = action
       Add: self.event_id = event_id

    4. In src/entities/map_entities.py Door, add method check_event_open:
       def check_event_open(self, event_flags):
           """For action='event' doors: open if event flag is set. Per D-03, D-04."""
           if self.action == "event" and self.event_id:
               if event_flags.get(self.event_id, False):
                   self.open()

    5. Create tests/test_event_doors.py with 6 tests per behavior spec above.
       Import Door from src.entities.map_entities. No pyxel dependency needed.
  </action>
  <verify>
    <automated>python -m pytest tests/test_event_doors.py -x --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - entity-schema.json Door action enum contains "event" as last element
    - entity-schema.json Door custom_fields contains "event_id" key with type "string"
    - map_entities.py Door.__init__ signature contains "action=None, event_id=None"
    - map_entities.py contains "def check_event_open(self, event_flags):"
    - map_entities.py contains "event_flags.get(self.event_id, False)"
    - tests/test_event_doors.py exists and contains at least 6 test functions
    - python -m pytest tests/test_event_doors.py exits 0
  </acceptance_criteria>
  <done>Door entity schema updated with action="event" + event_id field; Door class has check_event_open method; all 6 event door tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: main.py event_flags integration + close_gates legacy scan fix</name>
  <files>main.py, src/level/map.py</files>
  <read_first>
    - main.py (full file -- need boss spawn ~line 212, boss death ~line 378-381, game_state usage, door spawning in _load_room)
    - src/level/map.py (lines 184-201 -- close_gates method, the legacy scan at lines 196-201)
    - src/core/constants.py (VIEWPORT_W=320, VIEWPORT_H=176, TILE_SIZE=8)
  </read_first>
  <action>
    1. In main.py Game.__init__, add event_flags dict (MUST NOT reuse self.game_state which is the "PLAYING"/"WON" string per Pitfall 1):
       self.event_flags = {}  # Dict[str, bool] for event-gated doors (D-03)

    2. In main.py boss death handler (~line 378-381), after open_gates call, also set event flag:
       Keep existing: self.level_map.open_gates(self.cam_x, self.cam_y)
       Add after:     self.event_flags["boss_defeated"] = True

    3. In main.py _load_room (where Door entities are spawned from LDtk data), pass action and event_id from entity customFields to Door constructor:
       Find the Door instantiation line and update to include:
       action=ent.get("customFields", {}).get("action"),
       event_id=ent.get("customFields", {}).get("event_id")

    4. In main.py room entry logic (after loading entities), add event door check:
       After doors are loaded for a room, iterate and check event flags:
       for door in self.doors:
           door.check_event_open(self.event_flags)
       This implements D-04 (doors re-check on every room entry).

    5. In src/level/map.py close_gates method, fix lines 197-198 (D-18):
       Change: for ty in range(ty_start, ty_start + 16):
                   for tx in range(tx_start, tx_start + 16):
       To:     for ty in range(ty_start, ty_start + tiles_h):
                   for tx in range(tx_start, tx_start + tiles_w):
       NOTE: tiles_w and tiles_h are already computed on line 187 as:
       tiles_w, tiles_h = VIEWPORT_W // TILE_SIZE, VIEWPORT_H // TILE_SIZE
       (= 40, 22 for 320x176 viewport with 8px tiles)

       REGRESSION NOTE (D-18): No dedicated test_gate_scan.py is created for this fix.
       This is a 2-line variable substitution (16 -> tiles_w/tiles_h) in an existing method.
       Verification is via grep acceptance criteria (no "+ 16" in close_gates) and the full
       test suite. The risk is low -- the variables tiles_w/tiles_h are already used in the
       primary scan loop 8 lines above.

    6. In main.py Game.reset(), also reset event_flags:
       self.event_flags = {}
  </action>
  <verify>
    <automated>python -m pytest tests/ -x --tb=short -k "not test_bubble_shield and not test_drill_retcon and not test_sprite_scale and not test_phase05"</automated>
  </verify>
  <acceptance_criteria>
    - main.py contains "self.event_flags = {}"
    - main.py contains 'self.event_flags["boss_defeated"] = True'
    - main.py contains "check_event_open(self.event_flags)"
    - main.py Door instantiation passes action= and event_id= keyword arguments
    - map.py close_gates contains "ty_start + tiles_h" (not "ty_start + 16")
    - map.py close_gates contains "tx_start + tiles_w" (not "tx_start + 16")
    - grep for "+ 16" in close_gates method returns 0 matches
  </acceptance_criteria>
  <done>Boss death sets event_flags["boss_defeated"]; event doors check flags on room entry; close_gates scans correct viewport tile range (40x22 not 16x16).</done>
</task>

</tasks>

<verification>
- python -m pytest tests/test_event_doors.py -x --tb=short (event door tests)
- grep -n "event" assets/entity-schema.json (schema has event action + event_id field)
- grep -n "event_flags" main.py (integration points exist)
- grep -n "tiles_w\|tiles_h\|+ 16" src/level/map.py (legacy scan fixed)
</verification>

<success_criteria>
- Event-gated door system functional: Door with action="event" opens when event flag is set
- Boss death sets event_flags["boss_defeated"] = True
- Doors re-check event flags on room entry (D-04)
- close_gates legacy scan uses tiles_w/tiles_h (40x22) not hardcoded 16x16
- Switch-triggered gates still work (open_gates/close_gates methods preserved)
- All event door tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/14-tech-debt-schema-cleanup/14-01-SUMMARY.md`
</output>
