---
phase: 12-screen-size-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/constants.py
  - src/level/world.py
  - src/level/map.py
  - src/entities/effects.py
  - src/entities/boss.py
  - src/entities/projectile.py
  - main.py
  - tests/test_world_manager.py
  - tests/test_screen_constants.py
autonomous: true
requirements:
  - D-01
  - D-02
  - D-05
  - D-08
  - D-09
  - D-10

must_haves:
  truths:
    - "All screen dimensions derive from named constants in constants.py"
    - "Camera clamps to VIEWPORT_W/VIEWPORT_H (320x176), not SCREEN_W/SCREEN_H (320x192)"
    - "No hardcoded 128 values remain in production Python files (src/ and main.py)"
    - "Entity culling boundaries use VIEWPORT_W/VIEWPORT_H constants"
    - "Tests pass with updated room dimensions"
  artifacts:
    - path: "src/core/constants.py"
      provides: "SCREEN_W, SCREEN_H, VIEWPORT_W, VIEWPORT_H, HUD_H constants"
      contains: "SCREEN_W = 320"
    - path: "src/level/world.py"
      provides: "WorldManager using VIEWPORT_W/VIEWPORT_H for camera math"
      contains: "VIEWPORT_W"
    - path: "tests/test_screen_constants.py"
      provides: "Constant consistency validation"
      contains: "SCREEN_H == VIEWPORT_H + HUD_H"
  key_links:
    - from: "src/core/constants.py"
      to: "src/level/world.py"
      via: "import VIEWPORT_W, VIEWPORT_H"
      pattern: "from src.core.constants import.*VIEWPORT"
    - from: "src/core/constants.py"
      to: "main.py"
      via: "import SCREEN_W, SCREEN_H, VIEWPORT_W, VIEWPORT_H"
      pattern: "from src.core.constants import.*SCREEN_W"
    - from: "src/core/constants.py"
      to: "src/entities/effects.py"
      via: "import VIEWPORT_W, VIEWPORT_H"
      pattern: "from src.core.constants import.*VIEWPORT"
---

<objective>
Define central screen constants and propagate them across the entire codebase, replacing all hardcoded 128 values with named constants. Update tests to match new dimensions.

Purpose: Establish the single source of truth for screen/viewport dimensions (D-08) and eliminate all magic numbers (D-09) so that subsequent plans (HUD, assets) build on correct foundations.
Output: All Python files use constants from constants.py; zero hardcoded 128 values remain; all tests pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-screen-size-expansion/12-CONTEXT.md
@.planning/phases/12-screen-size-expansion/12-RESEARCH.md

@src/core/constants.py
@src/level/world.py
@main.py

<interfaces>
<!-- Key constants and patterns the executor needs -->

From src/core/constants.py (current):
```python
TILE_SIZE = 8
# No screen constants exist yet -- they must be added
```

From src/level/world.py (current):
```python
class WorldManager:
    SCREEN_W = 128  # Must become VIEWPORT_W (320)
    SCREEN_H = 128  # Must become VIEWPORT_H (176)
```

From main.py (current):
```python
pyxel.init(128, 128, title="Jelly Roll Proto")  # Line 16
room_w, room_h = 128, 128                        # Line 123
rx, ry, rw, rh = self.cam_x, self.cam_y, 128, 128  # Line 195
16 < rel_x < 112 and 16 < rel_y < 112            # Line 262
self.slime.x > self.cam_x + 128                   # Line 314
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add screen constants to constants.py and create constants test</name>
  <files>src/core/constants.py, tests/test_screen_constants.py</files>
  <read_first>src/core/constants.py, tests/test_world_manager.py</read_first>
  <behavior>
    - Test: SCREEN_W == 320
    - Test: SCREEN_H == 192
    - Test: VIEWPORT_W == 320
    - Test: VIEWPORT_H == 176
    - Test: HUD_H == 16
    - Test: SCREEN_H == VIEWPORT_H + HUD_H (consistency invariant)
    - Test: VIEWPORT_W % TILE_SIZE == 0 (tiles must align)
    - Test: VIEWPORT_H % TILE_SIZE == 0 (tiles must align)
  </behavior>
  <action>
    1. Create `tests/test_screen_constants.py` with tests for all constants listed in behavior block. Import from `src.core.constants`. Run tests -- they MUST fail (constants do not exist yet).

    2. Add the following block to `src/core/constants.py` AFTER the TILE_SIZE line and BEFORE the tile constant definitions (around line 6):

    ```python
    # Screen / Display (D-01, D-02, D-08)
    SCREEN_W = 320       # Full pyxel window width
    SCREEN_H = 192       # Full pyxel window height
    VIEWPORT_W = 320     # Playable area width (same as screen)
    VIEWPORT_H = 176     # Playable area height (above HUD)
    HUD_H = 16           # HUD strip height at bottom of screen

    # Culling margin for off-screen entity despawn (boss, projectiles)
    CULL_MARGIN = 16     # Extra pixels beyond viewport before culling
    ```

    3. Run tests -- they MUST pass.

    Per D-08: these are the central constants all other files will import.
  </action>
  <verify>
    <automated>python -m pytest tests/test_screen_constants.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `src/core/constants.py` contains `SCREEN_W = 320`
    - `src/core/constants.py` contains `SCREEN_H = 192`
    - `src/core/constants.py` contains `VIEWPORT_W = 320`
    - `src/core/constants.py` contains `VIEWPORT_H = 176`
    - `src/core/constants.py` contains `HUD_H = 16`
    - `src/core/constants.py` contains `CULL_MARGIN = 16`
    - `tests/test_screen_constants.py` exists and passes
    - `tests/test_screen_constants.py` contains `SCREEN_H == VIEWPORT_H + HUD_H`
  </acceptance_criteria>
  <done>All six screen constants defined in constants.py, test file validates values and consistency invariant, all tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Replace all hardcoded 128/144 values with constants across production code</name>
  <files>main.py, src/level/world.py, src/level/map.py, src/entities/effects.py, src/entities/boss.py, src/entities/projectile.py</files>
  <read_first>main.py, src/level/world.py, src/level/map.py, src/entities/effects.py, src/entities/boss.py, src/entities/projectile.py, src/core/constants.py</read_first>
  <action>
    Replace every hardcoded 128 and 144 value per D-09. Complete inventory:

    **main.py:**
    - Line 16: `pyxel.init(128, 128, ...)` -> `pyxel.init(SCREEN_W, SCREEN_H, title="Jelly Roll Proto")` (per D-01)
    - Add import at top: `from src.core.constants import SCREEN_W, SCREEN_H, VIEWPORT_W, VIEWPORT_H, HUD_H, CULL_MARGIN`
    - Line 123: `room_w, room_h = 128, 128` -> `room_w, room_h = VIEWPORT_W, VIEWPORT_H` (per D-05)
    - Line 195: `rw, rh = ... 128, 128` -> `rw, rh = ... VIEWPORT_W, VIEWPORT_H`
    - Line 262: `16 < rel_x < 112 and 16 < rel_y < 112` -> `16 < rel_x < VIEWPORT_W - 16 and 16 < rel_y < VIEWPORT_H - 16` (boss trigger safe distance)
    - Lines 314-315: `cam_x + 128`, `cam_y + 128` -> `cam_x + VIEWPORT_W`, `cam_y + VIEWPORT_H` (slime boundary)
    - Lines 556-561: HP drawing `self.cam_x + 4 + i * 10, self.cam_y + 4` -- LEAVE these for Plan 02 (HUD migration). Do NOT change HP drawing yet.
    - Lines 564-567: Victory text centering -- LEAVE for Plan 02 (HUD draw pipeline restructure).

    **src/level/world.py:**
    - Add import: `from src.core.constants import VIEWPORT_W, VIEWPORT_H`
    - Line 27-28: `SCREEN_W = 128`, `SCREEN_H = 128` -> `SCREEN_W = VIEWPORT_W`, `SCREEN_H = VIEWPORT_H`
    - CRITICAL: Camera math uses VIEWPORT dimensions (176), NOT SCREEN dimensions (192). Per Pitfall 1 in RESEARCH.

    **src/entities/effects.py:**
    - Add import: `from src.core.constants import VIEWPORT_W, VIEWPORT_H`
    - Lines 23-24: `cam_x + 128`, `cam_y + 128` -> `cam_x + VIEWPORT_W`, `cam_y + VIEWPORT_H`
    - Lines 56-57: `cam_x + 128`, `cam_y + 128` -> `cam_x + VIEWPORT_W`, `cam_y + VIEWPORT_H`

    **src/entities/boss.py:**
    - Add import: `from src.core.constants import VIEWPORT_W, VIEWPORT_H, CULL_MARGIN`
    - Lines 29-30: `cam_x + 144`, `cam_y + 144` -> `cam_x + VIEWPORT_W + CULL_MARGIN`, `cam_y + VIEWPORT_H + CULL_MARGIN`

    **src/entities/projectile.py:**
    - Add import: `from src.core.constants import VIEWPORT_W, VIEWPORT_H, CULL_MARGIN`
    - Lines 41-42: `cam_x + 144`, `cam_y + 144` -> `cam_x + VIEWPORT_W + CULL_MARGIN`, `cam_y + VIEWPORT_H + CULL_MARGIN`
    - Lines 90-91: `cam_x + 144`, `cam_y + 144` -> `cam_x + VIEWPORT_W + CULL_MARGIN`, `cam_y + VIEWPORT_H + CULL_MARGIN`

    **src/level/map.py:**
    - Add import: `from src.core.constants import VIEWPORT_W, VIEWPORT_H`
    - Lines 61-62: `data.get("width", 128)`, `data.get("height", 128)` -> `data.get("width", VIEWPORT_W)`, `data.get("height", VIEWPORT_H)`

    After all changes, run: `grep -rn "\b128\b" --include="*.py" src/ main.py` to verify NO hardcoded 128 remains in production code. Test files may still contain 128 for old room fixtures (addressed in Task 3).
  </action>
  <verify>
    <automated>python -c "import subprocess; r=subprocess.run(['grep','-rn','\\b128\\b','--include=*.py','src/','main.py'],capture_output=True,text=True); print(r.stdout or 'CLEAN'); assert not r.stdout.strip(), f'Hardcoded 128 found:\\n{r.stdout}'"</automated>
  </verify>
  <acceptance_criteria>
    - `main.py` contains `pyxel.init(SCREEN_W, SCREEN_H`
    - `main.py` contains `from src.core.constants import` with SCREEN_W, VIEWPORT_W
    - `main.py` contains `room_w, room_h = VIEWPORT_W, VIEWPORT_H`
    - `main.py` contains `VIEWPORT_W - 16` (boss trigger safe distance)
    - `src/level/world.py` contains `SCREEN_W = VIEWPORT_W`
    - `src/level/world.py` contains `SCREEN_H = VIEWPORT_H`
    - `src/level/world.py` contains `from src.core.constants import VIEWPORT_W, VIEWPORT_H`
    - `src/entities/effects.py` contains `cam_x + VIEWPORT_W`
    - `src/entities/boss.py` contains `cam_x + VIEWPORT_W + CULL_MARGIN`
    - `src/entities/projectile.py` contains `cam_x + VIEWPORT_W + CULL_MARGIN`
    - `src/level/map.py` contains `VIEWPORT_W` and `VIEWPORT_H`
    - `grep -rn "\b128\b" --include="*.py" src/ main.py` returns ZERO matches
  </acceptance_criteria>
  <done>All hardcoded 128/144 values in production Python files replaced with named constants from constants.py. grep confirms zero remaining 128 literals.</done>
</task>

<task type="auto">
  <name>Task 3: Update test fixtures from 128x128 to 320x176 room dimensions</name>
  <files>tests/test_world_manager.py</files>
  <read_first>tests/test_world_manager.py, src/level/world.py, src/core/constants.py</read_first>
  <action>
    Update ALL test fixtures in `tests/test_world_manager.py` to use 320x176 room dimensions instead of 128x128:

    1. Add import at top: `from src.core.constants import VIEWPORT_W, VIEWPORT_H`

    2. Replace every `LevelBounds(..., 128, 128)` with `LevelBounds(..., VIEWPORT_W, VIEWPORT_H)`:
       - `LevelBounds("room_a", 0, 0, 128, 128)` -> `LevelBounds("room_a", 0, 0, VIEWPORT_W, VIEWPORT_H)`
       - `LevelBounds("room_b", 128, 256, 128, 128)` -> `LevelBounds("room_b", VIEWPORT_W, 256, VIEWPORT_W, VIEWPORT_H)` (offset room uses VIEWPORT_W as x-offset since rooms tile horizontally)
       - Similar updates for ALL other test fixtures with 128 dimensions

    3. Update camera clamping assertions:
       - Any assertion checking `cam_x` or `cam_y` clamped values must reflect 320x176 dimensions
       - Room boundary tests: `contains(128, 64)` -> `contains(VIEWPORT_W, 64)` for "just outside right" test
       - `contains(64, 128)` -> `contains(64, VIEWPORT_H)` for "just outside bottom" test

    4. Update any grid-snap divisor tests: if tests check fallback behavior using 128 as a divisor, update to VIEWPORT_W or VIEWPORT_H as appropriate.

    5. Run the full test suite to verify everything passes.
  </action>
  <verify>
    <automated>python -m pytest tests/ -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_world_manager.py` contains `from src.core.constants import VIEWPORT_W, VIEWPORT_H`
    - `tests/test_world_manager.py` contains `LevelBounds("room_a", 0, 0, VIEWPORT_W, VIEWPORT_H)`
    - `tests/test_world_manager.py` does NOT contain `LevelBounds` with literal `128` as width or height argument
    - `python -m pytest tests/ -x` exits 0
  </acceptance_criteria>
  <done>All test fixtures use VIEWPORT_W/VIEWPORT_H constants, full test suite passes with new 320x176 dimensions.</done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/ -x -v` -- all tests pass
2. `grep -rn "\b128\b" --include="*.py" src/ main.py` -- returns zero matches in production code
3. `python -c "from src.core.constants import SCREEN_W, SCREEN_H, VIEWPORT_W, VIEWPORT_H, HUD_H; assert SCREEN_W==320; assert SCREEN_H==192; assert VIEWPORT_W==320; assert VIEWPORT_H==176; assert HUD_H==16; print('OK')"` -- prints OK
</verification>

<success_criteria>
- Six screen constants defined in constants.py (SCREEN_W, SCREEN_H, VIEWPORT_W, VIEWPORT_H, HUD_H, CULL_MARGIN)
- Zero hardcoded 128/144 values in production Python files
- All files import constants from src.core.constants
- WorldManager uses VIEWPORT dimensions (not SCREEN dimensions) for camera
- Full test suite passes with 320x176 room dimensions
</success_criteria>

<output>
After completion, create `.planning/phases/12-screen-size-expansion/12-01-SUMMARY.md`
</output>
