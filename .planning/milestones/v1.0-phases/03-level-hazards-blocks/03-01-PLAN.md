---
phase: 03-level-hazards-blocks
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/core/constants.py, src/level/map.py, assets/game.pyxres]
autonomous: true
requirements: [ENV-01, DRILL-02]

must_haves:
  truths:
    - "LevelMap can distinguish between solid, hazard, and destructible tiles"
  artifacts:
    - path: "src/level/map.py"
      provides: "Tile identification logic"
      exports: ["is_solid", "is_hazard", "is_destructible"]
    - path: "src/core/constants.py"
      provides: "Tile index constants"
  key_links:
    - from: "src/level/map.py"
      to: "pyxel.tilemap"
      via: "pget lookup"
---

<objective>
Update the level map system to identify different tile types (Solid, Hazard, Destructible) and prepare the assets for Phase 3.

Purpose: Foundation for environmental interaction and hazards.
Output: Enhanced `LevelMap` class and updated tile constants.
</objective>

<execution_context>
@./.gemini/get-shit-done/workflows/execute-plan.md
@./.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/level/map.py
@src/core/constants.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Tile Constants</name>
  <files>src/core/constants.py</files>
  <action>
    Add constants for tile identification.
    Use (u, v) tuples matching Pyxel's pget format.
    - TILE_EMPTY = (0, 0)
    - TILE_SOLID = (1, 0)
    - TILE_HAZARD = (2, 0)
    - TILE_DESTRUCTIBLE = (3, 0)
  </action>
  <verify>
    <automated>grep -E "TILE_HAZARD|TILE_DESTRUCTIBLE" src/core/constants.py</automated>
  </verify>
  <done>Tile constants defined in src/core/constants.py</done>
</task>

<task type="auto">
  <name>Task 2: Implement Tile Identification Logic</name>
  <files>src/level/map.py</files>
  <action>
    Update LevelMap to use the new constants.
    - `is_solid(tx, ty)`: Returns True if tile is TILE_SOLID or TILE_DESTRUCTIBLE.
    - `is_hazard(tx, ty)`: Returns True if tile is TILE_HAZARD.
    - `is_destructible(tx, ty)`: Returns True if tile is TILE_DESTRUCTIBLE.
    - Add `check_hazard(x, y, w, h)`: Similar to `check_collision` but calls `is_hazard`.
    - Add `get_destructible_at(x, y, w, h)`: Returns (tx, ty) of a destructible tile overlapping the AABB, or None.
  </action>
  <verify>
    <automated>pytest tests/test_map_identification.py</automated>
  </verify>
  <done>LevelMap identifies all tile types correctly</done>
</task>

<task type="auto">
  <name>Task 3: Update Asset Tilemap (CLI-only)</name>
  <files>assets/game.pyxres</files>
  <action>
    Update the tilemap in `assets/game.pyxres` using `generate_assets.py` (if available) or assume tiles are at the specified coordinates.
    Ensure tiles (1,0), (2,0), and (3,0) have distinct visual representations (placeholder colors are fine).
    Place some Spikes (2,0) and Blocks (3,0) in the "Physics Gym" area of the tilemap.
  </action>
  <verify>
    <automated>ls assets/game.pyxres</automated>
  </verify>
  <done>Tilemap updated with hazard and destructible tiles</done>
</task>

</tasks>

<verification>
Check that LevelMap can distinguish between a solid wall, a spike, and a destructible block using unit tests with mocked Pyxel tilemap data.
</verification>

<success_criteria>
LevelMap methods correctly categorize tiles according to the new constants.
</success_criteria>

<output>
After completion, create `.planning/phases/03-level-hazards-blocks/03-01-SUMMARY.md`
</output>
