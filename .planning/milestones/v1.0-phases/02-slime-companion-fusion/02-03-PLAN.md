---
phase: 02-slime-companion-fusion
plan: 03
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified: [src/entities/player.py, src/core/constants.py, src/entities/slime.py]
autonomous: true
requirements: [DRILL-01]

must_haves:
  truths:
    - "Player can activate Drill Dive (Down + X) while in the air"
    - "Drill Dive triggers a rapid downward movement with horizontal drift"
    - "Drill Dive consumes juice on impact with surfaces"
    - "Fusion disengages at 0 juice or manual cancel (Jump)"
  artifacts:
    - path: "src/entities/player.py"
      provides: "FSM updates for FUSED and DIVING states"
    - path: "src/core/constants.py"
      provides: "Drill speed and cost constants"
  key_links:
    - from: "src/entities/player.py"
      to: "src/entities/slime.py"
      via: "slime fusion state and juice consumption"
---

<objective>
Implement the core Drill Dive fusion ability.

Purpose: Provide a high-impact, resource-dependent destructive move.
Output: Player FSM with DIVING state, downward physics, and juice-linked consumption.
</objective>

<execution_context>
@./.gemini/get-shit-done/workflows/execute-plan.md
@./.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-slime-companion-fusion/02-CONTEXT.md
@.planning/phases/02-slime-companion-fusion/02-RESEARCH.md
@.planning/phases/02-02-SUMMARY.md
@src/entities/player.py
@src/entities/slime.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: FSM Updates and Drill Dive Activation</name>
  <files>src/entities/player.py, src/core/constants.py</files>
  <action>
    - Add `DRILL_SPEED`, `DRILL_DRIFT_SPEED` to `src/core/constants.py`.
    - Update `Player.update`: Add `is_fused` state and check for slime proximity to allow fusion.
    - In `Player.handle_input`: Detect `Down + X (Dash)` while `not self.is_grounded`.
    - Transition to `DIVING` state if conditions met (has slime, in air, juice > 0).
    - When `DIVING`: Set `self.dy = DRILL_SPEED` and disable standard Dash.
    - Update `Player.draw`: When in `DIVING` state, draw the player with a "drill attachment" sprite (from `assets/game.pyxres`).
  </action>
  <verify>
    <automated>python main.py (Verify Down + X triggers rapid downward dive while in air)</automated>
  </verify>
  <done>Drill Dive activation and basic physics are functional.</done>
</task>

<task type="auto">
  <name>Task 2: Impact Consumption and Disengagement</name>
  <files>src/entities/player.py, src/entities/slime.py, src/core/constants.py</files>
  <action>
    - Add `DRILL_IMPACT_COST` to `src/core/constants.py`.
    - Update `Player.apply_physics`: If `self.state == "DIVING"`, apply `DRILL_DRIFT_SPEED` based on horizontal input.
    - Update `Player.move_and_collide`: If in `DIVING` state and a collision occurs on `y + dy`, call `slime.consume(DRILL_IMPACT_COST)`.
    - If `slime.juice == 0` or `pyxel.btnp(pyxel.KEY_SPACE)`, transition from `DIVING` to `FALLING`.
    - Ensure slime becomes "unfused" and resumes following logic after disengagement.
  </action>
  <verify>
    <automated>python main.py (Verify dive consumes slime juice on landing and ends when juice is empty)</automated>
  </verify>
  <done>Drill Dive is fully integrated with the juice resource and handles disengagement.</done>
</task>

</tasks>

<verification>
- Drill Dive accelerates player vertically downwards.
- Horizontal drift allows minor adjustments during the dive.
- Juice is consumed only on impact (per HIT, or per surface frame as simplified).
- Jump button successfully cancels the dive.
</verification>

<success_criteria>
The Drill Dive is a high-speed, satisfying move that is visually linked to the slime and limited by its juice resource.
</success_criteria>

<output>
After completion, create `.planning/phases/02-slime-companion-fusion/02-03-SUMMARY.md`
</output>
