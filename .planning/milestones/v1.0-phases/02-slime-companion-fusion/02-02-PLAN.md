---
phase: 02-slime-companion-fusion
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified: [src/entities/slime.py, src/core/constants.py]
autonomous: true
requirements: [SLM-02]

must_haves:
  truths:
    - "Slime has a juice resource that replenishes passively"
    - "Slime visually scales based on current juice level"
    - "Slime size reaches minimum (2x2) at 0 juice"
  artifacts:
    - path: "src/entities/slime.py"
      provides: "Juice resource and scaling logic"
  key_links:
    - from: "src/entities/slime.py"
      to: "src/core/constants.py"
      via: "juice regeneration and scaling constants"
---

<objective>
Implement the Juice resource system and its dynamic visual representation.

Purpose: Provide a clear visual cue of the companion's power level.
Output: Slime entity with `juice` state and scaling `draw` method.
</objective>

<execution_context>
@./.gemini/get-shit-done/workflows/execute-plan.md
@./.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-slime-companion-fusion/02-CONTEXT.md
@.planning/phases/02-slime-companion-fusion/02-RESEARCH.md
@.planning/phases/02-slime-companion-fusion/02-01-SUMMARY.md
@src/entities/slime.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Juice Resource and Passive Regeneration</name>
  <files>src/entities/slime.py, src/core/constants.py</files>
  <action>
    - Add `JUICE_MAX`, `JUICE_REGEN_RATE` to `src/core/constants.py`.
    - In `src/entities/slime.py`, initialize `self.juice = JUICE_MAX`.
    - Implement `Slime.refill(amount)` and `Slime.consume(amount)`.
    - Update `Slime.update`: Implement passive regeneration of `self.juice` up to `JUICE_MAX`.
  </action>
  <verify>
    <automated>python -c "from src.entities.slime import Slime; from src.core.constants import JUICE_MAX; s = Slime(0,0); s.juice = 0; s.update(0,0); print(f'Juice regened to: {s.juice}')"</automated>
  </verify>
  <done>Juice system is functional with passive regeneration.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Dynamic Visual Scaling</name>
  <files>src/entities/slime.py</files>
  <action>
    - Define a `scale` property or method in `Slime` that returns a value between 0.25 (2x2) and 1.0 (8x8) based on `self.juice / JUICE_MAX`.
    - Update `Slime.draw`: Use the `scale` parameter in `pyxel.blt` to draw the slime at its current size.
    - Offset the `x, y` position in `draw` so the slime scales from its center.
  </action>
  <verify>
    <automated>python main.py (Observe slime shrinking and growing if you manually toggle juice in code)</automated>
  </verify>
  <done>Slime visual size correctly represents its resource level.</done>
</task>

</tasks>

<verification>
- Slime juice refills when not being used.
- Slime size changes dynamically as juice level changes.
- Minimum size (2x2) is enforced at 0 juice.
</verification>

<success_criteria>
The Slime's size is a clear and responsive indicator of its current juice resource.
</success_criteria>

<output>
After completion, create `.planning/phases/02-slime-companion-fusion/02-02-SUMMARY.md`
</output>
