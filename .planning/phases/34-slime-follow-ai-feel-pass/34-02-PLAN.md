---
phase: 34-slime-follow-ai-feel-pass
plan: 02
type: execute
wave: 1
depends_on: ["34-01"]
files_modified:
  - src/entities/slime.py
  - main.py
  - tests/test_input_remap.py
autonomous: true
requirements: [SLM-04]
requirements_addressed: [SLM-04]
tags: [housekeeping, dead-code-strip, slime]

must_haves:
  truths:
    - "Slime.is_punted instance attr removed; Slime.punt() method removed"
    - "Dead instance attrs accel/friction/max_speed/gravity/jump_force removed from Slime.__init__"
    - "main.py:912-916 punt collision block removed"
    - "tests/test_input_remap.py no longer references slime.is_punted"
    - "All 6 grep gates show zero matches in src/ + main.py"
    - "test_punt_attrs_stripped passes after this plan"
    - "Existing test suite still passes (no regressions)"
  artifacts:
    - path: "src/entities/slime.py"
      provides: "Slime class without is_punted/accel/friction/max_speed/gravity/jump_force/punt() — all D-04 strip targets gone"
    - path: "main.py"
      provides: "Game.update without the punt collision block at 912-916"
    - path: "tests/test_input_remap.py"
      provides: "L61 reference to slime.is_punted removed (legacy assertion no longer applicable)"
  key_links:
    - from: "src/entities/slime.py (post-strip)"
      to: "tests/test_slime.py::test_punt_attrs_stripped"
      via: "test asserts hasattr returns False for stripped attrs"
      pattern: "not hasattr.*is_punted"
---

<objective>
Execute D-04 dead-code strip as Phase 34 housekeeping. Separate from feel-tuning per CONTEXT.md D-04 — bundling them complicates rollback if feel-tuning needs revert (RESEARCH Anti-Patterns).

Purpose: removes the orphan `is_punted` code path with no live writers since Phase 31.5 strip. Slim Slime class down to only the surfaces Phase 34 will use. Turns `test_punt_attrs_stripped` from RED to GREEN.

Output: 3 files modified; ~30 lines deleted across src/entities/slime.py + main.py; 1 stale test reference removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/34-slime-follow-ai-feel-pass/34-CONTEXT.md
@.planning/phases/34-slime-follow-ai-feel-pass/34-RESEARCH.md
@.planning/phases/34-slime-follow-ai-feel-pass/34-PATTERNS.md
@src/entities/slime.py
@main.py
@tests/test_input_remap.py

<interfaces>
<!-- D-04 strip targets, exact line numbers as of Phase 33 close (commit 7381243):

src/entities/slime.py:
  L25:        self.is_punted = False
  L40-44:     self.accel = 0.05
              self.friction = 0.0375
              self.max_speed = 1.5
              self.gravity = 0.05
              self.jump_force = -1.75
  L51:        self.is_punted = False  # Override punt state  (inside recall())
  L123:       self.is_punted = False  (inside fused early-out)
  L129-143:   if self.is_punted: ... full collision branch (15 lines)
  L178-182:   def punt(self, dx, dy): ... (5 lines)

main.py:
  L912-916:   if self.slime.is_punted and e.check_collision(...): ...

tests/test_input_remap.py:
  L61:        slime.is_punted = False   (legacy assertion, no longer applicable)

KEEP UNCHANGED:
  - Slime.is_being_absorbed (slime.py:37) — RESEARCH notes this is an orphan
    field deliberately retained for compatibility with surviving render code
    paths. Out of scope for Phase 34 strip.
  - Slime.is_grounded (slime.py:16) — used by current path-follow logic at
    L171; KEEP. Distinct from the D-04 strip targets.
-->
</interfaces>
</context>

<threat_model>
| Boundary | Description |
|----------|-------------|
| (none) | Pure code deletion in offline single-player game. No untrusted input. |

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-02-N/A | — | src/entities/slime.py, main.py | accept | N/A: dead-code removal in offline single-player Pyxel game; no auth/network/persistence boundary. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Strip is_punted + dead physics attrs from Slime + main.py + test</name>
  <files>src/entities/slime.py, main.py, tests/test_input_remap.py</files>
  <read_first>
    - src/entities/slime.py (full file — confirm exact line numbers haven't shifted; the D-04 list above is the canonical strip set)
    - main.py:905-925 (read the surrounding context for L912-916; confirm no other references to slime.is_punted in nearby blocks)
    - tests/test_input_remap.py (full file; identify why L61 sets slime.is_punted = False — likely defensive setup that no longer matters)
    - .planning/phases/34-slime-follow-ai-feel-pass/34-CONTEXT.md (D-04 wording — strip is housekeeping, NOT bundled with feel-tuning)
  </read_first>
  <action>
    Make the following EXACT edits:

    **src/entities/slime.py:**
    1. Delete L25 (`self.is_punted = False`)
    2. Delete L40-44 (the 5 dead instance attrs `accel`, `friction`, `max_speed`, `gravity`, `jump_force`)
    3. Delete L51 (`self.is_punted = False  # Override punt state` inside `recall()`)
    4. Delete L123 (`self.is_punted = False` inside the `is_fused` early-out at L114-124)
    5. Delete L129-143 — the entire `if self.is_punted: ...` block. After deletion, the path-follow base (currently at L145+) becomes the immediate code after the passive-regen line at L127.
    6. Delete L178-182 — the entire `def punt(self, dx, dy): ...` method.

    **main.py:**
    7. Delete L912-916 — the entire `if self.slime.is_punted and e.check_collision(...):` block. Read 5 lines of context above and below before deletion to ensure no orphaned `else` clause or trailing comma remains.

    **tests/test_input_remap.py:**
    8. Delete the line at L61 (`slime.is_punted = False`). If that line is the only line in a setup block, leave the surrounding block intact (do NOT delete the test). The line is a stale defensive setup; the test logic does not depend on it.

    After deletions, verify by reading the modified files end-to-end. Confirm:
    - `Slime.__init__` no longer mentions `is_punted`, `accel`, `friction`, `max_speed`, `gravity`, `jump_force`
    - `Slime.update` flows directly from passive regen (`self.juice = min(...)` at the old L127) to the path-follow `self.history.append(...)` (the old L146)
    - `Slime` class has no `punt` method
    - main.py's enemy-iteration loop no longer references `self.slime.is_punted`

    DO NOT touch any of:
    - `Slime.is_being_absorbed` (orphan but explicitly kept per slime.py:33-37 comment)
    - `Slime.is_grounded` (live usage at slime.py:171)
    - `Slime.move_and_collide` (live usage path — currently called only by the deleted is_punted branch, but Plan 04 may reuse it; keep for now and let Plan 04 remove if confirmed dead)
    - Recall/dissipate/reform/spit/draw — all out of scope for D-04
  </action>
  <verify>
    <automated>python -m pytest tests/test_slime.py::test_punt_attrs_stripped tests/test_input_remap.py tests/test_slime.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "is_punted" src/entities/slime.py main.py` returns ZERO matches (after filtering binary __pycache__: `grep -rn "is_punted" src/entities/slime.py main.py | grep -v __pycache__ | wc -l` returns 0)
    - `grep -n "def punt(" src/entities/slime.py` returns ZERO matches
    - `grep -nE "self\.(accel|friction|max_speed|gravity|jump_force)" src/entities/slime.py | grep -v "^#" | wc -l` returns 0 (no references in non-comment lines)
    - `grep -n "is_punted" tests/test_input_remap.py` returns ZERO matches
    - `python -m pytest tests/test_slime.py::test_punt_attrs_stripped -x -q` PASSES (was RED in Plan 01)
    - `python -m pytest tests/ -x -q` shows no NEW failures attributable to this plan (existing failures from Plan 01 RED stubs are expected; this plan turns ONLY test_punt_attrs_stripped green)
  </acceptance_criteria>
  <done>All 8 deletions made; all 4 grep gates show zero matches; test_punt_attrs_stripped passes; no other tests regress.</done>
</task>

</tasks>

<verification>
- `grep -rn "is_punted\|def punt(" src/ main.py | grep -v __pycache__` returns zero hits
- `python -m pytest tests/test_slime.py::test_punt_attrs_stripped -x -q` passes
- Game still boots: `python -c "from src.entities.slime import Slime; s = Slime(0, 0); print('OK')"` prints OK without AttributeError
- No new test failures (run `python -m pytest tests/ -x -q` — only the pre-existing RED stubs from Plan 01 should fail)
</verification>

<success_criteria>
~30 lines of dead code removed; Slime class slimmer; test_punt_attrs_stripped flips RED→GREEN; no regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/34-slime-follow-ai-feel-pass/34-02-SUMMARY.md` listing exact line counts deleted per file and confirming all 4 grep gates pass.
</output>
