---
phase: 34-slime-follow-ai-feel-pass
plan: 03
type: execute
wave: 1
depends_on: ["34-01"]
files_modified:
  - assets/physics-schema.json
autonomous: true
requirements: [SLM-04]
requirements_addressed: [SLM-04]
tags: [schema, tuning, slime]

must_haves:
  truths:
    - "9 new keys exist under tuning.slime_follow group in assets/physics-schema.json"
    - "Each key has the RESEARCH-recommended seed value (SLIME_MAX_FOLLOW_SPEED=7.0, SLIME_CATCHUP_CURVE_K=0.50, ...)"
    - "tuning.SLIME_X reads return the seed values via PEP 562 __getattr__ (verified by Plan 01 test)"
    - "Panel auto-discovers all 9 keys in the existing 'Slime' tab without panel.py code changes"
    - "All existing presets in assets/presets/*.json continue to load (missing keys fall through to schema defaults per Pitfall 6)"
  artifacts:
    - path: "assets/physics-schema.json"
      provides: "slime_follow group expanded from 4 keys to 13 keys (4 existing + 9 new) per RESEARCH §Code Examples L478-494"
      contains: "SLIME_CATCHUP_CURVE_K"
  key_links:
    - from: "assets/physics-schema.json (slime_follow group)"
      to: "src/core/tuning.py (PEP 562 __getattr__)"
      via: "tuning.load() seeds defaults from schema for keys missing from preset (Pitfall 6 protection)"
      pattern: "slime_follow"
    - from: "assets/physics-schema.json"
      to: "src/ui/presets.py:18-22 FEEL_GROUPS"
      via: "slime_follow already in FEEL_GROUPS; new keys auto-discover into Slime tab"
      pattern: "slime_follow"
---

<objective>
Land the 9 new schema keys under the existing `slime_follow` group in `assets/physics-schema.json` per RESEARCH §Recommended Tunable Seeds. Additive only — preserves the 4 existing keys (SLIME_FOLLOW_DELAY, SLIME_MAX_DIST, SLIME_REFORM_DIST, SLIME_LERP_FACTOR).

Purpose: every other Phase 34 plan reads these tunables. Plan 04 (entity surface) reads SLIME_CATCHUP_CURVE_K + SLIME_MAX_FOLLOW_SPEED + lookahead/stuck/mode keys. Plan 05 (overlay) reads SLIME_STUCK_WINDOW_FRAMES + SLIME_LOOKAHEAD_*. Plan 07 (sign-off + bake) tunes them via the live panel and bakes final values into v2.0-default.json. Without this plan, every other plan AttributeErrors on first frame.

Output: 1 file modified (additive). Plan 01's `test_phase34_slime_keys_readable` and `test_phase34_keys_in_flat_index` flip RED→GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/34-slime-follow-ai-feel-pass/34-CONTEXT.md
@.planning/phases/34-slime-follow-ai-feel-pass/34-RESEARCH.md
@.planning/phases/34-slime-follow-ai-feel-pass/34-PATTERNS.md
@assets/physics-schema.json
@src/core/tuning.py
@src/ui/presets.py
@src/ui/panel.py
@assets/presets/_v1.3-reference.json

<interfaces>
<!-- Current slime_follow group at assets/physics-schema.json:60-65:
"slime_follow": {
  "SLIME_FOLLOW_DELAY": 16,
  "SLIME_MAX_DIST": 100,
  "SLIME_REFORM_DIST": 8,
  "SLIME_LERP_FACTOR": 0.4
},

Phase 34 target shape (additive — preserves existing 4 keys):
"slime_follow": {
  "SLIME_FOLLOW_DELAY": 16,
  "SLIME_MAX_DIST": 100,
  "SLIME_REFORM_DIST": 8,
  "SLIME_LERP_FACTOR": 0.4,
  "SLIME_MAX_FOLLOW_SPEED": 7.0,
  "SLIME_CATCHUP_CURVE_K": 0.50,
  "SLIME_LOOKAHEAD_FRAMES": 8,
  "SLIME_LOOKAHEAD_FALLBACK_BIAS": 4.0,
  "SLIME_LOOKAHEAD_EPSILON": 0.1,
  "SLIME_STUCK_WINDOW_FRAMES": 36,
  "SLIME_STUCK_RECOVERY_COOLDOWN": 30,
  "SLIME_FLOAT_GROUND_K_FRAMES": 12,
  "SLIME_MODE_HYSTERESIS_FRAMES": 6
}

Source / rationale per RESEARCH §Recommended Tunable Seeds:
- SLIME_MAX_FOLLOW_SPEED = 7.0      (D-13 budget math: 60-frame closure of 160 px requires peak ≥ 6.32 px/f at curve k=0.50; 7.0 adds ~10% headroom. Replaces hardcoded MAX_SHADOW_SPEED=4.0 per D-05.)
- SLIME_CATCHUP_CURVE_K = 0.50      (D-13 + D-09 closed form: k = 2*sqrt(160)/60 ≈ 0.42; round up to 0.50.)
- SLIME_LOOKAHEAD_FRAMES = 8        (D-11 constraint: ≤ SLIME_FOLLOW_DELAY=16. At MAX_WALK_SPEED=1.9 px/f, 8 frames = 15.2 px ≈ 1 tile lean.)
- SLIME_LOOKAHEAD_FALLBACK_BIAS = 4.0  (D-11 stationary-aim case S-L2; 4 px ≈ 0.25 tile.)
- SLIME_LOOKAHEAD_EPSILON = 0.1     (D-11 stationary threshold px/f.)
- SLIME_STUCK_WINDOW_FRAMES = 36    (D-10 platformer-companion convention 30-60f; 36 is mid-range; 0.6s @ 60fps.)
- SLIME_STUCK_RECOVERY_COOLDOWN = 30 (D-10 fade-arc length; half of SLIME_DISSIPATE_COOLDOWN=240; 0.5s @ 60fps.)
- SLIME_FLOAT_GROUND_K_FRAMES = 12  (D-08 reach-tile probe budget; at SLIME_MAX_FOLLOW_SPEED=7.0, probe distance = 84 px ≈ 5 tiles.)
- SLIME_MODE_HYSTERESIS_FRAMES = 6  (S-M3 anti-jitter; 6 frames is the minimum the eye perceives as a state change.)

KEEP UNCHANGED:
  - All other top-level groups (tile, display, sprite, hazards, movement,
    forgiving, wall, slime_juice, projectile, drill, juice_effects, health,
    fusion, save, death, save_point, gates, pogo).
  - All keys under derived.* (converter-facing values; not affected).
  - schema $schema/title/description/version/updated/fps/tile_size header.
-->
</interfaces>
</context>

<threat_model>
| Boundary | Description |
|----------|-------------|
| (none) | Schema-file edit in offline single-player game. No untrusted input. |

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-03-N/A | — | assets/physics-schema.json | accept | N/A: developer-authored config file in offline single-player Pyxel game; no auth/network/persistence boundary. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Append 9 new keys to slime_follow group in physics-schema.json</name>
  <files>assets/physics-schema.json</files>
  <read_first>
    - assets/physics-schema.json (full file, 188 lines — confirm slime_follow group at L60-65 has not shifted; verify no duplicate slime_follow group elsewhere)
    - .planning/phases/34-slime-follow-ai-feel-pass/34-RESEARCH.md §Recommended Tunable Seeds (the 9 (key, value, source) rows are the canonical reference)
    - src/core/tuning.py (skim — confirm tuning.load() seeds defaults from schema; this is Phase 24 contract preserved through Phase 33)
    - src/ui/presets.py (lines 18-22 — confirm slime_follow is in FEEL_GROUPS; if not, this task fails — would require panel-code change which is out of scope per RESEARCH)
    - assets/presets/_v1.3-reference.json (confirm preset does NOT contain SLIME_MAX_FOLLOW_SPEED — Pitfall 6 path: schema must seed default for missing-from-preset case)
  </read_first>
  <action>
    Modify the `tuning.slime_follow` block in `assets/physics-schema.json` (currently L60-65) to add the 9 new keys after the existing 4 keys. EXACT JSON block (preserve indentation: 6 spaces for key lines per surrounding style):

    ```json
        "slime_follow": {
          "SLIME_FOLLOW_DELAY": 16,
          "SLIME_MAX_DIST": 100,
          "SLIME_REFORM_DIST": 8,
          "SLIME_LERP_FACTOR": 0.4,
          "SLIME_MAX_FOLLOW_SPEED": 7.0,
          "SLIME_CATCHUP_CURVE_K": 0.50,
          "SLIME_LOOKAHEAD_FRAMES": 8,
          "SLIME_LOOKAHEAD_FALLBACK_BIAS": 4.0,
          "SLIME_LOOKAHEAD_EPSILON": 0.1,
          "SLIME_STUCK_WINDOW_FRAMES": 36,
          "SLIME_STUCK_RECOVERY_COOLDOWN": 30,
          "SLIME_FLOAT_GROUND_K_FRAMES": 12,
          "SLIME_MODE_HYSTERESIS_FRAMES": 6
        },
    ```

    Use exact values verbatim. SLIME_LOOKAHEAD_EPSILON is `0.1` (float — JSON serializer may render as `0.1` or `0.1` is fine). SLIME_CATCHUP_CURVE_K is `0.50` written as either `0.5` or `0.50` (JSON parser is agnostic; the test in Plan 01 uses `0.50`). SLIME_STUCK_WINDOW_FRAMES, SLIME_STUCK_RECOVERY_COOLDOWN, SLIME_LOOKAHEAD_FRAMES, SLIME_FLOAT_GROUND_K_FRAMES, SLIME_MODE_HYSTERESIS_FRAMES are integers (no decimal point). SLIME_MAX_FOLLOW_SPEED, SLIME_LOOKAHEAD_FALLBACK_BIAS are floats (`7.0`, `4.0`).

    Do NOT modify any other group. Do NOT bump the schema `version` field — this is an additive change within the existing 0.3.0 schema (no breaking change).

    After the edit, run `python -c "import json; json.load(open('assets/physics-schema.json'))"` to confirm the file is valid JSON.

    Per Pitfall 6: do NOT modify any preset file in `assets/presets/`. The Phase 24 loader contract is that missing-from-preset keys fall through to schema defaults via `tuning.load()`. If verification reveals this contract is broken, escalate — do NOT silently add the keys to presets.
  </action>
  <verify>
    <automated>python -c "import json; d = json.load(open('assets/physics-schema.json')); k = d['tuning']['slime_follow']; assert k['SLIME_MAX_FOLLOW_SPEED'] == 7.0 and k['SLIME_CATCHUP_CURVE_K'] == 0.5 and k['SLIME_LOOKAHEAD_FRAMES'] == 8 and k['SLIME_LOOKAHEAD_FALLBACK_BIAS'] == 4.0 and k['SLIME_LOOKAHEAD_EPSILON'] == 0.1 and k['SLIME_STUCK_WINDOW_FRAMES'] == 36 and k['SLIME_STUCK_RECOVERY_COOLDOWN'] == 30 and k['SLIME_FLOAT_GROUND_K_FRAMES'] == 12 and k['SLIME_MODE_HYSTERESIS_FRAMES'] == 6 and k['SLIME_FOLLOW_DELAY'] == 16, 'Phase 34 schema additions or existing keys mismatch'; print('schema OK')" && python -m pytest tests/test_tuning_migration.py -k phase34 -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import json; json.load(open('assets/physics-schema.json'))"` exits 0 (valid JSON)
    - `grep -c "SLIME_CATCHUP_CURVE_K\|SLIME_MAX_FOLLOW_SPEED\|SLIME_LOOKAHEAD_FRAMES\|SLIME_LOOKAHEAD_FALLBACK_BIAS\|SLIME_LOOKAHEAD_EPSILON\|SLIME_STUCK_WINDOW_FRAMES\|SLIME_STUCK_RECOVERY_COOLDOWN\|SLIME_FLOAT_GROUND_K_FRAMES\|SLIME_MODE_HYSTERESIS_FRAMES" assets/physics-schema.json` returns 9 (each key appears exactly once)
    - `grep -c "SLIME_FOLLOW_DELAY\|SLIME_MAX_DIST\|SLIME_REFORM_DIST\|SLIME_LERP_FACTOR" assets/physics-schema.json` returns 4 (existing keys preserved exactly once)
    - `python -m pytest tests/test_tuning_migration.py::test_phase34_slime_keys_readable -x -q` PASSES (all 9 parametrize cases)
    - `python -m pytest tests/test_tuning_migration.py::test_phase34_keys_in_flat_index -x -q` PASSES
    - `python -c "from src.core import tuning; tuning.load('_v1.3-reference'); print(tuning.SLIME_CATCHUP_CURVE_K)"` prints `0.5` (Pitfall 6 — schema default fills missing-from-preset key)
    - `python -m pytest tests/ -x -q` shows no NEW failures (only pre-existing RED stubs from Plan 01 entity-surface tests, which Plan 04 will fix)
  </acceptance_criteria>
  <done>9 new keys in slime_follow group with exact RESEARCH-recommended values; JSON valid; both Plan 01 schema-migration tests pass; v1.3 preset continues to load.</done>
</task>

</tasks>

<verification>
- All 9 new keys present + 4 existing keys preserved (grep counts above)
- JSON valid
- `tuning.SLIME_CATCHUP_CURVE_K == 0.5` after `tuning.load('_v1.3-reference')` — Pitfall 6 protection holds
- Plan 01's two schema-migration tests now pass
</verification>

<success_criteria>
1 file modified, ~10 lines added. Phase 34 tunables now reachable from `tuning.X` and from the live panel's Slime tab. Plan 04 can read all 9 keys without AttributeError. Plan 07 can tune them via the panel.
</success_criteria>

<output>
After completion, create `.planning/phases/34-slime-follow-ai-feel-pass/34-03-SUMMARY.md` recording the final 9 (key, value) tuples committed and a one-line confirmation that v1.3-reference preset still loads.
</output>
