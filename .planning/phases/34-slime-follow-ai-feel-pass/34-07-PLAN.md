---
phase: 34-slime-follow-ai-feel-pass
plan: 07
type: execute
wave: 4
depends_on: ["34-04", "34-05", "34-06"]
files_modified:
  - src/core/debug.py
  - .planning/phases/34-slime-follow-ai-feel-pass/34-FEEL-TARGETS.md
  - assets/presets/v2.0-default.json
autonomous: false
requirements: [SLM-04]
requirements_addressed: [SLM-04]
tags: [debug, feel-targets, preset, playtest, checkpoint, signoff]

must_haves:
  truths:
    - "Two new debug warp keys added: Ctrl+9 → Gym_WallSlide; Ctrl+0 → Gym_SlimeFollow (the new gym from Plan 06)"
    - "Existing Phase 33 warps (Ctrl+4..8) preserved unchanged — no regression to Phase 33 sign-off scenarios"
    - "All 5 S-* scenario buckets reach a real test gym from a hotkey: S-C/S-L (Ctrl+4 AccelRunway), S-M (Ctrl+5 GapTrio + Ctrl+6 HeightSteps + Ctrl+7 ZigzagShaft + Ctrl+9 WallSlide), S-S (Ctrl+0 Gym_SlimeFollow + Ctrl+7 ZigzagShaft)"
    - "34-FEEL-TARGETS.md Result column flipped from PENDING to PASS for every S-Cn / S-Sn / S-Mn / S-Ln / S-Pn row after user playtest sign-off"
    - "Final tuned values for the 9 new SLIME_* keys baked into assets/presets/v2.0-default.json (NOT _v1.3-reference.json which stays FROZEN)"
    - "v2.0-default preset still loads cleanly post-bake (panel reads it without errors)"
    - "Phase 34 closes with the Sign-off block in 34-FEEL-TARGETS.md filled in (date + user approval)"
  artifacts:
    - path: "src/core/debug.py"
      provides: "Two new module constants WARP_LEVEL_WALL_SLIDE='Gym_WallSlide' and WARP_LEVEL_SLIME_FOLLOW='Gym_SlimeFollow'; pyxel.btnp(KEY_9) and pyxel.btnp(KEY_0) handlers in update() setting warp_target accordingly"
    - path: ".planning/phases/34-slime-follow-ai-feel-pass/34-FEEL-TARGETS.md"
      provides: "All 13 S-* rows (S-C1-3, S-S1-3, S-M1-3, S-L1-2, S-P1-2) with Result column = PASS plus dated Sign-off block at bottom"
    - path: "assets/presets/v2.0-default.json"
      provides: "9 newly tuned values: SLIME_MAX_FOLLOW_SPEED, SLIME_CATCHUP_CURVE_K, SLIME_LOOKAHEAD_FRAMES, SLIME_LOOKAHEAD_FALLBACK_BIAS, SLIME_LOOKAHEAD_EPSILON, SLIME_STUCK_WINDOW_FRAMES, SLIME_STUCK_RECOVERY_COOLDOWN, SLIME_FLOAT_GROUND_K_FRAMES, SLIME_MODE_HYSTERESIS_FRAMES (final values from playtest, not necessarily the schema seeds)"
  key_links:
    - from: "src/core/debug.py:warp_target (Ctrl+0)"
      to: "Plan 06 Gym_SlimeFollow level"
      via: "string-flag consume + reset to None pattern (mirrors Phase 33 D-09)"
      pattern: "WARP_LEVEL_SLIME_FOLLOW"
    - from: "34-FEEL-TARGETS.md PASS sign-off"
      to: "assets/presets/v2.0-default.json"
      via: "PASS triggers preset bake at end-of-phase per Phase 29/33 pattern"
      pattern: "v2.0-default"
---

<objective>
Land the iterative tuning surface for Phase 34: extend `src/core/debug.py` with two additional warp hotkeys (Ctrl+9 Gym_WallSlide, Ctrl+0 Gym_SlimeFollow); iterate values via the live panel against the 13 S-* scenario rows in `34-FEEL-TARGETS.md`; sign off all rows; bake final values into `assets/presets/v2.0-default.json`. `_v1.3-reference.json` stays FROZEN.

Purpose: this is the human-in-the-loop feel pass. Plans 01-06 deliver the mechanism; Plan 07 delivers the *feel*. Without sign-off on 34-FEEL-TARGETS.md, success criterion #1 ("catches up across 10-tile gap within written frame budget"), #2 ("no longer permanently stuck"), and #3 ("all tuning reachable from live panel with smooth changes") cannot be claimed.

Output: 3 files modified. Two new warp constants + key handlers. 13 PASS markers + dated Sign-off in 34-FEEL-TARGETS.md. 9 baked values in v2.0-default.json.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/34-slime-follow-ai-feel-pass/34-CONTEXT.md
@.planning/phases/34-slime-follow-ai-feel-pass/34-RESEARCH.md
@.planning/phases/34-slime-follow-ai-feel-pass/34-PATTERNS.md
@.planning/phases/34-slime-follow-ai-feel-pass/34-FEEL-TARGETS.md
@.planning/phases/34-slime-follow-ai-feel-pass/34-04-SUMMARY.md
@.planning/phases/34-slime-follow-ai-feel-pass/34-05-SUMMARY.md
@.planning/phases/34-slime-follow-ai-feel-pass/34-06-SUMMARY.md
@.planning/phases/33-per-ability-feel-pass-drill-only-under-single-fusion-prototype/33-FEEL-TARGETS.md
@src/core/debug.py
@main.py
@assets/presets/v2.0-default.json
@assets/presets/_v1.3-reference.json

<interfaces>
<!-- Current src/core/debug.py warp surface (post-Phase-33):
- WARP_LEVEL_CRACKED_V    = "Gym_AccelRunway"   (Ctrl+4)
- WARP_LEVEL_SOFT_BLOCK   = "Gym_GapTrio"       (Ctrl+5)
- WARP_LEVEL_ENEMY_CLUSTER= "Gym_HeightSteps"   (Ctrl+6)
- WARP_LEVEL_JUICE_DRAIN  = "Gym_ZigzagShaft"   (Ctrl+7)
- WARP_LEVEL_BOSS         = "Level_15"          (Ctrl+8)

Phase 34 ADDS:
- WARP_LEVEL_WALL_SLIDE   = "Gym_WallSlide"     (Ctrl+9) — S-S1, S-M2 (wall-slide while player jumps)
- WARP_LEVEL_SLIME_FOLLOW = "Gym_SlimeFollow"   (Ctrl+0) — S-S2 forced-stuck pocket from Plan 06

Note: KEY_0 is the digit "0" key. pyxel exposes it as pyxel.KEY_0. Mirror the
Phase 33 pattern at debug.py:55-65 verbatim for the two new handlers.

Coverage map (every S-* row reaches a real gym from a hotkey):
- S-C1, S-C2, S-L1, S-L2  →  Ctrl+4 (Gym_AccelRunway)
- S-C3 (mid-air gap)       →  Ctrl+5 (Gym_GapTrio)
- S-M1 (jump→float)        →  Ctrl+4 or Ctrl+6 (any gym with flat ground)
- S-M2 (land→ground)       →  Ctrl+9 (Gym_WallSlide) or Ctrl+6 (Gym_HeightSteps)
- S-M3 (anti-oscillation)  →  Ctrl+4 (rapid-jump pattern works anywhere)
- S-S1 (random terrain)    →  Ctrl+7 (Gym_ZigzagShaft) + Ctrl+9 (Gym_WallSlide)
- S-S2 (forced-stuck pocket)→ Ctrl+0 (Gym_SlimeFollow — Plan 06)
- S-S3 (vertical-slice)    →  Game start (Room 0 → boss; Ctrl+8 if needed)
- S-P1, S-P2 (panel)       →  any gym (panel-based, scenario-agnostic)

D-10 layered tuning order (recommended for Plan 07 playtest):
1. Catch-up curve (S-C, S-L) — tune SLIME_CATCHUP_CURVE_K + SLIME_MAX_FOLLOW_SPEED
   first; lookahead bias (SLIME_LOOKAHEAD_FRAMES + FALLBACK_BIAS + EPSILON) next.
2. Mode FSM (S-M) — tune SLIME_FLOAT_GROUND_K_FRAMES + SLIME_MODE_HYSTERESIS_FRAMES.
3. Stuck recovery (S-S) — tune SLIME_STUCK_WINDOW_FRAMES + SLIME_STUCK_RECOVERY_COOLDOWN.
4. Panel smoothness (S-P) — sweep every slider through full range.

Final preset bake target: assets/presets/v2.0-default.json (alias v2.0-default,
backed by slot_1.json per Phase 33 sign-off note). Do NOT modify
_v1.3-reference.json — it stays FROZEN per Phase 24 contract.
-->
</interfaces>
</context>

<threat_model>
| Boundary | Description |
|----------|-------------|
| (none) | Debug-key extension + preset-file edit + doc update in offline single-player game. No untrusted input. |

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-07-N/A | — | src/core/debug.py, assets/presets/v2.0-default.json, FEEL-TARGETS.md | accept | N/A: developer-authored debug + preset + doc edits in offline single-player Pyxel game; no auth/network/persistence boundary. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Ctrl+9 / Ctrl+0 warp handlers to src/core/debug.py</name>
  <files>src/core/debug.py</files>
  <read_first>
    - src/core/debug.py (full file — 65 lines; confirm Ctrl+8 boss handler at L64-65 is the most recent; mirror that pattern for Ctrl+9 and Ctrl+0)
    - .planning/phases/33-per-ability-feel-pass-drill-only-under-single-fusion-prototype/33-06-debug-warps-tuning-feel-targets-PLAN.md (the canonical predecessor pattern — read its Task 1 implementation to match style and comment density)
    - main.py:696-705 (debug.warp_target consumer — confirm it switches on the level-id string and works for any new level identifier without code changes; if Gym_WallSlide does not exist as a level, the warp will silently no-op which is acceptable)
    - assets/output/simplified/Gym_WallSlide/data.json (verify this gym exists; if it does not, fail back to closest analog and document the choice in this plan's SUMMARY)
  </read_first>
  <action>
    Modify `src/core/debug.py` with these EXACT additions:

    **1. Add two new module constants** in the constants block (currently L35-39, after `WARP_LEVEL_BOSS`):
    ```python
    WARP_LEVEL_WALL_SLIDE = "Gym_WallSlide"       # Phase 34 S-S1/S-M2 wall-slide gym
    WARP_LEVEL_SLIME_FOLLOW = "Gym_SlimeFollow"   # Phase 34 S-S2 forced-stuck pocket (Plan 06)
    ```

    **2. Add two new key handlers** at the end of the `update()` function (currently L42-65). Insert AFTER the existing Ctrl+8 boss handler at L64-65:
    ```python
            # Phase 34: slime-follow / stuck-recovery test gyms.
            if pyxel.btnp(pyxel.KEY_9):
                warp_target = WARP_LEVEL_WALL_SLIDE
            if pyxel.btnp(pyxel.KEY_0):
                warp_target = WARP_LEVEL_SLIME_FOLLOW
    ```

    No new function. No new imports. No new globals (warp_target is already declared `global` at L45). The main.py consumer at L696-705 (Phase 33 D-09) handles any level-id string generically — no main.py change required.

    Verify Gym_WallSlide exists by running `ls assets/output/simplified/Gym_WallSlide/`. If it does NOT exist, replace `WARP_LEVEL_WALL_SLIDE = "Gym_WallSlide"` with the closest available wall-slide-like gym from `ls assets/output/simplified/` and document the substitution in 34-07-SUMMARY.md.
  </action>
  <verify>
    <automated>python -c "import src.core.debug as d; assert d.WARP_LEVEL_WALL_SLIDE == 'Gym_WallSlide' and d.WARP_LEVEL_SLIME_FOLLOW == 'Gym_SlimeFollow'; print('warp constants OK')" && python -m pytest tests/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "WARP_LEVEL_WALL_SLIDE\|WARP_LEVEL_SLIME_FOLLOW" src/core/debug.py | wc -l` returns at least 4 (each constant defined once + each used once in the key handler)
    - `grep -nE "pyxel\.KEY_9|pyxel\.KEY_0" src/core/debug.py | wc -l` returns at least 2 (both handlers present)
    - `grep -c "WARP_LEVEL_BOSS\|WARP_LEVEL_CRACKED_V\|WARP_LEVEL_SOFT_BLOCK\|WARP_LEVEL_ENEMY_CLUSTER\|WARP_LEVEL_JUICE_DRAIN" src/core/debug.py` returns at least 5 (Phase 33 constants preserved)
    - `python -c "import src.core.debug as d; print(d.WARP_LEVEL_WALL_SLIDE, d.WARP_LEVEL_SLIME_FOLLOW)"` prints `Gym_WallSlide Gym_SlimeFollow`
    - `python -m pytest tests/ -x -q` shows zero failures (all Plan 01 tests still GREEN; debug.py change is import-time only)
    - `ls assets/output/simplified/Gym_WallSlide/` and `ls assets/output/simplified/Gym_SlimeFollow/` both succeed (gym levels exist as targets)
  </acceptance_criteria>
  <done>Two new constants + two new key handlers added; existing Phase 33 warps preserved; both gym targets exist; full suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User playtest tuning loop — D-10 layered order; flip 13 S-* rows to PASS</name>
  <files>.planning/phases/34-slime-follow-ai-feel-pass/34-FEEL-TARGETS.md, assets/presets/v2.0-default.json</files>
  <action>
    HUMAN-DRIVEN PLAYTEST + DOC SIGN-OFF + PRESET BAKE. Agent cannot subjectively assess "feel". User boots game, F1 panel + F5 overlay, follows the D-10 layered tuning order in <how-to-verify>, flips 13 S-* rows in 34-FEEL-TARGETS.md from PENDING to PASS, bakes 9 final tuned values into assets/presets/v2.0-default.json. See <how-to-verify> for exact step-by-step.
  </action>
  <what-built>Plans 01-06 + Plan 07 Task 1 deliver: extended Slime entity surface, sqrt catch-up curve, lookahead bias, mode FSM, stuck-recovery, F5 overlay AI surfaces, schema tunables, Gym_SlimeFollow placeholder, Ctrl+9/Ctrl+0 warp hotkeys. The mechanism is in. Plan 07 Task 2 is the human-in-the-loop tuning pass that turns mechanism into FEEL.</what-built>
  <how-to-verify>
    EXACT STEPS:

    1. **Boot the game** with the v2.0-default preset active. F1 to toggle the panel; click the **Slime** tab. All 9 new SLIME_* keys (SLIME_MAX_FOLLOW_SPEED, SLIME_CATCHUP_CURVE_K, SLIME_LOOKAHEAD_FRAMES, SLIME_LOOKAHEAD_FALLBACK_BIAS, SLIME_LOOKAHEAD_EPSILON, SLIME_STUCK_WINDOW_FRAMES, SLIME_STUCK_RECOVERY_COOLDOWN, SLIME_FLOAT_GROUND_K_FRAMES, SLIME_MODE_HYSTERESIS_FRAMES) should be visible as sliders. **If any are missing, STOP — Plan 03 schema seed has not loaded; revisit before proceeding.**

    2. **F5 ON** to enable the slime overlay. You should see (per Plan 05): mode glyph (F or G) above the slime; lookahead bias arrow (white) in player.dx direction; stuck-countdown bar (red, only when stuck>window/2).

    3. **Tune in D-10 layered order:**
       - **Catch-up (S-C, S-L)** — Ctrl+4 (AccelRunway). Walk away 10 tiles, hold-still: confirm slime closes the gap in ≤60 frames (S-C1). Walk and stop suddenly (S-C2). Stand still and observe lookahead arrow (S-L1, S-L2). Adjust SLIME_CATCHUP_CURVE_K (around 0.50 starting), SLIME_MAX_FOLLOW_SPEED (around 7.0), SLIME_LOOKAHEAD_FRAMES (around 8), SLIME_LOOKAHEAD_FALLBACK_BIAS (around 4.0), SLIME_LOOKAHEAD_EPSILON (around 0.1) until S-C and S-L rows pass.
       - **Mode FSM (S-M)** — Ctrl+4 (jump→float promotion S-M1), Ctrl+6/Ctrl+9 (land→ground demotion S-M2 — confirm slime visibly demotes when ground is reachable; mode glyph flips F→G), rapid-jump-in-place at any gym (S-M3 anti-oscillation). Adjust SLIME_FLOAT_GROUND_K_FRAMES (around 12) and SLIME_MODE_HYSTERESIS_FRAMES (around 6) until S-M rows pass.
       - **Stuck-recovery (S-S)** — Ctrl+7 (ZigzagShaft) for natural-terrain S-S1; Ctrl+0 (Gym_SlimeFollow) for forced-stuck S-S2 (player teleports into the sealed pocket — slime gets stuck at the wall, glow-fade fires, slime reappears inside near player). Full v1.0 vertical-slice playthrough for S-S3. Adjust SLIME_STUCK_WINDOW_FRAMES (around 36) and SLIME_STUCK_RECOVERY_COOLDOWN (around 30) until S-S rows pass.
       - **Panel smoothness (S-P)** — drag every slime_follow slider through its full schema range during gameplay; mid-flight tunable change (S-P2).

    4. **Flip Result column to PASS** in `.planning/phases/34-slime-follow-ai-feel-pass/34-FEEL-TARGETS.md` for each row that passes its falsifiable Pass condition. If a row FAILS, document the failure in the row and either fix in this checkpoint loop or escalate (a fix to Plan 04 entity surface may be needed; a fix to Plan 05 overlay alone may be needed; a new schema key not anticipated in Plan 03 may be needed → escalate to plan-revision).

    5. **Bake final values into `assets/presets/v2.0-default.json`**. The preset file is a flat-key JSON dict. Add these 9 keys with the values you settled on during tuning:
       - SLIME_MAX_FOLLOW_SPEED
       - SLIME_CATCHUP_CURVE_K
       - SLIME_LOOKAHEAD_FRAMES
       - SLIME_LOOKAHEAD_FALLBACK_BIAS
       - SLIME_LOOKAHEAD_EPSILON
       - SLIME_STUCK_WINDOW_FRAMES
       - SLIME_STUCK_RECOVERY_COOLDOWN
       - SLIME_FLOAT_GROUND_K_FRAMES
       - SLIME_MODE_HYSTERESIS_FRAMES

       If a value matches the schema seed exactly, you may still bake it (explicit > implicit). If `assets/presets/slot_1.json` is the active backing file (per Phase 33 alias), bake to that file too if your panel writes to slot_1; the panel's autosave journal should handle this.

    6. **Verify**: re-load v2.0-default preset (panel "Reset to v2.0-default" or similar). Confirm the slime still feels right post-bake — no regression from your final tuning session.

    7. **Fill in the Sign-off block** at the bottom of `34-FEEL-TARGETS.md` with the date and a one-line approval. Mirror Phase 33 sign-off format.

    DO NOT modify `_v1.3-reference.json` (it stays FROZEN per Phase 24 contract; Plan 33 verified test_v1_3_reference_preset_remains_frozen).
  </how-to-verify>
  <resume-signal>Type "approved" once: (a) all 13 S-* rows in 34-FEEL-TARGETS.md show Result=PASS, (b) v2.0-default.json contains the 9 baked values, (c) Sign-off block is filled in with date. If any row failed, describe which and what fix is needed (this may trigger a plan-revision cycle for Plan 04 or Plan 05).</resume-signal>
  <verify>
    <automated>python -c "import json; d=json.load(open('assets/presets/v2.0-default.json')); keys=['SLIME_MAX_FOLLOW_SPEED','SLIME_CATCHUP_CURVE_K','SLIME_LOOKAHEAD_FRAMES','SLIME_LOOKAHEAD_FALLBACK_BIAS','SLIME_LOOKAHEAD_EPSILON','SLIME_STUCK_WINDOW_FRAMES','SLIME_STUCK_RECOVERY_COOLDOWN','SLIME_FLOAT_GROUND_K_FRAMES','SLIME_MODE_HYSTERESIS_FRAMES']; missing=[k for k in keys if k not in d]; assert not missing, f'unbaked: {missing}'; print('9 baked')" && python -m pytest tests/ -x -q</automated>
  </verify>
  <done>13 S-* rows PASS in 34-FEEL-TARGETS.md; 9 final values baked into v2.0-default.json; v1.3-reference.json unchanged; Sign-off dated; full pytest suite green.</done>
</task>

</tasks>

<verification>
- `grep -c "PASS\|PENDING" .planning/phases/34-slime-follow-ai-feel-pass/34-FEEL-TARGETS.md` shows PASS count ≥ 13 and PENDING count = 0 in the result-column rows (header text may still say PENDING — only the table Result columns matter)
- `python -c "import json; d = json.load(open('assets/presets/v2.0-default.json')); keys = ['SLIME_MAX_FOLLOW_SPEED', 'SLIME_CATCHUP_CURVE_K', 'SLIME_LOOKAHEAD_FRAMES', 'SLIME_LOOKAHEAD_FALLBACK_BIAS', 'SLIME_LOOKAHEAD_EPSILON', 'SLIME_STUCK_WINDOW_FRAMES', 'SLIME_STUCK_RECOVERY_COOLDOWN', 'SLIME_FLOAT_GROUND_K_FRAMES', 'SLIME_MODE_HYSTERESIS_FRAMES']; missing = [k for k in keys if k not in d]; assert not missing, f'missing: {missing}'; print('all 9 baked')"` exits 0
- `python -c "import json; d = json.load(open('assets/presets/_v1.3-reference.json')); slime_phase34 = ['SLIME_CATCHUP_CURVE_K', 'SLIME_LOOKAHEAD_FRAMES', 'SLIME_STUCK_WINDOW_FRAMES']; bad = [k for k in slime_phase34 if k in d]; assert not bad, f'v1.3-reference must NOT contain Phase 34 keys: {bad}'; print('v1.3-reference frozen OK')"` exits 0
- `python -m pytest tests/ -x -q` shows zero failures (full suite green at phase close)
- `grep -n "Sign-off" .planning/phases/34-slime-follow-ai-feel-pass/34-FEEL-TARGETS.md` returns the populated Sign-off block (not just the heading)
</verification>

<success_criteria>
- 13 S-* rows in 34-FEEL-TARGETS.md = PASS
- 9 tuned values baked into v2.0-default.json (NOT v1.3-reference)
- Two new debug warp hotkeys (Ctrl+9, Ctrl+0) active
- Sign-off block filled in with date
- Full pytest suite green
- Phase 34 ready for /gsd-verify-work and milestone advancement
</success_criteria>

<output>
After completion, create `.planning/phases/34-slime-follow-ai-feel-pass/34-07-SUMMARY.md` recording:
- The final 9 tuned values (one row per key with starting seed → tuned value)
- Any S-* row that required iteration (which schema key changed and why)
- Confirmation that v1.3-reference.json was NOT modified
- Confirmation that all 13 S-* rows PASSED and Sign-off is dated
- Any deviation from the Plan 06 placeholder (e.g. user finalized a 3x3 pocket instead of 2x2 — record it for downstream context)
</output>
