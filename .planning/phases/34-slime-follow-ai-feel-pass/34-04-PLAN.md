---
phase: 34-slime-follow-ai-feel-pass
plan: 04
type: execute
wave: 2
depends_on: ["34-02", "34-03"]
files_modified:
  - src/entities/slime.py
  - main.py
autonomous: true
requirements: [SLM-04]
requirements_addressed: [SLM-04]
tags: [entity, slime, ai, mode-fsm, lookahead, stuck-recovery]

must_haves:
  truths:
    - "Slime.update() signature accepts player_dx and player_is_grounded as new explicit kwargs (Pitfall 1)"
    - "main.py call site at L875 passes player.dx and player.is_grounded explicitly (no Player ref)"
    - "Sqrt ease-out catch-up curve replaces the linear MAX_SHADOW_SPEED clamp (per D-09)"
    - "Lookahead bias adds player.dx * SLIME_LOOKAHEAD_FRAMES to target_x (per D-11), with facing-direction fallback when |player.dx| < SLIME_LOOKAHEAD_EPSILON"
    - "Float↔ground mode FSM updates slime.mode_is_float each frame with hysteresis (per D-08)"
    - "Stuck detection counts no-progress frames (only when dist > SLIME_REFORM_DIST per Pitfall 5); fires reposition_with_fade after SLIME_STUCK_WINDOW_FRAMES"
    - "reposition_with_fade() reuses dissipate_timer arc but does NOT reset juice (D-10 no-fault)"
    - "Stuck-recovery target sampled from self.history deque midpoint (Open Q #3 — Option c)"
    - "Stuck detection runs BEFORE the existing reform-distance check (Pitfall 2 ordering)"
    - "Existing path-follow base preserved (D-03): self.history.append + history-deque read still happen each frame"
  artifacts:
    - path: "src/entities/slime.py"
      provides: "Extended Slime class with mode_is_float/mode_frames_in_state/stuck_frames/_stuck_recovery_target instance attrs; reposition_with_fade(); update_stuck_recovery() (or unified update_dissipation()); _lookahead_target() helper; _update_mode() helper; sqrt-curve catch-up replacing linear clamp; extended update() signature with player_dx + player_is_grounded kwargs"
    - path: "main.py"
      provides: "Updated self.slime.update() call site at L875 passing player_dx=self.player.dx and player_is_grounded=self.player.is_grounded"
  key_links:
    - from: "main.py L875 slime.update call"
      to: "src/entities/slime.py Slime.update signature"
      via: "explicit kwargs player_dx + player_is_grounded threaded each frame"
      pattern: "player_dx=|player_is_grounded="
    - from: "src/entities/slime.py Slime.update (stuck detection)"
      to: "src/entities/slime.py Slime.reposition_with_fade"
      via: "stuck_frames counter triggers fade-in reposition when window exceeded"
      pattern: "reposition_with_fade"
    - from: "src/entities/slime.py update_stuck_recovery"
      to: "src/entities/slime.py self.history (deque)"
      via: "stuck-recovery target = history[len//2] (Open Q #3 Option c)"
      pattern: "self\\.history\\[.*//.*2\\]|len.*self\\.history.*//"
---

<objective>
Land the Phase 34 entity surface in `src/entities/slime.py` per RESEARCH Patterns 1-4: sqrt ease-out catch-up curve (D-09), positional-offset lookahead bias (D-11), hybrid float↔ground mode FSM (D-08), stuck detection + glow-fade reposition (D-10). Thread `player.dx` and `player.is_grounded` through `slime.update()` from main.py per Pitfall 1.

Purpose: this is the gameplay surface for Phase 34. Plans 01-03 set up tests and tunables; Plan 05 extends the overlay to visualize what this plan does; Plan 07 tunes the values via the panel. Without this plan, the slime feels identical to v1.3.

Output: 2 files modified. ~80-100 lines added to slime.py (across __init__, update signature, sqrt curve, lookahead helper, mode FSM helper, stuck detection block, reposition_with_fade helper, update_stuck_recovery discriminator). 1 line modified in main.py. 9 of Plan 01's RED tests turn GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/34-slime-follow-ai-feel-pass/34-CONTEXT.md
@.planning/phases/34-slime-follow-ai-feel-pass/34-RESEARCH.md
@.planning/phases/34-slime-follow-ai-feel-pass/34-PATTERNS.md
@.planning/phases/34-slime-follow-ai-feel-pass/34-VALIDATION.md
@.planning/phases/34-slime-follow-ai-feel-pass/34-02-SUMMARY.md
@.planning/phases/34-slime-follow-ai-feel-pass/34-03-SUMMARY.md
@src/entities/slime.py
@main.py
@tests/test_slime.py

<interfaces>
<!-- Slime instance attrs to ADD in __init__ (RESEARCH PATTERNS L48-55):

# Phase 34 AI surfaces (D-08 mode FSM, D-10 stuck recovery, D-11 lookahead)
self.mode_is_float = True              # D-08: True=float, False=ground
self.mode_frames_in_state = 0          # D-08 hysteresis counter
self.stuck_frames = 0                  # D-10 consecutive no-progress frames
self._stuck_recovery_target = None     # D-10 (x, y) for fade-in reposition
self._last_pos = (self.x, self.y)      # D-10 stuck-detection prev-pos
-->

<!-- Slime.update NEW signature (Pitfall 1 + Open Q #2):

def update(self, player_x, player_y, player_facing_right, level_map,
           is_fused=False, player_dx=0.0, player_is_grounded=True):
    # Existing dissipated/recall/fused early-outs PRESERVED.
    # Existing passive juice regen PRESERVED.
    # NEW: lookahead-biased target via _lookahead_target()
    # NEW: float↔ground mode FSM via _update_mode()
    # NEW: sqrt ease-out catch-up curve REPLACES linear clamp
    # NEW: stuck detection BEFORE reform-distance check (Pitfall 2)
-->

<!-- main.py L875 — current call site:
self.slime.update(self.player.x, self.player.y, self.player.facing_right, self.level_map, self.player.is_fused)

Phase 34 form (RESEARCH PATTERNS L165-171):
self.slime.update(
    self.player.x, self.player.y, self.player.facing_right,
    self.level_map, self.player.is_fused,
    player_dx=self.player.dx,
    player_is_grounded=self.player.is_grounded,
)
-->

<!-- Sqrt curve replacement (Pattern 1, RESEARCH L219-237):
target_x, target_y = self._lookahead_target(player_x, player_y, player_dx, player_facing_right)
delta_x = target_x - self.x
delta_y = target_y - self.y
dist = (delta_x * delta_x + delta_y * delta_y) ** 0.5
if dist > 0.0:
    speed = tuning.SLIME_CATCHUP_CURVE_K * (dist ** 0.5)
    speed = min(speed, tuning.SLIME_MAX_FOLLOW_SPEED)
    self.dx = (delta_x / dist) * speed
    self.dy = (delta_y / dist) * speed
else:
    self.dx = 0.0
    self.dy = 0.0
# REPLACES the L151-159 block (target-x = ..., MAX_SHADOW_SPEED clamp)
# DELETE the literal MAX_SHADOW_SPEED = 4.0 line — promoted to schema per D-05.
-->

<!-- Lookahead helper (Pattern 2 positional-offset variant, RESEARCH L274-287):
def _lookahead_target(self, player_x, player_y, player_dx, player_facing_right):
    if len(self.history) >= tuning.SLIME_FOLLOW_DELAY:
        base_x, base_y = self.history[0]   # PRESERVED deque read at original L149
    else:
        base_x, base_y = player_x, player_y
    if abs(player_dx) >= tuning.SLIME_LOOKAHEAD_EPSILON:
        bias_x = player_dx * tuning.SLIME_LOOKAHEAD_FRAMES
    else:
        sign = 1.0 if player_facing_right else -1.0
        bias_x = sign * tuning.SLIME_LOOKAHEAD_FALLBACK_BIAS
    self.target_x = base_x + bias_x
    self.target_y = base_y
    return self.target_x, self.target_y
-->

<!-- Mode FSM (Pattern 4, RESEARCH L364-380):
def _can_reach_tile_in_k_frames(self, level_map, k_frames):
    probe_y = self.y + tuning.SLIME_MAX_FOLLOW_SPEED * k_frames
    return level_map.check_collision(self.x, probe_y, self.w, self.h)

def _update_mode(self, player_is_grounded, level_map):
    desired_float = (not player_is_grounded) or (
        not self._can_reach_tile_in_k_frames(level_map, tuning.SLIME_FLOAT_GROUND_K_FRAMES)
    )
    if desired_float != self.mode_is_float:
        if self.mode_frames_in_state >= tuning.SLIME_MODE_HYSTERESIS_FRAMES:
            self.mode_is_float = desired_float
            self.mode_frames_in_state = 0
        else:
            self.mode_frames_in_state += 1
    else:
        self.mode_frames_in_state += 1

# Note: float-mode currently does not change movement — RESEARCH says hybrid
# state handles "don't get blocked" implicitly via collision push-out, and
# explicit terrain-reactive nav is deferred (D-12). The flag drives overlay
# (Plan 05) and S-C3 mid-air gap test (slime promoting to float-mode while
# player is airborne is sufficient — no separate float-physics yet).
-->

<!-- reposition_with_fade + update_stuck_recovery (Pattern 3, RESEARCH L300-353):
def reposition_with_fade(self, target_x, target_y, player_facing_right, level_map):
    """D-10 stuck-recovery: glow-fade out, reposition, glow-fade in.
    Reuses dissipate/reform SF6-burnout primitive WITHOUT resetting juice."""
    self._stuck_recovery_target = (target_x, target_y)
    self.is_dissipated = True
    self.dissipate_timer = tuning.SLIME_STUCK_RECOVERY_COOLDOWN
    self.recall_trail.clear()
    # NOTE: do NOT reset juice. dissipate() at slime.py:79-86 sets juice=full as
    # punishment-mitigation for fused-burnout; stuck-recovery is no-fault.

def update_stuck_recovery(self, player_x, player_y, player_facing_right, level_map):
    """Tick stuck-recovery dissipation; reform at target on completion.
    Discriminator: if _stuck_recovery_target is set, this is a stuck-recovery
    (no juice reset); else fall through to existing fused-burnout reform."""
    if not self.is_dissipated:
        return False
    self.dissipate_timer -= 1
    if self.dissipate_timer <= 0:
        self.is_dissipated = False
        target = getattr(self, "_stuck_recovery_target", None)
        if target is not None:
            self.x, self.y = target
            self.dx = 0
            self.dy = 0
            self.target_x, self.target_y = target
            self.history.clear()
            self._stuck_recovery_target = None
        else:
            self.juice = self.max_juice
            self.reform(player_x, player_y, player_facing_right, level_map)
        return True
    return False

# CHANGE the existing update() early-out at slime.py:104-107: replace
# self.update_dissipation(...) with self.update_stuck_recovery(...). The
# new method handles BOTH cases via the _stuck_recovery_target discriminator.
# Optionally delete update_dissipation() — but keeping it as a thin wrapper
# that calls update_stuck_recovery is also acceptable for backward compat.
-->

<!-- Stuck detection (Pattern 3, ordering per Pitfall 2 + Pitfall 5):
# Run AFTER lookahead-target compute and AFTER catch-up movement,
# BEFORE the reform-distance check (Pitfall 2).
dist_to_target = ((self.target_x - self.x) ** 2 + (self.target_y - self.y) ** 2) ** 0.5
moved = ((self.x - self._last_pos[0]) ** 2 + (self.y - self._last_pos[1]) ** 2) ** 0.5
if dist_to_target > tuning.SLIME_REFORM_DIST and moved < 0.1:
    self.stuck_frames += 1
else:
    self.stuck_frames = 0
self._last_pos = (self.x, self.y)

if self.stuck_frames >= tuning.SLIME_STUCK_WINDOW_FRAMES:
    # Sample target from history deque midpoint (Open Q #3 Option c).
    if self.history:
        target = self.history[len(self.history) // 2]
    else:
        target = (player_x, player_y)
    self.reposition_with_fade(target[0], target[1], player_facing_right, level_map)
    self.stuck_frames = 0
    return  # Skip reform-distance check this frame
-->
</interfaces>
</context>

<threat_model>
| Boundary | Description |
|----------|-------------|
| (none) | Entity-code edits in offline single-player game. No untrusted input. |

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-04-N/A | — | src/entities/slime.py, main.py | accept | N/A: gameplay code in offline single-player Pyxel game; no auth/network/persistence boundary. Mode FSM and stuck recovery are pure entity-state mutations. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend Slime.__init__ + add helper methods (lookahead, mode, stuck-recovery)</name>
  <files>src/entities/slime.py</files>
  <read_first>
    - src/entities/slime.py (full file post-Plan-02 strip — confirm current line numbers; expected: __init__ ends around L36 (post-strip), `update()` starts where `dissipate()`/`update_dissipation()` end, around L80)
    - .planning/phases/34-slime-follow-ai-feel-pass/34-RESEARCH.md (Patterns 1-4 sections; Open Questions #1-4 for design rationale; Pitfalls 1, 2, 5, 8 for guard logic)
    - .planning/phases/34-slime-follow-ai-feel-pass/34-PATTERNS.md (slime.py extension recipes at lines ~25-115)
    - assets/physics-schema.json (confirm Plan 03 keys are present and readable)
    - tests/test_slime.py (the 11 RED tests are the executable spec — implementation must satisfy each one verbatim)
  </read_first>
  <action>
    Make these EXACT edits to `src/entities/slime.py`:

    **1. Extend `__init__`** (after `self.is_being_absorbed = False` line, before/after the post-strip end of __init__):

    Add the Phase 34 instance attrs block (verbatim per RESEARCH PATTERNS L48-55):
    ```python
            # Phase 34 AI surfaces (D-08 mode FSM, D-10 stuck recovery, D-11 lookahead)
            self.mode_is_float = True              # D-08: True=float, False=ground
            self.mode_frames_in_state = 0          # D-08 hysteresis counter
            self.stuck_frames = 0                  # D-10 consecutive no-progress frames
            self._stuck_recovery_target = None     # D-10 (x, y) for fade-in reposition
            self._last_pos = (self.x, self.y)      # D-10 stuck-detection prev-pos
    ```

    **2. Add `_lookahead_target` helper method** (Pattern 2 positional-offset variant per RESEARCH L274-287, verbatim block in <interfaces> above). Place after `update_dissipation` (or after the new `update_stuck_recovery` if you reorganize). Sets `self.target_x` and `self.target_y` as side-effect AND returns the tuple.

    **3. Add `_can_reach_tile_in_k_frames` and `_update_mode` helpers** (Pattern 4 per RESEARCH L364-380, verbatim block in <interfaces>). The mode flag does NOT change movement physics in Phase 34 (D-12 defers terrain-reactive nav); the flag exists for the overlay (Plan 05) and as instrumentation for S-M test scenarios.

    **4. Add `reposition_with_fade` and `update_stuck_recovery` helpers** (Pattern 3 per RESEARCH L300-336, verbatim block in <interfaces>).

    **5. Modify the existing `update_dissipation` method**: leave it intact OR replace its body with `return self.update_stuck_recovery(player_x, player_y, player_facing_right, level_map)` so existing callers (the dissipated early-out at the top of update()) work unchanged. Choose the wrapper approach for minimal blast radius.

    DO NOT remove `move_and_collide` (slime.py:184-215 in the post-Plan-02 file) — it has no live callers right now, but it is an existing public method. If 100% confident no caller exists, may strip; if any doubt, leave alone (out of scope).
  </action>
  <verify>
    <automated>python -c "from src.entities.slime import Slime; s = Slime(0, 0); assert hasattr(s, 'mode_is_float') and hasattr(s, 'mode_frames_in_state') and hasattr(s, 'stuck_frames') and hasattr(s, '_stuck_recovery_target') and hasattr(s, '_last_pos') and hasattr(s, 'reposition_with_fade') and hasattr(s, 'update_stuck_recovery') and hasattr(s, '_lookahead_target') and hasattr(s, '_update_mode') and hasattr(s, '_can_reach_tile_in_k_frames'); print('attrs+helpers OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "self\.(mode_is_float|mode_frames_in_state|stuck_frames|_stuck_recovery_target|_last_pos)" src/entities/slime.py | grep -v '^#' | wc -l` returns at least 5 (each attr initialized in __init__)
    - `grep -nE "def (_lookahead_target|_can_reach_tile_in_k_frames|_update_mode|reposition_with_fade|update_stuck_recovery)" src/entities/slime.py | grep -v '^#' | wc -l` returns 5 (all five new helpers defined)
    - `grep -n "tuning.SLIME_CATCHUP_CURVE_K\|tuning.SLIME_MAX_FOLLOW_SPEED\|tuning.SLIME_LOOKAHEAD_FRAMES\|tuning.SLIME_LOOKAHEAD_EPSILON\|tuning.SLIME_LOOKAHEAD_FALLBACK_BIAS\|tuning.SLIME_FLOAT_GROUND_K_FRAMES\|tuning.SLIME_MODE_HYSTERESIS_FRAMES\|tuning.SLIME_STUCK_RECOVERY_COOLDOWN" src/entities/slime.py | wc -l` returns at least 8 (each new schema key referenced at least once)
    - The construction `python -c "from src.entities.slime import Slime; s = Slime(0,0); s.reposition_with_fade(50, 50, True, None); assert s.is_dissipated and s.dissipate_timer > 0 and s._stuck_recovery_target == (50, 50); print('helper OK')"` exits 0
    - `python -m pytest tests/test_slime.py::test_punt_attrs_stripped -x -q` STILL passes (no regression from Plan 02)
  </acceptance_criteria>
  <done>__init__ extended with 5 new attrs; 5 new helper methods present; all reference Plan-03 schema keys; `Slime(0,0)` constructs without error; `reposition_with_fade` correctly stashes target without resetting juice.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend Slime.update signature + replace linear clamp with sqrt curve + integrate mode/stuck/lookahead</name>
  <files>src/entities/slime.py, main.py</files>
  <read_first>
    - src/entities/slime.py (after Task 1 — confirm helpers are in place; locate the current `update()` method; identify the linear MAX_SHADOW_SPEED block to replace)
    - main.py (find `self.slime.update(` call site — Bash grep confirmed it is at L875 pre-edit; may have shifted slightly post-Plan-02)
    - .planning/phases/34-slime-follow-ai-feel-pass/34-RESEARCH.md (Pattern 1 sqrt curve at L219-237; Pitfalls 1, 2, 5, 8; Open Q #2 explicit-primitives recommendation)
    - tests/test_slime.py (RED tests — every test calls update with `player_dx=` and `player_is_grounded=` kwargs; signature MUST accept these as named keyword args with defaults so existing callers do not break)
  </read_first>
  <action>
    **1. Modify `Slime.update` signature** in src/entities/slime.py:

    From:
    ```python
    def update(self, player_x, player_y, player_facing_right, level_map, is_fused=False):
    ```
    To:
    ```python
    def update(self, player_x, player_y, player_facing_right, level_map,
               is_fused=False, player_dx=0.0, player_is_grounded=True):
    ```

    The new kwargs have defaults (0.0 and True) so that any other caller — if it exists — continues to work. This satisfies the RED test contract from Plan 01 (every test calls update with these kwargs).

    **2. Update the dissipated early-out** (currently `self.update_dissipation(...)`):

    Change to `self.update_stuck_recovery(player_x, player_y, player_facing_right, level_map)`. (If you chose the wrapper approach in Task 1, no change needed — but the call now must dispatch through the unified discriminator.)

    **3. Replace the linear-clamp catch-up block with the sqrt ease-out curve.**

    REMOVE the existing block (post-Plan-02 line numbers approximate):
    ```python
        if len(self.history) >= tuning.SLIME_FOLLOW_DELAY:
            self.target_x, self.target_y = self.history[0]
        self.dx = self.target_x - self.x
        self.dy = self.target_y - self.y
        # 4.0 is faster than player's max speed (2.5)
        MAX_SHADOW_SPEED = 4.0
        self.dx = max(-MAX_SHADOW_SPEED, min(self.dx, MAX_SHADOW_SPEED))
        self.dy = max(-MAX_SHADOW_SPEED, min(self.dy, MAX_SHADOW_SPEED))
    ```

    REPLACE with (per RESEARCH Pattern 1, also includes lookahead-target call):
    ```python
        # NEW: lookahead-biased target (D-11). _lookahead_target sets self.target_x/y.
        target_x, target_y = self._lookahead_target(
            player_x, player_y, player_dx, player_facing_right
        )
        # NEW: sqrt ease-out catch-up curve (D-09). Replaces linear MAX_SHADOW_SPEED clamp.
        delta_x = target_x - self.x
        delta_y = target_y - self.y
        dist = (delta_x * delta_x + delta_y * delta_y) ** 0.5
        if dist > 0.0:
            speed = tuning.SLIME_CATCHUP_CURVE_K * (dist ** 0.5)
            speed = min(speed, tuning.SLIME_MAX_FOLLOW_SPEED)
            self.dx = (delta_x / dist) * speed
            self.dy = (delta_y / dist) * speed
        else:
            self.dx = 0.0
            self.dy = 0.0
    ```

    PRESERVE the existing `self.history.append((player_x, player_y))` line ABOVE this block (D-03 — the deque base is preserved). The deque write must happen BEFORE `_lookahead_target` is called (since the helper reads `self.history[0]`).

    **4. Add mode-FSM call** (after history.append, before lookahead so mode is fresh when target is computed — though mode does not affect movement in Phase 34):
    ```python
        self._update_mode(player_is_grounded, level_map)
    ```

    **5. Add stuck-detection block** AFTER the catch-up curve and AFTER the position update + collision push-out (currently `self.x += self.dx; ... if level_map.check_collision...`), BEFORE the reform-distance check at the bottom of update(). Pitfall 2 ordering — stuck check must precede reform distance.

    Insert per the <interfaces> block above (the `dist_to_target > SLIME_REFORM_DIST and moved < 0.1` heuristic per Pitfall 5). When the window threshold fires, call `self.reposition_with_fade(...)` and `return` — skipping the reform-distance check this frame.

    **6. main.py call site update** at L875 (or wherever `self.slime.update(...)` lives post-Plan-02):

    From:
    ```python
        self.slime.update(self.player.x, self.player.y, self.player.facing_right, self.level_map, self.player.is_fused)
    ```
    To (formatted across multiple lines for readability):
    ```python
        self.slime.update(
            self.player.x, self.player.y, self.player.facing_right,
            self.level_map, self.player.is_fused,
            player_dx=self.player.dx,
            player_is_grounded=self.player.is_grounded,
        )
    ```

    Search main.py for any OTHER `self.slime.update(` call sites and update them identically. Use Grep to confirm there is only one (Bash grep confirmed pre-edit there is one at L875).
  </action>
  <verify>
    <automated>python -m pytest tests/test_slime.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def update(self, player_x, player_y, player_facing_right, level_map" src/entities/slime.py` shows the new signature with `player_dx=0.0, player_is_grounded=True` kwargs (verify with `grep -n "player_dx=\|player_is_grounded=" src/entities/slime.py | wc -l` ≥ 1 in def line)
    - `grep -c "MAX_SHADOW_SPEED" src/entities/slime.py` returns 0 (linear clamp gone — replaced by sqrt curve)
    - `grep -n "tuning.SLIME_CATCHUP_CURVE_K" src/entities/slime.py` returns at least 1 hit inside update()
    - `grep -n "self\._lookahead_target\|self\._update_mode\|self\.reposition_with_fade" src/entities/slime.py | wc -l` returns at least 3 (each helper called from update)
    - `grep -n "player_dx=self.player.dx\|player_is_grounded=self.player.is_grounded" main.py` returns at least 2 (both kwargs threaded)
    - `python -m pytest tests/test_slime.py::test_catchup_60f_budget tests/test_slime.py::test_catchup_curve_shape tests/test_slime.py::test_lookahead_bias_dx tests/test_slime.py::test_lookahead_fallback_facing tests/test_slime.py::test_mode_float_when_player_airborne tests/test_slime.py::test_mode_ground_when_reachable tests/test_slime.py::test_mode_hysteresis tests/test_slime.py::test_stuck_recovery_fires tests/test_slime.py::test_stuck_recovery_uses_dissipate tests/test_slime.py::test_stuck_recovery_preserves_juice -x -q` PASSES (10 RED tests turned GREEN)
    - `python -m pytest tests/ -x -q --ignore=tests/test_overlays.py` shows no NEW failures (overlay tests still RED awaiting Plan 05; everything else green)
    - Game still boots: `python -c "import main" 2>&1` exits 0 (no syntax/import errors)
  </acceptance_criteria>
  <done>Slime.update accepts new kwargs with defaults; sqrt curve replaces linear clamp; lookahead/mode/stuck-detection wired in correct Pitfall-2 order; main.py threads player.dx + player.is_grounded; 10 of Plan 01's RED tests turn GREEN; no regressions.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_slime.py -x -q` shows all 11+ Phase 34 entity-surface tests passing
- `python -m pytest tests/ -x -q --ignore=tests/test_overlays.py` shows no new failures (overlay tests still RED until Plan 05)
- `grep -c MAX_SHADOW_SPEED src/entities/slime.py` returns 0 (linear clamp fully removed; magic number promoted)
- main.py threads both kwargs at the slime.update call site
- Game boots without import or runtime errors
</verification>

<success_criteria>
Entity surface complete: ease-out catch-up, lookahead bias, hybrid mode FSM, stuck-recovery glow-fade. 10+ Plan 01 tests turn GREEN. Phase 34 mechanics work in-game (visual confirmation in Plan 07 sign-off).
</success_criteria>

<output>
After completion, create `.planning/phases/34-slime-follow-ai-feel-pass/34-04-SUMMARY.md` listing each Plan 01 RED test that flipped GREEN, the exact slime.update signature shipped, and a one-paragraph note on the Pitfall 2 ordering rule.
</output>
