---
phase: 06-physics-refinement-test-gaps
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [tests/test_physics.py, src/entities/slime.py, src/entities/projectile.py]
autonomous: true
requirements: [PHY-01, PHY-02, PHY-03, TST-01]
must_haves:
  truths:
    - "Player physics (Walk, Jump, Wall Slide) are verified by automated tests"
    - "Slime follows player using acceleration/friction instead of simple lerp"
    - "Slime no longer gets stuck inside wall tiles"
    - "Projectiles spawned at point-blank range against walls correctly trigger impact"
  artifacts:
    - path: "tests/test_physics.py"
      provides: "Core physics unit tests"
    - path: "src/entities/slime.py"
      provides: "Refined companion physics"
---

# 06-01-PLAN: Physics Refinement & Test Gaps

<objective>
Stabilize the core physics engine by filling test gaps and refining companion/projectile collision logic.
</objective>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement Core Physics Unit Tests</name>
  <files>tests/test_physics.py</files>
  <behavior>
    - test_walk_logic: Verify acceleration towards max speed and friction deceleration.
    - test_jump_logic: Verify upward velocity on jump and gravity application.
    - test_wall_logic: Verify wall slide velocity cap and wall jump impulse.
  </behavior>
  <action>Fill the stubs in tests/test_physics.py. Use unittest.mock to simulate the LevelMap. Ensure tests cover horizontal movement, jumping, and wall interactions.</action>
  <verify>pytest tests/test_physics.py</verify>
  <done>All core physics tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Refine Slime Physics & Fix Wall Lodging</name>
  <files>src/entities/slime.py</files>
  <action>
    - Replace the Lerp-based horizontal/vertical movement in `Slime.update` (Standard History-Based Movement section) with a physics-based approach using `accel` and `friction`.
    - Use `move_and_collide(level_map)` even during the follow state to ensure the slime respects wall boundaries and doesn't lodge.
    - Ensure `target_x/y` from history acts as the directional influence rather than a direct position snap.
  </action>
  <verify>pytest tests/test_slime.py</verify>
  <done>Slime movement is weighted and respects collisions without sticking.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Fix Projectile Point-Blank Collision</name>
  <files>src/entities/projectile.py, tests/test_projectile.py</files>
  <behavior>
    - test_point_blank_collision: Spawn a projectile inside a wall and verify it immediately deactivates and returns a JuiceStain.
  </behavior>
  <action>
    - Create `tests/test_projectile.py` (or update if exists) with a point-blank collision test case.
    - Reduce `grace_timer` or add an immediate `check_collision` if the spawn point is already inside a wall in `src/entities/projectile.py`.
    - Ensure `Projectile.__init__` performs a safety check: if spawned in a wall, immediately deactivate.
  </action>
  <verify>pytest tests/test_projectile.py</verify>
  <done>Projectiles no longer phase through walls at point-blank range, verified by automated tests.</done>
</task>
</tasks>
