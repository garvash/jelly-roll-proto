---
phase: 06-physics-refinement-test-gaps
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified: [src/entities/enemies.py, .planning/phases/04-level-interactivity-items/*]
autonomous: true
requirements: [VIS-01, ORG-01]
must_haves:
  truths:
    - "Enemies trigger an explosion effect upon death"
    - "Phase 04 documentation is correctly located in its own directory"
  artifacts:
    - path: "src/entities/enemies.py"
      provides: "Enemy death triggers"
---

# 06-02-PLAN: Enemy Polish & Reorganization

<objective>
Enhance visual feedback for combat and clean up project structure technical debt.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Reorganize Phase 04 Documentation</name>
  <files>.planning/phases/04-level-interactivity-items/</files>
  <action>
    - Move `04-PLAN.md`, `04-RESEARCH.md`, and `04-SUMMARY.md` from `.planning/phases/03-level-hazards-blocks/` to `.planning/phases/04-level-interactivity-items/`.
    - Ensure any relative links in these files are updated if necessary.
  </action>
  <verify>ls .planning/phases/04-level-interactivity-items/04-PLAN.md && ls .planning/phases/03-level-hazards-blocks/04-PLAN.md; if ($?) { throw "Source file still exists" }</verify>
  <done>Phase 04 artifacts are correctly located and removed from the old location.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Enemy Destruction Visuals</name>
  <files>src/entities/enemies.py</files>
  <action>
    - Update `Enemy.take_damage` to trigger an `Effect` or `Particle` spawn when `hp <= 0`.
    - Since `Game` manages `self.effects`, the enemy needs a way to register the effect (e.g., returning an effect object or using a global-ish reference if available in the project pattern).
    - Use the existing `EXPLOSION` type in `src/entities/effects.py`.
  </action>
  <verify>pytest tests/test_enemies.py (ensure death still occurs) + Manual visual check.</verify>
  <done>Enemies explode visually when defeated.</done>
</task>
</tasks>
