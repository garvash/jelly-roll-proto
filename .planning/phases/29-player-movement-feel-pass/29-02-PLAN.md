---
phase: 29-player-movement-feel-pass
plan: 02
type: execute
wave: 2
depends_on: ["29-01"]
files_modified:
  - assets/physics-schema.json
  - assets/presets/slot_1.json
autonomous: false
requirements:
  - MOV-04
  - MOV-05

must_haves:
  truths:
    - "Ground movement (accel/friction/max speed) has been tuned and user confirmed it feels good"
    - "Air movement (gravity/jump/variable jump/fall multiplier) has been tuned against gap and height targets"
    - "Coyote time and jump buffer have been audited with F4 overlay across ground, air, and wall slide states"
    - "All M-G and M-A feel targets pass with the tuned values"
  artifacts:
    - path: "assets/physics-schema.json"
      provides: "Tuned ground and air values"
      contains: "WALK_ACCEL"
    - path: "assets/presets/slot_1.json"
      provides: "In-progress v2.0 default preset (ground+air locked)"
  key_links:
    - from: "assets/physics-schema.json"
      to: "src/core/tuning.py"
      via: "tuning loader reads schema on init"
      pattern: "physics-schema"
    - from: "src/ui/panel.py"
      to: "src/core/tuning.py"
      via: "slider change calls set_value()"
      pattern: "set_value"
---

<objective>
Tune ground and air movement systems against feel targets through a human-playtest loop. Ground first (accel/friction/max speed), then air (gravity/jump curves/coyote/buffer). Each system is tuned until the user confirms feel targets pass.

Purpose: This is the core tuning work of the phase. Ground must be locked before air because jump horizontal distance depends on walk speed. The F4 input overlay is used to audit coyote and buffer timing windows across all states (MOV-05).

Output: Tuned ground+air values saved to physics-schema.json, in-progress v2.0 default in slot_1.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-player-movement-feel-pass/29-CONTEXT.md
@.planning/phases/29-player-movement-feel-pass/29-RESEARCH.md
@.planning/phases/29-player-movement-feel-pass/29-01-SUMMARY.md

<interfaces>
<!-- Tuning API for playtest loop -->

From src/core/tuning.py:
```python
def set_value(key: str, value) -> None:  # Live mutation, next-frame visibility
def get_baseline(key: str):  # Frozen v1.3 value
def reset(key: str | None = None) -> None:  # Reset to v1.3 baseline
def save():  # Persist current values to physics-schema.json
```

From src/ui/presets.py:
```python
def save_preset(slot, alias=""):  # Save current values to preset slot
def load_preset(slot):  # Load preset into tuning
```

Ground tuning keys (Move tab):
- WALK_ACCEL: 0.125 (v1.3) -- acceleration per frame
- WALK_FRICTION: 0.15 (v1.3) -- deceleration per frame when no input
- MAX_WALK_SPEED: 1.25 (v1.3) -- px/frame cap

Air tuning keys (Jump tab):
- GRAVITY: 0.0875 (v1.3) -- downward acceleration per frame
- MAX_FALL_SPEED: 2.5 (v1.3) -- terminal velocity
- JUMP_FORCE: -3.25 (v1.3) -- initial vertical impulse (negative = up)
- VARIABLE_JUMP_REDUCTION: 0.5 (v1.3) -- dy multiplier on jump release
- FALLING_GRAVITY_MULTIPLIER: 1.8 (v1.3) -- gravity boost after peak

Forgiving keys (Jump tab):
- COYOTE_TIME: 12 (v1.3) -- frames of grace after leaving ground
- JUMP_BUFFER: 8 (v1.3) -- frames of pre-land jump input memory
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Ground tuning playtest loop</name>
  <files>
    assets/physics-schema.json
  </files>
  <action>
Load v1.3 baseline (slot_0) via `load_preset(0)` to start from known state. Launch game with `python main.py`. User tunes WALK_ACCEL, WALK_FRICTION, and MAX_WALK_SPEED using the F1 panel Move tab while playtesting in the test level's flat corridor (Ctrl+T to teleport).

Starting values (v1.3 baseline):
- WALK_ACCEL: 0.125 (10 frames to max speed)
- WALK_FRICTION: 0.15 (9 frames to stop)
- MAX_WALK_SPEED: 1.25 px/frame

Tuning guidance (per D-04 ground-first order):
- Start with WALK_ACCEL. Higher = snappier start. Try 0.15-0.20 for tighter feel.
- Then WALK_FRICTION. Higher = faster stop. Match to accel feel.
- MAX_WALK_SPEED affects ALL jump distances. Changing this after air tuning breaks gap targets.
- Once satisfied, LOCK these values before proceeding to air.

After user reports final values, apply them via `tuning.set_value()` and `tuning.save()` to persist to physics-schema.json.
  </action>
  <what-built>
    Claude sets up the ground tuning session by loading v1.3 baseline (slot_0) and preparing the panel. The user tunes WALK_ACCEL, WALK_FRICTION, and MAX_WALK_SPEED using the F1 panel while playtesting in the test level's flat corridor.

    **Feel targets to test:**
    - M-G01: Accel responsiveness (10 frames nominal)
    - M-G02: Stop distance (9 frames nominal)
    - M-G03: Run corridor feel (responsive direction changes)
  </what-built>
  <how-to-verify>
    1. Run `python main.py`
    2. Press Ctrl+T to teleport to test level
    3. Press F1 to open panel, select Move tab
    4. In the flat corridor (left side of test level):
       - Test M-G01: From standstill, hold right. Count frames to max speed (check feel, not exact count).
       - Test M-G02: At max speed, release. Does stop distance feel right?
       - Test M-G03: Run back and forth, reverse direction. Responsive or sluggish?
    5. Adjust WALK_ACCEL, WALK_FRICTION, MAX_WALK_SPEED sliders until M-G01/G02/G03 pass
    6. When satisfied, note final values
  </how-to-verify>
  <verify>
    <automated>python -c "from src.core import tuning; print(f'WALK_ACCEL={tuning.WALK_ACCEL}, WALK_FRICTION={tuning.WALK_FRICTION}, MAX_WALK_SPEED={tuning.MAX_WALK_SPEED}')"</automated>
  </verify>
  <done>User has confirmed ground values feel good. WALK_ACCEL, WALK_FRICTION, MAX_WALK_SPEED are locked and persisted to physics-schema.json. M-G01, M-G02, M-G03 pass.</done>
  <resume-signal>Report final ground values (WALK_ACCEL, WALK_FRICTION, MAX_WALK_SPEED) and type "ground locked" to proceed to air tuning. Or describe issues.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Air tuning + coyote/buffer audit playtest loop</name>
  <files>
    assets/physics-schema.json,
    assets/presets/slot_1.json
  </files>
  <action>
After ground values are locked, the user tunes air parameters using the F1 panel Jump tab. Apply ground-locked values first (they should already be active from Task 1). User tunes GRAVITY, JUMP_FORCE, VARIABLE_JUMP_REDUCTION, FALLING_GRAVITY_MULTIPLIER, MAX_FALL_SPEED, COYOTE_TIME, and JUMP_BUFFER in the test level's gap and height challenge areas.

Starting values (v1.3 baseline):
- GRAVITY: 0.0875
- JUMP_FORCE: -3.25 (3.87 tiles full jump)
- VARIABLE_JUMP_REDUCTION: 0.5 (0.99 tiles min jump)
- FALLING_GRAVITY_MULTIPLIER: 1.8
- MAX_FALL_SPEED: 2.5
- COYOTE_TIME: 12 frames
- JUMP_BUFFER: 8 frames

MOV-05 Input Audit (per D-06):
Press F4 to enable input overlay. During all tests, watch for:
- Green coyote blips appearing when walking off edges
- Blue buffer blips appearing when pressing jump before landing
- Verify coyote does NOT fire when jumping off walls (pitfall 3 from research)
- Verify buffer works when falling onto platforms from above

After user reports final values, apply via `tuning.set_value()`, persist with `tuning.save()`, and save to slot_1 with `save_preset(1, alias="v2.0-wip")`.
  </action>
  <what-built>
    After ground values are locked, Claude prepares air tuning. The user tunes GRAVITY, JUMP_FORCE, VARIABLE_JUMP_REDUCTION, FALLING_GRAVITY_MULTIPLIER, COYOTE_TIME, and JUMP_BUFFER.

    **Feel targets to test:**
    - M-A01: 3-tile gap from standing (should clear with margin)
    - M-A02: 4-tile gap at full speed (land 1+ tile in)
    - M-A03: 5-tile gap max range
    - M-A04: Variable jump min height (0.8-1.2 tiles)
    - M-A05: Full jump max height (3.5-4.2 tiles)
    - M-A06: Coyote time works within window
    - M-A07: Coyote boundary rejects late jumps
    - M-A08: Jump buffer triggers on landing
    - M-A09: Apex hang feel
  </what-built>
  <how-to-verify>
    1. Game should already be running from Task 1. If not, run `python main.py` and Ctrl+T to test level.
    2. Press F4 to enable input overlay (coyote/buffer blips)
    3. Press F1, select Jump tab
    4. Test each air target in order:
       - M-A01: Jump the 3-tile gap from standing
       - M-A02: Jump the 4-tile gap at full speed
       - M-A03: Jump the 5-tile gap at full speed
       - M-A04: Tap jump briefly -- check minimum height
       - M-A05: Hold jump fully -- check maximum height
       - M-A06: Walk off coyote ledge, jump within window -- green blip should show
       - M-A07: Walk off coyote ledge, delay jump beyond window -- should fall
       - M-A08: Fall onto platform, press jump early -- blue blip should show, jump on land
       - M-A09: At jump peak, does it feel like a brief hang or a sharp reversal?
    5. **Input audit (MOV-05):** With F4 on, verify:
       - Coyote blips appear ONLY when leaving ground (not when leaving wall slide)
       - Buffer blips appear when pressing jump before landing
       - No phantom inputs during wall slide state
    6. Adjust sliders until all M-A targets pass
    7. Save in-progress values to slot_1 with alias "v2.0-wip" via panel
  </how-to-verify>
  <verify>
    <automated>python -c "from src.core import tuning; print(f'GRAVITY={tuning.GRAVITY}, JUMP_FORCE={tuning.JUMP_FORCE}, COYOTE_TIME={tuning.COYOTE_TIME}, JUMP_BUFFER={tuning.JUMP_BUFFER}')"</automated>
  </verify>
  <done>User has confirmed air values feel good. All air tuning keys are locked. M-A01 through M-A09 pass. MOV-05 input audit completed -- coyote and buffer behavior verified with F4 overlay. In-progress preset saved to slot_1.</done>
  <resume-signal>Report final air values and input audit observations. Type "air locked" to proceed to wall tuning. Or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| preset files | JSON files written during tuning session |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-04 | Tampering | physics-schema.json during tuning | accept | Local dev workflow. User is the only actor. Journal file (Phase 28) tracks all changes. |
| T-29-05 | Information Disclosure | tuning values | accept | Game physics values are not sensitive data. |
</threat_model>

<verification>
- Ground values (WALK_ACCEL, WALK_FRICTION, MAX_WALK_SPEED) are user-approved
- Air values (GRAVITY, JUMP_FORCE, VARIABLE_JUMP_REDUCTION, FALLING_GRAVITY_MULTIPLIER, COYOTE_TIME, JUMP_BUFFER) are user-approved
- All M-G and M-A feel targets pass with tuned values
- F4 input overlay audit confirmed coyote and buffer behavior across states
- In-progress preset saved to slot_1
</verification>

<success_criteria>
- User has explicitly confirmed "ground locked" and "air locked"
- Feel targets M-G01-G03 and M-A01-A09 all pass
- MOV-05 input audit completed: coyote/buffer behavior verified with F4 overlay
- slot_1 contains in-progress v2.0 default with tuned ground+air values
</success_criteria>

<output>
After completion, create `.planning/phases/29-player-movement-feel-pass/29-02-SUMMARY.md`
</output>
