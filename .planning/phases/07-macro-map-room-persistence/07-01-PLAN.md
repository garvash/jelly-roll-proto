# Plan 07-01: WorldManager Refactor & Camera Clamping

Wave: 1
Depends on: None
Files modified: src/level/map.py, src/level/world.py, main.py
Autonomous: true

## Goal
Establish the foundation for the 5x5 world system by refactoring room management into a dedicated `WorldManager` and implementing camera clamping within room bounds.

## Tasks

<task id="07-01-01">
<read_first>
- `src/level/map.py` (Current map loading logic)
- `main.py` (Current camera and room sync logic)
- `.planning/phases/07-macro-map-room-persistence/07-RESEARCH.md` (LevelBounds structure)
</read_first>
<acceptance_criteria>
- `src/level/world.py` contains `LevelBounds` and `WorldManager` classes.
- `LevelBounds` stores id, x, y, w, h.
- `WorldManager` has a `detect_level(x, y)` method that returns the current level based on coordinates.
- `WorldManager` has a `get_camera_clamped(px, py)` method that returns clamped (cx, cy) for a given player position.
- `WorldManager` can be initialized with a list of `LevelBounds`.
</acceptance_criteria>
<action>
Create `src/level/world.py` with the core logic for world management and camera clamping as researched.
</action>
</task>

<task id="07-01-02">
<read_first>
- `src/level/map.py` (`load_from_ldtk_simplified` method)
</read_first>
<acceptance_criteria>
- `LevelMap` has a `levels` dictionary (or list) to store `LevelBounds` objects.
- `load_from_ldtk_simplified` populates this `levels` collection while loading levels.
- `LevelMap` exposes this collection to the rest of the engine.
</acceptance_criteria>
<action>
Modify `src/level/map.py` to store metadata for each level loaded from LDtk simplified export.
</action>
</task>

<task id="07-01-03">
<read_first>
- `main.py` (`Game.__init__`, `Game.reset`, and `Game.update`)
- `src/level/world.py` (New WorldManager)
</read_first>
<acceptance_criteria>
- `Game` instance in `main.py` initializes a `WorldManager` after loading the map.
- `Game.cam_x` and `Game.cam_y` are updated using `WorldManager.get_camera_clamped`.
- The immediate camera snapping in `Game.update` is replaced by `WorldManager`'s clamping logic.
- Player spawns correctly in the initial room.
- Clamping prevents "black bars" by keeping the camera within 128x128 bounds for standard rooms (or larger bounds for larger rooms).
</acceptance_criteria>
<action>
Integrate `WorldManager` into `main.py`, replacing the legacy room-sync logic with robust clamping and room detection.
</action>
</task>

<task id="07-01-04">
<read_first>
- `tests/test_physics.py` (Reference for existing tests)
- `.planning/phases/07-macro-map-room-persistence/07-VALIDATION.md`
</read_first>
<acceptance_criteria>
- `tests/test_world_manager.py` exists and passes.
- Test covers `detect_level` for various coordinates.
- Test covers `get_camera_clamped` for boundaries (top-left, bottom-right).
</acceptance_criteria>
<action>
Create and run unit tests for `WorldManager` to ensure clamping and level detection are mathematically correct.
</action>
</task>

## Verification Criteria
1. Run `pytest tests/test_world_manager.py`.
2. Manual verification: Play the game and ensure the camera never shows areas outside the current level's bounds.
3. Verify that moving between rooms (even if they are just 128x128) keeps the camera snapped to the grid when the player is near edges.

## Must Haves (Goal-Backward Verification)
- [ ] `WorldManager` exists and manages multiple rooms.
- [ ] Camera clamping is active and respects room boundaries.
- [ ] Level detection correctly identifies the active room based on player center.
- [ ] No regressions in player movement or tile collision.
