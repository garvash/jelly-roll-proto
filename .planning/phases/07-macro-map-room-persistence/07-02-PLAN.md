# Plan 07-02: Metroid Transitions & State Persistence

Wave: 2
Depends on: 07-01
Files modified: src/level/world.py, src/level/map.py, src/entities/items.py, src/entities/map_entities.py, main.py
Autonomous: true

## Goal
Implement the "freeze-and-slide" room transitions and a robust state persistence system for permanent items and regenerative environment blocks.

## Tasks

<task id="07-02-01">
<read_first>
- `src/level/world.py` (WorldManager)
- `main.py` (Game loop)
</read_first>
<acceptance_criteria>
- `WorldManager` has a `STATE_PLAYING` and `STATE_TRANSITIONING`.
- `WorldManager.trigger_transition(target_level)` initializes the slide.
- `WorldManager.update()` handles the LERP of `cam_x/y` over 20-30 frames.
- Gameplay (player/enemies) is frozen during transition.
- Player is repositioned into the target room to avoid immediate re-triggering.
</acceptance_criteria>
<action>
Implement the transition state machine and camera sliding logic in `WorldManager`.
</action>
</task>

<task id="07-02-02">
<read_first>
- `src/level/map.py` (Entity loading)
- `src/entities/player.py` (Collision/Kick)
</read_first>
<acceptance_criteria>
- `src/entities/map_entities.py` contains a `Door` class.
- `LevelMap` spawns `Door` entities from LDtk.
- Doors can be opened by Player Kick or Slime Spit.
- Colliding with an open door triggers `WorldManager.trigger_transition`.
</acceptance_criteria>
<action>
Create the `Door` entity and integrate it with the transition system.
</action>
</task>

<task id="07-02-03">
<read_first>
- `src/entities/items.py` (Item class)
- `src/level/world.py` (Persistence tracking)
</read_first>
<acceptance_criteria>
- `Item` class accepts an `iid` (instance ID) from LDtk.
- `WorldManager` maintains a `set` of `collected_iids`.
- `LevelMap.spawn_enemies` (refactored to `WorldManager.spawn_room_entities`) skips items already in `collected_iids`.
- Items are added to `collected_iids` upon collection.
</acceptance_criteria>
<action>
Implement global state persistence for unique items using LDtk instance IDs.
</action>
</task>

<task id="07-02-04">
<read_first>
- `src/level/map.py` (Tile removal)
- `src/level/world.py` (Regen logic)
</read_first>
<acceptance_criteria>
- `WorldManager` tracks broken destructible blocks in a `broken_blocks` dict with timers.
- Blocks regenerate (re-appear in `LevelMap` and Pyxel tilemap) after a configurable duration (e.g., 5s).
- All active regeneration timers are cleared, and blocks are restored when the player enters a new room (prevents soft-locks).
</acceptance_criteria>
<action>
Implement local state persistence and timed regeneration for destructible blocks.
</action>
</task>

<task id="07-02-05">
<read_first>
- `src/level/map.py` (`val_to_tile` mapping)
- `5x5mapdesign.txt` (Gating taxonomy)
</read_first>
<acceptance_criteria>
- `LevelMap` identifies IntGrid 10 as "Goo-Mold" (Negative Space).
- `LevelMap` distinguishes between horizontal and vertical cracked blocks if possible (optional but recommended for ABL-01/02).
- Engine correctly identifies these tiles for collision and logic.
</acceptance_criteria>
<action>
Update `LevelMap` tile identification to support biome-specific gates (Goo-Mold, etc.) per the Design Guide.
</action>
</task>

<task id="07-02-06">
<read_first>
- `tests/test_persistence.py`
- `.planning/phases/07-macro-map-room-persistence/07-VALIDATION.md`
</read_first>
<acceptance_criteria>
- `tests/test_persistence.py` passes.
- Unit test verifies item collection persistence.
- Unit test verifies block regeneration timer and room-entry reset.
</acceptance_criteria>
<action>
Create and run unit tests for the persistence system.
</action>
</task>

## Verification Criteria
1. Run `pytest tests/test_persistence.py`.
2. Manual verification: Collect an Energy Tank, leave the room, and return; the tank must not reappear.
3. Manual verification: Break a block, wait 5 seconds; it must regenerate.
4. Manual verification: Break a block, leave the room, and return; it must be present immediately.
5. Manual verification: Pass through a Door and observe the smooth camera slide.

## Must Haves (Goal-Backward Verification)
- [ ] Smooth 5x5 world traversal via transitions.
- [ ] Permanent upgrades stay collected.
- [ ] Environment hazards (blocks) regenerate to prevent soft-locks.
- [ ] Camera slide matches Metroid aesthetics.
