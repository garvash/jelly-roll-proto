---
phase: 12-screen-size-expansion
plan: 03
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - assets/entity-schema.json
  - assets/cave.ldtk
  - export_tilemap_csv.py
  - PML-to-LDtk Converter.md
autonomous: true
requirements:
  - D-05
  - D-06
  - D-07

must_haves:
  truths:
    - "entity-schema.json declares default_room_size as [320, 176]"
    - "cave.ldtk uses 320x176 as default level dimensions"
    - "PML-to-LDtk Converter.md documents the new 320x176 room spec"
    - "export_tilemap_csv.py no longer hardcodes 128"
  artifacts:
    - path: "assets/entity-schema.json"
      provides: "Updated room size contract for PML-to-LDtk converter"
      contains: "[320, 176]"
    - path: "assets/cave.ldtk"
      provides: "Updated LDtk world with 320x176 defaults"
      contains: "\"defaultLevelWidth\": 320"
    - path: "export_tilemap_csv.py"
      provides: "Export script without hardcoded dimensions"
      contains: "VIEWPORT_W"
  key_links:
    - from: "assets/entity-schema.json"
      to: "PML-to-LDtk converter (external)"
      via: "Shared contract"
      pattern: "default_room_size.*320.*176"
---

<objective>
Update asset files (LDtk, entity schema) and utility scripts to reflect the new 320x176 room dimensions. Ensure the PML-to-LDtk converter contract is updated.

Purpose: Per D-05/D-06/D-07, the level data and shared schema must match the new room dimensions so the game loads rooms at the correct size and the converter produces valid output.
Output: Updated cave.ldtk, entity-schema.json, export script, and converter docs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-screen-size-expansion/12-CONTEXT.md
@.planning/phases/12-screen-size-expansion/12-RESEARCH.md
@.planning/phases/12-screen-size-expansion/12-01-SUMMARY.md

@assets/entity-schema.json
@assets/cave.ldtk
@export_tilemap_csv.py

<interfaces>
<!-- After Plan 01, constants.py contains: -->
```python
SCREEN_W = 320
SCREEN_H = 192
VIEWPORT_W = 320
VIEWPORT_H = 176
HUD_H = 16
```

<!-- entity-schema.json level section (current): -->
```json
"level": {
    "grid_size": 8,
    "default_room_size": [128, 128],
    "variable_rooms": true,
    "variable_rooms_note": "Rooms can be any multiple of grid_size. Vertical shafts (128x384), large arenas, etc."
}
```

<!-- cave.ldtk relevant fields (current, lines 18-21): -->
```json
"worldGridWidth": 128,
"worldGridHeight": 128,
"defaultLevelWidth": 128,
"defaultLevelHeight": 128
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update entity-schema.json and export_tilemap_csv.py</name>
  <files>assets/entity-schema.json, export_tilemap_csv.py</files>
  <read_first>assets/entity-schema.json, export_tilemap_csv.py, src/core/constants.py</read_first>
  <action>
    **assets/entity-schema.json:**
    1. Change `"default_room_size": [128, 128]` to `"default_room_size": [320, 176]`
    2. Change `"variable_rooms_note"` to: `"Rooms can be any multiple of grid_size. Standard rooms are 320x176 (40x22 tiles). Large rooms use multiples like 320x352 (40x44 tiles) for vertical shafts or boss arenas."`
    3. Update `"updated"` field to `"2026-03-28"`

    **export_tilemap_csv.py:**
    1. Add import at top: `from src.core.constants import VIEWPORT_W, VIEWPORT_H, TILE_SIZE`
    2. Replace lines 21-22: `width = 128` / `height = 128` with:
       ```python
       # Export area matches standard room dimensions (in tiles)
       width = VIEWPORT_W   # 320 pixels
       height = VIEWPORT_H  # 176 pixels
       ```
    3. Update the comment on line 19 from "128x128 area (LDtk standard)" to "standard room area (320x176)"
  </action>
  <verify>
    <automated>python -c "import json; d=json.load(open('assets/entity-schema.json')); assert d['level']['default_room_size']==[320,176], f'Got {d[\"level\"][\"default_room_size\"]}'; print('Schema OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `assets/entity-schema.json` contains `"default_room_size": [320, 176]`
    - `assets/entity-schema.json` contains `"320x176"` in variable_rooms_note
    - `export_tilemap_csv.py` contains `from src.core.constants import`
    - `export_tilemap_csv.py` contains `VIEWPORT_W` (not hardcoded 128 for width)
    - `export_tilemap_csv.py` does NOT contain `width = 128`
  </acceptance_criteria>
  <done>Entity schema declares 320x176 default rooms. Export script uses constants instead of hardcoded values.</done>
</task>

<task type="auto">
  <name>Task 2: Update cave.ldtk world defaults and PML-to-LDtk Converter docs</name>
  <files>assets/cave.ldtk, PML-to-LDtk Converter.md</files>
  <read_first>assets/cave.ldtk, PML-to-LDtk Converter.md</read_first>
  <action>
    **assets/cave.ldtk:**
    Update the four global dimension fields near the top of the file:
    - `"worldGridWidth": 128` -> `"worldGridWidth": 320`
    - `"worldGridHeight": 128` -> `"worldGridHeight": 176`
    - `"defaultLevelWidth": 128` -> `"defaultLevelWidth": 320`
    - `"defaultLevelHeight": 128` -> `"defaultLevelHeight": 176`

    ALSO: Search for individual level entries in the `levels` array. Each level has its own `"pxWid"` and `"pxHei"` fields. For standard rooms (those currently at 128x128), update to:
    - `"pxWid": 320`
    - `"pxHei": 176`

    For any non-standard rooms (if they exist at sizes other than 128x128), scale proportionally:
    - If a room was 128x256 (tall room), it becomes 320x352 (matching D-06 example)
    - If a room was 256x128 (wide room), it becomes 640x176

    Also update the `"worldLayout"` grid positions: any level whose `"worldX"` or `"worldY"` was based on 128-pixel grid spacing needs to be recalculated to use 320-wide and 176-tall grid spacing. For example:
    - A level at worldX=128 (second column) becomes worldX=320
    - A level at worldY=128 (second row) becomes worldY=176
    - General formula: `new_worldX = (old_worldX / 128) * 320`, `new_worldY = (old_worldY / 128) * 176`

    **PML-to-LDtk Converter.md:**
    Search for any references to 128x128 room dimensions and update them:
    - Replace `128x128` room references with `320x176`
    - Note that TILE_SIZE remains 8
    - Note that standard rooms are 40x22 tiles
    - Add a note: "Room dimensions updated in Phase 12. Standard rooms: 320x176 (40x22 tiles). Large rooms: multiples like 320x352 (40x44 tiles). See entity-schema.json for authoritative room spec."
  </action>
  <verify>
    <automated>python -c "import json; d=json.load(open('assets/cave.ldtk')); assert d['defaultLevelWidth']==320, f'Got {d[\"defaultLevelWidth\"]}'; assert d['defaultLevelHeight']==176, f'Got {d[\"defaultLevelHeight\"]}'; print('LDtk defaults OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `assets/cave.ldtk` contains `"worldGridWidth": 320`
    - `assets/cave.ldtk` contains `"worldGridHeight": 176`
    - `assets/cave.ldtk` contains `"defaultLevelWidth": 320`
    - `assets/cave.ldtk` contains `"defaultLevelHeight": 176`
    - Individual level `pxWid`/`pxHei` values updated (no 128x128 standard rooms remain)
    - Level `worldX`/`worldY` positions recalculated for 320x176 grid spacing
    - `PML-to-LDtk Converter.md` contains `320x176`
  </acceptance_criteria>
  <done>cave.ldtk uses 320x176 defaults with all individual levels resized and repositioned. Converter docs updated with new room spec.</done>
</task>

</tasks>

<verification>
1. `python -c "import json; d=json.load(open('assets/entity-schema.json')); print(d['level']['default_room_size'])"` -- prints [320, 176]
2. `python -c "import json; d=json.load(open('assets/cave.ldtk')); print(d['defaultLevelWidth'], d['defaultLevelHeight'])"` -- prints 320 176
3. `grep -c "128" export_tilemap_csv.py` -- should be 0 or only in comments
</verification>

<success_criteria>
- entity-schema.json documents 320x176 as default room size
- cave.ldtk world and level dimensions all updated to 320x176 grid
- Export script uses constants, no hardcoded 128
- PML-to-LDtk converter docs reflect new room spec
</success_criteria>

<output>
After completion, create `.planning/phases/12-screen-size-expansion/12-03-SUMMARY.md`
</output>
