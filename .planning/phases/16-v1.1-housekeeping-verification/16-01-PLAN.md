---
phase: 16-v1.1-housekeeping-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "ABL-07 is archived as removed in REQUIREMENTS.md, not just unchecked"
    - "ROADMAP Phase 11 plan 11-03 checkbox is marked complete"
    - "No stale unchecked checkboxes exist for completed phases in ROADMAP"
    - "hold_position() does not exist in src/entities/slime.py"
    - "STATE.md pending todo referencing ABL-07 is cleaned up"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "ABL-07 archived with strikethrough and D-21 reference"
      contains: "~~ABL-07~~"
    - path: ".planning/ROADMAP.md"
      provides: "All completed phase checkboxes marked [x]"
      contains: "[x] 11-03-PLAN.md"
  key_links: []
---

<objective>
Archive ABL-07 as removed in REQUIREMENTS.md, fix stale ROADMAP checkboxes (11-03 and any others), verify hold_position() deletion, and clean STATE.md pending todo.

Purpose: Close 4 of the 6 housekeeping items from the v1.1 audit — all documentation-only changes.
Output: Updated REQUIREMENTS.md, ROADMAP.md, STATE.md with accurate project state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-v1.1-housekeeping-verification/16-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Archive ABL-07 in REQUIREMENTS.md and clean STATE.md</name>
  <files>.planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/STATE.md
    - .planning/phases/16-v1.1-housekeeping-verification/16-CONTEXT.md
  </read_first>
  <action>
In REQUIREMENTS.md, replace the ABL-07 line (currently line 16):
```
- [ ] **ABL-07**: Reform Block (Expend Max Juice to fill gaps in terrain). *Removed per D-21; checkbox reset by audit.*
```
with:
```
- ~~**ABL-07**: Reform Block (Expend Max Juice to fill gaps in terrain).~~ *Removed per D-21. Feature was never implemented; requirement archived Phase 16.*
```

This uses strikethrough per D-01 to clearly mark it as archived, not merely unchecked.

Update the header line from "14/15 satisfied" to "14/14 satisfied (1 removed)" to reflect the accurate count.

In the Traceability section, update the Phase 16 line from:
```
- **Phase 16**: ABL-07 (cleanup), tech debt housekeeping
```
to:
```
- **Phase 16**: ABL-07 (archived), tech debt housekeeping
```

In STATE.md, remove or update the stale pending todo line:
```
- Begin Phase 09: Defensive Mechanics (ABL-05, ABL-06, ABL-07)
```
Replace with:
```
- Phase 16 housekeeping in progress (ABL-07 archived, doc cleanup)
```

Per D-02, do NOT modify any prior CONTEXT.md files — they are historical record.
Per D-03, verify no ABL-07 references remain in src/ or tests/ by running grep. (Already confirmed: zero matches in src/ for ABL-07, reform_block, Reform.Block.)
  </action>
  <verify>
    <automated>grep -n "ABL-07" .planning/REQUIREMENTS.md && grep -c "hold_position" src/entities/slime.py; echo "exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS.md line 1 contains "14/14 satisfied (1 removed)"
    - REQUIREMENTS.md contains "~~**ABL-07**" (strikethrough markup)
    - REQUIREMENTS.md contains "archived Phase 16"
    - REQUIREMENTS.md Traceability Phase 16 line contains "ABL-07 (archived)"
    - STATE.md does NOT contain "Begin Phase 09"
    - STATE.md contains "ABL-07 archived"
    - No files in src/ contain "ABL-07" or "reform_block" (already true, verify maintained)
  </acceptance_criteria>
  <done>ABL-07 archived with strikethrough in REQUIREMENTS.md, STATE.md pending todo updated, no orphaned ABL-07 references in source code</done>
</task>

<task type="auto">
  <name>Task 2: Fix stale ROADMAP checkboxes and verify hold_position() deletion</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md
    - src/entities/slime.py
  </read_first>
  <action>
In ROADMAP.md, fix the 11-03 checkbox (per D-07). Currently at line 57:
```
  - [ ] 11-03-PLAN.md — Mini-map HUD + pause screen overlay + macro-map + D-18 verification + playtest
```
Change to:
```
  - [x] 11-03-PLAN.md — Mini-map HUD + pause screen overlay + macro-map + D-18 verification + playtest
```

Per D-08, scan ALL phase entries for stale checkboxes. The current state:
- Phase 11 status line (line 49) says "completed 2026-04-01" but shows "2/3" plans — update to "3/3":
```
- [x] Phase 11: Save System & HUD (SYS-01, SYS-02, SYS-03, SYS-04) (completed 2026-04-01)
```
- Progress table line 109 shows "2/3" — update to "3/3":
```
| 11. Save System & HUD | v1.1 | 3/3 | Complete    | 2026-04-01 |
```

Update the Phase 16 progress table row (line 114) from "0/0 | Pending" to "0/2 | In Progress" since plans now exist:
```
| 16. v1.1 Housekeeping & Verification | v1.1 | 0/2 | In Progress | — |
```

For D-04 (hold_position verification): run `grep -rn "hold_position" src/` to confirm zero matches. The function was deleted in Phase 14 per D-13. If zero matches, no action needed — just document in the verification output. (Already confirmed: zero matches.)
  </action>
  <verify>
    <automated>grep -n "\- \[ \] 11-03" .planning/ROADMAP.md; grep -n "2/3" .planning/ROADMAP.md; grep -rn "hold_position" src/ ; echo "all-checks-done"</automated>
  </verify>
  <acceptance_criteria>
    - ROADMAP.md contains "[x] 11-03-PLAN.md"
    - ROADMAP.md does NOT contain "- [ ] 11-03"
    - ROADMAP.md Phase 11 line contains "3/3"
    - ROADMAP.md progress table Phase 11 row contains "3/3"
    - ROADMAP.md Phase 16 progress table row contains "0/2"
    - grep for "hold_position" in src/ returns zero matches
  </acceptance_criteria>
  <done>All stale ROADMAP checkboxes fixed (11-03 marked complete, plan counts corrected), hold_position() confirmed deleted from codebase</done>
</task>

</tasks>

<verification>
- All ROADMAP phase entries have consistent checkbox/count states
- REQUIREMENTS.md accurately reflects 14/14 satisfied with ABL-07 archived
- STATE.md reflects current phase 16 work
- No orphaned references to ABL-07 or hold_position in source code
</verification>

<success_criteria>
- REQUIREMENTS.md: ABL-07 has strikethrough archive treatment, count says 14/14
- ROADMAP.md: 11-03 checkbox is [x], Phase 11 shows 3/3, no other stale checkboxes
- STATE.md: Pending todo updated to reflect Phase 16 status
- src/: Zero grep matches for hold_position and ABL-07
</success_criteria>

<output>
After completion, create `.planning/phases/16-v1.1-housekeeping-verification/16-01-SUMMARY.md`
</output>
