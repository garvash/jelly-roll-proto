---
phase: 13-sprite-scale-png-spritesheets
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - upscale_sprites.py
  - assets/sprites/player.png
  - assets/sprites/slime.png
  - assets/sprites/snail.png
  - assets/sprites/bat.png
  - assets/sprites/items.png
  - assets/sprites/projectile.png
  - assets/sprites/effects.png
  - assets/sprites/boss.png
  - assets/sprites/tiles.png
  - assets/sprites/player.json
  - assets/sprites/slime.json
  - assets/sprites/snail.json
  - assets/sprites/bat.json
  - assets/sprites/items.json
  - assets/sprites/projectile.json
  - assets/sprites/effects.json
  - assets/sprites/boss.json
autonomous: true
requirements:
  - SPR-01
  - SPR-02

must_haves:
  truths:
    - "upscale_sprites.py produces all PNG spritesheets and JSON sidecars when run"
    - "tiles.png preserves exact 8x8 tile layout from bank 0 for bltm compatibility"
    - "Each entity PNG is a horizontal strip of 16x16 frames (32x32 for boss)"
    - "JSON sidecars contain frameTags with animation names matching game states"
  artifacts:
    - path: "upscale_sprites.py"
      provides: "Migration tool: reads pyxres, outputs upscaled PNGs + JSON sidecars"
      min_lines: 80
    - path: "assets/sprites/player.png"
      provides: "Player spritesheet (16x16 frames, horizontal strip)"
    - path: "assets/sprites/tiles.png"
      provides: "Tile graphics for bank 0 (8x8 cells, same layout as pyxres bank 0)"
    - path: "assets/sprites/player.json"
      provides: "Aseprite-format JSON sidecar with frameTags"
  key_links:
    - from: "upscale_sprites.py"
      to: "assets/game.pyxres"
      via: "pyxel.load() to read existing sprites"
      pattern: "pyxel\\.load.*game\\.pyxres"
    - from: "upscale_sprites.py"
      to: "assets/sprites/*.png"
      via: "Image.save() after pixel doubling"
      pattern: "\\.save.*assets/sprites/"
---

<objective>
Create the upscale_sprites.py migration tool that reads all sprites from the existing game.pyxres file and outputs:
1. Per-entity PNG spritesheets with 2x pixel-doubled frames (16x16 standard, 32x32 boss)
2. Aseprite-format JSON sidecars with frameTags for each entity
3. tiles.png preserving the exact bank 0 tile layout at 8x8

This is the foundation step -- all subsequent plans depend on these PNG assets existing.

Purpose: Per D-05, D-19 -- auto-upscaled PNGs serve as reference templates for the artist. Per D-06, D-07 -- one PNG per entity in horizontal strip layout.
Output: upscale_sprites.py script + all PNG and JSON files in assets/sprites/
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-sprite-scale-png-spritesheets/13-CONTEXT.md
@.planning/phases/13-sprite-scale-png-spritesheets/13-RESEARCH.md
@generate_assets.py
@main.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create upscale_sprites.py migration tool</name>
  <files>upscale_sprites.py</files>
  <read_first>
    - generate_assets.py (current sprite generation code -- understand all sprite positions, colors, shapes)
    - main.py (lines 19-45 -- pyxel.load and explosion sprite injection code that must be replicated)
    - .planning/phases/13-sprite-scale-png-spritesheets/13-RESEARCH.md (Entity Sprite Inventory table + New Bank 1 Layout + upscale_sprites.py Core Logic)
  </read_first>
  <action>
Create `upscale_sprites.py` in the project root. This script:

1. **Init and load existing assets:**
   ```python
   pyxel.init(256, 256, display_scale=1)
   pyxel.load("assets/game.pyxres")
   ```

2. **Inject runtime explosion sprites** (copy exact logic from main.py lines 28-45 -- these are NOT in pyxres):
   - Frame 1 at (0,48) in bank 1: inner white (color 7) dist<4, outer yellow (color 10) dist<9
   - Frame 2 at (8,48) in bank 1: inner yellow (color 10) dist<9, outer orange (color 9) dist<16
   - Frame 3 at (16,48) in bank 1: inner orange (color 9) dist<16, outer dark (color 4) dist<25
   - Center points: (3.5, 51.5), (11.5, 51.5), (19.5, 51.5) respectively

3. **Create `assets/sprites/` directory** if not exists.

4. **Pixel-double helper function** `upscale_region(src_bank, src_x, src_y, src_w, src_h, dst_img, dst_x=0, dst_y=0)`:
   - For each pixel in source region, write 2x2 block in destination
   - Use `pyxel.images[src_bank].pget(src_x + x, src_y + y)` to read
   - Use `dst_img.pset(dst_x + x*2+dx, dst_y + y*2+dy, color)` to write

5. **Export each entity** using this sprite inventory (current bank 1 positions):

   | Entity | Source (bank, x, y) | Frames | Source Size | Output Size | Output File |
   |--------|---------------------|--------|-------------|-------------|-------------|
   | Player | (1, 0, 0) | 3 frames at 8px stride | 8x8 each | 16x16 each | player.png |
   | Slime | (1, 0, 8) | 3 frames: normal0=(0,8), normal1=(8,8), fused=(16,8) | 8x8 each | 16x16 each | slime.png |
   | Spit | (1, 24, 8) | 1 frame | 4x4 (pad to 8x8) | 16x16 | projectile.png |
   | Snail | (1, 0, 16) | 2 frames at 8px stride | 8x8 each | 16x16 each | snail.png |
   | Bat | (1, 0, 24) | 2 frames at 8px stride | 8x8 each | 16x16 each | bat.png |
   | Boss Mole | (1, 0, 32) | 2 frames at 16px stride | 16x16 each | 32x32 each | boss.png |
   | Rock | (1, 32, 32) | 1 frame | 8x8 | 16x16 | (include in projectile.png after spit) |
   | Explosion | (1, 0, 48) | 3 frames at 8px stride | 8x8 each | 16x16 each | effects.png |
   | Energy Tank | (1, 56, 0) | 1 frame | 8x8 | 16x16 | items.png |
   | Missile Tank | (1, 48, 8) | 1 frame | 8x8 | 16x16 | items.png |

   Items currently on bank 0 that must also be exported (per Pitfall 2):
   | Item | Source (bank, x, y) | Output position in items.png |
   |------|---------------------|------------------------------|
   | Dash Pickup | (0, 24, 0) | frame 2 |
   | Shield Pickup | (0, 32, 0) | frame 3 |
   | Boost Pickup | (0, 40, 0) | frame 4 |
   | Shield T2 | (0, 48, 0) | frame 5 |

6. **Export tiles.png** from bank 0:
   - Save the FULL 256x256 bank 0 image as `assets/sprites/tiles.png` using `pyxel.images[0].save("assets/sprites/tiles.png", 1)`
   - This preserves all tile positions so `pyxel.bltm()` references stay valid

7. **Generate JSON sidecars** for each entity with Aseprite-format `frameTags`:

   ```python
   # player.json frameTags:
   {"idle": (0, 0), "walk": (1, 2), "jump": (2, 2), "fall": (2, 2), "dash": (2, 2), "drill": (2, 2)}

   # slime.json frameTags:
   {"idle": (0, 1), "fused": (2, 2)}

   # snail.json frameTags:
   {"walk": (0, 1)}

   # bat.json frameTags:
   {"hang": (0, 0), "flap": (0, 1)}

   # items.json frameTags:
   {"energy": (0, 0), "missile": (1, 1), "dash_pickup": (2, 2), "shield_pickup": (3, 3), "boost_pickup": (4, 4), "shield_t2": (5, 5)}

   # projectile.json frameTags:
   {"spit": (0, 0), "rock": (1, 1)}

   # effects.json frameTags:
   {"explosion": (0, 2)}

   # boss.json frameTags:
   {"idle": (0, 1)}
   ```

   JSON format per D-08, D-22:
   ```json
   {
     "meta": {
       "frameTags": [
         {"name": "idle", "from": 0, "to": 0, "direction": "forward"}
       ],
       "size": {"w": 48, "h": 16}
     }
   }
   ```

8. **Print summary** of all files generated with frame counts.

Per D-19: This replaces generate_assets.py as the migration tool. generate_assets.py is NOT deleted (kept as historical reference) but upscale_sprites.py is the new tool.
  </action>
  <verify>
    <automated>python upscale_sprites.py 2>&1 | head -20</automated>
  </verify>
  <done>
    - upscale_sprites.py runs without errors
    - assets/sprites/ directory contains: player.png, slime.png, snail.png, bat.png, items.png, projectile.png, effects.png, boss.png, tiles.png
    - assets/sprites/ directory contains matching JSON sidecars: player.json, slime.json, snail.json, bat.json, items.json, projectile.json, effects.json, boss.json
    - tiles.png is a full 256x256 export of bank 0
    - Entity PNGs are horizontal strips (height=16 for standard, height=32 for boss)
  </done>
</task>

<task type="auto">
  <name>Task 2: Validate PNG outputs with inspect_bank</name>
  <files></files>
  <read_first>
    - upscale_sprites.py (just created -- verify it produced correct output)
    - assets/sprites/player.json (verify JSON sidecar format)
  </read_first>
  <action>
Write a quick validation script `_validate_sprites.py` (temporary, prefix underscore) that:

1. Loads each PNG via `pyxel.images[1].load(0, y_offset, path)` at the designated bank 1 positions from the manifest:
   - player.png at (0, 0)
   - slime.png at (0, 16)
   - snail.png at (0, 32)
   - bat.png at (0, 48)
   - items.png at (0, 64)
   - projectile.png at (0, 80)
   - effects.png at (0, 96)
   - boss.png at (0, 128)
   - tiles.png at bank 0 (0, 0)

2. For each entity PNG, verify:
   - File exists and is loadable
   - Frame dimensions match expected (16x16 standard, 32x32 boss)
   - At least one non-transparent pixel exists in first frame (not all color 0)

3. For tiles.png, verify:
   - TILE_SOLID position (0*8, 1*8) = (0, 8) has non-transparent pixels
   - TILE_HAZARD position (1*8, 1*8) = (8, 8) has non-transparent pixels

4. For each JSON sidecar, verify:
   - Valid JSON
   - Contains `meta.frameTags` array
   - Each tag has `name`, `from`, `to`, `direction` fields

5. Print PASS/FAIL for each check.

After validation passes, delete `_validate_sprites.py`.
  </action>
  <verify>
    <automated>python _validate_sprites.py 2>&1 | tail -5</automated>
  </verify>
  <done>
    - All PNG files load into Pyxel image banks without error
    - All sprite frames contain non-transparent pixel data
    - All JSON sidecars have valid frameTags structure
    - tiles.png preserves tile positions (TILE_SOLID at (0,8) has pixels)
  </done>
</task>

</tasks>

<verification>
- `ls assets/sprites/*.png | wc -l` returns 9 (8 entity sheets + tiles)
- `ls assets/sprites/*.json | wc -l` returns 8 (one per entity, no tiles JSON)
- `python -c "import json; [json.load(open(f'assets/sprites/{n}.json')) for n in ['player','slime','snail','bat','items','projectile','effects','boss']]"` exits 0
</verification>

<success_criteria>
All PNG spritesheets and JSON sidecars exist in assets/sprites/ with correct pixel data and animation tag metadata. The migration tool can be re-run idempotently.
</success_criteria>

<output>
After completion, create `.planning/phases/13-sprite-scale-png-spritesheets/13-01-SUMMARY.md`
</output>
