---
phase: 23-converter-handoff
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CONVERTER-HANDOFF.md
autonomous: true
requirements:
  - CONV-01
  - CONV-02
  - CONV-03

must_haves:
  truths:
    - "A converter maintainer can read CONVERTER-HANDOFF.md and know every value that changed without inspecting game code"
    - "All breaking changes to the entity-schema contract (v1.0.0 to v2.0.0) are explicitly listed with before/after values"
    - "Physics placement rule changes (tile-unit values halved) are documented with before/after values"
    - "LDtk output format changes (grid sizes, tileset path, IntGrid dimensions) are documented"
  artifacts:
    - path: "CONVERTER-HANDOFF.md"
      provides: "Complete v1.3 migration handoff for pml-to-ldtk converter"
      contains: "grid_size"
  key_links:
    - from: "CONVERTER-HANDOFF.md"
      to: "assets/entity-schema.json"
      via: "documents changes to shared contract"
      pattern: "v1\\.0\\.0.*v2\\.0\\.0"
    - from: "CONVERTER-HANDOFF.md"
      to: "assets/physics-schema.json"
      via: "documents physics placement rule changes"
      pattern: "v0\\.1\\.0.*v0\\.2\\.0"
---

<objective>
Create CONVERTER-HANDOFF.md documenting all schema and grid changes from the v1.3 migration (16x16 tile migration) for the pml-to-ldtk converter maintainer.

Purpose: The pml-to-ldtk converter is in a separate repository. Its maintainer needs a single, self-contained document describing every breaking change so they can update the converter without inspecting game code.

Output: CONVERTER-HANDOFF.md at repo root (per D-05), next to PML-to-LDtk Converter.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-converter-handoff/23-CONTEXT.md
@.planning/phases/23-converter-handoff/23-RESEARCH.md
@assets/entity-schema.json
@assets/physics-schema.json
@PML-to-LDtk Converter.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write CONVERTER-HANDOFF.md with complete change inventory</name>
  <files>CONVERTER-HANDOFF.md</files>
  <read_first>
    - assets/entity-schema.json (current v2.0.0 state -- source of truth for all current values)
    - assets/physics-schema.json (current v0.2.0 state -- source of truth for physics values)
    - PML-to-LDtk Converter.md (existing living reference -- handoff supplements, does not replace per D-02)
    - .planning/phases/23-converter-handoff/23-RESEARCH.md (contains complete verified before/after change inventory)
    - .planning/phases/23-converter-handoff/23-CONTEXT.md (user decisions D-01 through D-06)
  </read_first>
  <action>
Create CONVERTER-HANDOFF.md at the repo root (per D-05). The document must be fully self-contained with all values inline (per D-04). Do NOT include "no change" sections for unchanged areas like converter_mapping or intgrid values (per D-06).

Structure the document as follows:

**Header section:**
- Title: "v1.3 Migration Handoff: 16x16 Tile Migration"
- Date, scope (Phases 20-22), schema versions affected
- Relationship note: this supplements PML-to-LDtk Converter.md (per D-02)

**TL;DR / Quick Reference:**
- Central insight: "Same pixels, different tiles" -- pixel dimensions unchanged, tile counts halved
- List the 3-4 most critical breaking changes upfront

**Section 1: entity-schema.json changes (v1.0.0 to v2.0.0)**

Before/after table for Level Structure (BREAKING):
| Property | Before (v1.0.0) | After (v2.0.0) |
|----------|-----------------|-----------------|
| version | 1.0.0 | 2.0.0 |
| level.grid_size | 8 | 16 |
| level.default_room_size | [320, 176] | [320, 176] (unchanged) |
| Room size in tiles | 40x22 | 20x11 |
| variable_rooms_note | "40x22 tiles" | "20x11 tiles" |

Before/after table for Tileset (BREAKING):
| Property | Before (v1.0.0) | After (v2.0.0) |
|----------|-----------------|-----------------|
| biomes.cavern.tileset | assets/tiles.png | assets/tilesets/cavern.png |
| tile_coords description | "8px tile grid" | "16px tile grid" |
| tile_coords values | Same [col, row] pairs | Same [col, row] pairs (unchanged) |

Note that entity definition sizes in the schema did NOT change (they were already 16x16 in v1.0.0). Explicitly state this to prevent confusion -- the converter entity placement sizes remain the same. Do NOT create a before/after table for entity sizes since nothing changed (per D-06).

**Section 2: physics-schema.json changes (v0.1.0 to v0.2.0)**

Before/after table with ALL changed values:
| Property | Before (v0.1.0) | After (v0.2.0) |
|----------|-----------------|-----------------|
| version | 0.1.0 | 0.2.0 |
| tile_size | 8 | 16 |
| player.hitbox_px | [8, 8] | [10, 14] |
| player.visual_tiles | [2, 2] | [1, 1] |
| jump.max_height_tiles | 6 | 3 |
| jump.max_width_tiles | 10 | 5 |
| jump.comfortable_height_tiles | 4 | 2 |
| jump.comfortable_width_tiles | 7 | 3 |
| clearance.min_vertical_tiles | 2 | 1 |
| clearance.min_horizontal_tiles | 2 | 1 |
| placement_rules.max_gap_horizontal | 10 tiles | 5 tiles |
| placement_rules.max_gap_vertical_up | 6 tiles | 3 tiles |
| placement_rules.platform_min_width_tiles | 2 | 1 |
| source_constants.TILE_SIZE | 8 | 16 |

Emphasize: pixel values are unchanged (jump height still 62px, room still 320x176). Only tile-unit values halved.

**Section 3: LDtk Output Format Changes**

Before/after table for what the converter must produce:
| Property | Before | After |
|----------|--------|-------|
| defaultGridSize | 8 | 16 |
| Tileset tileGridSize | 8 | 16 |
| Tileset relPath | tileset.png | tilesets/cavern.png |
| Layer gridSize | 8 | 16 |
| AutoLayerTile src coords | 8px-based | 16px-based |
| Tiles per row in tileset | 32 (256/8) | 16 (256/16) |
| IntGrid dimensions (standard room) | 40x22 | 20x11 |
| Entity positions | 8px-aligned | 16px-aligned |

**Section 4: Suggested Converter Actions (per D-03)**

Practical action items for the converter maintainer:
1. Update grid_size constant from 8 to 16
2. Update tileset path from assets/tiles.png to assets/tilesets/cavern.png
3. Halve all tile-count room dimensions (40x22 becomes 20x11)
4. Update tile ID calculation: tiles_per_row = 256/16 = 16 (was 256/8 = 32)
5. Snap entity positions to 16px grid
6. Update physics placement rules to use halved tile values
7. Validate against entity-schema.json v2.0.0

Use Markdown formatting throughout. Keep the tone direct and practical. No filler content.
  </action>
  <verify>
    <automated>bash -c "test -f CONVERTER-HANDOFF.md && grep -c 'grid_size' CONVERTER-HANDOFF.md && grep -c 'BREAKING' CONVERTER-HANDOFF.md && grep -c 'v1.0.0' CONVERTER-HANDOFF.md && grep -c 'v2.0.0' CONVERTER-HANDOFF.md && echo PASS"</automated>
  </verify>
  <acceptance_criteria>
    - CONVERTER-HANDOFF.md exists at repo root (not in .planning/)
    - File contains "v1.0.0" and "v2.0.0" (entity-schema version transition per CONV-03)
    - File contains "v0.1.0" and "v0.2.0" (physics-schema version transition)
    - File contains "grid_size" with before value "8" and after value "16" (per CONV-02)
    - File contains "20x11" (new room tile dimensions per CONV-02)
    - File contains "40x22" (old room tile dimensions for before/after per CONV-02)
    - File contains "assets/tilesets/cavern.png" (new tileset path)
    - File contains "assets/tiles.png" (old tileset path for before/after)
    - File contains "BREAKING" to flag breaking changes (per CONV-03)
    - File contains "placement_rules" documenting physics changes
    - File does NOT contain a section header for "converter_mapping" (per D-06, nothing changed)
    - File does NOT contain a section header for "IntGrid Values" with a before/after table (per D-06, nothing changed)
    - File contains "PML-to-LDtk Converter.md" reference noting the relationship (per D-02)
    - File is at least 80 lines long (sufficient depth for self-contained document per D-04)
  </acceptance_criteria>
  <done>CONVERTER-HANDOFF.md exists at repo root with complete before/after tables for entity-schema (v1.0.0->v2.0.0), physics-schema (v0.1.0->v0.2.0), and LDtk output format changes. A converter maintainer can read the document and know exactly what to change without inspecting game code.</done>
</task>

</tasks>

<verification>
1. `test -f CONVERTER-HANDOFF.md` -- file exists at repo root
2. `grep -q "grid_size" CONVERTER-HANDOFF.md` -- documents the core breaking change
3. `grep -q "v2.0.0" CONVERTER-HANDOFF.md` -- references new schema version
4. `grep -q "v0.2.0" CONVERTER-HANDOFF.md` -- references new physics version
5. `grep -q "20x11" CONVERTER-HANDOFF.md` -- documents new room dimensions
6. `grep -q "placement_rules" CONVERTER-HANDOFF.md` -- documents physics changes
7. `grep -q "tilesets/cavern.png" CONVERTER-HANDOFF.md` -- documents tileset path change
8. `! grep -q "^## Converter Mapping" CONVERTER-HANDOFF.md` -- no unchanged section per D-06
</verification>

<success_criteria>
- CONVERTER-HANDOFF.md is a self-contained document at the repo root
- All three CONV requirements are satisfied: CONV-01 (documents all changes), CONV-02 (before/after values), CONV-03 (breaking changes listed)
- A pml-to-ldtk maintainer can read the handoff and know exactly what to change without inspecting game code
</success_criteria>

<output>
After completion, create `.planning/phases/23-converter-handoff/23-01-SUMMARY.md`
</output>
