---
phase: 28
slug: live-tuning-panel-mvp
status: draft
shadcn_initialized: false
preset: none
created: 2026-04-12
---

# Phase 28 — UI Design Contract

> Visual and interaction contract for the live-tuning panel overlay. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (Pyxel built-in primitives) |
| Preset | not applicable |
| Component library | none — hand-drawn with `pyxel.rect`, `pyxel.rectb`, `pyxel.text` |
| Icon library | none — single-char glyphs from Pyxel bitmap font |
| Font | Pyxel built-in bitmap font (4px wide, 6px tall per character) |

---

## Spacing Scale

Pyxel operates at 320x192 logical pixels. Every value below is in logical pixels.

Declared values (multiples of 2 for pixel-art clarity; 4px base unit):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 2px | Inline padding between label and track, icon gaps |
| sm | 4px | Compact element spacing, slider row internal padding |
| md | 8px | Default spacing between slider rows (row height) |
| lg | 10px | Sub-group header height, tab bar vertical padding |
| xl | 12px | Tab bar height, header/footer bar height |
| 2xl | 16px | Panel margin (not used — panel is full-screen) |

Exceptions:
- Slider row height is 8px (md) to fit 20 visible rows in the 160px content area
- Tab clickable area is 80px wide x 12px tall (320px / 4 tabs)
- Reset arrow icon is 5x5px (centered in 8px row)

---

## Typography

Pyxel has exactly one font: built-in bitmap, 4px wide x 6px tall. No font size or weight variation is possible. All text differentiation is via COLOR, not size or weight.

| Role | Size | Weight | Line Height | Color (Pyxel palette index) |
|------|------|--------|-------------|-----------------------------|
| Label (slider key name) | 4x6 bitmap | fixed | 8px row | 7 (white) |
| Value (numeric readout) | 4x6 bitmap | fixed | 8px row | 7 (white), 10 (yellow) when editing |
| Tab name (active) | 4x6 bitmap | fixed | 12px row | 7 (white) |
| Tab name (inactive) | 4x6 bitmap | fixed | 12px row | 13 (light grey) |
| Sub-group header | 4x6 bitmap | fixed | 10px row | 6 (light blue) |
| Status text (preset name, slot indicator) | 4x6 bitmap | fixed | 10px row | 10 (yellow) |

---

## Color

Pyxel uses a fixed 16-color palette (indices 0-15). No hex values — all colors are palette indices.

### Panel Surface Colors

| Role | Palette Index | Pyxel Color | Usage |
|------|---------------|-------------|-------|
| Panel background (dominant) | 1 | Dark navy | Semi-transparent overlay fill (drawn as solid rect; game renders beneath each frame) |
| Content area background | 0 | Black | Slider track background, content region behind sliders |
| Tab bar background (inactive) | 1 | Dark navy | Inactive tab fill |
| Tab bar background (active) | 5 | Dark grey | Active tab fill |
| Sub-group header background | 2 | Dark purple | Collapsible section header fill |

### Interactive Element Colors

| Role | Palette Index | Pyxel Color | Usage |
|------|---------------|-------------|-------|
| Slider handle | 7 | White | Drag handle (3px wide x 5px tall) |
| Slider track (below baseline) | 5 | Dark grey | Fill from handle to baseline when value < baseline |
| Slider track (above baseline) | 8 | Red | Fill from baseline to handle when value > baseline |
| Baseline tick mark | 13 | Light grey | 1px vertical line at center of track |
| Reset arrow icon | 11 | Green | Arrow glyph next to slider; flashes 10 (yellow) on reset |
| Save button | 3 | Dark teal | Button background |
| Save button text | 7 | White | Button label |

### State Indication Colors

| Role | Palette Index | Pyxel Color | Usage |
|------|---------------|-------------|-------|
| Value modified from baseline | 8 | Red | Numeric value text when current != baseline |
| Value at baseline | 7 | White | Numeric value text when current == baseline |
| Keyboard edit mode | 10 | Yellow | Value text and cursor underline during keyboard entry |
| Active preset indicator | 10 | Yellow | Slot number and alias in header bar |
| Protected preset warning | 8 | Red | Confirmation text when overwriting v1.3 baseline |
| Boolean toggle ON | 11 | Green | Checkbox fill for true values |
| Boolean toggle OFF | 2 | Dark purple | Checkbox fill for false values |

### 60/30/10 Distribution

- **60% (panel background):** Palette 1 (dark navy) — covers the full 320x192 overlay area
- **30% (content surfaces):** Palette 0 (black) for track backgrounds + Palette 5 (dark grey) for active tab + Palette 2 (purple) for sub-group headers
- **10% (accent, interactive):** Palette 7 (white) for handles/text, Palette 8 (red) for above-baseline drift, Palette 11 (green) for reset arrows

Accent (palette 8 red) reserved for: slider track above-baseline fill, modified-value text, protected-preset warning text. Never used for labels, backgrounds, or decorative elements.

---

## Layout Contract

### Screen Space Budget

```
+--[ Tab Bar: 320 x 12px ]---------------------------+
| [Move]  [Jump]  [Slime]  [Fuse]                    |
+--[ Header: 320 x 10px ]----------------------------+
| Slot 2: "tight"  |  [Save]                         |
+--[ Content Area: 320 x 160px ]---------------------+
| > movement                          (sub-group hdr) |
|   WALK_ACCEL    [===|====o====] 0.125  <            |
|   WALK_FRICTION [===|=o=======] 0.15   <            |
|   MAX_WALK_SPEED[===|======o==] 1.25   <            |
|   ...                                               |
| > dash                              (sub-group hdr) |
|   DASH_SPEED    [===|====o====] 2.00   <            |
|   ...                                               |
+--[ Footer: 320 x 10px ]----------------------------+
| [1] v1.3  [2] tight  [3] floaty  | Tab=slow-mo     |
+-----------------------------------------------------|

Total: 12 + 10 + 160 + 10 = 192px (exact screen height)
```

### Slider Row Layout (8px tall, 320px wide)

```
[2px pad][label 48px][2px gap][track 180px][2px gap][value 44px][2px gap][reset 12px][28px pad]
```

- **Label:** Truncated key name, left-aligned, max 12 chars (48px / 4px per char)
- **Track:** 180px horizontal bar with baseline tick at center (90px)
- **Value:** Right-aligned numeric, max 11 chars (e.g. "-1234.5678")
- **Reset:** Arrow glyph "<" (reset-to-baseline button), 12px hit area

### Sub-Group Header Row (10px tall, 320px wide)

```
[2px pad][expand/collapse ">" or "v" 6px][2px gap][group name text][fill to 320px]
```

- Click anywhere on header row to toggle collapse
- Collapsed: hides all child slider rows, shows ">" indicator
- Expanded: shows all child slider rows, shows "v" indicator

### Tab Bar (12px tall, 320px wide)

```
[tab 0: 80px][tab 1: 80px][tab 2: 80px][tab 3: 80px]
```

- Active tab: palette 5 background, palette 7 text
- Inactive tab: palette 1 background, palette 13 text
- Click anywhere in tab area to switch

---

## Interaction Contract

### F1 Toggle (D-01)
- `pyxel.btnp(pyxel.KEY_F1)` toggles `show_panel` boolean
- Panel open: call `pyxel.mouse(True)` to show OS cursor
- Panel close: call `pyxel.mouse(False)` to hide cursor
- Game continues running underneath at all times

### Slider Drag (D-10)
- Mouse down on track area (180px wide, 8px tall): begin drag, set `slider.dragging = True`
- Mouse held: map mouse_x to [0.25x, 4.0x] of baseline using log2 scale
- Mouse released: end drag, set `slider.dragging = False`
- Each drag frame: call `tuning.set_value(key, new_value)` immediately
- Integer baseline values: snap result to nearest integer
- Dead zone: 0px (no dead zone — every pixel maps to a value)

### Keyboard Entry (D-09)
- Click on the numeric value text (44px area): enter edit mode
- Edit mode: value text turns palette 10 (yellow), shows blinking cursor
- Type digits/decimal/minus via `pyxel.input_text`
- Enter: commit value via `tuning.set_value()`, exit edit mode
- Escape: discard, exit edit mode
- Backspace: delete last character from buffer
- While editing: suppress ALL other key processing (preset hotkeys, game input, F-keys)

### Reset Arrow (D-08)
- Click on reset area (12px wide, 8px tall): call `tuning.reset(key)`
- Visual feedback: flash the arrow from palette 11 (green) to palette 10 (yellow) for 6 frames, then back

### Tab Switching (D-02)
- Click on tab bar area (80px wide, 12px tall per tab)
- Switch content area to selected tab's sliders
- Reset scroll_y to 0 on tab switch
- Active tab gets palette 5 background

### Scroll (Research Pattern 3)
- Mouse wheel within content area: scroll_y += wheel_delta * 8px per notch
- Clamp scroll_y between 0 and (total_content_height - 160)
- Only needed when expanded content exceeds 160px (primarily Fuse tab)

### Collapsible Sub-Groups (D-04)
- Click on sub-group header row: toggle collapsed/expanded
- Default state on panel open: all sub-groups expanded except Fuse tab where only the first sub-group (drill) is expanded
- Collapsed: sub-group header shows ">" and 0px for child sliders
- Expanded: sub-group header shows "v" and full child slider rows

### Preset Hotkeys (D-11, D-12)
- Keys 1, 2, 3: load preset from `assets/presets/slot_N.json`
- Only active when panel is open AND no slider is in keyboard edit mode
- Loading calls `tuning.set_value()` for every feel-relevant key
- Header bar updates to show active slot number and alias

### Save Button (D-13)
- Click on "Save" button in header bar
- Saves current feel-relevant values to active slot's JSON file
- If active slot is 1 (v1.3 baseline, protected per D-14): show confirmation text "Overwrite v1.3 baseline? Click Save again" in palette 8 (red) for 120 frames. Second click within that window confirms. Otherwise cancels.

### Slow-Mo Toggle (D-05)
- Hold Tab key: game runs at half speed (every other frame skips entity updates, draw still runs)
- Release Tab: full speed resumes
- Status indicator in footer bar: "Tab=slow-mo" when not held, "SLOW" in palette 10 (yellow) when held

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Tab labels | "Move", "Jump", "Slime", "Fuse" |
| Header — active preset | "Slot N: alias" (e.g. "Slot 2: tight") |
| Header — no preset loaded | "Slot -: custom" |
| Save button label | "Save" |
| Save confirmation (protected slot) | "Overwrite baseline? Save again" |
| Footer — preset hints | "[1] v1.3  [2] tight  [3] floaty" |
| Footer — slow-mo hint | "Tab=slow" |
| Footer — slow-mo active | "SLOW" |
| Sub-group header | Schema group name lowercase (e.g. "movement", "forgiving", "wall") |
| Slider label | Truncated key name, uppercase (e.g. "WALK_ACCEL", "COYOTE_TIME") |
| Empty state (no presets dir) | Not applicable — panel creates dir on first save |
| Error state (preset load fail) | "Load failed" shown in header for 120 frames, palette 8 |
| Error state (preset parse error) | "Bad preset" shown in header for 120 frames, palette 8 |
| Reset flash confirmation | No text — visual flash of arrow icon (yellow 6 frames) |

---

## Widget Inventory

| Widget | Count | States | Source |
|--------|-------|--------|--------|
| Tab | 4 | active, inactive | Hand-built (pyxel.rect + pyxel.text) |
| Slider | ~54 | idle, dragging, editing, modified, at-baseline | Hand-built (pyxel.rect for track/handle) |
| CollapsibleGroup | 13 | expanded, collapsed | Hand-built (pyxel.text ">" / "v") |
| Button (Save) | 1 | idle, confirming (protected slot) | Hand-built (pyxel.rect + pyxel.text) |
| Toggle (boolean values) | ~2 | on, off | Hand-built (pyxel.rect checkbox) |
| Status text | 3 | header preset, footer hints, slow-mo | pyxel.text only |

---

## Tab-to-Group Mapping

Source: D-02 from CONTEXT.md + Research open question 1.

| Tab | Schema Groups | Key Count (approx) |
|-----|---------------|---------------------|
| Move | movement (walk subset: WALK_ACCEL, WALK_FRICTION, MAX_WALK_SPEED), dash | 7 |
| Jump | movement (jump subset: GRAVITY, MAX_FALL_SPEED, JUMP_FORCE, VARIABLE_JUMP_REDUCTION, FALLING_GRAVITY_MULTIPLIER), forgiving, wall | 10 |
| Slime | slime_follow, slime_juice, projectile | 11 |
| Fuse | drill, fusion, slime_ram, charge_shot, boost | 26 |

Movement group split hardcoded:
- `JUMP_TAB_MOVEMENT_KEYS = {"GRAVITY", "MAX_FALL_SPEED", "JUMP_FORCE", "VARIABLE_JUMP_REDUCTION", "FALLING_GRAVITY_MULTIPLIER"}`
- Remaining movement keys go to Move tab

---

## Non-Numeric Value Handling

Source: Research pitfall 6.

| Key | Type | Widget | Behavior |
|-----|------|--------|----------|
| RAM_INVINCIBLE | bool | Toggle checkbox | Click toggles True/False |
| RECALL_TRAIL_COLOR | int (0-15) | Integer stepper (no log scale) | Click left/right arrows, range 0-15 |
| SPIT_HOLD_THRESHOLD | int (frames) | Slider with integer snap | Standard slider, output rounded to int |
| HOLD_TAP_THRESHOLD | int (frames) | Slider with integer snap | Standard slider, output rounded to int |
| All frame-count keys (COYOTE_TIME, JUMP_BUFFER, etc.) | int | Slider with integer snap | Standard slider, output rounded to int |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| Not applicable | N/A | Pyxel game — no component registries |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
