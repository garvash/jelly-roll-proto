---
phase: 22-entity-alignment-physics-tuning
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_phase22.py
  - src/entities/enemies.py
  - src/entities/slime.py
  - src/entities/boss.py
  - src/entities/effects.py
  - main.py
autonomous: true
requirements:
  - ENT-01
  - ENT-02
  - ENT-03
  - ENT-04
  - ENT-05
must_haves:
  truths:
    - "Slime collision box is 16x16 matching its visual sprite"
    - "Snail and Bat collision boxes are 16x16 matching their visual sprites"
    - "Boss (Mole) collision box is 24x28 with centered rock spawns"
    - "BossRock collision box is 16x16 matching its visual sprite"
    - "Explosion effect renders at 16x16 matching its visual sprite"
    - "Legacy tile-scan spawn path uses TILE_SIZE instead of hardcoded 8"
    - "Player hitbox remains 10x14 (unchanged per D-01)"
    - "Door dimensions remain 8x32/32x8 (unchanged per D-02)"
  artifacts:
    - path: "tests/test_phase22.py"
      provides: "Regression tests for all entity hitbox sizes"
      contains: "test_player_hitbox"
    - path: "src/entities/enemies.py"
      provides: "Enemy base class with 16x16 default hitbox"
      contains: "w=16, h=16"
    - path: "src/entities/slime.py"
      provides: "Slime with 16x16 collision"
      contains: "self.w = 16"
    - path: "src/entities/boss.py"
      provides: "Mole 24x28, BossRock 16x16, centered spawn offsets"
      contains: "self.w = 24"
    - path: "src/entities/effects.py"
      provides: "Effect draw calls using 16x16 collision size"
      contains: "draw_sprite(self.x, self.y, 16, 16"
  key_links:
    - from: "src/entities/enemies.py"
      to: "src/entities/enemies.py (Snail, Bat)"
      via: "Enemy base class default w=16, h=16 propagates to subclasses"
      pattern: "w=16, h=16"
    - from: "src/entities/boss.py"
      to: "src/entities/boss.py (BossRock spawn)"
      via: "self.x + self.w // 2 centers rock on new hitbox"
      pattern: "self\\.w // 2"
---

<objective>
Update all entity collision boxes to match 16x16 visual sprites and fix legacy spawn path pixel math.

Purpose: Entities still use 8x8 collision from the old tile size. This plan brings collision boxes in line with the 16x16 visual grid established in Phase 21.
Output: Updated entity files with correct hitbox sizes, tests proving correctness, legacy spawn path using TILE_SIZE constant.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-entity-alignment-physics-tuning/22-CONTEXT.md
@.planning/phases/22-entity-alignment-physics-tuning/22-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/entities/enemies.py line 6:
```python
class Enemy:
    def __init__(self, x, y, w=8, h=8, game=None):  # defaults need changing to w=16, h=16
```
Snail and Bat call super().__init__ without explicit w/h -- they inherit the base default.

From src/entities/slime.py lines 27-28:
```python
self.w = 8  # needs changing to 16
self.h = 8  # needs changing to 16
```

From src/entities/boss.py:
```python
# BossRock lines 13-14
self.w = 8   # needs changing to 16
self.h = 8   # needs changing to 16

# BossRock line 26 (damage direction)
player.take_damage(1, self.x + 4, slime=slime)  # change to self.x + self.w // 2

# Mole lines 43-44
self.w = 16  # needs changing to 24
self.h = 16  # needs changing to 28

# Mole line 96 (contact damage direction)
player.take_damage(1, self.x + 8, slime=slime)  # change to self.x + self.w // 2

# Mole line 103 (rock spawn offset)
self.rocks.append(BossRock(self.x + 8, self.y + 8, dx, dy))  # change to self.x + self.w // 2, self.y + self.h // 2
```

From src/entities/effects.py line 32:
```python
draw_sprite(self.x, self.y, 8, 8, 1, u, 96, SPRITE_SIZE, SPRITE_SIZE, True)
# change collision args from 8, 8 to 16, 16
```

From main.py lines 218, 362-382 (legacy spawn path):
```python
spawn_x, spawn_y = tx * 8, ty * 8          # line 218
tx_start = int(level.x // 8)               # line 362
Snail(tx * 8, ty * 8)                       # line 369
Bat(tx * 8, ty * 8)                         # line 372
Item(tx * 8, ty * 8, ...)                   # lines 375, 378, 381
```
All `* 8` and `// 8` must become `* TILE_SIZE` and `// TILE_SIZE`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create phase 22 test scaffold</name>
  <files>tests/test_phase22.py</files>
  <read_first>
    - src/entities/enemies.py
    - src/entities/slime.py
    - src/entities/boss.py
    - src/entities/effects.py
    - src/entities/player.py
    - src/entities/map_entities.py
    - src/core/constants.py
    - tests/test_enemies.py
    - tests/test_boss.py
  </read_first>
  <behavior>
    - test_player_hitbox: Player(0, 0, mock_level, mock_game).w == 10, .h == 14 (ENT-01, D-01: unchanged)
    - test_slime_hitbox: Slime(0, 0).w == 16, .h == 16 (D-05)
    - test_enemy_hitboxes: Enemy(0, 0).w == 16, .h == 16; Snail(0, 0).w == 16; Bat(0, 0).w == 16 (ENT-02)
    - test_boss_hitbox: Mole(0, 0, mock_level).w == 24, .h == 28 (ENT-03, D-04)
    - test_boss_rock_hitbox: BossRock(0, 0, 1, 0).w == 16, .h == 16 (D-05)
    - test_door_dimensions: Door vertical w==8, h==32; Door horizontal w==32, h==8 (ENT-04, D-02: unchanged)
    - test_effect_explosion_size: Verify Effect draw call passes 16, 16 as collision w/h (ENT-05)
    - test_physics_constants_unchanged: GRAVITY == 0.0875, JUMP_FORCE == -3.25, MAX_WALK_SPEED == 1.25 (PHYS-01, D-03)
    - test_passage_clearance: Player w=10 < TILE_SIZE=16, Player h=14 < TILE_SIZE=16 (PHYS-02: fits in 1 tile)
  </behavior>
  <action>
Create tests/test_phase22.py with the following test functions. Use the same mock/fixture patterns as tests/test_enemies.py and tests/test_boss.py.

For Player instantiation, create a minimal mock level_map (needs .entities attribute returning [], .get_tile returning None, .w/.h attributes) and mock game object (needs .effects list). Pattern from test_boss.py.

Test functions:
1. `test_player_hitbox` - instantiate Player, assert w==10, h==14 (per D-01)
2. `test_slime_hitbox` - Slime(0,0), assert w==16, h==16
3. `test_enemy_base_hitbox` - Enemy(0,0), assert w==16, h==16
4. `test_snail_hitbox` - Snail(0,0), assert w==16, h==16
5. `test_bat_hitbox` - Bat(0,0), assert w==16, h==16
6. `test_boss_hitbox` - Mole(0,0,mock_level), assert w==24, h==28
7. `test_boss_rock_hitbox` - BossRock(0,0,1,0), assert w==16, h==16
8. `test_door_vertical_dimensions` - assert Door vertical has w==8, h==32
9. `test_door_horizontal_dimensions` - assert Door horizontal has w==32, h==8
10. `test_physics_constants_unchanged` - from constants import GRAVITY, JUMP_FORCE, MAX_WALK_SPEED; assert exact float values 0.0875, -3.25, 1.25
11. `test_passage_clearance` - Player w < TILE_SIZE and Player h < TILE_SIZE (10<16, 14<16)

Import paths: `from src.entities.enemies import Enemy, Snail, Bat`, `from src.entities.boss import BossRock, Mole`, `from src.entities.slime import Slime`, `from src.entities.player import Player`, `from src.core.constants import GRAVITY, JUMP_FORCE, MAX_WALK_SPEED, TILE_SIZE`.

All tests should FAIL initially (before hitbox changes in Task 2) for: slime (8!=16), enemy base (8!=16), snail (8!=16), bat (8!=16), boss (16!=24), boss_rock (8!=16). Player, door, physics constants, and passage clearance tests should PASS immediately (these are unchanged values).
  </action>
  <verify>
    <automated>python -m pytest tests/test_phase22.py -x -q 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_phase22.py exists and contains `def test_player_hitbox`
    - tests/test_phase22.py contains `def test_slime_hitbox`
    - tests/test_phase22.py contains `def test_enemy_base_hitbox`
    - tests/test_phase22.py contains `def test_snail_hitbox`
    - tests/test_phase22.py contains `def test_bat_hitbox`
    - tests/test_phase22.py contains `def test_boss_hitbox`
    - tests/test_phase22.py contains `def test_boss_rock_hitbox`
    - tests/test_phase22.py contains `def test_door_vertical_dimensions`
    - tests/test_phase22.py contains `assert.*== 10` (player w) and `assert.*== 14` (player h)
    - tests/test_phase22.py contains `assert.*== 24` (boss w) and `assert.*== 28` (boss h)
    - test_player_hitbox, test_door_*, test_physics_constants_unchanged, test_passage_clearance PASS
    - test_slime_hitbox, test_enemy_base_hitbox, test_snail_hitbox, test_bat_hitbox, test_boss_hitbox, test_boss_rock_hitbox FAIL (expected: values still 8)
  </acceptance_criteria>
  <done>Test scaffold exists with all 11 test functions. Unchanged-value tests pass, hitbox-change tests fail (RED state).</done>
</task>

<task type="auto">
  <name>Task 2: Update entity hitboxes and fix legacy spawn path</name>
  <files>src/entities/enemies.py, src/entities/slime.py, src/entities/boss.py, src/entities/effects.py, main.py</files>
  <read_first>
    - src/entities/enemies.py
    - src/entities/slime.py
    - src/entities/boss.py
    - src/entities/effects.py
    - main.py
    - src/entities/projectile.py
    - src/core/constants.py
  </read_first>
  <action>
Make the following exact changes:

**src/entities/enemies.py line 6:**
Change `def __init__(self, x, y, w=8, h=8, game=None):` to `def __init__(self, x, y, w=16, h=16, game=None):`
This propagates to Snail and Bat automatically since they inherit defaults.

**src/entities/slime.py lines 27-28:**
Change `self.w = 8` to `self.w = 16`
Change `self.h = 8` to `self.h = 16`

**src/entities/boss.py - BossRock class:**
- Line 13: change `self.w = 8` to `self.w = 16`
- Line 14: change `self.h = 8` to `self.h = 16`
- Line 26: change `player.take_damage(1, self.x + 4, slime=slime)` to `player.take_damage(1, self.x + self.w // 2, slime=slime)`

**src/entities/boss.py - Mole class:**
- Line 43: change `self.w = 16` to `self.w = 24`
- Line 44: change `self.h = 16` to `self.h = 28`
- Line 96: change `player.take_damage(1, self.x + 8, slime=slime)` to `player.take_damage(1, self.x + self.w // 2, slime=slime)`
- Line 103: change `BossRock(self.x + 8, self.y + 8, dx, dy)` to `BossRock(self.x + self.w // 2, self.y + self.h // 2, dx, dy)`

**src/entities/effects.py line 32:**
Change `draw_sprite(self.x, self.y, 8, 8, 1, u, 96, SPRITE_SIZE, SPRITE_SIZE, True)` to `draw_sprite(self.x, self.y, 16, 16, 1, u, 96, SPRITE_SIZE, SPRITE_SIZE, True)`

**main.py line 218 (player spawn fallback):**
Change `spawn_x, spawn_y = tx * 8, ty * 8` to `spawn_x, spawn_y = tx * TILE_SIZE, ty * TILE_SIZE`
Ensure TILE_SIZE is imported (check existing imports at top of main.py).

**main.py lines 362-382 (legacy tile-scan spawn path):**
- Line 362: change `int(level.x // 8)` to `int(level.x // TILE_SIZE)` and same for `level.y // 8`
- Line 363: change `level.w // 8` to `level.w // TILE_SIZE` and same for `level.h // 8`
- Line 369: change `Snail(tx * 8, ty * 8)` to `Snail(tx * TILE_SIZE, ty * TILE_SIZE)`
- Line 372: change `Bat(tx * 8, ty * 8)` to `Bat(tx * TILE_SIZE, ty * TILE_SIZE)`
- Line 375: change `Item(tx * 8, ty * 8, "DASH_PICKUP")` to `Item(tx * TILE_SIZE, ty * TILE_SIZE, "DASH_PICKUP")`
- Line 378: change `Item(tx * 8, ty * 8, "ENERGY")` to `Item(tx * TILE_SIZE, ty * TILE_SIZE, "ENERGY")`
- Line 381: change `Item(tx * 8, ty * 8, "MISSILE")` to `Item(tx * TILE_SIZE, ty * TILE_SIZE, "MISSILE")`

**DO NOT change:**
- src/entities/projectile.py (spit Projectile stays 4x4, ChargeProjectile stays CHARGE_SHOT_SIZE) -- per RESEARCH pitfall 1
- src/entities/player.py (player stays 10x14 per D-01)
- src/entities/map_entities.py (doors stay 8x32/32x8 per D-02)
- src/entities/items.py (items already 16x16)
  </action>
  <verify>
    <automated>python -m pytest tests/test_phase22.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - src/entities/enemies.py contains `w=16, h=16`
    - src/entities/slime.py contains `self.w = 16` and `self.h = 16`
    - src/entities/boss.py BossRock contains `self.w = 16` and `self.h = 16`
    - src/entities/boss.py Mole contains `self.w = 24` and `self.h = 28`
    - src/entities/boss.py contains `self.x + self.w // 2` (at least 3 occurrences: rock spawn x, mole damage, boss rock damage)
    - src/entities/boss.py contains `self.y + self.h // 2` (rock spawn y)
    - src/entities/effects.py contains `draw_sprite(self.x, self.y, 16, 16`
    - main.py does NOT contain `* 8` in spawn paths (no `tx * 8` or `ty * 8`)
    - main.py contains `tx * TILE_SIZE` and `ty * TILE_SIZE`
    - main.py contains `// TILE_SIZE` (legacy spawn scan bounds)
    - src/entities/projectile.py still contains `self.w = 4` (spit unchanged)
    - All tests in tests/test_phase22.py PASS
  </acceptance_criteria>
  <done>All entity hitboxes match D-05 target sizes. Legacy spawn path uses TILE_SIZE. Boss hardcoded offsets replaced with self.w//2 and self.h//2. All phase 22 tests GREEN.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_phase22.py -v` -- all 11 tests pass
- `python -m pytest tests/ -x -q` -- full suite passes (no regressions)
- `grep -n "w=16, h=16" src/entities/enemies.py` -- confirms Enemy default
- `grep -n "self.w = 24" src/entities/boss.py` -- confirms Mole hitbox
- `grep -c "\* 8" main.py` -- returns 0 for spawn path lines (may still have other `* 8` elsewhere)
</verification>

<success_criteria>
- All entity hitbox sizes match D-05 inventory table
- Player 10x14, Slime 16x16, Snail 16x16, Bat 16x16, Mole 24x28, BossRock 16x16, Effect 16x16
- Doors unchanged at 8x32/32x8, spit projectile unchanged at 4x4
- No hardcoded `* 8` in legacy spawn path
- No hardcoded `+ 8` offsets in boss.py (replaced with self.w//2, self.h//2)
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/22-entity-alignment-physics-tuning/22-01-SUMMARY.md`
</output>
