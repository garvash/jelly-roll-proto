---
phase: 22-entity-alignment-physics-tuning
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/physics-schema.json
  - tests/test_phase05_gaps.py
  - tests/test_phase05_nyquist.py
  - tests/test_enemies.py
  - tests/test_boss.py
autonomous: true
requirements:
  - PHYS-01
  - PHYS-02
  - PHYS-03
must_haves:
  truths:
    - "physics-schema.json has tile_size 16 and all tile-unit values recalculated for 16px base"
    - "Player hitbox_px is [10, 14] in physics-schema.json"
    - "Player fits in 1-tile passage (clearance min_vertical_tiles=1, min_horizontal_tiles=1)"
    - "Physics constants (GRAVITY, JUMP_FORCE, etc.) are unchanged in source_constants"
    - "Existing tests updated to expect new entity sizes and pass"
  artifacts:
    - path: "assets/physics-schema.json"
      provides: "Physics contract with 16px tile-unit values"
      contains: "\"tile_size\": 16"
    - path: "tests/test_enemies.py"
      provides: "Enemy tests updated for 16x16 hitbox"
    - path: "tests/test_boss.py"
      provides: "Boss tests updated for 24x28 hitbox"
  key_links:
    - from: "assets/physics-schema.json"
      to: "pml-to-ldtk converter (external)"
      via: "JSON contract consumed by converter"
      pattern: "tile_size.*16"
---

<objective>
Recalculate physics-schema.json for 16px tile base and fix existing tests broken by entity size changes.

Purpose: The physics schema is consumed by the pml-to-ldtk converter for gap sizing and platform placement. It currently has stale 8px values. Existing tests reference old 8x8 entity sizes and will fail after Plan 01's changes.
Output: Updated physics-schema.json with correct 16px tile-unit values, all existing tests passing.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-entity-alignment-physics-tuning/22-CONTEXT.md
@.planning/phases/22-entity-alignment-physics-tuning/22-RESEARCH.md

<interfaces>
<!-- Current physics-schema.json structure (stale values to be replaced) -->

From assets/physics-schema.json:
```json
{
  "tile_size": 8,
  "player": {
    "hitbox_tiles": [1, 1],
    "hitbox_px": [8, 8],
    "visual_tiles": [2, 2],
    "visual_px": [16, 16],
    "note": "Collision box is 8x8 (1 tile). Visual sprite is 16x16 (2 tiles) via SPRITE_SCALE=2."
  },
  "jump": {
    "max_height_tiles": 6, "max_height_px": 62,
    "max_width_tiles": 10, "max_width_px": 89,
    "comfortable_height_tiles": 4,
    "comfortable_width_tiles": 7
  },
  "clearance": {
    "min_vertical_tiles": 2,
    "min_horizontal_tiles": 2
  },
  "placement_rules": {
    "max_gap_horizontal": {"value_tiles": 10},
    "max_gap_vertical_up": {"value_tiles": 6},
    "platform_min_width_tiles": 2
  },
  "source_constants": {
    "GRAVITY": 0.0875,
    "JUMP_FORCE": -3.25,
    "MAX_WALK_SPEED": 1.25,
    "MAX_FALL_SPEED": 2.5,
    "FALLING_GRAVITY_MULTIPLIER": 1.8,
    "TILE_SIZE": 8
  }
}
```

Target values (from RESEARCH.md exact change inventory):
- tile_size: 8 -> 16
- player.hitbox_tiles: [1,1] (unchanged -- 10/16 < 1, 14/16 < 1)
- player.hitbox_px: [8,8] -> [10, 14] (actual collision size)
- player.visual_tiles: [2,2] -> [1, 1] (16/16 = 1)
- player.visual_px: [16, 16] (unchanged)
- player.note: update text
- jump.max_height_tiles: 6 -> 3 (floor(62/16) = 3, safety margin)
- jump.max_width_tiles: 10 -> 5 (floor(89/16) = 5)
- jump.comfortable_height_tiles: 4 -> 2
- jump.comfortable_width_tiles: 7 -> 3
- clearance.min_vertical_tiles: 2 -> 1
- clearance.min_horizontal_tiles: 2 -> 1
- placement_rules.max_gap_horizontal.value_tiles: 10 -> 5
- placement_rules.max_gap_vertical_up.value_tiles: 6 -> 3
- placement_rules.platform_min_width_tiles: 2 -> 1
- source_constants.TILE_SIZE: 8 -> 16
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Recalculate physics-schema.json for 16px tile base</name>
  <files>assets/physics-schema.json</files>
  <read_first>
    - assets/physics-schema.json
    - src/core/constants.py
    - .planning/phases/22-entity-alignment-physics-tuning/22-RESEARCH.md
  </read_first>
  <action>
Rewrite assets/physics-schema.json with all tile-unit values recalculated for TILE_SIZE=16. Pixel values are UNCHANGED (per D-03: no physics constant changes). Only tile-unit derivations change (divide px by 16 instead of 8).

Exact new values:

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Jelly Roll Proto -- Physics Contract",
  "description": "Derived physics constants for map generation. The pml-to-ldtk converter uses these to place stepping stones, validate gaps, and ensure traversability. Values are computed from the game's Euler integration at 60fps.",
  "version": "0.2.0",
  "updated": "auto-generated",
  "tile_size": 16,
  "fps": 60,
  "player": {
    "hitbox_tiles": [1, 1],
    "hitbox_px": [10, 14],
    "visual_tiles": [1, 1],
    "visual_px": [16, 16],
    "note": "Collision box is 10x14 (~1 tile). Visual sprite is 16x16 (1 tile). No SPRITE_SCALE indirection."
  },
  "jump": {
    "max_height_tiles": 3,
    "max_height_px": 62,
    "max_height_note": "Absolute peak from Euler integration (3.875 tiles at 16px). Floored to 3 for safe platform placement.",
    "max_width_tiles": 5,
    "max_width_px": 89,
    "max_width_note": "Horizontal distance at full run speed over jump arc (5.5625 tiles at 16px). Floored to 5 for safe gap sizing.",
    "comfortable_height_tiles": 2,
    "comfortable_height_note": "Reachable without pixel-perfect timing. Use for standard platforming.",
    "comfortable_width_tiles": 3,
    "comfortable_width_note": "Reachable without a full-speed running start. Use for standard gaps."
  },
  "fall": {
    "damage_threshold_tiles": null,
    "damage_threshold_note": "No fall damage. Terminal velocity is capped. Any drop is safe."
  },
  "clearance": {
    "min_vertical_tiles": 1,
    "min_vertical_note": "Player collision is 14px tall, fits in 1 tile (16px). Passages must be at least 1 tile high.",
    "min_horizontal_tiles": 1,
    "min_horizontal_note": "Player collision is 10px wide, fits in 1 tile (16px). Passages must be at least 1 tile wide."
  },
  "placement_rules": {
    "description": "Rules the converter should follow when placing stepping stones and validating room traversability.",
    "max_gap_horizontal": {
      "value_tiles": 5,
      "note": "No horizontal gap wider than this without a platform."
    },
    "max_gap_vertical_up": {
      "value_tiles": 3,
      "note": "No upward gap taller than this without a reachable platform."
    },
    "max_gap_vertical_down": {
      "value_tiles": null,
      "note": "No limit -- no fall damage. Any drop is safe."
    },
    "platform_min_width_tiles": 1,
    "platform_min_width_note": "Minimum landing platform width. Player fits in 1 tile at 16px."
  },
  "source_constants": {
    "description": "Raw game constants these values derive from. Auto-populated from src/core/constants.py.",
    "GRAVITY": 0.0875,
    "JUMP_FORCE": -3.25,
    "MAX_WALK_SPEED": 1.25,
    "MAX_FALL_SPEED": 2.5,
    "FALLING_GRAVITY_MULTIPLIER": 1.8,
    "TILE_SIZE": 16
  }
}
```

Key changes from old schema:
- version: "0.1.0" -> "0.2.0" (breaking change)
- tile_size: 8 -> 16
- player.hitbox_px: [8,8] -> [10,14] (actual collision, was wrong before)
- player.visual_tiles: [2,2] -> [1,1] (16px = 1 tile now)
- player.note: removed SPRITE_SCALE mention
- All tile-unit values halved (dividing by 16 instead of 8)
- clearance: 2 -> 1 (player fits in 1 tile)
- platform_min_width: 2 -> 1 (player fits in 1 tile)
- source_constants.TILE_SIZE: 8 -> 16
  </action>
  <verify>
    <automated>python -c "import json; d=json.load(open('assets/physics-schema.json')); assert d['tile_size']==16; assert d['player']['hitbox_px']==[10,14]; assert d['clearance']['min_vertical_tiles']==1; assert d['source_constants']['TILE_SIZE']==16; assert d['source_constants']['GRAVITY']==0.0875; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - assets/physics-schema.json contains `"tile_size": 16`
    - assets/physics-schema.json contains `"hitbox_px": [10, 14]`
    - assets/physics-schema.json contains `"visual_tiles": [1, 1]`
    - assets/physics-schema.json contains `"max_height_tiles": 3`
    - assets/physics-schema.json contains `"max_width_tiles": 5`
    - assets/physics-schema.json contains `"comfortable_height_tiles": 2`
    - assets/physics-schema.json contains `"comfortable_width_tiles": 3`
    - assets/physics-schema.json contains `"min_vertical_tiles": 1`
    - assets/physics-schema.json contains `"min_horizontal_tiles": 1`
    - assets/physics-schema.json contains `"platform_min_width_tiles": 1`
    - assets/physics-schema.json contains `"TILE_SIZE": 16`
    - assets/physics-schema.json contains `"GRAVITY": 0.0875` (unchanged per D-03)
    - assets/physics-schema.json contains `"version": "0.2.0"`
  </acceptance_criteria>
  <done>physics-schema.json has all tile-unit values recalculated for 16px base. Pixel physics values unchanged. Version bumped to 0.2.0.</done>
</task>

<task type="auto">
  <name>Task 2: Fix existing tests for new entity sizes</name>
  <files>tests/test_enemies.py, tests/test_boss.py, tests/test_phase05_gaps.py, tests/test_phase05_nyquist.py</files>
  <read_first>
    - tests/test_enemies.py
    - tests/test_boss.py
    - tests/test_phase05_gaps.py
    - tests/test_phase05_nyquist.py
    - src/entities/enemies.py
    - src/entities/boss.py
    - src/core/constants.py
  </read_first>
  <action>
After Plan 01 changes entity sizes, some existing tests will have stale assertions. Read each test file and update:

**tests/test_enemies.py:**
- Find any assertions on enemy .w or .h that expect 8. Change to 16.
- Find any hardcoded `* 8` pixel coordinate math. Change to `* TILE_SIZE` or `* 16`.

**tests/test_boss.py:**
- Find any assertions on Mole .w or .h that expect 16x16. Change to w=24, h=28.
- Find any assertions on BossRock .w or .h that expect 8. Change to 16.
- Find any hardcoded `+ 8` offset expectations. Update to match `self.w // 2` pattern (Mole: +12 for x, +14 for y; BossRock: +8 for x).

**tests/test_phase05_gaps.py:**
- Find any `tx * 8` or `ty * 8` spawn coordinate math. Change to `tx * 16` or use TILE_SIZE.
- Find any enemy size assertions expecting 8. Change to 16.

**tests/test_phase05_nyquist.py:**
- Find any Bat or Snail size assumptions (w=8, h=8). Update to 16.
- Find any `TILE_SIZE` related assertions that assumed 8.

For each file: read it completely, identify all lines referencing old sizes (8x8 enemies, 16x16 boss, * 8 spawn math), and update them. If a test file has no stale references, leave it unchanged.

Add `from src.core.constants import TILE_SIZE` import where needed for clarity, but using literal 16 is also acceptable.
  </action>
  <verify>
    <automated>python -m pytest tests/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/ -x -q` exits with code 0 (all tests pass)
    - tests/test_enemies.py does NOT contain assertions expecting enemy w==8 or h==8
    - tests/test_boss.py does NOT contain assertions expecting Mole w==16 or h==16 (should be 24, 28)
    - tests/test_phase05_gaps.py does NOT contain `* 8` for spawn coordinates (should be * 16 or * TILE_SIZE)
  </acceptance_criteria>
  <done>All existing tests updated to expect new entity sizes. Full test suite passes with no regressions.</done>
</task>

</tasks>

<verification>
- `python -c "import json; d=json.load(open('assets/physics-schema.json')); assert d['tile_size']==16; print('PHYS-03: OK')"` -- schema updated
- `python -m pytest tests/ -v` -- full suite passes
- `grep -c '"tile_size": 8' assets/physics-schema.json` -- returns 0 (no stale values)
</verification>

<success_criteria>
- physics-schema.json tile_size is 16 with all tile-unit values correctly derived
- Pixel physics values (GRAVITY, JUMP_FORCE, etc.) unchanged in schema
- Player hitbox_px is [10, 14] (actual values, not old [8, 8])
- Clearance values are 1 tile (player fits in 16px tile)
- All existing tests pass after updates
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/22-entity-alignment-physics-tuning/22-02-SUMMARY.md`
</output>
