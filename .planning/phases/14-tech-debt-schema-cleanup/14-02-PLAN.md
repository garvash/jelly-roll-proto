---
phase: 14-tech-debt-schema-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/constants.py
  - src/core/debug.py
  - src/entities/player.py
  - tests/test_bubble_shield.py
  - tests/test_drill_retcon.py
  - tests/test_phase05_gaps.py
  - tests/test_sprite_scale.py
  - tests/test_debug.py
autonomous: true
requirements: [MAP-02]

must_haves:
  truths:
    - "DEBUG_ALL_ABILITIES constant no longer exists in constants.py"
    - "Player abilities all default to False in __init__ without any debug override"
    - "God-mode runtime toggles exist in src/core/debug.py with 3 tiers"
    - "All 6 previously failing tests now pass"
    - "Full test suite runs green (0 failures)"
  artifacts:
    - path: "src/core/debug.py"
      provides: "Runtime god-mode toggles: god_abilities, god_invincible, god_infinite_juice"
      exports: ["god_abilities", "god_invincible", "god_infinite_juice", "update"]
    - path: "src/core/constants.py"
      provides: "Constants file without DEBUG_ALL_ABILITIES"
    - path: "src/entities/player.py"
      provides: "Player.__init__ without DEBUG_ALL_ABILITIES block"
    - path: "tests/test_debug.py"
      provides: "Tests for god-mode default state"
  key_links:
    - from: "src/entities/player.py"
      to: "src/core/debug.py"
      via: "import debug; check debug.god_abilities in update"
      pattern: "debug\\.god_abilities"
    - from: "src/core/debug.py"
      to: "pyxel key input"
      via: "Ctrl+1/2/3 toggles in debug.update()"
      pattern: "pyxel\\.btn.*KEY_CTRL"
---

<objective>
Remove DEBUG_ALL_ABILITIES, create runtime god-mode debug module, and fix all 6 test failures (D-08 through D-12).

Purpose: Tests currently fail because DEBUG_ALL_ABILITIES=True at import time forces abilities on (especially has_shield_t2 which zeroes drain rates). Replacing with runtime toggles decouples debug playtesting from test execution. Fixes 4 of 6 test failures directly; remaining 2 are unrelated test isolation bugs.

Output: New debug.py module, cleaned constants.py and player.py, all 6 tests fixed, test suite green.
</objective>

<execution_context>
@C:\Users\garva\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\garva\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-tech-debt-schema-cleanup/14-CONTEXT.md
@.planning/phases/14-tech-debt-schema-cleanup/14-RESEARCH.md

<interfaces>
<!-- constants.py line 2 (to delete) -->
DEBUG_ALL_ABILITIES = True

<!-- player.py lines 47-53 (to delete) -->
# Debug: unlock all abilities
if DEBUG_ALL_ABILITIES:
    self.has_drill = True
    self.has_dash = True
    self.has_shield = True
    self.has_shield_t2 = True
    self.has_boost = True

<!-- player.py line 2 (uses wildcard import from constants) -->
from src.core.constants import *
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create debug.py + remove DEBUG_ALL_ABILITIES + fix player.py</name>
  <files>src/core/debug.py, src/core/constants.py, src/entities/player.py, tests/test_debug.py</files>
  <read_first>
    - src/core/constants.py (line 2: DEBUG_ALL_ABILITIES = True)
    - src/entities/player.py (lines 1-55: imports, __init__ with DEBUG_ALL_ABILITIES block)
    - src/core/input.py (for pattern reference on key combo handling)
  </read_first>
  <behavior>
    - Test 1: debug.god_abilities defaults to False
    - Test 2: debug.god_invincible defaults to False
    - Test 3: debug.god_infinite_juice defaults to False
    - Test 4: Player.__init__ creates player with has_shield=False, has_dash=False, has_boost=False by default
    - Test 5: "DEBUG_ALL_ABILITIES" string does NOT appear in constants.py
  </behavior>
  <action>
    1. Create src/core/debug.py with the following exact content:
       ```python
       """Runtime god-mode toggles for debug playtesting (D-08, D-09, D-10).

       All flags default to False. Toggled at runtime via Ctrl+1/2/3.
       Tests are never affected because flags only change via key press.
       """
       import pyxel

       # God-mode tier toggles (D-09)
       god_abilities = False      # Toggle 1: unlock all abilities
       god_invincible = False     # Toggle 2: no damage taken
       god_infinite_juice = False # Toggle 3: infinite juice

       def update():
           """Check debug key combos. Call from Game.update()."""
           global god_abilities, god_invincible, god_infinite_juice
           if pyxel.btn(pyxel.KEY_CTRL):
               if pyxel.btnp(pyxel.KEY_1):
                   god_abilities = not god_abilities
               if pyxel.btnp(pyxel.KEY_2):
                   god_invincible = not god_invincible
               if pyxel.btnp(pyxel.KEY_3):
                   god_infinite_juice = not god_infinite_juice
       ```

    2. In src/core/constants.py, DELETE line 2 entirely:
       Delete: DEBUG_ALL_ABILITIES = True
       Also delete the comment on line 1: "# Debug: unlock all abilities at start (set False for normal gameplay)"

    3. In src/entities/player.py, DELETE the DEBUG_ALL_ABILITIES block (lines 47-53):
       Delete: # Debug: unlock all abilities
       Delete: if DEBUG_ALL_ABILITIES:
       Delete:     self.has_drill = True
       Delete:     self.has_dash = True
       Delete:     self.has_shield = True
       Delete:     self.has_shield_t2 = True
       Delete:     self.has_boost = True

    4. In src/entities/player.py, add god-mode check in the update method.
       Add import at top: import src.core.debug as debug
       In the update() method, near the beginning (after input processing), add:
       # God-mode ability override (D-10)
       if debug.god_abilities:
           self.has_drill = True
           self.has_dash = True
           self.has_shield = True
           self.has_shield_t2 = True
           self.has_boost = True

    5. Create tests/test_debug.py with tests per behavior spec. Import debug module,
       verify all 3 flags default to False. Import Player and verify abilities default
       to False (use the existing make_player pattern or direct instantiation with mocked level_map).
       Also verify constants.py source does not contain "DEBUG_ALL_ABILITIES".
  </action>
  <verify>
    <automated>python -m pytest tests/test_debug.py -x --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - src/core/debug.py exists and contains "god_abilities = False"
    - src/core/debug.py contains "god_invincible = False"
    - src/core/debug.py contains "god_infinite_juice = False"
    - src/core/debug.py contains "def update():"
    - src/core/constants.py does NOT contain "DEBUG_ALL_ABILITIES"
    - src/entities/player.py does NOT contain "DEBUG_ALL_ABILITIES"
    - src/entities/player.py contains "import src.core.debug as debug"
    - src/entities/player.py contains "debug.god_abilities"
    - tests/test_debug.py exits 0
  </acceptance_criteria>
  <done>DEBUG_ALL_ABILITIES removed from codebase; debug.py provides runtime god-mode toggles; player.py checks debug flags at runtime not import time.</done>
</task>

<task type="auto">
  <name>Task 2: Fix 6 test failures (shield drain, drill retcon, projectile mock, sprite isolation)</name>
  <files>tests/test_bubble_shield.py, tests/test_drill_retcon.py, tests/test_phase05_gaps.py, tests/test_sprite_scale.py, src/entities/items.py</files>
  <read_first>
    - tests/test_bubble_shield.py (find the 3 drain rate tests that fail)
    - tests/test_drill_retcon.py (test_no_drill_item_type reads items.py source, asserts no "DRILL" item type)
    - tests/test_phase05_gaps.py (test_combat_projectile_collision -- MagicMock level_map issue)
    - tests/test_sprite_scale.py (test_draw_sprite_offset_standard -- pyxel module mock issue)
    - src/entities/items.py (ITEM_FRAMES dict with "DRILL": 2 entry to delete)
  </read_first>
  <action>
    1. BUBBLE SHIELD DRAIN TESTS (3 failures, D-11):
       After Task 1 removes DEBUG_ALL_ABILITIES, these 3 tests should now pass automatically
       because Player.__init__ no longer forces has_shield_t2=True. The tests use
       make_player(has_shield=True) which sets has_shield=True but leaves has_shield_t2=False,
       so drain rate stays at 0.5 as expected.

       VERIFY by running: python -m pytest tests/test_bubble_shield.py -x --tb=short
       If any still fail, inspect the make_player function to ensure it does not set has_shield_t2.

    2. DRILL RETCON TEST (1 failure, D-14):
       In src/entities/items.py, delete the "DRILL": 2 line from ITEM_FRAMES dict.
       Current dict (~line 43-51):
         ITEM_FRAMES = {
             "ENERGY": 0,
             "MISSILE": 1,
             "DRILL": 2,        # <-- DELETE THIS LINE
             "DASH_PICKUP": 2,
             "SHIELD_PICKUP": 3,
             "BOOST_PICKUP": 4,
             "SHIELD_T2": 5,
         }
       After deletion, "DASH_PICKUP": 2 remains as the correct frame index 2 mapping.

    3. PROJECTILE COLLISION TEST (1 failure, D-12 / Pitfall 2):
       In tests/test_phase05_gaps.py, find test_combat_projectile_collision.
       The test creates a Game() then sets game.level_map = MagicMock().
       MagicMock returns truthy for check_collision(), causing Projectile to
       self-deactivate on init.

       Fix: After game.level_map = MagicMock(), add these lines:
       game.level_map.check_collision.return_value = False
       game.level_map.find_tile.return_value = None

       This ensures projectiles don't deactivate during construction.

    4. SPRITE SCALE TEST (1 failure, D-12 / Pitfall 4):
       In tests/test_sprite_scale.py test_draw_sprite_offset_standard, the monkeypatch
       targets pyxel.blt directly but another test file replaces sys.modules["pyxel"].

       Fix: Change the monkeypatch target to patch through the importing module:
       monkeypatch.setattr("src.core.sprite_utils.pyxel.blt", mock_blt)
       instead of:
       monkeypatch.setattr(pyxel, "blt", mock_blt)

       Read the test carefully first -- if it uses `import pyxel` then does
       `monkeypatch.setattr(pyxel, 'blt', ...)`, change to the module-path form above.

    5. Run full test suite to confirm 0 failures:
       python -m pytest tests/ --tb=short
  </action>
  <verify>
    <automated>python -m pytest tests/ --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - src/entities/items.py ITEM_FRAMES dict does NOT contain "DRILL" key
    - tests/test_phase05_gaps.py contains "check_collision.return_value = False"
    - tests/test_sprite_scale.py contains "sprite_utils.pyxel.blt" (module-path patching)
    - python -m pytest tests/ --tb=short exits 0 with 0 failures
    - python -m pytest tests/test_bubble_shield.py exits 0
    - python -m pytest tests/test_drill_retcon.py exits 0
  </acceptance_criteria>
  <done>All 6 previously failing tests pass; full test suite green (0 failures); ITEM_FRAMES["DRILL"] removed.</done>
</task>

</tasks>

<verification>
- python -m pytest tests/ --tb=short (full suite, 0 failures)
- python -m pytest tests/test_debug.py -x (god-mode tests)
- grep -r "DEBUG_ALL_ABILITIES" src/ (returns no matches)
- grep "DRILL" src/entities/items.py (returns no ITEM_FRAMES match)
</verification>

<success_criteria>
- DEBUG_ALL_ABILITIES completely removed from src/ directory
- debug.py provides 3-tier runtime god-mode toggles defaulting to False
- Player.__init__ abilities all default to False
- Player.update() checks debug.god_abilities for runtime override
- All 6 previously failing tests pass
- Full test suite exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/14-tech-debt-schema-cleanup/14-02-SUMMARY.md`
</output>
