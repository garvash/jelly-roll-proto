---
phase: 14-tech-debt-schema-cleanup
plan: 03
type: execute
wave: 2
depends_on: [14-01, 14-02]
files_modified:
  - src/entities/slime.py
  - assets/entity-schema.json
  - .planning/REQUIREMENTS.md
  - .planning/phases/14-tech-debt-schema-cleanup/14-VERIFICATION.md
autonomous: true
requirements: [ABL-02]

must_haves:
  truths:
    - "slime.py has no hold_position method"
    - "MAP-02 requirement text describes pml-to-ldtk pipeline with event-gated doors"
    - "ABL-02 requirement text splits vertical gating (Phase 10) from infinite flight (Phase 11)"
    - "entity-schema.json intgrid value 4 updated to event_marker semantics"
    - "entity-schema.json version bumped"
    - "CRACKED_V breaking via Drill Dive + Boost is verified as working"
  artifacts:
    - path: "src/entities/slime.py"
      provides: "Slime without orphaned hold_position method"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated MAP-02 and ABL-02 requirement text"
      contains: "pml-to-ldtk"
    - path: "assets/entity-schema.json"
      provides: "Schema with intgrid 4 as event_marker, version bumped"
      contains: "event_marker"
    - path: ".planning/phases/14-tech-debt-schema-cleanup/14-VERIFICATION.md"
      provides: "Phase 10 ABL-02 verification document"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: "assets/entity-schema.json"
      via: "MAP-02 text references event-gated doors defined in schema"
      pattern: "event-gated"
---

<objective>
Clean orphaned code (D-13, D-14), update entity schema IntGrid value 4 semantics (D-05), bump schema version (D-19), verify Phase 10 ABL-02 CRACKED_V breaking (D-15), and rewrite MAP-02/ABL-02 requirement text (D-07, D-16).

Purpose: Close remaining tech debt items and update documentation to reflect the current system state. ABL-02 verification confirms Phase 10 work is complete for vertical gating.

Output: Cleaned codebase, updated schema, rewritten requirements, ABL-02 VERIFICATION.md.
</objective>

<execution_context>
@C:\Users\garva\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\garva\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-tech-debt-schema-cleanup/14-CONTEXT.md
@.planning/phases/14-tech-debt-schema-cleanup/14-RESEARCH.md
@.planning/phases/14-tech-debt-schema-cleanup/14-01-SUMMARY.md
@.planning/phases/14-tech-debt-schema-cleanup/14-02-SUMMARY.md

<interfaces>
<!-- slime.py hold_position (lines 153+) -- to DELETE entirely (D-13) -->
def hold_position(self, direction, player_x, player_y, level_map):
    """Hold slime at a fixed position (long hold). Sets is_holding_position=True."""

<!-- slime.py reposition (lines 117+) -- KEEP, references hold_position in docstring only -->
def reposition(self, direction, player_x, player_y, level_map):
    """Reposition slime in tapped direction without disabling follow (UAT gap fix).
    Same position-finding logic as hold_position but does NOT set is_holding_position."""

<!-- entity-schema.json intgrid value 4 (current) -->
"4": { "name": "gate", "behavior": "collision+gate", "broken_by": null, "note": "Boss gates -- open/close programmatically" }

<!-- REQUIREMENTS.md current MAP-02 text -->
MAP-02: Room layout driven by pml-to-ldtk pipeline with event-gated doors (replaces tile ID 4 boss gates).

<!-- REQUIREMENTS.md current ABL-02 text -->
ABL-02: Nitro-Ejection fusion (Infinite Jump) for vertical endgame traversal.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Orphaned code cleanup + schema intgrid/version update</name>
  <files>src/entities/slime.py, assets/entity-schema.json</files>
  <read_first>
    - src/entities/slime.py (find hold_position method, note its line range; also check reposition docstring reference)
    - assets/entity-schema.json (intgrid value 4, version field)
  </read_first>
  <action>
    1. In src/entities/slime.py, DELETE the entire hold_position method (D-13).
       The method starts at the line "def hold_position(self, direction, player_x, player_y, level_map):"
       and extends to the end of its body. Delete from the def line through
       the last line of the method body.

    2. In src/entities/slime.py reposition() docstring, update the reference:
       Change: "Same position-finding logic as hold_position but does NOT set is_holding_position."
       To:     "Tap reposition without disabling follow (UAT gap fix)."
       (Remove the hold_position reference since the method no longer exists.)

    3. Verify no other file in src/ imports or calls hold_position:
       grep -rn "hold_position" src/
       If any callers exist besides slime.py itself, they need updating too.

    4. In assets/entity-schema.json intgrid values, update value "4" (D-05):
       Change: "4": { "name": "gate", "behavior": "collision+gate", "broken_by": null, "note": "Boss gates -- open/close programmatically" }
       To:     "4": { "name": "event_marker", "behavior": "collision+gate", "broken_by": null, "note": "Event-gated position marker. Used by switch tiles and event doors." }

    5. In assets/entity-schema.json, bump version (D-19):
       Change: "version": "0.2.0"
       To:     "version": "0.3.0"
       (Minor bump -- adding event door fields is a backwards-compatible addition, not a breaking change.)

    6. Update entity-schema.json "updated" field to current date:
       "updated": "2026-03-29" (or current date if different)
  </action>
  <verify>
    <automated>python -m pytest tests/ -x --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - src/entities/slime.py does NOT contain "def hold_position"
    - src/entities/slime.py reposition docstring does NOT contain "hold_position"
    - grep -rn "hold_position" src/ returns 0 matches
    - entity-schema.json intgrid value 4 name is "event_marker" (not "gate")
    - entity-schema.json version is "0.3.0"
    - python -m pytest tests/ -x exits 0
  </acceptance_criteria>
  <done>hold_position deleted from slime.py; intgrid value 4 repurposed to event_marker; schema version bumped to 0.3.0.</done>
</task>

<task type="auto">
  <name>Task 2: ABL-02 verification + MAP-02/ABL-02 requirement rewrites</name>
  <files>.planning/REQUIREMENTS.md, .planning/phases/14-tech-debt-schema-cleanup/14-VERIFICATION.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (current MAP-02 and ABL-02 text)
    - src/level/map.py (grep for CRACKED_V, TILE_CRACKED_V to verify breaking logic exists)
    - src/entities/player.py (grep for CRACKED_V, drill_dive, boost -- verify ability code handles cracked_v blocks)
    - src/core/constants.py (DRILL_CRACKED_V_COST, BOOST_CRACKED_V_COST constants confirm Phase 10 work)
    - .planning/phases/10-nitro-ejection-endgame/10-CONTEXT.md (D-01, D-02, D-03 define CRACKED_V breaking)
  </read_first>
  <action>
    1. VERIFY ABL-02 (D-15): Confirm CRACKED_V breaking code exists:
       - grep for TILE_CRACKED_V in map.py (should show collision handling)
       - grep for CRACKED_V in player.py (should show drill dive + boost breaking logic)
       - Confirm DRILL_CRACKED_V_COST=20.0 and BOOST_CRACKED_V_COST=25.0 in constants.py
       - Confirm intgrid value 12 in entity-schema.json has broken_by: ["drill_dive", "slime_boost"]
       Record findings in VERIFICATION.md.

    2. Create .planning/phases/14-tech-debt-schema-cleanup/14-VERIFICATION.md:
       ```markdown
       # Phase 14: ABL-02 Verification

       ## CRACKED_V Breaking (Phase 10 Implementation)

       **Status:** VERIFIED

       ### Evidence
       - constants.py: DRILL_CRACKED_V_COST = 20.0, BOOST_CRACKED_V_COST = 25.0
       - entity-schema.json intgrid 12: broken_by ["drill_dive", "slime_boost"]
       - player.py: [describe drill dive cracked_v breaking code location]
       - player.py: [describe boost cracked_v breaking code location]
       - map.py: [describe TILE_CRACKED_V collision handling]

       ### Scope
       - VERIFIED: Vertical gating via CRACKED_V blocks (Drill Dive down + Boost up)
       - DEFERRED: Infinite flight / Nitro-Ejection capstone (Phase 11, requires SYS-04 Juice Capacity)

       ### Test Coverage
       - Existing tests cover CRACKED_V block interaction
       - Run: python -m pytest tests/ -k "cracked" --tb=short
       ```

    3. In .planning/REQUIREMENTS.md, rewrite MAP-02 (D-07):
       Change: "- [ ] **MAP-02**: Room layout driven by pml-to-ldtk pipeline with event-gated doors (replaces tile ID 4 boss gates)."
       To:     "- [x] **MAP-02**: Room layouts driven by pml-to-ldtk pipeline with event-gated doors (replaces tile ID 4 boss gates)."

    4. In .planning/REQUIREMENTS.md, rewrite ABL-02 (D-16):
       Change: "- [ ] **ABL-02**: Nitro-Ejection fusion (Infinite Jump) for vertical endgame traversal. (2026-03-28)"
       To:     "- [x] **ABL-02**: CRACKED_V vertical gating via Drill Dive (down) and Slime Boost (up). Infinite flight capstone deferred to Phase 11 (requires SYS-04 Juice Capacity upgrades). (2026-03-29)"
  </action>
  <verify>
    <automated>python -m pytest tests/ --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - .planning/REQUIREMENTS.md MAP-02 line starts with "- [x]" and contains "pml-to-ldtk pipeline with event-gated doors"
    - .planning/REQUIREMENTS.md ABL-02 line starts with "- [x]" and contains "CRACKED_V vertical gating"
    - .planning/REQUIREMENTS.md ABL-02 line contains "Infinite flight capstone deferred to Phase 11"
    - 14-VERIFICATION.md exists and contains "Status: VERIFIED" (or "VERIFIED")
    - 14-VERIFICATION.md contains "DRILL_CRACKED_V_COST" and "BOOST_CRACKED_V_COST"
    - python -m pytest tests/ --tb=short exits 0
  </acceptance_criteria>
  <done>ABL-02 CRACKED_V breaking verified and documented; MAP-02 marked complete with event-gated door text; ABL-02 requirement split between Phase 10 (done) and Phase 11 (infinite flight).</done>
</task>

</tasks>

<verification>
- grep -n "hold_position" src/entities/slime.py (0 matches)
- grep "event_marker" assets/entity-schema.json (intgrid 4 updated)
- grep "0.3.0" assets/entity-schema.json (version bumped)
- grep "MAP-02" .planning/REQUIREMENTS.md (rewritten text)
- grep "ABL-02" .planning/REQUIREMENTS.md (split text)
- cat .planning/phases/14-tech-debt-schema-cleanup/14-VERIFICATION.md (exists, VERIFIED)
- python -m pytest tests/ --tb=short (full suite green)
</verification>

<success_criteria>
- hold_position deleted, no references remain in src/
- IntGrid value 4 is "event_marker" in schema
- Schema version is 0.3.0
- MAP-02 requirement rewritten and marked complete
- ABL-02 requirement split: vertical gating complete, infinite flight deferred
- 14-VERIFICATION.md documents CRACKED_V evidence
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/14-tech-debt-schema-cleanup/14-03-SUMMARY.md`
</output>
