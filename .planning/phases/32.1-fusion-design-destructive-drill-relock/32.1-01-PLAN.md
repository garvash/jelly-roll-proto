---
phase: 32.1-fusion-design-destructive-drill-relock
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/FUSION-DESIGN.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "FUSION-DESIGN.md is LOCKED at end of phase with a new locked_commit SHA (cycle 3)"
    - "The new locked_commit SHA points at the doc-write commit (commit 2 of D-15), not the frontmatter-amend commit"
    - "Cycle-2 SHA 9047b590cc648184f8c6c17c0ed3830296edc72c is preserved in prior_locked_commit and prior_lock_chain"
    - "§Drill-Dive Contract contains a new ### Enemy Interaction subsection between Block-break branch contract and Two exit conditions"
    - "Three named identifiers DRILL_DAMAGE, DRILL_ENEMY_COST, drill_enemy_hit appear verbatim in the doc"
    - "Five ripple amendments landed: FUS-03 (D-04), §Juice Economy mana-shield carve-out (D-05/D-13), §Fusion FSM event registry (D-06), §Cut Abilities Bubble Shield clarifying sentence (D-07), §Drill-Dive Contract i-frames row resolved-note + Two-exit footnote (D-02/D-08)"
    - "Three new commits visible in git log -- .planning/FUSION-DESIGN.md in correct order: UNLOCK → DOC-WRITE → FRONTMATTER-AMEND"
    - "§Acceptance Checklist and the Mermaid FSM diagram are NOT modified (D-09 respected)"
    - "No .py files are touched in any commit of this phase"
  artifacts:
    - path: ".planning/FUSION-DESIGN.md"
      provides: "Locked FUSION-DESIGN at cycle-3 SHA with destructive-drill Enemy Interaction subsection + 5 ripple amendments"
      contains: "### Enemy Interaction"
  key_links:
    - from: "FUSION-DESIGN.md frontmatter locked_commit"
      to: "Doc-write commit (commit 2 of D-15)"
      via: "git rev-parse HEAD~1 captured between commit 2 and commit 3"
      pattern: "^locked_commit: [0-9a-f]{40}$"
    - from: "FUSION-DESIGN.md frontmatter prior_locked_commit"
      to: "Cycle-2 doc-write commit"
      via: "Verbatim cycle-2 SHA"
      pattern: "9047b590cc648184f8c6c17c0ed3830296edc72c"
    - from: "§Drill-Dive Contract → Enemy Interaction"
      to: "§Juice Economy mana-shield carve-out"
      via: "Bidirectional cross-reference (D-05/D-13)"
      pattern: "see §Drill-Dive Contract → Enemy Interaction"
---

<rationale>
Ten tasks justified: this plan implements the staging-then-single-commit-2 pattern. Tasks 2-7
each stage one body edit (Enemy Interaction subsection + 5 ripple amendments) without committing;
Task 8 atomically commits all six edits as commit 2 of the three-commit dance (UNLOCK → DOC-WRITE
→ FRONTMATTER-AMEND per D-15). Collapsing Tasks 2-7 into one giant task would bury 6 distinct
edit specifications inside one `<action>` block and make per-edit acceptance criteria unreadable.
The staged-task form preserves auditability: each ripple amendment has its own `<read_first>`,
verbatim before/after prose, `<verify>` grep, and dedicated acceptance criteria. Task 1 (UNLOCK
commit), Task 8 (DOC-WRITE commit), Task 9 (FRONTMATTER-AMEND commit), and Task 10 (verification
suite) are structurally indivisible. Total: 10 tasks, 3 commits, 1 file modified.
</rationale>

<objective>
Re-lock FUSION-DESIGN.md at a new SHA (cycle 3) with a new "Enemy Interaction" subsection
inside §Drill-Dive Contract documenting destructive-drill behavior per Phase 33's 33-CONTEXT.md
D-21/D-22, plus five ripple amendments, via the established three-commit dance
(UNLOCK → DOC-WRITE → FRONTMATTER-AMEND) per D-15/D-16.

Purpose: Pre-phase Hard Gate before Phase 33. /gsd-plan-phase 33 cannot run until
the new locked_commit SHA is observable in this doc's frontmatter. Locks the names
DRILL_DAMAGE, DRILL_ENEMY_COST, drill_enemy_hit so Phase 33 builds against a fixed
contract; values remain Phase 33-tunable per D-11/D-12.

Output: One file modified (.planning/FUSION-DESIGN.md), three new commits on main.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/FUSION-DESIGN.md
@.planning/phases/32.1-fusion-design-destructive-drill-relock/32.1-CONTEXT.md
@.planning/phases/33-per-ability-feel-pass-drill-only-under-single-fusion-prototype/33-CONTEXT.md

<scope_invariants>
THIS IS A DOC-ONLY PHASE.
- The ONLY file modified is .planning/FUSION-DESIGN.md.
- If any task touches a .py file, the task is wrong — abort and re-read CONTEXT.md.
- Out of scope (explicitly Phase 33's work):
  - Implementing destructive-drill in src/fusion/drill_dive.py
  - Adding DRILL_ENEMY_COST / SLIME_DAZE_COST to assets/physics-schema.json
  - Wiring drill_enemy_hit subscribers in main.py:Game.__init__ or src/anim/event_bus.py
  - Authoring 33-FEEL-TARGETS.md
  - Updating ROADMAP.md §Phase 33 success criterion #3 ("three exit conditions" stale wording)
- D-09 sections are OFF-LIMITS:
  - §Acceptance Checklist (Phase 32 historical — modifying muddies audit trail)
  - Mermaid FSM diagram inside §Fusion FSM (note right of FUSED still accurate)
</scope_invariants>

<commit_sequence_invariants>
EXACT three-commit order (D-15) — non-negotiable:

  Commit 1 (UNLOCK):  flip status: LOCKED → UNLOCKED. Frontmatter only. NO body changes.
                      Message: "docs(32.1): unlock FUSION-DESIGN.md to add destructive-drill subsection"

  Commit 2 (DOC-WRITE): author Enemy Interaction subsection + ALL 5 ripple amendments
                        + flip status: UNLOCKED → LOCKED + set locked_commit: TBD.
                        Message: "docs(32.1): add destructive-drill enemy-interaction subsection to FUSION-DESIGN"

  Commit 3 (FRONTMATTER-AMEND): capture commit 2's SHA via `git rev-parse HEAD~1` BEFORE writing,
                                populate locked_commit:, set prior_locked_commit, extend prior_lock_chain,
                                rewrite relock_reason. NO body changes.
                                Message: "docs(32.1): re-lock FUSION-DESIGN at <sha> (cycle 3)"

Cycle-2 precedent SHAs (load-bearing — typo breaks audit trail):
  af7a4c4 — UNLOCK commit (cycle 2)
  9047b590cc648184f8c6c17c0ed3830296edc72c — Doc-write commit (cycle 2) → BECOMES prior_locked_commit
  e655e55 — Frontmatter amendment (cycle 2)
</commit_sequence_invariants>

<interfaces>
<!-- Frontmatter schema — current state of the doc (verbatim, lines 1-10) -->
```yaml
---
status: LOCKED
locked_at: 2026-04-20
locked_commit: 9047b590cc648184f8c6c17c0ed3830296edc72c
prior_locked_commit: 2bc5cfd68ab0c77661572ad6f6f377cbf60971c5
prior_lock_chain:
  - e6263693dc7d3baee2cefc4bea757610bfe6b51e  # original 3-exit draft (auto/manual exits, drill on DOWN+V via dash action)
  - 2bc5cfd68ab0c77661572ad6f6f377cbf60971c5  # manual-exit stripped (still on DOWN+V via dash)
relock_reason: Drill + pogo relocated to DOWN+SPACE (jump action) — Mario-64 ground-pound mental model; same input branches on is_fused (pogo unfused, drill fused) per D-06. Dash dropped from prototype scope; V button reserved/unused. Matches v1.3 drill code so no Phase 32 input remap needed; Phase 32 only ADDS the unfused pogo branch on the existing DOWN+SPACE airborne input.
---
```

<!-- Sibling-subsection model for the new Enemy Interaction subsection (Block-break branch contract; doc lines 297-315 verbatim shape) -->
The new subsection mirrors the SHAPE of "Block-break branch contract":
- Heading level: ###
- Anchor comment line directly under heading (one short italic sentence pointing at the rule)
- Numbered or bulleted rule list with named identifiers cited verbatim
- Citation line at end pointing at design source

<!-- Existing FUS-03 statement (doc line 30 verbatim) -->
"- **FUS-03**: Drill-dive v1.3 regression contract. Documented velocity, per-block juice cost, CRACKED_V handling, and entry/exit conditions serve as Phase 32's parity target — verified by inspection and smoke test, no pytest required (per D-28). See [§ Drill-Dive Contract](#drill-dive-contract)."

<!-- Existing mana-shield line in §Juice Economy → Juice Consumption During Fuse (doc line 251 verbatim) -->
"- **Mana shield** (fused damage taken): `MANA_SHIELD_COST = 20.0 juice per hit` (v1.1 D-04 retained). Any fused damage drains juice instead of HP."

<!-- Existing event-emissions "New events proposed by this doc" list (doc lines 177-181) -->
"- `drill_start` — fired at drill-dive activation (in FUSED state with DOWN+SPACE held in air). Anim hook for drill windup/plunge frame. Today code has no per-activation drill event; only `drill_impact` on landing.
- `drill_block_break` — fired per-block destruction during drill. **Distinct from `drill_impact`** (which is landing on solid). Enables per-break particle/shake in Phase 31/35 without conflating with the landing event. Today code has no per-block event (the shake/hitstop is triggered by `on_block_break()` directly at `player.py:235-239`).
- `drill_end` — fired on any drill exit (solid contact, or juice=0). Pairs with `drill_start` as the anim lifecycle bookend."

<!-- Existing Bubble Shield rationale (doc line 376 verbatim) -->
"- **Bubble Shield** — absorb-damage fusion shield. Cut because the mana-shield behavior (`MANA_SHIELD_COST = 20.0 juice per fused damage hit`, v1.1 D-04 retained in this doc — see [§ Juice Economy](#juice-economy)) already provides the \"fused damage drains juice\" primitive; a separate shield ability would double-count."

<!-- Existing i-frames row in "What Phase 32 is allowed to change vs. preserve" table (doc line 361 verbatim within the May tune row) -->
"`DRILL_SPEED`, `DRILL_DRIFT_SPEED`, all drill costs, tap/hold threshold, WINDUP duration, accelerated-regen multiplier, whether drill gains i-frames (per Open-Q #1 — currently NONE), pogo bounce force / cooldown / contact rules"

<!-- Existing "Two exit conditions" callout opening (doc line 321 verbatim) -->
"> **D-08(c) (manual unfuse via Z-hold mid-drill) was removed from the design 2026-04-20** (post-lock decision). The original draft included a third exit (Z-hold → UNFUSE_WINDUP → EXIT_MANUAL → slime ejects unharmed). It was stripped because the second-pass commitment ritual is meant to feel committed, and an easy mid-drill bail-out cheapened it. Once drill begins, it cannot be aborted — only solid contact (a) or juice empty (b) ends it. The v1.3 jump-press mid-drill cancel at `src/entities/player.py:463-468` is **removed entirely** in Phase 32 with no replacement. (See `prior_locked_commit` in this doc's frontmatter for the original three-exit draft.)"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: UNLOCK commit — flip status field only</name>
  <files>.planning/FUSION-DESIGN.md</files>
  <read_first>
    - .planning/FUSION-DESIGN.md (lines 1-10 — verify current frontmatter is `status: LOCKED` and locked_commit equals 9047b590cc648184f8c6c17c0ed3830296edc72c)
    - .planning/phases/32.1-fusion-design-destructive-drill-relock/32.1-CONTEXT.md (D-14, D-15)
  </read_first>
  <action>
    Edit ONLY line 2 of .planning/FUSION-DESIGN.md.

    BEFORE: `status: LOCKED`
    AFTER:  `status: UNLOCKED`

    No other changes — leave locked_commit, locked_at, prior_locked_commit, prior_lock_chain,
    relock_reason, and the entire doc body untouched. The only diff hunk should be the single
    line flip.

    Then commit with EXACTLY this message (no Co-Authored-By trailer; the doc-only re-lock dance
    historically uses bare commit messages — cycle 2 precedent: af7a4c4 has no trailer):

      docs(32.1): unlock FUSION-DESIGN.md to add destructive-drill subsection

    Stage only .planning/FUSION-DESIGN.md.
  </action>
  <verify>
    <automated>git diff HEAD~1 HEAD -- .planning/FUSION-DESIGN.md | grep -E "^[+-]status:" | sort | tr -d ' ' | tr '\n' '|' | grep -q "^-status:LOCKED|+status:UNLOCKED|$" &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `git log --oneline -1 -- .planning/FUSION-DESIGN.md` first line ends with `docs(32.1): unlock FUSION-DESIGN.md to add destructive-drill subsection`
    - `grep -c "^status: UNLOCKED$" .planning/FUSION-DESIGN.md` outputs `1`
    - `grep -c "^status: LOCKED$" .planning/FUSION-DESIGN.md` outputs `0`
    - `grep -c "^locked_commit: 9047b590cc648184f8c6c17c0ed3830296edc72c$" .planning/FUSION-DESIGN.md` outputs `1` (locked_commit unchanged in commit 1)
    - `git diff HEAD~1 HEAD --stat -- .planning/FUSION-DESIGN.md` shows 1 file changed, 1 insertion, 1 deletion (single-line flip — no body changes)
    - `git log --name-only -1 --pretty=format:"" -- .planning/FUSION-DESIGN.md | grep -v '^$' | wc -l` outputs `1` (only FUSION-DESIGN.md modified in this commit)
  </acceptance_criteria>
  <done>
    Commit 1 of 3 on main. status: UNLOCKED. locked_commit unchanged. No body diff.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author Enemy Interaction subsection (D-01/D-03/D-10/D-11)</name>
  <files>.planning/FUSION-DESIGN.md</files>
  <read_first>
    - .planning/FUSION-DESIGN.md (lines 264-321 — read the full §Drill-Dive Contract to identify the exact insertion point: AFTER the "Block-break branch contract" subsection ends at line 315 and BEFORE the "### Two exit conditions" heading at line 317)
    - .planning/phases/32.1-fusion-design-destructive-drill-relock/32.1-CONTEXT.md (D-01, D-03, D-10, D-11, D-12, plus the "Specific Ideas" §item-1 about narrative flow)
  </read_first>
  <action>
    Insert the following subsection block VERBATIM into .planning/FUSION-DESIGN.md, positioned
    between the end of the "### Block-break branch contract" subsection (after the line ending
    `Emit new \`drill_block_break\` event.`) and the start of the "### Two exit conditions"
    heading (D-01).

    Locate the insertion point: find the bullet block ending in:
      `5. Per MEMORY block-gate-hierarchy constraint: **drill is the CRACKED_V opener**. Other block gates (soft/kick, CRACKED_H/ram, goo-mold/late-game) are NOT drill-eligible — attempting to drill into a non-destructible / non-CRACKED_V solid triggers Exit (a) below.`
    Then the bullet:
      `- **Solid (non-destructible)**: triggers Exit (a) — see below. Drill does NOT pass through.`
    Then the bullet:
      `- **No per-block event in v1.3**: ...`

    The Block-break branch contract subsection ends after that "No per-block event in v1.3"
    bullet. Insert the new subsection AFTER that bullet (and any blank line immediately following),
    BEFORE the `### Two exit conditions` heading.

    EXACT prose to insert (copy verbatim — every backtick, em-dash, and citation is locked):

    ```
    ### Enemy Interaction

    *Anchor: `enemy-interaction`. Defines destructive-drill behavior — the third per-frame collision rule alongside soft/CRACKED_V tile interaction. Source: `33-CONTEXT.md` D-03/D-04/D-05/D-06.*

    Drill is **destructive offense, not invulnerability**. While DIVING, intersecting an enemy AABB damages the enemy and drains juice; the drill takes no damage from the contact and continues plunging. The "safety" of drill comes from killing what's in front of you fast enough to clear the lane — not from i-frames.

    | Rule | Value | Source |
    |------|-------|--------|
    | Continue-through | Drill in flight intersecting an enemy AABB deals damage to the enemy, takes no damage, and continues drilling. No exit, no bounce, no hitstop. Same passthrough behavior as soft destructibles (per § Block-break branch contract). | 33-CONTEXT.md D-03 |
    | `DRILL_DAMAGE` | **1 per hit** (matches `POGO_DAMAGE`). The "upgrade" relative to pogo is structural, not numeric — drill's `DRILL_SPEED = 2.0 px/frame` plunge means a multi-tile-tall enemy gets hit ~7 times during a single drill chain (4 px/frame contact rate vs. pogo's single-bounce contact), so drill out-damages pogo via repeated-frame contact. | 33-CONTEXT.md D-04 |
    | `DRILL_ENEMY_COST` | **TBD per hit; Phase 33 picks via panel iteration, target range 10–20 juice.** Drains juice on each enemy AABB intersection — analog to `DRILL_CRACKED_V_COST = 20.0` but tunable. Naturally caps drill chains; enemies become "tough destructibles you spend juice to kill." | 33-CONTEXT.md D-05 |

    1. **Continue-through rule.** Per-frame, while DIVING, the drill checks for enemy AABB intersection alongside the existing tile-collision check. On intersection: deal `DRILL_DAMAGE = 1` to the enemy, drain `DRILL_ENEMY_COST` juice from slime, emit the new `drill_enemy_hit` event (see § Fusion FSM event emissions), and continue drilling — `dy` stays at `DRILL_SPEED`, no `state` change, no exit transition. The drill is **invulnerable to enemies during DIVING** (this is the carve-out asserted in § Juice Economy mana-shield rule).
    2. **`DRILL_DAMAGE = 1 per hit`** (matches `POGO_DAMAGE`). The contract locks the *name* and the *parity-with-pogo numeric value*; Phase 33 may reposition the constant in code (hardcoded vs. schema is Phase 33 planner discretion per 33-CONTEXT.md D-04). Repeated-frame contact does the heavy lifting — a 16-px-tall enemy absorbs ~4 hits during a single drill plunge (drill's 2 px/frame at 60fps gives 4-px coverage every 2 frames, plus AABB overlap at entry/exit edges, totalling ~7 hits across a multi-tile enemy stack).
    3. **`DRILL_ENEMY_COST` per hit.** Drains juice the same way `DRILL_CRACKED_V_COST = 20.0` does for gate blocks. Value is TBD; Phase 33 picks via panel iteration with a target range of **10–20 juice** per hit. The cost is what makes drill chains feel finite — once juice empties mid-chain, Exit (b) (juice = 0 → dissipate) triggers naturally without needing a separate "drill broke" rule. Schema-vs-hardcoded placement is Phase 33's call (recommendation in 33-CONTEXT.md D-05: schema, so the panel can tune live).

    **Names locked by this re-lock.** `DRILL_DAMAGE`, `DRILL_ENEMY_COST`, and `drill_enemy_hit` are canonical identifiers — Phase 33 builds against these names verbatim. Renaming requires another re-lock cycle. Values (`DRILL_DAMAGE = 1`, `DRILL_ENEMY_COST = TBD in 10–20 range`) are tunable in Phase 33 without re-lock per the same rule the existing contract applies to `DRILL_SPEED` and `DRILL_IMPACT_COST`.

    **Resolves Open-Q #1 (drill i-frames) by offense, not by adding invulnerability.** The earlier i-frames question — "should drill gain i-frames per Phase 33 playtest?" — is answered structurally: drill is invulnerable to enemies during DIVING because enemies cannot damage the drilling player (mana shield is bypassed by the continue-through rule), but the drill does NOT gain i-frames against environmental hazards. Drill's safety against enemies is destructive; environmental hazards remain a real threat. See § Juice Economy mana-shield carve-out and the resolved-note in the "May tune" table below.
    ```

    Stage but DO NOT commit yet — Tasks 2 through 7 stage incrementally; Task 8 commits all of them as commit 2.
  </action>
  <verify>
    <automated>grep -c "^### Enemy Interaction$" .planning/FUSION-DESIGN.md | grep -q "^1$" &amp;&amp; grep -c "DRILL_DAMAGE" .planning/FUSION-DESIGN.md | awk '$1 &gt;= 3' | grep -q DRILL_DAMAGE &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^### Enemy Interaction$" .planning/FUSION-DESIGN.md` outputs `1`
    - `grep -c "DRILL_DAMAGE" .planning/FUSION-DESIGN.md` outputs `>= 3` (table row + numbered rule + names-locked sentence)
    - `grep -c "DRILL_ENEMY_COST" .planning/FUSION-DESIGN.md` outputs `>= 3`
    - `grep -c "drill_enemy_hit" .planning/FUSION-DESIGN.md` outputs `>= 1` (cross-reference to § Fusion FSM)
    - `grep -c "enemy-interaction" .planning/FUSION-DESIGN.md` outputs `>= 1` (anchor)
    - `grep -nE "^### (Block-break branch contract|Enemy Interaction|Two exit conditions)$" .planning/FUSION-DESIGN.md` shows the three headings in that exact order (D-01 placement)
    - `grep -c "33-CONTEXT.md D-0[3-5]" .planning/FUSION-DESIGN.md` outputs `>= 1` (citation discipline per D-03)
    - `grep -c "POGO_DAMAGE" .planning/FUSION-DESIGN.md` outputs `>= 1` (parity reference per D-04)
    - `grep -c "target range 10–20" .planning/FUSION-DESIGN.md` outputs `>= 1` (D-05 value range cited verbatim) — note: em-dash (U+2013) between 10 and 20
  </acceptance_criteria>
  <done>
    Enemy Interaction subsection inserted into §Drill-Dive Contract between Block-break branch contract
    and Two exit conditions. Three named identifiers cited verbatim. Anchor + 33-CONTEXT.md citations present.
    File staged, not yet committed.
  </done>
</task>

<task type="auto">
  <name>Task 3: Ripple amendment — FUS-03 statement (D-04)</name>
  <files>.planning/FUSION-DESIGN.md</files>
  <read_first>
    - .planning/FUSION-DESIGN.md (line 30 — locate the existing FUS-03 bullet verbatim)
    - .planning/phases/32.1-fusion-design-destructive-drill-relock/32.1-CONTEXT.md (D-04)
  </read_first>
  <action>
    Find the existing FUS-03 bullet in §Requirements (line 30 of the doc as currently authored).
    Verbatim current text:

      `- **FUS-03**: Drill-dive v1.3 regression contract. Documented velocity, per-block juice cost, CRACKED_V handling, and entry/exit conditions serve as Phase 32's parity target — verified by inspection and smoke test, no pytest required (per D-28). See [§ Drill-Dive Contract](#drill-dive-contract).`

    Replace it with (preserving the FUS-03 anchor; the only change is appending one sentence
    BEFORE the "See [§ Drill-Dive Contract](#drill-dive-contract)." cross-reference):

      `- **FUS-03**: Drill-dive v1.3 regression contract. Documented velocity, per-block juice cost, CRACKED_V handling, and entry/exit conditions serve as Phase 32's parity target — verified by inspection and smoke test, no pytest required (per D-28). Phase 33 expands the contract with a destructive-drill Enemy Interaction subsection (`DRILL_DAMAGE`, `DRILL_ENEMY_COST`, `drill_enemy_hit`) — see [§ Drill-Dive Contract → Enemy Interaction](#enemy-interaction) (within [§ Drill-Dive Contract](#drill-dive-contract)).`

    Stage but do not commit.
  </action>
  <verify>
    <automated>grep -c "Phase 33 expands the contract with a destructive-drill Enemy Interaction subsection" .planning/FUSION-DESIGN.md | grep -q "^1$" &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Phase 33 expands the contract with a destructive-drill Enemy Interaction subsection" .planning/FUSION-DESIGN.md` outputs `1`
    - `grep -c "(#enemy-interaction)" .planning/FUSION-DESIGN.md` outputs `>= 1` (cross-reference link to the new anchor)
    - `grep -c "FUS-03" .planning/FUSION-DESIGN.md` count is **unchanged** vs. pre-edit (the bullet itself is amended, not duplicated; original count stays the same)
    - The FUS-03 bullet still references the `#drill-dive-contract` anchor (verifiable: `grep -c "FUS-03.*#drill-dive-contract" .planning/FUSION-DESIGN.md` outputs `>= 1`) — preserved per D-04 "preserves the existing FUS-03 anchor". (Note: the NIT collapse merged the trailing double-See into a single parenthetical `(within [§ Drill-Dive Contract](#drill-dive-contract))`; the anchor is retained, only the redundant outer sentence dropped.)
  </acceptance_criteria>
  <done>
    FUS-03 amended with the Phase 33 Enemy Interaction expansion clause. Original anchor preserved.
  </done>
</task>

<task type="auto">
  <name>Task 4: Ripple amendment — §Juice Economy mana-shield carve-out (D-05/D-13)</name>
  <files>.planning/FUSION-DESIGN.md</files>
  <read_first>
    - .planning/FUSION-DESIGN.md (line 251 region — locate the existing "Mana shield (fused damage taken)" line inside §Juice Economy → Juice Consumption During Fuse)
    - .planning/phases/32.1-fusion-design-destructive-drill-relock/32.1-CONTEXT.md (D-05, D-13, plus the "Specific Ideas" §item-3 about one-liner shape)
  </read_first>
  <action>
    Find the existing mana-shield bullet in §Juice Economy → Juice Consumption During Fuse (D-19, D-20).
    Verbatim current text (one full bullet in a list):

      `- **Mana shield** (fused damage taken): \`MANA_SHIELD_COST = 20.0 juice per hit\` (v1.1 D-04 retained). Any fused damage drains juice instead of HP.`

    Append the carve-out as a sub-bullet (or inline sentence — pick the inline form for readability;
    matches D-05 which calls this a "one-liner"). Replace the bullet with:

      `- **Mana shield** (fused damage taken): \`MANA_SHIELD_COST = 20.0 juice per hit\` (v1.1 D-04 retained). Any fused damage drains juice instead of HP. **During DIVING, mana shield is bypassed** — enemies cannot damage the drilling player; drill's safety comes from offense, not invulnerability. See [§ Drill-Dive Contract → Enemy Interaction](#enemy-interaction).`

    Stage but do not commit.
  </action>
  <verify>
    <automated>grep -c "During DIVING, mana shield is bypassed" .planning/FUSION-DESIGN.md | grep -q "^1$" &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "During DIVING, mana shield is bypassed" .planning/FUSION-DESIGN.md` outputs `1`
    - `grep -c "drill's safety comes from offense, not invulnerability" .planning/FUSION-DESIGN.md` outputs `1`
    - `grep -c "MANA_SHIELD_COST = 20.0" .planning/FUSION-DESIGN.md` count is **unchanged** vs. pre-edit (original line preserved, only appended-to)
    - The line containing "MANA_SHIELD_COST = 20.0 juice per hit" remains in §Juice Economy (verifiable by `grep -B 2 "MANA_SHIELD_COST = 20.0 juice per hit" .planning/FUSION-DESIGN.md` showing nearby §Juice Economy context, not §Cut Abilities — Cut Abilities still references MANA_SHIELD_COST elsewhere; this check confirms the §Juice Economy occurrence wasn't accidentally moved)
  </acceptance_criteria>
  <done>
    Mana-shield bullet amended with DIVING bypass clause + cross-ref to new Enemy Interaction subsection.
  </done>
</task>

<task type="auto">
  <name>Task 5: Ripple amendment — §Fusion FSM event registry adds drill_enemy_hit (D-06)</name>
  <files>.planning/FUSION-DESIGN.md</files>
  <read_first>
    - .planning/FUSION-DESIGN.md (lines 168-185 — locate "### Event emissions" subsection inside §Fusion FSM, including the existing "New events proposed by this doc" block ending with the `drill_end` bullet)
    - .planning/phases/32.1-fusion-design-destructive-drill-relock/32.1-CONTEXT.md (D-06)
  </read_first>
  <action>
    Find the "New events proposed by this doc" block in §Fusion FSM → Event emissions. The block
    currently ends with:

      `- \`drill_end\` — fired on any drill exit (solid contact, or juice=0). Pairs with \`drill_start\` as the anim lifecycle bookend.`

    Append a new bullet AFTER `drill_end` (and before the empty line preceding "### Cross-references"):

      `- \`drill_enemy_hit\` — fired per enemy AABB intersection during DIVING. Anim hook for drill enemy-hit particle + SFX. Phase 33 implements; this doc names it. Mirrors \`drill_block_break\` shape (per-collision event, distinct from lifecycle bookends).`

    Stage but do not commit.
  </action>
  <verify>
    <automated>grep -c "^- \`drill_enemy_hit\`" .planning/FUSION-DESIGN.md | grep -q "^1$" &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "drill_enemy_hit" .planning/FUSION-DESIGN.md` outputs `>= 2` (the new event-emissions bullet PLUS the cross-reference inside the Enemy Interaction subsection from Task 2)
    - `grep -c "fired per enemy AABB intersection during DIVING" .planning/FUSION-DESIGN.md` outputs `1`
    - `grep -nE "^- \`drill_(start|block_break|end|enemy_hit)\`" .planning/FUSION-DESIGN.md` shows four bullets in event-emissions order: `drill_start`, `drill_block_break`, `drill_end`, `drill_enemy_hit` (new appended last)
  </acceptance_criteria>
  <done>
    drill_enemy_hit registered alongside existing new events in §Fusion FSM event registry.
  </done>
</task>

<task type="auto">
  <name>Task 6: Ripple amendment — §Cut Abilities Bubble Shield clarifying sentence (D-07)</name>
  <files>.planning/FUSION-DESIGN.md</files>
  <read_first>
    - .planning/FUSION-DESIGN.md (line 376 — locate the Bubble Shield bullet in §Cut Abilities)
    - .planning/phases/32.1-fusion-design-destructive-drill-relock/32.1-CONTEXT.md (D-07)
  </read_first>
  <action>
    Find the Bubble Shield bullet in §Cut Abilities. Verbatim current text:

      `- **Bubble Shield** — absorb-damage fusion shield. Cut because the mana-shield behavior (\`MANA_SHIELD_COST = 20.0 juice per fused damage hit\`, v1.1 D-04 retained in this doc — see [§ Juice Economy](#juice-economy)) already provides the "fused damage drains juice" primitive; a separate shield ability would double-count.`

    Replace with (appending one clarifying sentence per D-07; the cut decision is unchanged):

      `- **Bubble Shield** — absorb-damage fusion shield. Cut because the mana-shield behavior (\`MANA_SHIELD_COST = 20.0 juice per fused damage hit\`, v1.1 D-04 retained in this doc — see [§ Juice Economy](#juice-economy)) already provides the "fused damage drains juice" primitive; a separate shield ability would double-count. Note: with destructive-drill (Phase 33), mana shield narrows further — drill is invulnerable to enemies during DIVING (see [§ Drill-Dive Contract → Enemy Interaction](#enemy-interaction)); mana shield only applies in fused-non-DIVING state. The narrowing does not change the cut decision — Bubble Shield's role is still covered by the residual fused-non-DIVING mana shield.`

    Stage but do not commit.
  </action>
  <verify>
    <automated>grep -c "with destructive-drill (Phase 33), mana shield narrows further" .planning/FUSION-DESIGN.md | grep -q "^1$" &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "with destructive-drill (Phase 33), mana shield narrows further" .planning/FUSION-DESIGN.md` outputs `1`
    - `grep -c "fused-non-DIVING mana shield" .planning/FUSION-DESIGN.md` outputs `1`
    - `grep -c "The narrowing does not change the cut decision" .planning/FUSION-DESIGN.md` outputs `1`
    - The original Bubble Shield bullet still begins with "Cut because the mana-shield behavior" (verify the cut rationale is preserved, not replaced)
  </acceptance_criteria>
  <done>
    Bubble Shield rationale appended with destructive-drill narrowing clause; cut decision unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 7: Ripple amendment — i-frames row resolved-note + Two-exit footnote (D-02/D-08)</name>
  <files>.planning/FUSION-DESIGN.md</files>
  <read_first>
    - .planning/FUSION-DESIGN.md (line 361 — "May tune" row in the "What Phase 32 is allowed to change vs. preserve" table, AND line 321 — the D-08(c) callout block at the start of "### Two exit conditions")
    - .planning/phases/32.1-fusion-design-destructive-drill-relock/32.1-CONTEXT.md (D-02, D-08)
  </read_first>
  <action>
    Two edits, both staged together:

    EDIT 1 (D-08 — i-frames row resolved-note). In the "What Phase 32 is allowed to change vs.
    preserve" table inside §Drill-Dive Contract, find the "May tune" row. Verbatim current
    "Examples" cell text contains:

      `whether drill gains i-frames (per Open-Q #1 — currently NONE)`

    Replace with:

      `whether drill gains i-frames (per Open-Q #1 — currently NONE; resolved by Phase 33 destructive-drill: drill is invulnerable to enemies during DIVING but NOT to environmental hazards — see [§ Drill-Dive Contract → Enemy Interaction](#enemy-interaction))`

    EDIT 2 (D-02 — Two exit conditions footnote). In §Drill-Dive Contract → Two exit conditions,
    find the existing D-08(c) callout block (a `>` blockquote starting with "**D-08(c) (manual unfuse
    via Z-hold mid-drill) was removed from the design 2026-04-20**" and ending "...for the original
    three-exit draft.)"). Immediately AFTER that closing `)` and on a new line still inside the
    blockquote (continue the `>` prefix), append:

      `>`
      `> **Enemy contact is a non-exit case** — see [§ Drill-Dive Contract → Enemy Interaction](#enemy-interaction). Drill intersecting an enemy AABB does NOT trigger an exit; it deals damage, drains juice, and continues drilling per the destructive-drill rules added in Phase 33's re-lock cycle.`

    Stage but do not commit.
  </action>
  <verify>
    <automated>grep -c "resolved by Phase 33 destructive-drill" .planning/FUSION-DESIGN.md | grep -q "^1$" &amp;&amp; grep -c "Enemy contact is a non-exit case" .planning/FUSION-DESIGN.md | grep -q "^1$" &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "resolved by Phase 33 destructive-drill" .planning/FUSION-DESIGN.md` outputs `1` (i-frames row resolved-note per D-08)
    - `grep -c "Enemy contact is a non-exit case" .planning/FUSION-DESIGN.md` outputs `1` (Two-exit footnote per D-02)
    - `grep -c "drill is invulnerable to enemies during DIVING but NOT to environmental hazards" .planning/FUSION-DESIGN.md` outputs `1`
    - The original "May tune" row text "currently NONE" is preserved (verifiable: `grep -c "currently NONE" .planning/FUSION-DESIGN.md` outputs `>= 1`)
    - The original D-08(c) callout opening "**D-08(c) (manual unfuse via Z-hold mid-drill) was removed from the design 2026-04-20**" is preserved unchanged
  </acceptance_criteria>
  <done>
    Both ripple edits staged: i-frames row notes the destructive-drill resolution; Two exit conditions footnote clarifies enemy contact is non-exit.
  </done>
</task>

<task type="auto">
  <name>Task 8: DOC-WRITE commit — flip status, set locked_commit: TBD, commit all body changes</name>
  <files>.planning/FUSION-DESIGN.md</files>
  <read_first>
    - .planning/FUSION-DESIGN.md (lines 1-10 — verify status is currently UNLOCKED from Task 1; locked_commit still equals 9047b590cc648184f8c6c17c0ed3830296edc72c — Task 8 changes BOTH)
    - .planning/phases/32.1-fusion-design-destructive-drill-relock/32.1-CONTEXT.md (D-14, D-15 commit 2 specification)
    - Run `git status` to verify the only staged/unstaged file is .planning/FUSION-DESIGN.md and Tasks 2-7 edits are present
  </read_first>
  <action>
    Final pre-commit edits to .planning/FUSION-DESIGN.md frontmatter (lines 1-10):

    EDIT A — flip status:
      BEFORE: `status: UNLOCKED`
      AFTER:  `status: LOCKED`

    EDIT B — flip locked_commit to TBD placeholder:
      BEFORE: `locked_commit: 9047b590cc648184f8c6c17c0ed3830296edc72c`
      AFTER:  `locked_commit: TBD`

    Do NOT touch prior_locked_commit, prior_lock_chain, locked_at, or relock_reason in this commit —
    those are Task 9's job. The reason locked_commit becomes "TBD" mid-cycle (rather than the cycle-2
    SHA): per D-15 commit 2 specification, `locked_commit: TBD` is the placeholder the doc-write
    commit ships with, and Task 9's frontmatter-amend commit replaces TBD with the SHA of THIS commit.
    This is the canonical pattern from Lock Protocol §What `locked_commit` points at.

    Verify all Task 2-7 body changes are staged (single git diff inspection — Enemy Interaction
    subsection, FUS-03 amendment, mana-shield carve-out, drill_enemy_hit registry entry, Bubble
    Shield clarifying sentence, i-frames resolved-note, Two-exit footnote).

    Stage .planning/FUSION-DESIGN.md and commit with EXACTLY this message (no Co-Authored-By
    trailer — cycle 2 precedent commit 9047b59 has none):

      docs(32.1): add destructive-drill enemy-interaction subsection to FUSION-DESIGN
  </action>
  <verify>
    <automated>git log --oneline -1 -- .planning/FUSION-DESIGN.md | grep -q "docs(32.1): add destructive-drill enemy-interaction subsection to FUSION-DESIGN" &amp;&amp; grep -c "^status: LOCKED$" .planning/FUSION-DESIGN.md | grep -q "^1$" &amp;&amp; grep -c "^locked_commit: TBD$" .planning/FUSION-DESIGN.md | grep -q "^1$" &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `git log --oneline -1 -- .planning/FUSION-DESIGN.md` first line ends with `docs(32.1): add destructive-drill enemy-interaction subsection to FUSION-DESIGN`
    - `grep -c "^status: LOCKED$" .planning/FUSION-DESIGN.md` outputs `1`
    - `grep -c "^status: UNLOCKED$" .planning/FUSION-DESIGN.md` outputs `0`
    - `grep -c "^locked_commit: TBD$" .planning/FUSION-DESIGN.md` outputs `1`
    - `grep -c "^### Enemy Interaction$" .planning/FUSION-DESIGN.md` outputs `1`
    - `grep -c "DRILL_DAMAGE" .planning/FUSION-DESIGN.md` outputs `>= 3`
    - `grep -c "DRILL_ENEMY_COST" .planning/FUSION-DESIGN.md` outputs `>= 3`
    - `grep -c "drill_enemy_hit" .planning/FUSION-DESIGN.md` outputs `>= 2`
    - `grep -c "Phase 33 expands the contract with a destructive-drill Enemy Interaction subsection" .planning/FUSION-DESIGN.md` outputs `1` (FUS-03 amendment landed)
    - `grep -c "During DIVING, mana shield is bypassed" .planning/FUSION-DESIGN.md` outputs `1` (Juice Economy carve-out landed)
    - `grep -c "with destructive-drill (Phase 33), mana shield narrows further" .planning/FUSION-DESIGN.md` outputs `1` (Bubble Shield clarifying sentence landed)
    - `grep -c "resolved by Phase 33 destructive-drill" .planning/FUSION-DESIGN.md` outputs `1` (i-frames row resolved-note landed)
    - `grep -c "Enemy contact is a non-exit case" .planning/FUSION-DESIGN.md` outputs `1` (Two-exit footnote landed)
    - `git log --name-only -1 --pretty=format:"" | grep -v '^$' | grep -v '^\.planning/FUSION-DESIGN\.md$' | wc -l` outputs `0` (only FUSION-DESIGN.md modified — no `.py` files, no other files)
    - `git diff HEAD~1 HEAD -- .planning/FUSION-DESIGN.md | grep -c "^[+-]## Acceptance Checklist$"` outputs `0` (D-09: Acceptance Checklist h2 heading not modified — verified by reading live doc: `grep -n "^## Acceptance Checklist" .planning/FUSION-DESIGN.md` returns line 386)
    - `git diff HEAD~1 HEAD -- .planning/FUSION-DESIGN.md | grep -E "^[+-].*note right of FUSED" | wc -l` outputs `0` (D-09: Mermaid `note right of FUSED` not touched)
  </acceptance_criteria>
  <done>
    Commit 2 of 3 on main. status: LOCKED, locked_commit: TBD, all body changes from Tasks 2-7 in this single commit. No `.py` files modified.
  </done>
</task>

<task type="auto">
  <name>Task 9: FRONTMATTER-AMEND commit — populate cycle-3 SHA chain (D-14/D-16)</name>
  <files>.planning/FUSION-DESIGN.md</files>
  <read_first>
    - Output of `git rev-parse HEAD` (this is commit 2's SHA — capture BEFORE editing per D-16)
    - Output of `git log --oneline -3 -- .planning/FUSION-DESIGN.md` (verify the top entry is commit 2: "docs(32.1): add destructive-drill enemy-interaction subsection to FUSION-DESIGN")
    - Output of `git cat-file -p HEAD | head -5` (sanity check: confirm HEAD commit message matches commit 2's expected message)
    - .planning/FUSION-DESIGN.md (lines 1-10 — current frontmatter after Task 8: status=LOCKED, locked_commit=TBD, prior_locked_commit=2bc5cfd6..., prior_lock_chain has 2 entries, relock_reason is the cycle-2 wording)
    - .planning/phases/32.1-fusion-design-destructive-drill-relock/32.1-CONTEXT.md (D-14, D-15 commit 3 specification, D-16 SHA-capture invariant)
  </read_first>
  <action>
    STEP 1 — Capture commit 2's SHA. Run:
      git rev-parse HEAD

    Save the output as the variable CYCLE3_SHA (a 40-char lowercase hex string).

    STEP 2 — Sanity check. Run:
      git log --oneline -1 --format="%s" $CYCLE3_SHA

    Confirm output is exactly: `docs(32.1): add destructive-drill enemy-interaction subsection to FUSION-DESIGN`

    If the output does not match, ABORT — the commit chain is wrong; do not proceed. (T-01 mitigation.)

    STEP 3 — Edit frontmatter (lines 1-10 of .planning/FUSION-DESIGN.md). Make exactly four changes:

    CHANGE 1 — locked_commit:
      BEFORE: `locked_commit: TBD`
      AFTER:  `locked_commit: <CYCLE3_SHA>`     (literal 40-char SHA from STEP 1)

    CHANGE 2 — prior_locked_commit:
      BEFORE: `prior_locked_commit: 2bc5cfd68ab0c77661572ad6f6f377cbf60971c5`
      AFTER:  `prior_locked_commit: 9047b590cc648184f8c6c17c0ed3830296edc72c`

    CHANGE 3 — prior_lock_chain (extend by appending one new entry; preserve existing 2 entries
    in original order):
      BEFORE:
        prior_lock_chain:
          - e6263693dc7d3baee2cefc4bea757610bfe6b51e  # original 3-exit draft (auto/manual exits, drill on DOWN+V via dash action)
          - 2bc5cfd68ab0c77661572ad6f6f377cbf60971c5  # manual-exit stripped (still on DOWN+V via dash)
      AFTER:
        prior_lock_chain:
          - e6263693dc7d3baee2cefc4bea757610bfe6b51e  # original 3-exit draft (auto/manual exits, drill on DOWN+V via dash action)
          - 2bc5cfd68ab0c77661572ad6f6f377cbf60971c5  # manual-exit stripped (still on DOWN+V via dash)
          - 9047b590cc648184f8c6c17c0ed3830296edc72c  # cycle 2 — drill+pogo on DOWN+SPACE (Mario-64 ground-pound)

    CHANGE 4 — relock_reason (rewrite to the cycle-3 prose; ~3-5 sentences per D-14 spec, polished
    per Specifics §item-5 "should be short"):
      BEFORE (single line, cycle-2 wording — verbatim current content):
        relock_reason: Drill + pogo relocated to DOWN+SPACE (jump action) — Mario-64 ground-pound mental model; same input branches on is_fused (pogo unfused, drill fused) per D-06. Dash dropped from prototype scope; V button reserved/unused. Matches v1.3 drill code so no Phase 32 input remap needed; Phase 32 only ADDS the unfused pogo branch on the existing DOWN+SPACE airborne input.
      AFTER (single-line YAML scalar — ~3-5 sentences):
        relock_reason: Destructive-drill Enemy Interaction subsection added to §Drill-Dive Contract per Phase 33 33-CONTEXT.md D-21/D-22. Locks three names — DRILL_DAMAGE = 1 per hit (matches POGO_DAMAGE), DRILL_ENEMY_COST = TBD in 10-20 juice range (Phase 33 panel-tunes), drill_enemy_hit event — and the continue-through rule (drill damages enemies, takes no damage, continues drilling). Expands FUS-03 contract from pure tile interaction to enemy interaction. Resolves Open-Q #1 (drill i-frames) structurally — drill is invulnerable to enemies via offense, not via i-frames. Five ripple amendments landed (FUS-03, §Juice Economy mana-shield carve-out, §Fusion FSM event registry, §Cut Abilities Bubble Shield clarifying sentence, §Drill-Dive Contract i-frames resolved-note + Two-exit footnote). Phase 33 builds against this SHA.

    STEP 4 — Commit. Stage .planning/FUSION-DESIGN.md and commit with EXACTLY this message,
    substituting the captured SHA into the message (cycle-2 precedent e655e55 uses this format):

      docs(32.1): re-lock FUSION-DESIGN at <CYCLE3_SHA_first_8_chars> (cycle 3)

    For example, if CYCLE3_SHA is `abcdef0123456789...`, the message is exactly:
      docs(32.1): re-lock FUSION-DESIGN at abcdef01 (cycle 3)

    No Co-Authored-By trailer (cycle-2 precedent has none).
  </action>
  <verify>
    <automated>SHA=$(grep "^locked_commit: " .planning/FUSION-DESIGN.md | awk '{print $2}'); git cat-file -p $SHA | head -1 | grep -q "tree" &amp;&amp; git log --oneline -1 $SHA --format="%s" | grep -q "docs(32.1): add destructive-drill enemy-interaction subsection to FUSION-DESIGN" &amp;&amp; grep -c "9047b590cc648184f8c6c17c0ed3830296edc72c" .planning/FUSION-DESIGN.md | awk '$1 &gt;= 2' &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `git log --oneline -1 -- .planning/FUSION-DESIGN.md | grep -qE 'docs\(32\.1\): re-lock FUSION-DESIGN at [0-9a-f]{8} \(cycle 3\)$'` exits 0 (escaped parens are literal-paren regex escapes; trailing `$` anchors end-of-line so any junk after fails the check)
    - `grep -E "^locked_commit: [0-9a-f]{40}$" .planning/FUSION-DESIGN.md` matches exactly one line (locked_commit is now a 40-char SHA, no longer TBD)
    - `grep -c "^locked_commit: TBD$" .planning/FUSION-DESIGN.md` outputs `0`
    - `grep -c "^prior_locked_commit: 9047b590cc648184f8c6c17c0ed3830296edc72c$" .planning/FUSION-DESIGN.md` outputs `1`
    - `grep -c "9047b590cc648184f8c6c17c0ed3830296edc72c" .planning/FUSION-DESIGN.md` outputs `>= 2` (appears in prior_locked_commit AND in prior_lock_chain — T-03 mitigation)
    - `grep -c "# cycle 2 — drill+pogo on DOWN+SPACE (Mario-64 ground-pound)" .planning/FUSION-DESIGN.md` outputs `1` (cycle-2 annotation in prior_lock_chain)
    - `grep -c "Destructive-drill Enemy Interaction subsection added" .planning/FUSION-DESIGN.md` outputs `1` (relock_reason rewritten)
    - SHA-capture verification: `SHA=$(grep "^locked_commit: " .planning/FUSION-DESIGN.md | awk '{print $2}'); git rev-parse HEAD~1` outputs the same SHA as `$SHA` (T-01 mitigation: locked_commit field equals commit 2's SHA)
    - SHA-existence verification: `git cat-file -p $(grep "^locked_commit: " .planning/FUSION-DESIGN.md | awk '{print $2}') | head -1` succeeds and starts with `tree ` (T-01 mitigation: SHA is a valid commit object)
    - `git diff HEAD~1 HEAD -- .planning/FUSION-DESIGN.md | grep -E "^[+-]" | grep -vE "^[+-]{3}" | grep -vE "^[+-](locked_commit|prior_locked_commit|prior_lock_chain|relock_reason|  - 9047b590|  - 2bc5cfd6|  - e6263693)" | wc -l` outputs `0` (T-02 mitigation: ONLY frontmatter fields changed in commit 3 — no body edits)
    - `git log --name-only -1 --pretty=format:"" | grep -v '^$' | grep -v '^\.planning/FUSION-DESIGN\.md$' | wc -l` outputs `0` (only FUSION-DESIGN.md modified)
  </acceptance_criteria>
  <done>
    Commit 3 of 3 on main. locked_commit holds commit 2's SHA. prior_locked_commit holds cycle-2's SHA. prior_lock_chain extended. relock_reason rewritten. status: LOCKED. Phase 33 hard gate satisfied.
  </done>
</task>

<task type="auto">
  <name>Task 10: End-of-phase verification — three-commit chain + locked-state invariants</name>
  <files></files>
  <read_first>
    - Output of `git log --oneline -5 -- .planning/FUSION-DESIGN.md` (verify all three new commits present in correct order)
    - .planning/FUSION-DESIGN.md (lines 1-12 — verify final frontmatter state)
  </read_first>
  <action>
    Run the following verification suite as a single sequence. Every check must pass; any failure
    means the phase is NOT complete and the issue must be diagnosed before reporting Done.

    Check 1 — three-commit order:
      git log --oneline -3 -- .planning/FUSION-DESIGN.md

    Expected output (top to bottom — most recent first):
      <8-char-cycle3-sha> docs(32.1): re-lock FUSION-DESIGN at <same-8-char-sha> (cycle 3)
      <8-char-doc-write-sha> docs(32.1): add destructive-drill enemy-interaction subsection to FUSION-DESIGN
      <8-char-unlock-sha> docs(32.1): unlock FUSION-DESIGN.md to add destructive-drill subsection

    Check 2 — locked_commit field references the doc-write commit (not the frontmatter-amend commit):
      LOCKED=$(grep "^locked_commit: " .planning/FUSION-DESIGN.md | awk '{print $2}')
      DOCWRITE=$(git rev-parse HEAD~1)
      [ "$LOCKED" = "$DOCWRITE" ] && echo "OK: locked_commit points at doc-write commit"

    Check 3 — final frontmatter invariants:
      grep -c "^status: LOCKED$" .planning/FUSION-DESIGN.md             # expect 1
      grep -c "^status: UNLOCKED$" .planning/FUSION-DESIGN.md           # expect 0
      grep -c "^locked_commit: TBD$" .planning/FUSION-DESIGN.md         # expect 0
      grep -cE "^locked_commit: [0-9a-f]{40}$" .planning/FUSION-DESIGN.md  # expect 1

    Check 4 — cycle-2 SHA preservation in audit trail:
      grep -c "9047b590cc648184f8c6c17c0ed3830296edc72c" .planning/FUSION-DESIGN.md  # expect >= 2

    Check 5 — destructive-drill content present:
      grep -c "^### Enemy Interaction$" .planning/FUSION-DESIGN.md      # expect 1
      grep -c "DRILL_DAMAGE" .planning/FUSION-DESIGN.md                 # expect >= 3
      grep -c "DRILL_ENEMY_COST" .planning/FUSION-DESIGN.md             # expect >= 3
      grep -c "drill_enemy_hit" .planning/FUSION-DESIGN.md              # expect >= 2

    Check 6 — five ripple amendments landed:
      grep -c "Phase 33 expands the contract with a destructive-drill Enemy Interaction subsection" .planning/FUSION-DESIGN.md  # expect 1 — D-04
      grep -c "During DIVING, mana shield is bypassed" .planning/FUSION-DESIGN.md                                              # expect 1 — D-05
      grep -c "fired per enemy AABB intersection during DIVING" .planning/FUSION-DESIGN.md                                     # expect 1 — D-06
      grep -c "with destructive-drill (Phase 33), mana shield narrows further" .planning/FUSION-DESIGN.md                      # expect 1 — D-07
      grep -c "resolved by Phase 33 destructive-drill" .planning/FUSION-DESIGN.md                                              # expect 1 — D-08
      grep -c "Enemy contact is a non-exit case" .planning/FUSION-DESIGN.md                                                    # expect 1 — D-02

    Check 7 — D-09 sections NOT modified across the three commits:
      git diff HEAD~3 HEAD -- .planning/FUSION-DESIGN.md | grep -cE "^[+-].*note right of FUSED"   # expect 0
      git diff HEAD~3 HEAD -- .planning/FUSION-DESIGN.md | grep -cE "^[+-]## Acceptance Checklist$"  # expect 0 (D-09: live doc declares h2 — `## Acceptance Checklist` at FUSION-DESIGN.md:386, NOT h3)

    Check 8 — no `.py` files touched in any of the three commits:
      git log --name-only HEAD~3..HEAD --pretty=format: | grep -v '^$' | grep -v '^\.planning/FUSION-DESIGN\.md$' | wc -l  # expect 0

    Check 9 — Phase 33 hard gate satisfied (planner of /gsd-plan-phase 33 will do the same):
      SHA=$(grep "^locked_commit: " .planning/FUSION-DESIGN.md | awk '{print $2}')
      git cat-file -p $SHA >/dev/null && echo "OK: locked SHA exists in git history"

    If any check fails, stop and report the failed check + diagnostic context. Do not attempt
    a fourth commit to "fix" — diagnose the failure and re-run the appropriate task with corrections.

    On all-pass, report: "Phase 32.1 complete. New locked_commit: <SHA>. Three commits on main. Phase 33 unblocked."
  </action>
  <verify>
    <automated>SHA=$(grep "^locked_commit: " .planning/FUSION-DESIGN.md | awk '{print $2}'); HEAD1=$(git rev-parse HEAD~1); [ "$SHA" = "$HEAD1" ] &amp;&amp; git log --oneline -3 -- .planning/FUSION-DESIGN.md | head -3 | grep -c "docs(32.1):" | grep -q "^3$" &amp;&amp; grep -c "^### Enemy Interaction$" .planning/FUSION-DESIGN.md | grep -q "^1$" &amp;&amp; grep -c "9047b590cc648184f8c6c17c0ed3830296edc72c" .planning/FUSION-DESIGN.md | awk '$1 &gt;= 2 {ok=1} END {exit !ok}' &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - All nine verification checks above pass.
    - `git log --oneline -3 -- .planning/FUSION-DESIGN.md` shows three new commits in correct order: re-lock (top), doc-write (middle), unlock (bottom).
    - `locked_commit:` field SHA in frontmatter matches output of `git rev-parse HEAD~1` (the doc-write commit, not the frontmatter-amend commit).
    - `prior_locked_commit:` equals the cycle-2 SHA `9047b590cc648184f8c6c17c0ed3830296edc72c`.
    - `prior_lock_chain` contains all three historical SHAs in order: `e6263693...`, `2bc5cfd6...`, `9047b590...`.
    - status: LOCKED in final frontmatter.
    - Three named identifiers DRILL_DAMAGE / DRILL_ENEMY_COST / drill_enemy_hit grep-present.
    - All five ripple amendments + Two-exit footnote grep-present.
    - No .py files in any commit (T-02 mitigation observable).
    - Mermaid diagram and Acceptance Checklist sections untouched (D-09 respected).
  </acceptance_criteria>
  <done>
    Phase 32.1 verification suite passes. Three commits on main. New locked_commit observable in frontmatter. /gsd-plan-phase 33 is unblocked.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Doc-write commit (commit 2) → Frontmatter-amend commit (commit 3) | Cross-commit dependency: commit 2's SHA becomes a value embedded in commit 3's content. The `git rev-parse HEAD` capture moment between commits is the only point where the SHA-binding can drift. |
| Cycle-2 SHA literal (`9047b590cc648184f8c6c17c0ed3830296edc72c`) | Load-bearing string copied from existing frontmatter into two new fields (prior_locked_commit + prior_lock_chain). Typo here breaks the audit trail forever — lock chain is append-only. |
| Phase 32.1 boundary → Phase 33 planner | Phase 33's `/gsd-plan-phase 33` reads the new `locked_commit` SHA and runs `git cat-file -p <sha>` to verify it exists. If the SHA is wrong, Phase 33 aborts before writing any plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32.1-01 | Tampering | `locked_commit` field SHA in commit 3 | mitigate | Task 9 captures SHA via `git rev-parse HEAD` BEFORE editing per D-16, sanity-checks via `git log --oneline -1 $SHA --format="%s"` matches commit 2's expected message, then Task 10 final check confirms `locked_commit == git rev-parse HEAD~1`. Three-layer verification before, during, and after the write. |
| T-32.1-02 | Tampering | Doc-write commit completeness | mitigate | Task 8 acceptance criteria includes 13 grep checks confirming Enemy Interaction subsection AND all 5 ripple amendments are present in the same commit. Task 10 Check 6 re-verifies all 5 ripple-amendment markers are still present after commit 3 (no accidental loss in frontmatter-amend diff). |
| T-32.1-03 | Tampering | Cycle-2 SHA literal in audit trail | mitigate | Task 9 Change 2 and Change 3 both use the verbatim 40-char SHA `9047b590cc648184f8c6c17c0ed3830296edc72c` (copied from interfaces section of this PLAN, which was extracted from CONTEXT.md, which was extracted from current frontmatter). Task 9 acceptance criterion `grep -c "9047b590cc648184f8c6c17c0ed3830296edc72c" .planning/FUSION-DESIGN.md` outputs `>= 2` confirms the literal appears in BOTH new fields (prior_locked_commit + prior_lock_chain) post-write. |
| T-32.1-04 | Information Disclosure | None — doc is public; no secrets involved | accept | DOC-only re-lock; no credentials, env vars, or PII. The "audit trail" is intentionally public. |
| T-32.1-05 | Denial of Service | Phase 33 hard gate | mitigate | Task 10 Check 9 runs `git cat-file -p <locked_commit_sha>` confirming the SHA is reachable in git history before declaring phase complete. If commit 2 was somehow rewritten (e.g., rebase) between commits 2 and 3, this check fails and the planner aborts before declaring success. |
| T-32.1-06 | Elevation of Privilege | D-09 boundary (Acceptance Checklist + Mermaid) | mitigate | Task 8 acceptance criteria include `git diff HEAD~1 HEAD -- .planning/FUSION-DESIGN.md | grep -cE "^[+-]## Acceptance Checklist$"` outputs `0` (h2 — matches FUSION-DESIGN.md:386) and `git diff ... | grep "note right of FUSED"` outputs `0`. Task 10 Check 7 re-verifies across all three commits via `git diff HEAD~3 HEAD`. Two-pass enforcement of the D-09 boundary. |
</threat_model>

<verification>
End-of-phase verification (Task 10) runs nine grep + git checks covering: three-commit order,
locked_commit-points-at-doc-write-commit invariant, frontmatter state (status=LOCKED, no TBD),
cycle-2 SHA audit-trail preservation, destructive-drill content present (3 named identifiers),
all 5 ripple amendments + Two-exit footnote landed, D-09 sections untouched (Acceptance Checklist
+ Mermaid `note right of FUSED`), no `.py` files in any of the three commits, and Phase 33 hard
gate satisfied (`git cat-file -p <sha>` resolves). Failure of any check halts phase completion.
</verification>

<success_criteria>
Phase 32.1 closes when:

1. Three new commits exist on `main`, in correct order, touching ONLY `.planning/FUSION-DESIGN.md`:
   - `docs(32.1): unlock FUSION-DESIGN.md to add destructive-drill subsection`
   - `docs(32.1): add destructive-drill enemy-interaction subsection to FUSION-DESIGN`
   - `docs(32.1): re-lock FUSION-DESIGN at <8-char-sha> (cycle 3)`

2. Final FUSION-DESIGN.md frontmatter has:
   - `status: LOCKED`
   - `locked_commit: <40-char SHA matching git rev-parse HEAD~1>`
   - `prior_locked_commit: 9047b590cc648184f8c6c17c0ed3830296edc72c`
   - `prior_lock_chain` with three entries (cycle 1 original, cycle 1 manual-exit-strip, cycle 2 drill+pogo-on-DOWN+SPACE)
   - `relock_reason` rewritten to ~3-5 sentences describing the destructive-drill addition

3. Doc body contains:
   - `### Enemy Interaction` subsection inside §Drill-Dive Contract (between Block-break branch contract and Two exit conditions)
   - Three named identifiers verbatim: `DRILL_DAMAGE`, `DRILL_ENEMY_COST`, `drill_enemy_hit`
   - Five ripple amendments: FUS-03 expansion clause, Juice Economy mana-shield carve-out, Fusion FSM event-emissions `drill_enemy_hit` bullet, Cut Abilities Bubble Shield narrowing sentence, Drill-Dive Contract i-frames row resolved-note + Two-exit footnote

4. `/gsd-plan-phase 33` can now run — the new `locked_commit` SHA resolves via `git cat-file -p`.
</success_criteria>

<output>
After completion, create `.planning/phases/32.1-fusion-design-destructive-drill-relock/32.1-01-SUMMARY.md`
recording: (a) the new cycle-3 locked_commit SHA, (b) confirmation that all 5 ripple amendments landed,
(c) the three-commit chain SHAs in order, (d) Phase 33 unblocked notice.
</output>
