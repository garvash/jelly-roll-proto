---
phase: 02-slime-companion-fusion
plan: 04
type: execute
wave: 4
depends_on: [02-01, 02-02, 02-03]
files_modified: [tests/test_slime.py, src/entities/player.py, src/entities/slime.py]
autonomous: true
requirements: [SLM-01, SLM-02, DRILL-01]

must_haves:
  truths:
    - "Automated tests confirm slime follow distance and reform logic"
    - "Automated tests confirm juice scaling and regeneration"
    - "Automated tests confirm drill dive activation and cost"
  artifacts:
    - path: "tests/test_slime.py"
      provides: "Full suite of regression tests for Phase 2"
  key_links:
    - from: "tests/test_slime.py"
      to: "src/entities/slime.py"
      via: "behavioral verification"
---

<objective>
Finalize Phase 2 with comprehensive tests and final "feel" adjustments.

Purpose: Ensure long-term stability and a high-quality player experience.
Output: A new test suite and refined mechanical parameters.
</objective>

<execution_context>
@./.gemini/get-shit-done/workflows/execute-plan.md
@./.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-slime-companion-fusion/02-03-SUMMARY.md
@src/entities/player.py
@src/entities/slime.py
@tests/test_physics.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Automated Tests for Slime Mechanics</name>
  <files>tests/test_slime.py</files>
  <action>
    - Create `tests/test_slime.py` using `pytest` (matching the project pattern).
    - Add `test_slime_follow_logic`: Confirm slime updates its target based on player history.
    - Add `test_slime_juice_regeneration`: Verify juice increases over time.
    - Add `test_slime_scaling`: Verify `scale` property outputs expected values for given juice levels.
    - Add `test_drill_dive_activation`: Mock input and verify `player.state == "DIVING"`.
  </action>
  <verify>
    <automated>pytest tests/test_slime.py</automated>
  </verify>
  <done>Phase 2 mechanics are protected by automated tests.</done>
</task>

<task type="checkpoint:human-verify">
  <what-built>Full Phase 2 feature set (Companion, Juice, Drill Dive)</what-built>
  <how-to-verify>
    - Run `python main.py`.
    - Observe the slime trailing behind the player.
    - Dash away to see the slime "reform" (teleport).
    - Trigger Drill Dive (Down + X in air) and observe rapid descent.
    - Confirm juice consumption on impact and regeneration when idle.
    - Tune `DRILL_SPEED` or `SLIME_FOLLOW_DELAY` in `src/core/constants.py` if needed for better "feel".
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<verification>
- All automated tests pass.
- Human playtest confirms satisfying mechanical loop.
</verification>

<success_criteria>
The Slime companion system is robust, tested, and feels polished according to the prototype's core vision.
</success_criteria>

<output>
After completion, create `.planning/phases/02-slime-companion-fusion/02-04-SUMMARY.md`
</output>
