---
phase: 02-slime-companion-fusion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/entities/slime.py, main.py, src/core/constants.py]
autonomous: true
requirements: [SLM-01]

must_haves:
  truths:
    - "Slime entity exists in the game world"
    - "Slime follows the player with a short lag"
    - "Slime reforms near player if it falls too far behind"
  artifacts:
    - path: "src/entities/slime.py"
      provides: "Slime entity and follow logic"
    - path: "src/core/constants.py"
      provides: "Slime follow speed and distance constants"
  key_links:
    - from: "main.py"
      to: "src/entities/slime.py"
      via: "instantiation and update loop"
---

<objective>
Implement the core Slime entity and its independent follow behavior.

Purpose: Establish the companion's presence and its movement logic using a position history queue for a smooth, trailing effect.
Output: A `Slime` class and integration into the main game loop.
</objective>

<execution_context>
@./.gemini/get-shit-done/workflows/execute-plan.md
@./.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-slime-companion-fusion/02-CONTEXT.md
@.planning/phases/02-slime-companion-fusion/02-RESEARCH.md
@src/entities/player.py
@main.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Slime Entity and Follow Constants</name>
  <files>src/entities/slime.py, src/core/constants.py</files>
  <action>
    - Add constants to `src/core/constants.py`: `SLIME_FOLLOW_DELAY` (e.g., 8 frames), `SLIME_MAX_DIST` (e.g., 64px), `SLIME_REFORM_DIST` (e.g., 16px).
    - Create `src/entities/slime.py` with `Slime` class.
    - Implement a `history` queue (list of tuples) in `Slime.__init__` to store player positions.
    - Implement `Slime.update(player_x, player_y)`: 
      - Append current player position to history.
      - If history exceeds `SLIME_FOLLOW_DELAY`, pop the oldest and set it as target.
      - Lerp `self.x, self.y` towards target for smooth movement.
      - Add "side-switching" logic: if player moves right, slime targets left of player; if left, targets right.
    - Implement `Slime.draw()`: Draw a temporary 8x8 slime sprite (image 0, 8, 0).
  </action>
  <verify>
    <automated>python -c "from src.entities.slime import Slime; s = Slime(0, 0); s.update(10, 10); print('Slime update success')"</automated>
  </verify>
  <done>Slime class exists with follow logic and constants defined.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate Slime and Implement Reform Logic</name>
  <files>main.py, src/entities/slime.py</files>
  <action>
    - In `main.py`:
      - Import `Slime`.
      - Instantiate `self.slime` in `Game.__init__`.
      - Call `self.slime.update(self.player.x, self.player.y)` in `Game.update`.
      - Call `self.slime.draw()` in `Game.draw`.
    - In `src/entities/slime.py`:
      - Implement "Reform" logic: If distance between `(self.x, self.y)` and `(player_x, player_y)` > `SLIME_MAX_DIST`, immediately set `self.x, self.y` to a position `SLIME_REFORM_DIST` behind the player and clear the history queue.
  </action>
  <verify>
    <automated>python main.py (Check if slime follows and reforms if player dashes far away)</automated>
  </verify>
  <done>Slime is integrated into the game loop and handles distance thresholds correctly.</done>
</task>

</tasks>

<verification>
- Slime follows the player with a trailing effect.
- Slime switches sides when player changes direction.
- Slime "teleports" (reforms) near the player if separated by too much distance.
</verification>

<success_criteria>
The Slime companion is a permanent fixture in the game world that stays near the player without manual input.
</success_criteria>

<output>
After completion, create `.planning/phases/02-slime-companion-fusion/02-01-SUMMARY.md`
</output>
