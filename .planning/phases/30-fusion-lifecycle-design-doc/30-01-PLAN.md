---
phase: 30-fusion-lifecycle-design-doc
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/FUSION-DESIGN.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - FUS-01
  - FUS-02
  - FUS-03
tags:
  - design-doc
  - fusion
  - drill-dive
  - locked-contract

must_haves:
  truths:
    - "Locked FUSION-DESIGN.md exists at .planning/FUSION-DESIGN.md"
    - "Doc frontmatter declares status: LOCKED, locked_at: YYYY-MM-DD, and locked_commit: <real 7+ char hex SHA> (not TBD)"
    - "FUS-01 defines the IDLE -> RECALL -> WINDUP -> FUSED -> EXIT FSM with 100% juice gate and auto/manual exits"
    - "FUS-02 defines the unified Z input model (tap = spit/daze, hold = recall/fuse/manual-unfuse) and DOWN+V as the dive verb (pogo unfused, drill fused)"
    - "FUS-03 documents v1.3 drill behavior as Phase 32 regression target with cited values (DRILL_SPEED=2.0, DRILL_ACTIVATION_COST=5.0, DRILL_IMPACT_COST=20.0, DRILL_BLOCK_REFUND=15.0, DRILL_CRACKED_V_COST=20.0, DRILL_DRIFT_SPEED=0.5, i-frames=NONE)"
    - "Three drill exit conditions are enumerated: (a) solid terrain, (b) juice=0 with dissipate, (c) manual unfuse via Z-hold"
    - "Cut abilities (Slime Ram, Directional Hold, Charge Shot, Bubble Shield, Slime Boost) are enumerated in a one-line-each 'out of prototype scope, revisit post-prototype' section"
    - "Acceptance checklist section lists the behavioral checks Phase 32 must satisfy before it can close"
    - "ROADMAP.md reflects the scope pivot: Phase 30 marked complete, Phase 32/33 scope narrowed to single-ability, plus pointer/note to the needed code-strip phase between 30 and 32"
  artifacts:
    - path: ".planning/FUSION-DESIGN.md"
      provides: "Locked fusion lifecycle design contract consumed by Phase 32 (refactor target) and Phase 33 (feel pass)"
      contains:
        - "---\nstatus: LOCKED"
        - "locked_commit:"
        - "**FUS-01**"
        - "**FUS-02**"
        - "**FUS-03**"
        - "IDLE"
        - "RECALL"
        - "WINDUP"
        - "FUSED"
        - "EXIT"
        - "DRILL_SPEED"
        - "DRILL_IMPACT_COST"
        - "CRACKED_V"
        - "100%"
        - "Ram"
        - "Directional Hold"
        - "Charge Shot"
        - "Bubble Shield"
        - "Slime Boost"
    - path: ".planning/ROADMAP.md"
      provides: "Updated to reflect Phase 30 scope pivot (one fusion, not six); marks Phase 30 Plans count as 1 with plan list; adds note about required cut-ability-strip phase before 32; narrows Phase 32/33 scope references"
  key_links:
    - from: ".planning/FUSION-DESIGN.md (frontmatter locked_commit)"
      to: "git commit SHA of the doc-write commit"
      via: "Two-commit lock dance: write with TBD, commit, git rev-parse HEAD, amend frontmatter, commit lock"
      pattern: "^locked_commit:\\s*[0-9a-f]{7,40}"
    - from: ".planning/FUSION-DESIGN.md § Drill-Dive Contract"
      to: "assets/presets/_v1.3-reference.json + assets/physics-schema.json + src/entities/player.py:443-802"
      via: "Inline citations naming the source of each concrete value"
      pattern: "player\\.py|physics-schema\\.json|_v1\\.3-reference\\.json"
    - from: ".planning/FUSION-DESIGN.md § Acceptance Checklist"
      to: "Phase 32 regression target"
      via: "Behavioral checklist items Phase 32 must verify by inspection + smoke test"
      pattern: "Phase 32|must (verify|preserve|match)"
---

<objective>
Author and lock `.planning/FUSION-DESIGN.md` — a single comprehensive design doc that narrows the v2.0 prototype to ONE fusion mechanic (Drill Dive), specifies the unified Z/V input model, defines the IDLE→RECALL→WINDUP→FUSED→EXIT FSM under a 100%-gated juice-as-mana economy, captures v1.3 drill-dive behavior as the Phase 32 regression target with cited values, enumerates cut abilities as one-liners, lists Phase 32 acceptance checks, and locks the doc via YAML frontmatter with a real git SHA. Fold the ROADMAP scope-pivot update into the same lock commit so downstream phase numbers reflect the narrowed scope.

Purpose: Phase 32 (fusion refactor) is HARD GATED on this doc being locked. Phase 33 (fusion feel pass) reads the drill-dive contract as its tuning target. Without a locked contract, both phases would be building against vibes.

Output: `.planning/FUSION-DESIGN.md` (status: LOCKED, locked_commit: real SHA) + updated `.planning/ROADMAP.md` reflecting the scope pivot.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-fusion-lifecycle-design-doc/30-CONTEXT.md
@.planning/phases/30-fusion-lifecycle-design-doc/30-RESEARCH.md
@.planning/phases/30-fusion-lifecycle-design-doc/30-VALIDATION.md
@.planning/phases/29-player-movement-feel-pass/29-CONTEXT.md

<!-- Code-archaeology sources (read these for drill-contract values; do NOT paraphrase from memory) -->
@assets/physics-schema.json
@assets/presets/_v1.3-reference.json
@src/entities/player.py
@src/entities/slime.py
@src/anim/event_bus.py
@src/core/input.py

<interfaces>
<!-- Values already extracted in 30-RESEARCH.md § Code Archaeology (v1.3 Facts). -->
<!-- Executor MUST cite these with file:line, not re-derive from memory. -->

From `assets/presets/_v1.3-reference.json` (the authoritative v1.3 baseline):
- `drill.DRILL_SPEED = 2.0` (px/frame, vertical velocity during drill)
- `drill.DRILL_DRIFT_SPEED = 0.5` (px/frame, horizontal drift when LEFT/RIGHT held)
- `drill.DRILL_ACTIVATION_COST = 5.0` (juice paid at drill entry)
- `drill.DRILL_IMPACT_COST = 20.0` (juice paid on solid-terrain landing)
- `drill.DRILL_BLOCK_REFUND = 15.0` (juice REFUNDED per soft-destructible passthrough)
- `gates.DRILL_CRACKED_V_COST = 20.0` (juice paid per CRACKED_V gate-block break)
- `slime.JUICE_MAX = 200.0` (bar capacity)
- `slime.JUICE_REGEN_RATE = 0.5` (juice/frame passive regen = 30/sec)
- `slime.SLIME_DISSIPATE_COOLDOWN = 240` (frames of burnout cooldown = 4s @ 60fps)
- `slime.SLIME_MAX_DIST = 100` (px, fuse-eligibility range)
- `slime.SPIT_HOLD_THRESHOLD = 16` (frames; current v1.3 tap/hold threshold — doc targets ~8)
- `slime.RECALL_SPEED = 4.0` (px/frame)
- `slime.RECALL_OVERLAP_DIST = 4` (px, docked detection threshold)
- `fusion.MANA_SHIELD_COST = 20.0` (juice per fused damage hit — v1.1 D-04)

From `src/entities/player.py` (line ranges — read for behavior, not values):
- L89-97: `fuse(slime)` — single entry point; emits `fuse_start`
- L99-110: `unfuse(slime, dissipate=False)` — single exit point; emits `fuse_end`; calls `slime.dissipate()` if dissipate=True else `slime.reform()`
- L419-423: existing charge-to-fuse 100% gate: `if arrived and slime.juice >= slime.max_juice: self.fuse(slime)`
- L443-456: drill entry (current code uses `btnp("jump")` + `btn("down")` — DOWN+SPACE, NOT DOWN+V; this is the Phase 32 remap target)
- L463-468: drill mid-dive cancel (current code: `btnp("jump")` → FALLING + unfuse; doc proposes Z-hold manual unfuse instead)
- L662-675: `apply_diving_physics` — clamps `dy = DRILL_SPEED` each frame, drift from LEFT/RIGHT, auto-unfuse+dissipate on juice<=0
- L770-786: drill block-break branch — CRACKED_V consumes DRILL_CRACKED_V_COST; other destructibles refund DRILL_BLOCK_REFUND
- L797-802: drill landing (normal exit) — consume DRILL_IMPACT_COST, emit `drill_impact`, state→IDLE, unfuse (NO dissipate)

From `src/anim/event_bus.py`:
- Event naming convention: `snake_case` verb-noun-tense (`fuse_start`, `fuse_end`, `drill_impact`, `ram_start`, `spit`)
- Existing events emitted today: `fuse_start`, `fuse_end`, `drill_impact`, `ram_start`, `ram_impact`, `charge_shot_fire`, `boost_tap`, `spit` (+ anim events not relevant to fusion FSM)
- NEW events proposed by this doc (documented, NOT implemented here): `drill_start`, `drill_block_break`, `drill_end`, `manual_unfuse_start`

From `src/core/input.py:4-14`:
- `jump` action → SPACE / GAMEPAD A (currently drives drill activation — v1.3 implementation detail)
- `spit` action → Z / J / GAMEPAD B (the "slime/fusion button" the doc calls Z)
- `dash` action → V / K / GAMEPAD X (the "V button" the doc calls the dive verb; pogo unfused / drill fused — Phase 32 remap target)
</interfaces>

<draft_decisions>
<!-- Per RESEARCH open-questions, the doc-author makes these calls while writing. -->
<!-- User reviews at lock time. Planner records defaults here so the author doesn't rediscover. -->

1. Drill i-frames: **NONE** (preserve v1.3; flag in doc as "Phase 33 may add if playtest demands").
2. FUS-02 activation button: **V (`dash` action)** per PROJECT.md canonical. Note inline: "current v1.3 code routes drill through `jump`/SPACE; Phase 32 remaps activation to `dash`/V as part of the single-fusion-ability refactor."
3. `manual_unfuse_start` event timing: **fires at WINDUP-begin** (mirrors `fuse_start` firing at fuse-windup-begin).
4. Free-cancel-on-release symmetry: **YES, both fuse-windup AND unfuse-windup are cancellable on Z release.** D-23 symmetric.
5. Accelerated-regen multiplier: **2× JUICE_REGEN_RATE = 1.0 juice/frame (100 juice/sec)** as draft with "Phase 33 retunes" inline note.
6. FSM diagram format: **Mermaid stateDiagram-v2** (renders in GitHub/Obsidian/VS Code). Include an ASCII-table fallback immediately after the Mermaid block so grep-based checks pass even if Mermaid rendering is unavailable.
</draft_decisions>

<doc_section_order>
<!-- Required order per 30-RESEARCH Pitfall 6 (input model BEFORE FSM) and D-29 doc structure. -->

1. YAML frontmatter (status, locked_at, locked_commit)
2. Title + one-paragraph summary + FUS-01/02/03 inline definitions with anchor links
3. § Scope Pivot Rationale (one-fusion-not-six; shoot-to-daze → drill-to-kill loop)
4. § Input Model (FUS-02) — Z semantics, V semantics, tap/hold threshold, pogo-vs-drill
5. § Fusion FSM (FUS-01) — Mermaid diagram + ASCII-table fallback; state-by-state rules
6. § Juice Economy (FUS-01) — 100% gate, accelerated regen, MANA_SHIELD_COST retention, dissipate cooldown
7. § Drill-Dive Contract (FUS-03) — v1.3 values table with file:line citations; entry/physics/exit behavior; block-break handling; CRACKED_V
8. § Cut Abilities — one-line-each rationale for Ram, Directional Hold, Charge Shot, Bubble Shield, Slime Boost
9. § Acceptance Checklist (Phase 32) — behavioral items Phase 32 must verify by inspection + smoke test
10. § Lock Protocol — explains what `locked_commit` points at and that re-editing requires user re-lock
</doc_section_order>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write frontmatter + summary + scope-pivot section</name>
  <files>.planning/FUSION-DESIGN.md</files>
  <read_first>
    - .planning/FUSION-DESIGN.md (file does not exist yet; will be created)
    - .planning/phases/30-fusion-lifecycle-design-doc/30-CONTEXT.md (D-01, D-02, D-03, D-29, D-31, D-32 bind this task)
    - .planning/phases/30-fusion-lifecycle-design-doc/30-RESEARCH.md (§ Pattern 1 Locked-Doc YAML Frontmatter; § Pattern 2 Inline REQ-ID Definition)
    - .planning/phases/30-fusion-lifecycle-design-doc/30-VALIDATION.md (LOCK-01, LOCK-02, LOCK-03, FUS-01/02/03 anchor checks)
    - .planning/PROJECT.md (core value framing: "destructive Drill Dive"; V-button unification declaration)
  </read_first>
  <action>
    Create `.planning/FUSION-DESIGN.md` with:

    1. YAML frontmatter block (exact format):
       ```
       ---
       status: LOCKED
       locked_at: 2026-04-19
       locked_commit: TBD
       ---
       ```
       NOTE: `locked_commit: TBD` is intentional — Task 8 replaces it with the real SHA via the two-commit lock dance. The initial frontmatter check (LOCK-04) is explicitly documented as PERMITTING `TBD` during plan execution per VALIDATION.md sampling rate.

    2. Title: `# Fusion Lifecycle Design (v2.0 prototype)`

    3. One-paragraph summary describing: (a) this is the LOCKED design contract for v2.0 fusion; (b) Phase 32 refactor is gated on the SHA in frontmatter; (c) Phase 33 feel pass reads the drill-dive contract; (d) the prototype ships one fusion (Drill Dive) not six.

    4. `## Requirements (defined in this document)` section with inline bold-id paragraphs per Pattern 2 (mirror `.planning/milestones/v1.1-REQUIREMENTS.md` style):
       - `**FUS-01**: Fusion lifecycle FSM defines IDLE → RECALL → WINDUP → FUSED → EXIT with activation input, 100% juice gate at WINDUP entry, cancel/release rules at each phase, and auto/manual EXIT paths. See [§ Fusion FSM](#fusion-fsm) and [§ Juice Economy](#juice-economy).`
       - `**FUS-02**: Unified input model. Z is the slime/fusion button (tap = spit/daze, hold = recall/fuse/manual-unfuse); DOWN+V in air is the dive verb (pogo unfused, drill fused); tap/hold disambiguation uses a ~8-frame threshold (tuned in Phase 33). See [§ Input Model](#input-model).`
       - `**FUS-03**: Drill-dive v1.3 regression contract. Documented velocity, per-block juice cost, CRACKED_V handling, and entry/exit conditions serve as Phase 32's parity target — verified by inspection and smoke test, no pytest required (per D-28). See [§ Drill-Dive Contract](#drill-dive-contract).`

    5. `## Scope Pivot Rationale` section covering:
       - Per D-01: prototype ships ONE fusion mechanic (Drill Dive). The 5 other v1.1 fusion abilities are cut.
       - Per D-03: the prototype's fusion loop is "shoot to daze the boss → drill to finish." This is the combat fantasy PROJECT.md already names ("destructive Drill Dive that enables both exploration and combat").
       - Per D-02: cut abilities listed later in § Cut Abilities are "out of prototype scope, revisit post-prototype" — NOT deleted from design thinking, just out of scope for this build.
       - Flag: cut-ability code still exists in `player.py` / `slime.py` / `physics-schema.json`; a separate cut-ability-strip phase between 30 and 32 removes it (tracked as a Deferred Idea in 30-CONTEXT.md; Task 8 of this plan notes the ROADMAP follow-up).

    DO NOT yet write Input Model, FSM, Juice Economy, Drill Contract, Cut Abilities, Acceptance Checklist, or Lock Protocol sections — those are Tasks 2-7.

    Cite decisions inline using the D-XX notation from 30-CONTEXT.md so user can trace the doc back to their locked decisions (e.g., "per D-01", "per D-29").
  </action>
  <verify>
    <automated>test -f .planning/FUSION-DESIGN.md && head -10 .planning/FUSION-DESIGN.md | grep -E '^(status:\s*LOCKED|locked_at:\s*[0-9]{4}-[0-9]{2}-[0-9]{2}|locked_commit:)' | wc -l | grep -qE '^\s*3\s*$' && grep -q '^## Requirements' .planning/FUSION-DESIGN.md && grep -q '\*\*FUS-01\*\*' .planning/FUSION-DESIGN.md && grep -q '\*\*FUS-02\*\*' .planning/FUSION-DESIGN.md && grep -q '\*\*FUS-03\*\*' .planning/FUSION-DESIGN.md && grep -q '^## Scope Pivot' .planning/FUSION-DESIGN.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/FUSION-DESIGN.md` exists.
    - Frontmatter (first 10 lines) contains `status: LOCKED`, `locked_at: YYYY-MM-DD`, and `locked_commit:` (value MAY be `TBD` at this task; LOCK-04 check defers to Task 8).
    - All three of `**FUS-01**`, `**FUS-02**`, `**FUS-03**` appear as bold-ID definitions.
    - `## Requirements` section exists.
    - `## Scope Pivot` section exists.
    - Scope pivot text explicitly names all five cut abilities OR references a later § Cut Abilities section (Task 6 enumerates them).
  </acceptance_criteria>
  <done>File created; frontmatter parseable; FUS-01/02/03 defined with anchor references; scope pivot rationale present.</done>
</task>

<task type="auto">
  <name>Task 2: Write § Input Model (FUS-02)</name>
  <files>.planning/FUSION-DESIGN.md</files>
  <read_first>
    - .planning/FUSION-DESIGN.md (read what Task 1 wrote; append to end)
    - .planning/phases/30-fusion-lifecycle-design-doc/30-CONTEXT.md (D-04 through D-14 bind this section)
    - .planning/phases/30-fusion-lifecycle-design-doc/30-RESEARCH.md (§ Input Action Names; § Open Questions #2 — V vs SPACE)
    - src/core/input.py (action-name mapping: jump=SPACE, spit=Z, dash=V)
    - .planning/PROJECT.md (V-button unification declaration)
  </read_first>
  <action>
    Append `## Input Model` (anchor: `input-model`) section to `.planning/FUSION-DESIGN.md`. The section MUST be written BEFORE the FSM section (Task 3) because the FSM transitions reference Z-tap / Z-hold / DOWN+V semantics defined here (per 30-RESEARCH Pitfall 6).

    Content MUST include:

    1. **Z semantics (the slime/fusion button)** per D-10, D-11, D-12, D-13:
       - Logical action: `spit` (physical: Z / J / GAMEPAD B per `src/core/input.py:4-14`).
       - Uniform semantic: `tap = projectile, hold = toggle fusion state`. No mode-specific rebinds.
       - Tap/hold disambiguation threshold: **~8 frames** (doc target — Phase 33 tunes). Note current v1.3 uses `SPIT_HOLD_THRESHOLD = 16` frames — the doc is targeting a tighter value.
       - **Unfused Z actions:**
         - Tap (held ≤ threshold): spit projectile (weak, free).
         - Hold (juice < 100%): recall slime + accelerated juice regen while held (regen rule formalized in § Juice Economy).
         - Hold (juice = 100% AND slime docked at player): auto-triggers WINDUP → FUSED. No re-press needed — continuous hold completes the ritual.
         - Release any time before WINDUP completes: slime returns to follow mode (NOT freeze — D-12 explicitly forbids freeze because Z is also spit).
       - **Fused Z actions:**
         - Tap: daze shot (same projectile sprite/physics as spit, upgraded — juice cost + daze-on-hit effect; reuses spit code path per D-14).
         - Hold past threshold: manual unfuse (short windup, slime ejects back to follow).

    2. **V semantics (the dive verb)** per D-04 through D-09:
       - Logical action: `dash` (physical: V / K / GAMEPAD X per `src/core/input.py:4-14`).
       - **Unfused DOWN+V in air = pogo bounce** (Shovel Knight shovel-drop style). Strikes downward; bounces on contact with enemies and breakables only; pure solid ground = no bounce (just lands). Pogo is FREE — no juice cost, no cooldown, always available (D-05). Pogo teaches drill's downward commitment before the player ever fuses (D-06).
       - **Fused DOWN+V in air = pure plunge (Drill Dive)**. No bounce. Drills through soft/CRACKED blocks. Consumes juice per block. Exits on (a) solid terrain, (b) juice=0 (auto-dissipate), (c) manual unfuse via Z-hold. See § Drill-Dive Contract for values.

    3. **Implementation remap note** (per 30-RESEARCH Pitfall 2 / Open Question #2):
       > Current v1.3 code routes drill activation through the `jump` action (DOWN+SPACE — see `src/entities/player.py:443-456`). This doc targets the `dash` action (DOWN+V) per PROJECT.md D-07/D-10/D-22. Phase 32 remaps activation from `jump` to `dash` as part of the single-fusion-ability refactor. The remap is an implementation detail, not a design change — design intent has always been V.

    4. **Tap/hold disambiguation — shared primitive**:
       - Source: `src/core/input.py` `was_tap(action, threshold)` / `hold_frames(action)`.
       - Both Z and V tap/hold disambiguation use the same frame-threshold primitive. The threshold value per-action is named and set in § Drill-Dive Contract / tuning (Phase 33 authoritative).

    Quantify at least one frame count in this section (the ~8-frame tap/hold threshold) to satisfy VALIDATION.md check "tap/hold threshold is quantified (frame count)". Name-cite DRILL_SPEED if useful — the grep passes if the constant appears anywhere in the doc, and Task 5 also cites it.

    Do NOT define FSM states here — that is Task 3. Reference forward to § Fusion FSM and § Juice Economy with `[§ Fusion FSM](#fusion-fsm)` / `[§ Juice Economy](#juice-economy)` markdown anchors.
  </action>
  <verify>
    <automated>grep -qE '^## Input Model' .planning/FUSION-DESIGN.md && grep -qE 'Z.*tap|Z.*hold' .planning/FUSION-DESIGN.md && grep -q 'DOWN.*V' .planning/FUSION-DESIGN.md && grep -qE '[0-9]+[[:space:]]*frame' .planning/FUSION-DESIGN.md && grep -qi 'pogo' .planning/FUSION-DESIGN.md && grep -qE '(SPIT_HOLD_THRESHOLD|8[[:space:]]*frame|~8)' .planning/FUSION-DESIGN.md && grep -qE '(SPACE|jump).*(V|dash)|Phase 32.*remap' .planning/FUSION-DESIGN.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `## Input Model` heading exists.
    - Z tap-and-hold semantics both documented (unfused AND fused sub-cases).
    - V semantics both documented (unfused pogo AND fused drill).
    - Tap/hold threshold quantified with a frame count.
    - Pogo verb explicitly named (per D-04).
    - Implementation remap note from `jump` (SPACE) to `dash` (V) present as Phase 32 target.
    - VALIDATION.md checks FUS-02 row 1 (≥3 matches of Z.tap / Z.hold / DOWN.V) and FUS-02 row 2 (≥1 frame count) both pass against this section.
  </acceptance_criteria>
  <done>§ Input Model section appended; all six D-04..D-14 decisions represented; V-remap implementation note included.</done>
</task>

<task type="auto">
  <name>Task 3: Write § Fusion FSM (FUS-01) with Mermaid diagram + ASCII fallback</name>
  <files>.planning/FUSION-DESIGN.md</files>
  <read_first>
    - .planning/FUSION-DESIGN.md (read what Tasks 1-2 wrote; append after § Input Model)
    - .planning/phases/30-fusion-lifecycle-design-doc/30-CONTEXT.md (D-21, D-22, D-23, D-24 bind this section)
    - .planning/phases/30-fusion-lifecycle-design-doc/30-RESEARCH.md (§ Pattern 3 Mermaid State Diagram — use the reference Mermaid block; § Open Questions #3 and #4 manual-unfuse-start timing and free-cancel symmetry)
    - src/anim/event_bus.py (existing event names; snake_case convention)
    - src/entities/player.py (L89-110 fuse/unfuse entry-exit points for FSM transitions)
  </read_first>
  <action>
    Append `## Fusion FSM` (anchor: `fusion-fsm`) section to `.planning/FUSION-DESIGN.md`.

    Content MUST include:

    1. **Opening paragraph**: name the five FSM states (IDLE, RECALL, WINDUP, FUSED, EXIT) and state that D-22 makes "docked" a frame-0 sub-state of WINDUP (not a separate state). Reference FUS-01 anchor.

    2. **Mermaid state diagram** (copy from 30-RESEARCH.md § Pattern 3 reference, adjusted for clarity):

       ```mermaid
       stateDiagram-v2
           [*] --> IDLE
           IDLE --> RECALL: Z held past ~8f threshold\n(slime not dissipated)
           RECALL --> IDLE: Z released OR\nslime docks with juice < 100%
           RECALL --> WINDUP: Slime docked AND juice = 100%\nAND Z still held
           WINDUP --> IDLE: Z released (free cancel — D-23)
           WINDUP --> FUSED: Windup frames elapse\n(target 8-16f, Phase 33 tunes)
           FUSED --> EXIT_AUTO: juice = 0
           FUSED --> UNFUSE_WINDUP: Z held past threshold\n(manual unfuse)
           UNFUSE_WINDUP --> FUSED: Z released (free cancel — symmetric)
           UNFUSE_WINDUP --> EXIT_MANUAL: Unfuse windup elapses
           EXIT_AUTO --> IDLE: Dissipate + 240f cooldown\nSlime reforms at full juice
           EXIT_MANUAL --> IDLE: Slime ejects unharmed\n(no cooldown)
           note right of FUSED
               In FUSED:
               - Tap Z = daze shot (juice cost)
               - DOWN+V air = drill dive
               - Mana shield: fused damage
                 drains juice (MANA_SHIELD_COST=20.0)
           end note
       ```

    3. **ASCII table fallback** (immediately after Mermaid — ensures grep-based content checks pass and the doc is readable without Mermaid rendering):

       | From | To | Trigger | Side effects |
       |------|----|---------|--------------|
       | IDLE | RECALL | Z held ≥ ~8f threshold, slime not dissipated | Slime begins recall (RECALL_SPEED=4.0 px/f) |
       | RECALL | IDLE | Z released OR slime docks with juice < 100% | Slime returns to follow mode (NOT freeze, per D-12) |
       | RECALL | WINDUP | Slime docked (dist ≤ 4 px, RECALL_OVERLAP_DIST) AND juice = 100% AND Z still held | Windup timer starts; `fuse_start` NOT yet emitted |
       | WINDUP | IDLE | Z released (free cancel, D-23) | Slime returns to follow; no cost, no punishment |
       | WINDUP | FUSED | Windup frames elapse (target 8-16f, Phase 33 tunes) | `fuse_start` emitted; player and slime latched |
       | FUSED | UNFUSE_WINDUP | Z held past threshold (manual unfuse) | `manual_unfuse_start` emitted (NEW event — fires at windup-begin per Open-Q #3 resolution) |
       | UNFUSE_WINDUP | FUSED | Z released (free cancel — symmetric, Open-Q #4 resolution) | No-op; stay fused |
       | UNFUSE_WINDUP | EXIT_MANUAL | Unfuse windup elapses | `fuse_end` emitted; slime ejects unharmed; NO dissipate |
       | FUSED | EXIT_AUTO | Juice = 0 | `fuse_end` emitted; slime dissipates; SLIME_DISSIPATE_COOLDOWN=240f before reform |
       | EXIT_AUTO | IDLE | 240f cooldown elapses | Slime reforms at full juice (v1.1 D-05 retained) |
       | EXIT_MANUAL | IDLE | Immediate (no cooldown) | Back to normal follow |

    4. **State-by-state rules** subsection. Brief prose per state:
       - **IDLE**: Slime following, player unfused. Baseline passive regen active if slime is active (not dissipated). See § Juice Economy.
       - **RECALL**: Z held, slime moving toward player at RECALL_SPEED=4.0. Accelerated regen activates once slime is docked at player with Z still held (docked = distance ≤ RECALL_OVERLAP_DIST = 4 px). D-17, D-22.
       - **WINDUP**: Juice hit 100% with slime docked; Z still held. Short merge animation (target 8-16 frames, Phase 33 tunes). D-21, D-22. Frame 0 of WINDUP IS the "docked" moment (D-22 — not a separate state).
       - **FUSED**: Latched state. Z is free for daze shot (tap). DOWN+V in air = drill. Mana shield remains (MANA_SHIELD_COST=20.0 per fused damage hit, v1.1 D-04 retained). Remaining juice burned by fused actions (D-19, D-20). See § Drill-Dive Contract.
       - **EXIT_AUTO** (juice → 0): Slime dissipates; SLIME_DISSIPATE_COOLDOWN=240 frames before reform. Dissipation IS the punishment for over-spending (D-24).
       - **EXIT_MANUAL** (Z-hold while FUSED): Short unfuse windup; slime ejects unharmed; no cooldown.

    5. **Event emissions subsection** — enumerate existing and proposed events:

       **Existing events (already emitted by `src/anim/event_bus.py`):**
       - `fuse_start` — emitted on WINDUP → FUSED transition (mirrors current v1.1 `player.fuse()` site).
       - `fuse_end` — emitted on both EXIT_AUTO (dissipate) and EXIT_MANUAL (eject).

       **New events proposed by this doc (NOT implemented in Phase 30 — implemented in Phase 32):**
       - `drill_start` — fired at drill-dive activation (FUSED state + DOWN+V held). Anim hook for drill windup/plunge frame.
       - `drill_block_break` — fired per-block destruction during drill. Distinct from `drill_impact` (which is landing-on-solid). Enables per-break particle/shake in Phase 31/35.
       - `drill_end` — fired on any drill exit (solid, juice=0, manual cancel).
       - `manual_unfuse_start` — fired at FUSED → UNFUSE_WINDUP transition (Z crosses hold threshold). Earliest anim signal per Open-Q #3.

       **Naming convention**: snake_case verb-noun-tense, mirroring existing events (`fuse_start`, `ram_impact`). These events are anim side-channel hooks per MEMORY's Reanimator-style anim architecture constraint — they do NOT drive the anim FSM, they mirror gameplay state.

    6. **Cross-reference**: note that the 100% juice gate logic is specified in § Juice Economy (Task 4); this section only references it.

    Cite 30-CONTEXT decisions inline (D-21, D-22, D-23, D-24) for traceability.
  </action>
  <verify>
    <automated>grep -qE '^## Fusion FSM' .planning/FUSION-DESIGN.md && for s in IDLE RECALL WINDUP FUSED EXIT; do grep -q "\b$s\b" .planning/FUSION-DESIGN.md || { echo "MISSING: $s"; exit 1; }; done && grep -q 'stateDiagram-v2' .planning/FUSION-DESIGN.md && grep -qE 'drill_start|drill_block_break|drill_end|manual_unfuse_start' .planning/FUSION-DESIGN.md && grep -q 'MANA_SHIELD_COST' .planning/FUSION-DESIGN.md && grep -q 'SLIME_DISSIPATE_COOLDOWN' .planning/FUSION-DESIGN.md && grep -q 'RECALL_OVERLAP_DIST' .planning/FUSION-DESIGN.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `## Fusion FSM` heading exists.
    - All five FSM states (IDLE, RECALL, WINDUP, FUSED, EXIT) are named as standalone words at least once.
    - Mermaid `stateDiagram-v2` fenced block present.
    - ASCII table fallback present (so grep-based checks don't depend on Mermaid rendering).
    - New events (`drill_start`, `drill_block_break`, `drill_end`, `manual_unfuse_start`) documented.
    - Existing events (`fuse_start`, `fuse_end`) referenced.
    - MANA_SHIELD_COST=20.0 mentioned (v1.1 D-04 retention).
    - SLIME_DISSIPATE_COOLDOWN=240 mentioned (v1.1 D-05 retention).
    - Free-cancel symmetry documented (both WINDUP → IDLE and UNFUSE_WINDUP → FUSED).
    - VALIDATION.md check FUS-01 "all five FSM states named" passes.
  </acceptance_criteria>
  <done>§ Fusion FSM section appended; Mermaid + ASCII both present; all transitions enumerated; new events documented.</done>
</task>

<task type="auto">
  <name>Task 4: Write § Juice Economy (FUS-01) with 100% gate and accelerated regen</name>
  <files>.planning/FUSION-DESIGN.md</files>
  <read_first>
    - .planning/FUSION-DESIGN.md (read what Tasks 1-3 wrote; append after § Fusion FSM)
    - .planning/phases/30-fusion-lifecycle-design-doc/30-CONTEXT.md (D-15 through D-20 bind this section; D-24 for dissipation cooldown)
    - .planning/phases/30-fusion-lifecycle-design-doc/30-RESEARCH.md (§ Juice Regen — Current Rates; Pitfall 2 on 100% gate framing; Open-Q #5 accelerated-regen multiplier default)
    - assets/presets/_v1.3-reference.json (authoritative v1.3 values: JUICE_MAX, JUICE_REGEN_RATE, SLIME_DISSIPATE_COOLDOWN, MANA_SHIELD_COST)
    - src/entities/slime.py (lines 50-101, 166 — dissipate/regen tick sites)
  </read_first>
  <action>
    Append `## Juice Economy` (anchor: `juice-economy`) section to `.planning/FUSION-DESIGN.md`.

    Content MUST include:

    1. **Framing paragraph**: "Juice is mana. The juice bar is a **readiness meter, not a duration bar** (D-15, D-16)." Reference FUS-01.

    2. **Current v1.3 values table** (cite sources):

       | Property | v1.3 Value | Source |
       |----------|-----------|--------|
       | `JUICE_MAX` | 200.0 | `assets/presets/_v1.3-reference.json` (slime group) |
       | `JUICE_REGEN_RATE` (passive) | 0.5 juice/frame (= 30/sec) | `_v1.3-reference.json` (slime group) |
       | Accelerated regen rate (NEW) | 2× passive = 1.0 juice/frame (= 60/sec) DRAFT — Phase 33 tunes | This doc (per Open-Q #5) |
       | `MANA_SHIELD_COST` (per fused damage hit) | 20.0 juice | `_v1.3-reference.json` (fusion group) |
       | `SLIME_DISSIPATE_COOLDOWN` | 240 frames (= 4.0s @ 60fps) | `_v1.3-reference.json` (slime group) |
       | `SLIME_MAX_DIST` (fuse-eligibility range) | 100 px | `_v1.3-reference.json` (slime group) |

    3. **The 100% Gate (D-15, D-16)**:
       - Fusion requires juice = 100% of JUICE_MAX to initiate (enter WINDUP → FUSED).
       - This is a **binary readiness meter**: full = ready; anything less = waiting.
       - Design primitive: enables "oh I need 1 more juice for this puzzle" — level design can intentionally place drill-gated blocks adjacent to juice-starved zones.
       - **Implementation note** (per 30-RESEARCH Pitfall 2): the 100% gate ALREADY exists for the v1.1 charge-to-fuse path (`src/entities/player.py:419-423` — `if arrived and slime.juice >= slime.max_juice: self.fuse(slime)`). The v1.3 drill-entry path currently gates on `slime.juice > 0` (`player.py:447`), NOT 100%. Phase 32 aligns the drill-entry path to the same 100% gate the charge-to-fuse path already uses. This is consolidation, not invention.

    4. **Accelerated Regen Ritual (D-17, D-18)**:
       - While Z is held AND slime is active (not dissipated) AND slime is docked at the player, juice regenerates at the **accelerated rate** (DRAFT: 2× passive = 1.0 juice/frame; Phase 33 tunes).
       - Baseline (passive) regen continues at `JUICE_REGEN_RATE = 0.5 juice/frame` any time slime is active and not fused/holding (D-18; existing `src/entities/slime.py:166`).
       - Semantic: this is the "power up for fusion" ritual — stand safe, pull slime in, charge, commit. Hollow-Knight-Focus-heal / Zelda-boomerang-charge texture.
       - **Implementation note**: no "accelerated regen" code exists today (30-RESEARCH § Juice Regen). Phase 32 implements; Phase 33 tunes multiplier.

    5. **Juice Consumption During Fuse (D-19, D-20)**:
       - Once FUSED, remaining juice is spent by fused actions:
         - Daze shot (tap Z while FUSED): cost TBD — uses existing `SLIME_SPIT_COST` primitive; Phase 33 picks a value (likely equal to or slightly above v1.3 spit cost, since it's mechanically the same projectile with an upgrade per D-14).
         - Drill per-block cost: DRILL_CRACKED_V_COST=20.0 / DRILL_BLOCK_REFUND=+15.0 (refund) — see § Drill-Dive Contract for full table.
         - Drill impact (solid terrain landing): DRILL_IMPACT_COST=20.0.
         - Drill activation: DRILL_ACTIVATION_COST=5.0.
         - Mana shield (fused damage): MANA_SHIELD_COST=20.0 per hit (v1.1 D-04 retained).
       - Fused duration = juice-at-fuse-moment minus what fused actions consume (D-20). Every fused action burns the timer; player is always trading "stay fused longer" against "drill/shoot now."
       - Regen does NOT apply while fused (current v1.1 behavior — `slime.update` early-returns before regen line when `is_fused`; confirmed per 30-RESEARCH § Juice during fuse).

    6. **Dissipation on Juice-Empty (D-24)**:
       - Auto-unfuse when juice reaches 0 → `unfuse(slime, dissipate=True)` path → `slime.dissipate()` → SLIME_DISSIPATE_COOLDOWN=240 frames before slime can reform or be recalled.
       - During dissipation, `slime.recall()` early-returns (slime uncontrollable). THIS is the punishment for over-spending — lose slime for 4 seconds.
       - Slime reforms at full juice after cooldown (v1.1 D-05 retained — `slime.py:91-101`).

    Cite 30-CONTEXT decisions inline (D-15..D-20, D-24).
  </action>
  <verify>
    <automated>grep -qE '^## Juice Economy' .planning/FUSION-DESIGN.md && grep -q '100%' .planning/FUSION-DESIGN.md && grep -q 'JUICE_MAX' .planning/FUSION-DESIGN.md && grep -q 'JUICE_REGEN_RATE' .planning/FUSION-DESIGN.md && grep -qi 'accelerated regen' .planning/FUSION-DESIGN.md && grep -q 'MANA_SHIELD_COST' .planning/FUSION-DESIGN.md && grep -qE '_v1\.3-reference\.json' .planning/FUSION-DESIGN.md && grep -q 'dissipate' .planning/FUSION-DESIGN.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `## Juice Economy` heading exists.
    - "100%" string appears at least once (satisfies VALIDATION.md FUS-01 row 3).
    - Named constants cited: JUICE_MAX, JUICE_REGEN_RATE, MANA_SHIELD_COST, SLIME_DISSIPATE_COOLDOWN.
    - File:line citations to `_v1.3-reference.json` or `slime.py` appear at least once.
    - Accelerated regen rule present with draft multiplier (2×) and "Phase 33 tunes" note.
    - 100%-gate-already-exists note (Pitfall 2 avoidance) present — frames the doc as formalizing existing behavior, not inventing a new rule.
    - Dissipation cooldown documented with SLIME_DISSIPATE_COOLDOWN=240f value.
  </acceptance_criteria>
  <done>§ Juice Economy section appended; 100% gate + accelerated regen + mana shield + dissipation all specified with v1.3 values cited.</done>
</task>

<task type="auto">
  <name>Task 5: Write § Drill-Dive Contract (FUS-03) with v1.3 values and file:line citations</name>
  <files>.planning/FUSION-DESIGN.md</files>
  <read_first>
    - .planning/FUSION-DESIGN.md (read what Tasks 1-4 wrote; append after § Juice Economy)
    - .planning/phases/30-fusion-lifecycle-design-doc/30-CONTEXT.md (D-07, D-08, D-09, D-25, D-26, D-27, D-28 bind this section)
    - .planning/phases/30-fusion-lifecycle-design-doc/30-RESEARCH.md (§ Code Archaeology (v1.3 Facts) — CANONICAL value source; Pitfall 1 v1.3 vs v2.0 drift; Code Examples subsection)
    - assets/physics-schema.json (current schema; v2.0-default values)
    - assets/presets/_v1.3-reference.json (authoritative v1.3 baseline — USE THIS for drill values per Pitfall 1)
    - src/entities/player.py (lines 443-468, 662-675, 770-786, 797-802 — drill entry/physics/block-break/landing/cancel)
  </read_first>
  <action>
    Append `## Drill-Dive Contract` (anchor: `drill-dive-contract`) section to `.planning/FUSION-DESIGN.md`. This section is FUS-03. It captures v1.3 drill-dive behavior precisely enough to serve as Phase 32's regression target — verified by inspection + smoke test (no pytest required per D-28).

    **CRITICAL citation rule (30-RESEARCH Anti-patterns):** Every concrete value cites a source. Prose like "about 20 juice" is WRONG; "20 juice (`physics-schema.json` `DRILL_IMPACT_COST`)" is RIGHT. Use `_v1.3-reference.json` as the authoritative value source for drill keys per 30-RESEARCH Pitfall 1.

    Content MUST include:

    1. **Opening paragraph**: name FUS-03, state that these values are Phase 32's parity target, note that Phase 33 is permitted to retune values during the feel pass (per D-27 — the contract is behavioral, not frame-for-frame identity).

    2. **Activation contract table**:

       | Property | Value | Source |
       |----------|-------|--------|
       | Target activation input | **DOWN + V** (logical `dash` action) | D-07 + `src/core/input.py:4-14` |
       | Current v1.3 activation input (implementation) | DOWN + SPACE (logical `jump` action — Phase 32 remap target) | `src/entities/player.py:443-456` |
       | Preconditions | `has_drill` item, airborne (not grounded), `slime.juice > 0` (v1.3), slime distance² < `SLIME_MAX_DIST²` (=100²) | `player.py:446-450`, `_v1.3-reference.json` |
       | Post-doc precondition (v2.0 target) | Same + **juice = 100%** (adopts the existing charge-to-fuse gate — see § Juice Economy) | This doc + `player.py:419-423` |
       | Entry side-effects | `state = "DIVING"`, `fuse(slime)`, `dy = DRILL_SPEED`, `dx = 0`, `slime.consume(DRILL_ACTIVATION_COST)`, emit NEW `drill_start` | `player.py:451-455` |

    3. **Physics contract table** (from `_v1.3-reference.json` per Pitfall 1):

       | Property | v1.3 Value | Source |
       |----------|-----------|--------|
       | `DRILL_SPEED` (vertical velocity; re-clamped each frame) | **2.0 px/frame** | `_v1.3-reference.json` drill group; `physics-schema.json:79`; applied in `player.py:662-663` |
       | `DRILL_DRIFT_SPEED` (horizontal drift when LEFT/RIGHT held) | **0.5 px/frame** | `_v1.3-reference.json` drill group; `physics-schema.json:80`; `player.py:665-671` |
       | `DRILL_ACTIVATION_COST` (paid at entry) | **5.0 juice** | `_v1.3-reference.json` drill group; `physics-schema.json:82` |
       | `DRILL_IMPACT_COST` (paid on solid-terrain landing) | **20.0 juice** | `_v1.3-reference.json` drill group; `physics-schema.json:81` |
       | `DRILL_BLOCK_REFUND` (soft-destructible passthrough) | **+15.0 juice** (REFUND, not cost) | `_v1.3-reference.json` drill group; `physics-schema.json:83` |
       | `DRILL_CRACKED_V_COST` (gate block) | **20.0 juice** | `_v1.3-reference.json` gates group; `physics-schema.json:131` |
       | Gravity during drill | `dy` re-assigned to DRILL_SPEED each frame, so net gravity effect = 0 (drill is a clamp, not an additive velocity) | `player.py:662-663, 689-693` |
       | MAX_FALL_SPEED clamp | N/A during drill (v1.3 MAX_FALL_SPEED=2.5; drill explicitly sets dy=2.0 each frame, not affected by the clamp) | `_v1.3-reference.json` |
       | i-frames during drill | **NONE** (preserve v1.3; per Open-Q #1 — flag for Phase 33 playtest) | `player.py` — no `invuln_timer` hit during DIVING |

    4. **Block-break branch contract**:
       - **Soft destructible** (non-CRACKED_V): `get_destructible_at` → `remove_tile` → `slime.refill(DRILL_BLOCK_REFUND)` (+15.0 juice) → `on_block_break()` (triggers DRILL_SHAKE_DURATION=12f + DRILL_HITSTOP_FRAMES=6f) → drill continues through. `player.py:770-786`.
       - **CRACKED_V (gate block, INTGRID_CRACKED_V=12)**: same path but `slime.consume(DRILL_CRACKED_V_COST)` (20.0 juice spent, not refunded). Drill continues through. Per MEMORY's block-gate-hierarchy constraint, drill is the CRACKED_V opener.
       - **Event emission**: current code emits nothing per-block. This doc proposes new `drill_block_break` event fired per-destruction (Phase 32 implements).

    5. **Three exit conditions** (per D-08 — use these exact three labels so VALIDATION.md grep check "three exit conditions" finds matches for `solid`, `juice.*0`, `cancel`):
       - **Exit (a): solid terrain contact**: `move_and_collide` → `collision` true on non-destructible → snap to floor → `is_grounded=True` → `slime.consume(DRILL_IMPACT_COST)` (20.0 juice) → emit `drill_impact` (existing) AND emit new `drill_end` → `state = "IDLE"` → `unfuse(slime)` (NO dissipate — normal exit, slime reforms next to player immediately). `player.py:797-802`.
       - **Exit (b): juice reaches 0**: In `apply_diving_physics`, if `slime.juice <= 0` → `state = "FALLING"` → `unfuse(slime, dissipate=True)` → slime dissipates, SLIME_DISSIPATE_COOLDOWN=240f. Emit new `drill_end`. `player.py:672-675`.
       - **Exit (c): manual unfuse via Z-hold mid-drill**: Design target per D-08. Current v1.3 code uses a jump-press cancel (`btnp("jump")` → FALLING + unfuse, `player.py:463-468`). Phase 32 replaces this with Z-hold manual unfuse routing through the UNFUSE_WINDUP state (see § Fusion FSM). Emit new `manual_unfuse_start` on Z-crosses-threshold, then `drill_end` + `fuse_end` on unfuse windup elapse. Flag: may be disabled in Phase 33 if playtest shows it feels wrong (D-08 tunable clause).

    6. **Block-gate hierarchy tie-in** (MEMORY constraint): drill is the CRACKED_V opener. The design contract ties DRILL to CRACKED_V explicitly — other block gates (soft/kick, CRACKED_H/ram, goo-mold/late) are NOT drill-eligible.

    7. **What's Phase 32 allowed to change vs. preserve**:
       - **Must preserve** (behavioral): DRILL_SPEED clamp each frame, per-block refund/cost, exit conditions (a)(b), dissipate on juice=0, mana shield retention during drill.
       - **Must change** (per this doc): activation input routing (jump→dash), entry gate (>0 juice → =100% juice), cancel input (jump-press → Z-hold), new events (drill_start / drill_block_break / drill_end / manual_unfuse_start), FSM state-machine structure (see § Fusion FSM).
       - **May tune** (Phase 33 authority): DRILL_SPEED, DRILL_DRIFT_SPEED, all drill costs, tap/hold threshold, windup duration, accelerated-regen multiplier, whether mid-drill manual unfuse is enabled, whether drill gains i-frames.

    Cite 30-CONTEXT decisions inline (D-07, D-08, D-09, D-25, D-26, D-27, D-28).
  </action>
  <verify>
    <automated>grep -qE '^## Drill-Dive Contract' .planning/FUSION-DESIGN.md && grep -q 'DRILL_SPEED' .planning/FUSION-DESIGN.md && grep -q 'DRILL_IMPACT_COST' .planning/FUSION-DESIGN.md && grep -q 'DRILL_ACTIVATION_COST' .planning/FUSION-DESIGN.md && grep -q 'DRILL_BLOCK_REFUND' .planning/FUSION-DESIGN.md && grep -q 'DRILL_CRACKED_V_COST' .planning/FUSION-DESIGN.md && grep -q 'CRACKED_V' .planning/FUSION-DESIGN.md && grep -qE 'solid' .planning/FUSION-DESIGN.md && grep -qE 'juice.*0|juice = 0|juice = 0' .planning/FUSION-DESIGN.md && grep -qi 'cancel' .planning/FUSION-DESIGN.md && grep -q 'player\.py' .planning/FUSION-DESIGN.md && grep -q '_v1\.3-reference\.json' .planning/FUSION-DESIGN.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `## Drill-Dive Contract` heading exists.
    - DRILL_SPEED, DRILL_IMPACT_COST, DRILL_ACTIVATION_COST, DRILL_BLOCK_REFUND, DRILL_CRACKED_V_COST, DRILL_DRIFT_SPEED named with explicit values.
    - CRACKED_V handling documented (VALIDATION.md FUS-03 row 4).
    - Three exit conditions identifiable by grep: `solid`, `juice.*0`, `cancel` (VALIDATION.md FUS-03 row 5).
    - File:line citations to `player.py` AND `_v1.3-reference.json` appear at least once each.
    - i-frames = NONE noted per Open-Q #1 default resolution.
    - Phase 32 preserve/change/may-tune table present.
    - Every numeric value has a named constant or file:line source (Anti-pattern: "about 20 juice" forbidden).
  </acceptance_criteria>
  <done>§ Drill-Dive Contract appended; every drill value cited with source; three exit conditions enumerated; Phase 32 scope delimited.</done>
</task>

<task type="auto">
  <name>Task 6: Write § Cut Abilities (one-line rationale each)</name>
  <files>.planning/FUSION-DESIGN.md</files>
  <read_first>
    - .planning/FUSION-DESIGN.md (read what Tasks 1-5 wrote; append after § Drill-Dive Contract)
    - .planning/phases/30-fusion-lifecycle-design-doc/30-CONTEXT.md (D-02 binding rule: cut abilities get one-line rationale each; NO full contracts)
    - .planning/phases/30-fusion-lifecycle-design-doc/30-RESEARCH.md (§ State of the Art; Pitfall 5 forbidding per-ability contracts for cut abilities)
    - .planning/ROADMAP.md (Phase 8 entries for the six v1.1 abilities; provides the names to cite)
  </read_first>
  <action>
    Append `## Cut Abilities` (anchor: `cut-abilities`) section to `.planning/FUSION-DESIGN.md`.

    **CRITICAL constraint (per D-02 and 30-RESEARCH Pitfall 5):** each cut ability gets **ONE LINE** of rationale. No FSM entries. No juice math. No per-ability contract. Doc sections > 1 paragraph per cut ability = scope creep.

    Content MUST include:

    1. **Opening paragraph (2-3 sentences)**: frame the cuts as prototype focus, not rejection. Quote the CONTEXT framing: "expansion-era content that doesn't earn its keep in a feel-first prototype." State that these abilities are out-of-prototype-scope — revisit post-prototype when transitioning to Godot/Unity is a natural re-evaluation point.

    2. **Cut list** — exactly this bullet format (one line each), naming the five cut abilities verbatim so VALIDATION.md CUT-01 grep succeeds:

       - **Slime Ram** — horizontal dash-through-CRACKED_H fusion. Cut because drill covers vertical gating and the prototype boss fight (shoot-to-daze → drill-to-kill per D-03) doesn't need a horizontal plunge.
       - **Directional Hold** — mid-air directional lock fusion. Cut because its value was compounding precision with other abilities; stripped down to single-fusion, it has no partner to compound with.
       - **Charge Shot** — hold-Z long-charge projectile fusion. Cut because the prototype Z-button is the unified slime/fusion button (D-10), and a charge-shot mode adds a tap/hold/long-hold three-way disambiguation on top of the tap/hold split we're already stabilizing.
       - **Bubble Shield** — absorb-damage fusion shield. Cut because the mana-shield behavior (MANA_SHIELD_COST — v1.1 D-04, retained in this doc) already provides the "fused damage drains juice" primitive; a separate shield ability would double-count.
       - **Slime Boost** — upward-plunge fusion (vertical push). Cut because DRILL is the single fusion verb for commitment-based vertical movement; boost was the paired "up" to drill's "down", and we're picking one.

    3. **Code-strip follow-up callout** (references the Deferred Idea from 30-CONTEXT.md):

       > **Code-strip phase required before Phase 32.** Code for the cut abilities still exists in `src/entities/player.py` (ram_dx/dy, shield_*, charge_shot_*, boost_*, has_shield/has_boost flags), `src/entities/slime.py`, and as tuning groups in `assets/physics-schema.json` (`ram`, `charge_shot`, `boost`, `bubble_shield`). A separate code-strip phase MUST run between Phase 30 (this doc) and Phase 32 (fusion refactor) to remove that code. Track via `/gsd-insert-phase` after this phase closes — see Task 8's ROADMAP update.

    4. **Post-prototype revisit note** (per Deferred → "Post-prototype abilities"): these abilities are NOT deleted from design thinking. Post-prototype (Godot/Unity transition per 30-CONTEXT Deferred) is the natural re-evaluation point where fresh paradigms might give them a better home.

    Cite 30-CONTEXT decisions (D-02, D-03) for traceability.
  </action>
  <verify>
    <automated>grep -qE '^## Cut Abilities' .planning/FUSION-DESIGN.md && for a in "Slime Ram" "Directional Hold" "Charge Shot" "Bubble Shield" "Slime Boost"; do grep -q "$a" .planning/FUSION-DESIGN.md || { echo "MISSING-CUT: $a"; exit 1; }; done && grep -qi 'code-strip' .planning/FUSION-DESIGN.md && grep -qi 'post.prototype' .planning/FUSION-DESIGN.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `## Cut Abilities` heading exists.
    - All five cut-ability names present exactly as VALIDATION.md CUT-01 checks: `Ram`, `Directional Hold`, `Charge Shot`, `Bubble Shield`, `Slime Boost`.
    - Each cut ability is ONE LINE (or at most one sentence) — no sub-headings, no sub-bullets, no tables for cut abilities (per D-02 + Pitfall 5).
    - Code-strip phase requirement noted.
    - Post-prototype revisit framing present.
  </acceptance_criteria>
  <done>§ Cut Abilities section appended; all five abilities named as one-liners; code-strip follow-up flagged; framing = prototype-focus-not-rejection.</done>
</task>

<task type="auto">
  <name>Task 7: Write § Acceptance Checklist (Phase 32) + § Lock Protocol</name>
  <files>.planning/FUSION-DESIGN.md</files>
  <read_first>
    - .planning/FUSION-DESIGN.md (read what Tasks 1-6 wrote; append after § Cut Abilities)
    - .planning/phases/30-fusion-lifecycle-design-doc/30-CONTEXT.md (D-27, D-28 binding: behavioral checklist, no pytest)
    - .planning/phases/30-fusion-lifecycle-design-doc/30-RESEARCH.md (Pitfall 4 missing cross-reference for locked_commit semantics — dictates § Lock Protocol content)
    - .planning/phases/29-player-movement-feel-pass/29-FEEL-TARGETS.md (precedent for checklist-style acceptance documents — uses [ ] checkbox syntax)
  </read_first>
  <action>
    Append two final content sections to `.planning/FUSION-DESIGN.md`:

    **A. `## Acceptance Checklist` (anchor: `acceptance-checklist`)** — the behavioral contract Phase 32 must satisfy by inspection + smoke test (no pytest per D-28). Use markdown checkbox syntax `- [ ]` per Phase 29 precedent.

    Content MUST include:

    1. **Opening paragraph**: Phase 32's exit criteria — verified by code inspection + manual smoke test. No automated regression harness required.

    2. **Input Model Checklist (FUS-02)**:
       - [ ] Z (logical `spit` action) tap fires spit when unfused, daze shot when fused (same projectile code path per D-14).
       - [ ] Z hold past ~8-frame threshold triggers RECALL (unfused) or UNFUSE_WINDUP (fused).
       - [ ] V (logical `dash` action) unfused DOWN+V in air triggers pogo bounce (on enemies/breakables; no bounce on solid ground).
       - [ ] V fused DOWN+V in air triggers drill dive (no bounce; pure plunge).
       - [ ] Drill activation is routed through `dash` action, NOT `jump` action (Phase 32 remap verified by grep of `btnp("dash")` AND `btn("down")` in drill entry).
       - [ ] Release-before-WINDUP-completes returns slime to follow (NOT freeze) — spit still fires on tap after release.

    3. **FSM Checklist (FUS-01)**:
       - [ ] All five FSM states observable in code: IDLE, RECALL, WINDUP, FUSED, EXIT (auto + manual sub-paths).
       - [ ] `fuse_start` emits on WINDUP → FUSED transition (not on RECALL entry).
       - [ ] `fuse_end` emits on every EXIT path (auto-dissipate, manual-eject).
       - [ ] `drill_start` / `drill_block_break` / `drill_end` / `manual_unfuse_start` events implemented per § Fusion FSM event table.
       - [ ] WINDUP release = free cancel (no cost, no punishment) — verify by releasing Z mid-windup, slime returns to follow.
       - [ ] UNFUSE_WINDUP release = free cancel (symmetric — stay fused) — verify by releasing Z mid-unfuse-windup.
       - [ ] 100% juice gate applies to drill-dive entry (currently `>0` in v1.3 per `player.py:447` — align to `>= max_juice`).
       - [ ] Accelerated regen activates only while Z held + slime docked at player + slime not dissipated.

    4. **Drill-Dive Checklist (FUS-03)**:
       - [ ] DRILL_SPEED=2.0 applied as a clamp each frame during drill (`dy` re-set to DRILL_SPEED in `apply_diving_physics`).
       - [ ] DRILL_ACTIVATION_COST=5.0 consumed at drill entry.
       - [ ] DRILL_IMPACT_COST=20.0 consumed on solid-terrain landing.
       - [ ] DRILL_BLOCK_REFUND=+15.0 refunded per soft-destructible passthrough.
       - [ ] DRILL_CRACKED_V_COST=20.0 consumed per CRACKED_V gate-block break.
       - [ ] Drill has NO i-frames (preserve v1.3 — per Open-Q #1 resolution). If Phase 32 adds i-frames, that's a Phase 33 feel-pass change, not a Phase 32 refactor change.
       - [ ] Three exit conditions implemented: (a) solid-terrain contact → consume DRILL_IMPACT_COST, emit `drill_impact` + `drill_end`, unfuse NO dissipate; (b) juice=0 → unfuse WITH dissipate, SLIME_DISSIPATE_COOLDOWN=240f; (c) Z-hold past threshold → UNFUSE_WINDUP path → EXIT_MANUAL.
       - [ ] Drill smoke test: player in a room with alternating CRACKED_V + soft destructibles + solid floor, full juice, activate drill, confirm all three exit conditions trigger correctly in three separate runs.

    5. **Out-of-scope reminder for Phase 32** (cut abilities):
       - [ ] Phase 32 does NOT add back ram, hold, charge_shot, bubble_shield, boost — those were code-stripped in the pre-32 phase.
       - [ ] Phase 32 ships `src/fusion/` with FusionAbility Protocol, FusionManager shell, **one** DrillDive module (not six) per 30-CONTEXT Deferred "Phase 32 scope shrinks".

    **B. `## Lock Protocol` (anchor: `lock-protocol`)** — per 30-RESEARCH Pitfall 4 (missing cross-reference for locked_commit semantics):

    Content MUST include:

    1. **What `locked_commit` points at**: the git SHA of the commit at which this doc transitioned to `status: LOCKED`. Any edit after that SHA requires user approval to re-lock at a new SHA.

    2. **Lock workflow (two-commit dance, matches Task 8 execution):**
       - Author the doc with frontmatter `locked_commit: TBD`.
       - Commit the doc (this is the **doc-write commit**).
       - Run `git rev-parse HEAD` to capture the SHA of the doc-write commit.
       - Amend the frontmatter `locked_commit:` field with that SHA.
       - Commit the frontmatter amendment with message including "lock FUSION-DESIGN at <sha>" (this is the **lock commit**, distinct from the doc-write commit).
       - Phase 32 reads `locked_commit` from frontmatter → verifies the SHA exists in git history → builds against that spec.

    3. **Re-lock policy**: if the locked doc requires a correction, the user must explicitly approve. The corrective flow:
       - Change `status: LOCKED` → `status: UNLOCKED` OR open a new doc `.planning/FUSION-DESIGN-v2.md` (preserve audit trail).
       - Make edits.
       - Re-lock with a new `locked_commit` SHA.
       - Notify Phase 32 / Phase 33 planners that the target has shifted.

    4. **What downstream phases verify**:
       - Phase 32 plan YAML includes `depends_on_doc: FUSION-DESIGN.md` with a `depends_on_sha` field matching `locked_commit`. Phase 32 planner verifies the SHA via `git cat-file -p <sha>` before writing PLAN.
       - Phase 33 plan YAML references `drill-dive-contract` anchor — reads values from this doc, tunes from the named constants.
  </action>
  <verify>
    <automated>grep -qE '^## Acceptance Checklist' .planning/FUSION-DESIGN.md && grep -qE '^- \[ \]' .planning/FUSION-DESIGN.md && grep -qE '^## Lock Protocol' .planning/FUSION-DESIGN.md && grep -qi 'git rev-parse' .planning/FUSION-DESIGN.md && grep -qi 'two-commit\|two commit' .planning/FUSION-DESIGN.md && grep -q 'Phase 32' .planning/FUSION-DESIGN.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `## Acceptance Checklist` heading exists (VALIDATION.md ACCEPT-01 passes).
    - Checklist uses `- [ ]` markdown-checkbox syntax (Phase 29 FEEL-TARGETS precedent).
    - Input model checklist, FSM checklist, drill-dive checklist, and out-of-scope reminder all present.
    - `## Lock Protocol` heading exists.
    - `git rev-parse` workflow documented.
    - Re-lock policy documented.
  </acceptance_criteria>
  <done>Both final content sections appended. Doc is now complete in content — only the lock-commit dance remains.</done>
</task>

<task type="auto">
  <name>Task 8: Doc-write commit, lock-commit dance, ROADMAP scope-pivot update</name>
  <files>
    .planning/FUSION-DESIGN.md
    .planning/ROADMAP.md
  </files>
  <read_first>
    - .planning/FUSION-DESIGN.md (verify complete — all 7 content sections present)
    - .planning/ROADMAP.md (locate Phase 30 entry, Phase 32 entry, Phase 33 entry, and Progress table row for Phase 30)
    - .planning/phases/30-fusion-lifecycle-design-doc/30-CONTEXT.md (Deferred Ideas section — code-strip phase required, Phase 32 scope shrinks, Phase 33 scope shrinks)
    - .planning/phases/30-fusion-lifecycle-design-doc/30-VALIDATION.md (LOCK-04 SHA hex check; Manual-Only Verifications table)
  </read_first>
  <action>
    Execute the lock-commit dance and fold in the ROADMAP scope-pivot update. This is a sequential multi-step task — each step must complete before the next.

    **Step 1: Pre-flight verify** that `.planning/FUSION-DESIGN.md` is complete. Run the full VALIDATION.md Per-Task Verification Map as a single compound check. If any check fails, abort the task and fix the failing section before proceeding. Explicit validation grep (adapted from 30-VALIDATION.md):
    ```bash
    test -f .planning/FUSION-DESIGN.md \
      && head -10 .planning/FUSION-DESIGN.md | grep -qE '^status:\s*LOCKED' \
      && head -10 .planning/FUSION-DESIGN.md | grep -qE '^locked_at:\s*[0-9]{4}-[0-9]{2}-[0-9]{2}' \
      && head -10 .planning/FUSION-DESIGN.md | grep -qE '^locked_commit:' \
      && for s in IDLE RECALL WINDUP FUSED EXIT; do grep -q "\b$s\b" .planning/FUSION-DESIGN.md || { echo "MISSING: $s"; exit 1; }; done \
      && grep -q 'DRILL_SPEED' .planning/FUSION-DESIGN.md \
      && grep -q 'DRILL_IMPACT_COST' .planning/FUSION-DESIGN.md \
      && grep -q 'CRACKED_V' .planning/FUSION-DESIGN.md \
      && grep -q '100%' .planning/FUSION-DESIGN.md \
      && for r in FUS-01 FUS-02 FUS-03; do grep -qE "(^##.*$r|\*\*$r\*\*)" .planning/FUSION-DESIGN.md || { echo "MISSING-DEF: $r"; exit 1; }; done \
      && for a in Ram "Directional Hold" "Charge Shot" "Bubble Shield" "Slime Boost"; do grep -q "$a" .planning/FUSION-DESIGN.md || { echo "MISSING-CUT: $a"; exit 1; }; done \
      && grep -q '^## .*[Aa]cceptance' .planning/FUSION-DESIGN.md \
      && echo "PRE-FLIGHT OK"
    ```

    **Step 2: Update ROADMAP.md to reflect the scope pivot.** Locate and modify (via Edit tool, not rewrite):

    a. **Phase 30 entry** (line ~165-174 in current ROADMAP.md): Narrow the Goal to reflect the single-fusion pivot. Replace old 6-ability goal with:
       > **Goal**: Produce a locked `.planning/FUSION-DESIGN.md` that narrows the prototype to **one fusion mechanic (Drill Dive)**, defines the initiate/sustain/end FSM under a 100%-gated juice-as-mana economy, specifies a unified single-button input model, captures v1.3 drill behavior as Phase 32 regression target, and lists acceptance checks Phase 32 must satisfy. Design only — no code changes.

       Update the Success Criteria bullet #2 and #3 to reference the single-fusion narrowing (e.g., "The doc enumerates drill-dive's juice cost, regen rate, mana shield drain, and empty-state behavior under the juice-as-mana model" and "A drill-dive contract captures current v1.3 behavior as Phase 32 regression target; cut abilities are enumerated as one-liners").

       Update **Plans**: `TBD` → `1 plan`:
       ```
       Plans:
       - [ ] 30-01-PLAN.md — Author and lock FUSION-DESIGN.md (scope pivot rationale, input model, FSM, juice economy, drill-dive contract, cut abilities, acceptance checklist, two-commit lock dance, ROADMAP update)
       ```

    b. **New Phase 30b / pre-32 note** (insert between Phase 30 and Phase 31 sections, or append to Phase 30 entry as a follow-up note): add a short note indicating a cut-ability-code-strip phase is required between Phase 30 and Phase 32. Exact text (append at end of Phase 30 entry):
       > **Follow-up (code-strip phase, TBD number — insert via `/gsd-insert-phase`)**: Remove cut-ability code from `src/entities/player.py` (ram_dx/dy, shield_*, charge_shot_*, boost_*, has_shield/has_boost flags), `src/entities/slime.py`, and tuning groups `ram` / `charge_shot` / `boost` / `bubble_shield` from `assets/physics-schema.json`. Hard gate before Phase 32.

    c. **Phase 32 entry** (line ~187-195): shrink scope. Replace the 6-ability Goal with:
       > **Goal**: Refactor fusion out of `player.py` into `src/fusion/` with a `FusionAbility` Protocol, `FusionManager` state shell, `ChargeController` pre-manager, and **one** ability module (`drill_dive`). Pure refactor gated on the Phase 30 design doc. Save format gains a `save_version` field; v1.3 save round-trip is explicitly not required.

       Update Success Criteria bullet #1 to reference single module: "`src/fusion/` exists with `FusionAbility` Protocol, `FusionManager`, `ChargeController`, and a `drill_dive` module; old fusion code (including the five cut abilities' remnants) is removed from `player.py`" and bullet #2 to "A regression playthrough against the Phase 30 drill-dive contract confirms FUS-03 behaves identically to v1.3 after the refactor."

    d. **Phase 33 entry** (line ~197-205): shrink scope. Replace the 6-ability Goal with:
       > **Goal**: Retune drill-dive against the new lifecycle using the live panel — windup timing, sustain behavior, end/cancel feel, particle color, button-mapping confirmation, SFX identity. Per-ability identity goal reduces to drill identity under the single-fusion prototype.

       Update Success Criteria to reference drill only: "Drill-dive has a distinguishable windup → sustain → end curve tuned through the panel against the Phase 30 drill-dive contract" (bullet #1), "Drill has a distinct particle color and SFX cue" (bullet #2), "Drill still satisfies its Phase 30 contract — no regression from Phase 32 refactor" (bullet #3).

    e. **Progress table** (line ~240-259): update Phase 30 row: `0/TBD` → `0/1` (plan count now known; will become `1/1` when Plan 01 completes).

    **Step 3: Doc-write commit** (first of the two-commit dance). Stage `.planning/FUSION-DESIGN.md` AND `.planning/ROADMAP.md` and commit via gsd-sdk:
    ```bash
    gsd-sdk query commit "docs(30): author FUSION-DESIGN.md + ROADMAP scope-pivot (one fusion, not six)" \
      .planning/FUSION-DESIGN.md .planning/ROADMAP.md
    ```
    This commit has `locked_commit: TBD` in the frontmatter. The ROADMAP update is folded into the same commit per `planning_context > roadmap_followup_decisions > #4` recommendation.

    **Step 4: Capture SHA of the doc-write commit**:
    ```bash
    DOC_WRITE_SHA=$(git rev-parse HEAD)
    echo "Doc-write commit SHA: $DOC_WRITE_SHA"
    ```

    **Step 5: Amend frontmatter `locked_commit` field** with the captured SHA. Use the Edit tool on `.planning/FUSION-DESIGN.md`:
    ```
    old: locked_commit: TBD
    new: locked_commit: <full 40-char SHA from $DOC_WRITE_SHA>
    ```

    **Step 6: Lock commit** (second of the two-commit dance):
    ```bash
    gsd-sdk query commit "docs(30): lock FUSION-DESIGN at $DOC_WRITE_SHA" \
      .planning/FUSION-DESIGN.md
    ```

    **Step 7: Post-flight verification** — run the full VALIDATION.md Per-Task Verification Map. The critical post-lock check that was permissibly deferred earlier:
    ```bash
    head -10 .planning/FUSION-DESIGN.md | grep -qE '^locked_commit:\s*[0-9a-f]{7,40}$' && echo "LOCK-04 OK"
    git log -1 --format=%H -- .planning/FUSION-DESIGN.md
    ```
    The `git log -1` output should be the **lock commit** SHA (Step 6's result). The `locked_commit` field in frontmatter should be the **doc-write commit** SHA (Step 4's result — the commit BEFORE the lock amendment). This is correct — `locked_commit` points at what was locked, not at the act of locking.

    **Step 8: Diff-verify no regression** (per MEMORY: worktree-merges-cause-regressions). Phase 30 ran on main, but still:
    ```bash
    git diff HEAD~2 HEAD --stat
    ```
    Expect exactly two files changed: `.planning/FUSION-DESIGN.md` (new) and `.planning/ROADMAP.md` (modified). No unexpected changes.
  </action>
  <verify>
    <automated>head -10 .planning/FUSION-DESIGN.md | grep -qE '^locked_commit:\s*[0-9a-f]{7,40}$' && head -10 .planning/FUSION-DESIGN.md | grep -qE '^status:\s*LOCKED' && grep -q '30-01-PLAN.md' .planning/ROADMAP.md && grep -qi 'one fusion mechanic' .planning/ROADMAP.md && grep -qi 'drill_dive' .planning/ROADMAP.md && git log -2 --oneline -- .planning/FUSION-DESIGN.md | wc -l | grep -qE '^\s*2\s*$' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Pre-flight: all 18 VALIDATION.md checks pass before the doc-write commit.
    - `.planning/FUSION-DESIGN.md` frontmatter `locked_commit` is a hex SHA (7-40 chars), NOT `TBD` (VALIDATION.md LOCK-04 passes strictly).
    - `locked_commit` SHA equals the doc-write commit SHA (what was locked), not the lock commit SHA (the act of locking).
    - `git log -2 --oneline -- .planning/FUSION-DESIGN.md` shows exactly two commits: the doc-write commit and the lock commit.
    - `.planning/ROADMAP.md` Phase 30 entry: Goal narrowed to one-fusion, Plans count = 1, plan list includes `30-01-PLAN.md`.
    - `.planning/ROADMAP.md` Phase 32 entry: Goal mentions single ability (`drill_dive`) not six.
    - `.planning/ROADMAP.md` Phase 33 entry: Goal mentions drill-dive only, not six abilities.
    - `.planning/ROADMAP.md` has code-strip follow-up note (searchable via `grep -i code-strip` or `grep -i "insert-phase"`).
    - Post-flight `git diff HEAD~2 HEAD --stat` shows exactly two files: FUSION-DESIGN.md + ROADMAP.md.
  </acceptance_criteria>
  <done>Doc committed with real SHA in frontmatter; ROADMAP reflects scope pivot; two-commit dance complete; phase ready for checker + user sign-off.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Doc author → Phase 32 planner | Downstream tooling reads `locked_commit` from frontmatter; malformed YAML breaks the read. |
| Git history → future planners | `locked_commit` SHA must resolve in `git log`; amended/orphaned commits would break verification. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | Tampering | `.planning/FUSION-DESIGN.md` frontmatter | mitigate | Task 8 Step 7 verifies frontmatter parses via `head -10 | grep -qE '^locked_commit:\s*[0-9a-f]{7,40}$'` — catches malformed YAML that would break Phase 32's downstream read. |
| T-30-02 | Information disclosure | `locked_commit` points at wrong commit | mitigate | Task 8 Step 4 captures SHA via `git rev-parse HEAD` immediately after the doc-write commit; Step 5 amends frontmatter; Step 7 asserts SHA matches the doc-write commit (not the lock commit). VALIDATION.md's Manual-Only Verifications table documents this invariant. |
| T-30-03 | Denial of service | Content checks fail silently | mitigate | Task 8 Step 1 pre-flight runs the full 18-check VALIDATION.md map and aborts on ANY missing reference. No partial doc gets committed. |
| T-30-04 | Data handling / PII / network / auth | N/A | accept | Rationale: Phase 30 writes one markdown file under `.planning/`. No data handling, no auth, no network, no runtime state. Attack surface is essentially nil per planning_context > threat_model_note. |
</threat_model>

<verification>
## Phase-Level Verification (VALIDATION.md Per-Task Verification Map)

Run this compound one-liner from repo root after Task 8 completes. All 18 row-checks must succeed (exit 0):

```bash
test -f .planning/FUSION-DESIGN.md \
  && head -10 .planning/FUSION-DESIGN.md | grep -qE '^status:\s*LOCKED' \
  && head -10 .planning/FUSION-DESIGN.md | grep -qE '^locked_at:\s*[0-9]{4}-[0-9]{2}-[0-9]{2}' \
  && head -10 .planning/FUSION-DESIGN.md | grep -qE '^locked_commit:\s*[0-9a-f]{7,40}$' \
  && grep -qE '^## .*FSM' .planning/FUSION-DESIGN.md \
  && for s in IDLE RECALL WINDUP FUSED EXIT; do grep -q "\b$s\b" .planning/FUSION-DESIGN.md || { echo "MISSING: $s"; exit 1; }; done \
  && grep -q '100%' .planning/FUSION-DESIGN.md \
  && grep -qE '(Z.*tap|Z.*hold|DOWN.*V)' .planning/FUSION-DESIGN.md \
  && grep -qE '[0-9]+[[:space:]]*frame' .planning/FUSION-DESIGN.md \
  && grep -qE '^## .*[Dd]rill' .planning/FUSION-DESIGN.md \
  && grep -q 'DRILL_SPEED' .planning/FUSION-DESIGN.md \
  && grep -q 'DRILL_IMPACT_COST' .planning/FUSION-DESIGN.md \
  && grep -q 'CRACKED_V' .planning/FUSION-DESIGN.md \
  && for r in FUS-01 FUS-02 FUS-03; do grep -qE "(^##.*$r|\*\*$r\*\*)" .planning/FUSION-DESIGN.md || { echo "MISSING-DEF: $r"; exit 1; }; done \
  && for a in Ram "Directional Hold" "Charge Shot" "Bubble Shield" "Slime Boost"; do grep -q "$a" .planning/FUSION-DESIGN.md || { echo "MISSING-CUT: $a"; exit 1; }; done \
  && grep -qE '^## .*[Aa]cceptance' .planning/FUSION-DESIGN.md \
  && echo "ALL 18 CHECKS PASS"
```

Post-lock manual verification (per VALIDATION.md Manual-Only Verifications):
- User reviews rendered doc in GitHub / Obsidian / VS Code preview.
- User explicitly signs off on the locked design.
- If user requests changes, re-lock via the § Lock Protocol re-lock policy (new SHA).
</verification>

<success_criteria>
Phase 30 is COMPLETE when:
- All 18 VALIDATION.md Per-Task Verification Map checks pass.
- `locked_commit` in FUSION-DESIGN.md frontmatter is a real 7-40 char hex SHA (not `TBD`).
- That SHA resolves in `git log` (verified via `git cat-file -p <sha>`).
- `.planning/ROADMAP.md` reflects scope pivot: Phase 30 plans = 1, Phase 32 goal = single ability, Phase 33 goal = drill-only, code-strip follow-up noted.
- Two-commit dance artifacts visible in `git log --oneline -2 -- .planning/FUSION-DESIGN.md` (doc-write commit + lock commit).
- User signs off on the locked doc (manual verification, outside this plan's scope; tracked in Phase 30 SUMMARY).
</success_criteria>

<output>
After completion, create `.planning/phases/30-fusion-lifecycle-design-doc/30-01-SUMMARY.md` covering:
- What was built: locked FUSION-DESIGN.md with 7 content sections + YAML frontmatter lock.
- Key decisions made while writing the doc (resolutions of Open-Questions #1-5 from 30-RESEARCH.md): i-frames preserved as NONE, FUS-02 button = V (remap target), manual_unfuse_start fires at windup-begin, free-cancel symmetric at both windups, accelerated-regen draft = 2× passive.
- Doc-write commit SHA (the `locked_commit` value) AND lock commit SHA.
- ROADMAP pivot applied: Phase 30 (1 plan), Phase 32 (single-ability refactor), Phase 33 (drill-only feel pass), code-strip follow-up flagged.
- Next-steps: run `/gsd-insert-phase` to add the cut-ability-code-strip phase between 30 and 32 before Phase 32 begins.
</output>
