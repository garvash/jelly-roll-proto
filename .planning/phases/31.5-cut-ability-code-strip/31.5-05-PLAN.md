---
phase: 31.5-cut-ability-code-strip
plan: 05
type: execute
wave: 3
depends_on: [31.5-01, 31.5-02, 31.5-03, 31.5-04]
files_modified:
  - gates/grep-checklist.sh
  - .planning/phases/31.5-cut-ability-code-strip/31.5-SMOKE-CHECKLIST.md
  - tests/test_bubble_shield.py
  - tests/test_charge_recoil.py
  - tests/test_charge_shot.py
  - tests/test_charge_shot_windup.py
  - tests/test_dash.py
  - tests/test_ram.py
  - tests/test_slime_boost.py
  - tests/test_slime_hold.py
  - tests/test_tap_reposition.py
  - tests/test_save_system.py
  - tests/test_input.py
  - tests/test_input_remap.py
  - tests/test_fusion.py
  - tests/test_schema.py
  - tests/test_tuning.py
  - .planning/phases/31.5-cut-ability-code-strip/31.5-VERIFICATION.md
autonomous: false
requirements: []
threat_model_severity: low
nyquist_compliant: true

must_haves:
  truths:
    - "Wave 0 artifact `gates/grep-checklist.sh` exists, is executable, and exits 0 (Gate 1 PASS)"
    - "Wave 0 artifact `31.5-SMOKE-CHECKLIST.md` exists at phase-dir root with 8 actions modeled on 30-HUMAN-UAT.md"
    - "All 9 cut-only test files DELETED (test_bubble_shield, test_charge_recoil, test_charge_shot, test_charge_shot_windup, test_dash, test_ram, test_slime_boost, test_slime_hold, test_tap_reposition)"
    - "6 mixed test files UPDATED (test_save_system, test_input, test_input_remap, test_fusion, test_schema, test_tuning) — cut references removed where present"
    - "Gate 1 (grep checklist): `bash gates/grep-checklist.sh` exits 0"
    - "Gate 2 (boot smoke): `timeout 30 python main.py` returns rc=124"
    - "Gate 3 (human smoke): user runs `31.5-SMOKE-CHECKLIST.md`; 8 actions ticked"
    - "Gate 4 (pytest): `python -m pytest` exits 0 across full suite"
    - "`31.5-VERIFICATION.md` written with all 4 gates recorded as pass/fail"
    - "ONE commit per CONTEXT D-15"
  artifacts:
    - path: "gates/grep-checklist.sh"
      provides: "Executable Gate 1 checklist; exits non-zero if any cut symbol survives in src/"
      min_lines: 30
    - path: ".planning/phases/31.5-cut-ability-code-strip/31.5-SMOKE-CHECKLIST.md"
      provides: "Human-runnable Gate 3 checklist (8 actions)"
    - path: ".planning/phases/31.5-cut-ability-code-strip/31.5-VERIFICATION.md"
      provides: "Gate-by-gate proof-of-work record"
      contains: "Gate 1, Gate 2, Gate 3, Gate 4"
  key_links:
    - from: "gates/grep-checklist.sh"
      to: "Gate 1 of CONTEXT D-18"
      via: "Plan 05 Task 1 authors script; Task 4 runs it"
      pattern: "git grep -nE"
    - from: "31.5-VERIFICATION.md"
      to: "All 4 D-18 gates"
      via: "Final task records pass/fail per gate"
      pattern: "Gate (1|2|3|4)"
---

<objective>
Author Wave 0 artifacts (`gates/grep-checklist.sh` + `31.5-SMOKE-CHECKLIST.md`), delete 9 cut-only test files, update 6 mixed test files, run all four CONTEXT D-18 gates, and write `31.5-VERIFICATION.md` recording pass/fail. Hard gate before Phase 32.

Purpose: Acceptance phase. Plans 01-04 stripped the code, schema, presets, and wiring; this plan proves the strip is complete and the surviving system is healthy.

Output: Wave 0 gate artifacts + 9 deleted test files + 6 updated test files + verification doc; pytest green; one commit. Phase 31.5 ready to merge → Phase 32 unblocked.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/31.5-cut-ability-code-strip/31.5-CONTEXT.md
@.planning/phases/31.5-cut-ability-code-strip/31.5-PATTERNS.md
@.planning/phases/31.5-cut-ability-code-strip/31.5-RESEARCH.md
@.planning/phases/31.5-cut-ability-code-strip/31.5-VALIDATION.md
@.planning/phases/30-fusion-lifecycle-design-doc/30-HUMAN-UAT.md
@.planning/phases/31.5-cut-ability-code-strip/31.5-01-SUMMARY.md
@.planning/phases/31.5-cut-ability-code-strip/31.5-02-SUMMARY.md
@.planning/phases/31.5-cut-ability-code-strip/31.5-03-SUMMARY.md
@.planning/phases/31.5-cut-ability-code-strip/31.5-04-SUMMARY.md
@tests/conftest.py

<interfaces>
<!-- Test files to DELETE (CONTEXT D-07, D-21; PATTERNS test section) -->

| File | Reason |
|------|--------|
| tests/test_bubble_shield.py | exercises `update_shield` (deleted in Plan 01 sub-step 4) |
| tests/test_charge_recoil.py | exercises CHARGE_RECOIL_FORCE (deleted in Plan 03) |
| tests/test_charge_shot.py | exercises `update_charge_shot` / `_tick_charge_shot_windup` (deleted in Plan 01 sub-step 5) |
| tests/test_charge_shot_windup.py | same |
| tests/test_dash.py | exercises `start_dash` / `apply_dash_physics` (deleted in Plan 01 sub-step 3) |
| tests/test_ram.py | exercises `start_ram` / `apply_ram_physics` / `end_ram` (deleted in Plan 01 sub-step 1) |
| tests/test_slime_boost.py | exercises `start_boost` / `update_boost` / `end_boost` (deleted in Plan 01 sub-step 2) |
| tests/test_slime_hold.py | exercises Hold state (deleted in Plan 02) |
| tests/test_tap_reposition.py | exercises `was_tap("left"/"right", HOLD_TAP_THRESHOLD)` Hold input (deleted in Plan 01 sub-step 5; D-07) |

<!-- Test files to UPDATE (PATTERNS test section) -->

| File | Edit |
|------|------|
| tests/test_save_system.py | Drop has_dash/has_shield/has_shield_t2/has_boost assertions; update _make_save_data fixture to {max_hp, max_juice, has_drill, ...} |
| tests/test_input.py | Drop "dash" from `expected_actions` set (line 163); drop `"dash":` entry from any mock _ACTION_MAP rebuild (line 50) |
| tests/test_input_remap.py | Drop `has_dash=False` kwarg from line 56 + line 70 make_player call sites; delete `test_dash_button_no_drill_dive`, `test_v_still_triggers_ram_when_fused`, `test_v_still_triggers_dash_when_unfused` if present |
| tests/test_fusion.py | PATTERNS verified zero `ram` references → likely no edit needed; confirm during execution |
| tests/test_schema.py | PATTERNS verified does not assert against cut tuning groups → likely no edit needed; confirm during execution |
| tests/test_tuning.py | Drop `assert tuning.RAM_INVINCIBLE is True` at line ~100 (PEP-562 access to deleted key) |

<!-- Wave 0 grep checklist symbols (CONTEXT D-18 Gate 1) -->

CUT_SYMBOLS to grep in src/:
- ram_dx, has_shield, charge_shot, start_boost, start_dash, start_ram
- RAMMING, DASHING, BOOSTING, CHARGING_SHOT
- is_holding_position, HOLD_SCAN_RANGE
- DASH_PICKUP, SHIELD_PICKUP, BOOST_PICKUP, SHIELD_T2

Plus targeted file checks:
- `grep -n '"dash"' src/core/input.py` returns no match
- `grep -nE '^\s*"(dash|slime_ram|charge_shot|boost)":' assets/physics-schema.json` returns no matches

<!-- 31.5-SMOKE-CHECKLIST.md format (modeled on 30-HUMAN-UAT.md) -->

8 actions:
1. Boot game (`python main.py`) — title screen renders
2. Walk left/right — player moves
3. Jump — player jumps with variable height
4. Wall-jump — player wall-slides + wall-jumps
5. Kick a block — slime kicks a block (spit projectile)
6. Spit while unfused — projectile fires from slime via Z press
7. Drill into a CRACKED_V tile with full juice — DOWN+SPACE while airborne fuses + drills
8. Save + reload — save at SavePoint, exit, reload — has_drill preserved
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (n/a) | Test layer + acceptance docs; no runtime/network surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31.5-07 | Tampering | n/a | accept | Test layer; ASVS V2-V6 do not apply. |

**Severity:** low.
</threat_model>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Wave 0 — author gates/grep-checklist.sh + 31.5-SMOKE-CHECKLIST.md</name>
  <files>gates/grep-checklist.sh, .planning/phases/31.5-cut-ability-code-strip/31.5-SMOKE-CHECKLIST.md</files>
  <read_first>
    - .planning/phases/31.5-cut-ability-code-strip/31.5-RESEARCH.md § Code Examples — Example 4 (gate script) + Example 5 (boot smoke)
    - .planning/phases/31.5-cut-ability-code-strip/31.5-VALIDATION.md § Wave 0 Requirements
    - .planning/phases/30-fusion-lifecycle-design-doc/30-HUMAN-UAT.md (precedent format for human checklist)
    - .planning/phases/31.5-cut-ability-code-strip/31.5-CONTEXT.md D-18 (4 gates), D-19 (smoke is human-driven), D-20 (artifacts location)
  </read_first>
  <action>
    **A. Create `gates/grep-checklist.sh`** at the repo root (per RESEARCH § Open Question 4 default + CONTEXT Discretion: phase-dir root vs `gates/` subdir — choose `gates/` at repo root for symmetry with potential future gate scripts):

    ```bash
    #!/bin/bash
    # gates/grep-checklist.sh — Phase 31.5 Gate 1
    # Exits non-zero if any cut symbol survives in src/.

    set -e
    FAIL=0

    CUT_SYMBOLS=(
        "ram_dx" "has_shield" "charge_shot" "start_boost" "start_dash"
        "start_ram" "RAMMING" "DASHING" "BOOSTING" "CHARGING_SHOT"
        "is_holding_position" "HOLD_SCAN_RANGE"
        "DASH_PICKUP" "SHIELD_PICKUP" "BOOST_PICKUP" "SHIELD_T2"
    )

    for sym in "${CUT_SYMBOLS[@]}"; do
        if git grep -nE "\b$sym\b" -- 'src/' > /dev/null 2>&1; then
            echo "FAIL: '$sym' still present in src/"
            git grep -nE "\b$sym\b" -- 'src/'
            FAIL=1
        fi
    done

    # input.py specific check
    if grep -nE '^\s*"dash"\s*:' src/core/input.py 2>/dev/null; then
        echo "FAIL: 'dash' still in input.py _ACTION_MAP"
        FAIL=1
    fi

    # physics-schema specific check
    if grep -nE '^\s*"(dash|slime_ram|charge_shot|boost)":' assets/physics-schema.json 2>/dev/null; then
        echo "FAIL: cut group still in physics-schema.json"
        FAIL=1
    fi

    if [[ $FAIL -eq 0 ]]; then
        echo "Gate 1 PASS: all cut symbols absent from src/"
    fi
    exit $FAIL
    ```

    Make executable: `chmod +x gates/grep-checklist.sh` (informational; on Windows the `bash` interpreter handles execution).

    **B. Create `.planning/phases/31.5-cut-ability-code-strip/31.5-SMOKE-CHECKLIST.md`** modeled on `30-HUMAN-UAT.md`:

    ```markdown
    # Phase 31.5: Cut-Ability Code-Strip — Human Smoke Checklist

    **Purpose:** Gate 3 of CONTEXT D-18 four-gate proof-of-work. Confirm the strip
    didn't break any surviving gameplay path.

    **Run instructions:**
    1. `python main.py`
    2. Execute each action in order; tick each checkbox.
    3. Record pass/fail in `31.5-VERIFICATION.md` Gate 3 section.

    ## Actions

    - [ ] **1. Boot game** — Title screen renders without crash. No `KeyError`,
          `AttributeError`, or `JSONDecodeError` in stdout.
    - [ ] **2. Walk left/right** — Player moves at expected walk speed; no
          jitter or stuck state.
    - [ ] **3. Jump** — `SPACE` produces a jump; releasing early reduces height
          (variable jump). Coyote time + jump buffer feel preserved.
    - [ ] **4. Wall-jump** — Run into a wall while airborne → wall-slide; press
          jump → wall-jump in opposite direction.
    - [ ] **5. Kick a block** — Recall slime onto a kick-target block; the
          slime kicks it. Block animates and triggers expected effect.
    - [ ] **6. Spit while unfused** — Press `Z` (unfused) → slime fires a spit
          projectile from its position. Projectile arcs and disappears on
          impact / off-screen.
    - [ ] **7. Drill into CRACKED_V with full juice** — While airborne with
          `has_drill = True` and `slime.juice > 0`, hold `DOWN` and press
          `SPACE` (DOWN+SPACE per Phase 30). Player enters DIVING state, drills
          through a CRACKED_V tile, breaks it, and exits cleanly when juice
          runs out OR ground is hit.
    - [ ] **8. Save + reload round-trip** — Touch a SavePoint to save. Quit and
          relaunch. Load the save. Player's `has_drill` is preserved; max_hp,
          max_juice, collected_iids, visited_rooms all match.

    ## Sign-off

    - **Tester:** _____________
    - **Date:** _____________
    - **Result:** PASS / FAIL
    - **Notes:** _____________

    Forward to `31.5-VERIFICATION.md` Gate 3.
    ```

    Both artifacts are Wave 0 — they must exist BEFORE Tasks 4-5 run the gates.
  </action>
  <verify>
    <automated>test -f gates/grep-checklist.sh && test -f .planning/phases/31.5-cut-ability-code-strip/31.5-SMOKE-CHECKLIST.md && bash -n gates/grep-checklist.sh && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `gates/grep-checklist.sh` exists and passes shell-syntax check (`bash -n gates/grep-checklist.sh` exits 0)
    - `.planning/phases/31.5-cut-ability-code-strip/31.5-SMOKE-CHECKLIST.md` exists
    - Smoke checklist contains 8 numbered action checkboxes
    - Grep checklist enumerates all 16 cut symbols from CONTEXT D-18 Gate 1
    - Grep checklist includes the input.py + physics-schema.json specific checks
  </acceptance_criteria>
  <done>Both Wave 0 gate artifacts authored at correct paths.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Delete 9 cut-only test files</name>
  <files>tests/test_bubble_shield.py, tests/test_charge_recoil.py, tests/test_charge_shot.py, tests/test_charge_shot_windup.py, tests/test_dash.py, tests/test_ram.py, tests/test_slime_boost.py, tests/test_slime_hold.py, tests/test_tap_reposition.py</files>
  <read_first>
    - .planning/phases/31.5-cut-ability-code-strip/31.5-PATTERNS.md § "Test files" — confirm 9 files marked DELETE FILE
    - .planning/phases/31.5-cut-ability-code-strip/31.5-CONTEXT.md D-07, D-21
  </read_first>
  <action>
    Delete the 9 cut-only test files via `git rm`:

    ```
    git rm tests/test_bubble_shield.py
    git rm tests/test_charge_recoil.py
    git rm tests/test_charge_shot.py
    git rm tests/test_charge_shot_windup.py
    git rm tests/test_dash.py
    git rm tests/test_ram.py
    git rm tests/test_slime_boost.py
    git rm tests/test_slime_hold.py
    git rm tests/test_tap_reposition.py
    ```

    If any of these files do not exist (e.g., `test_tap_reposition.py` per CONTEXT D-07 noted "if it exists"), `git rm` will report — re-verify via `ls tests/` first if needed.

    No file edits in this task — pure deletes. The cut-only tests exercise methods/state that no longer exists post Plans 01-02 — keeping them would yield ImportError or AttributeError under pytest.
  </action>
  <verify>
    <automated>for f in tests/test_bubble_shield.py tests/test_charge_recoil.py tests/test_charge_shot.py tests/test_charge_shot_windup.py tests/test_dash.py tests/test_ram.py tests/test_slime_boost.py tests/test_slime_hold.py tests/test_tap_reposition.py; do test ! -e "$f" && echo "DELETED: $f" || { echo "STILL EXISTS: $f"; exit 1; }; done</automated>
  </verify>
  <acceptance_criteria>
    - All 9 cut-only test files absent from `tests/` directory
    - `git status` shows the deletions staged
    - `tests/test_drill_retcon.py`, `tests/test_kick_removal.py`, `tests/test_goo_mold_removal.py` still present (precedent absence-tests are unrelated and stay)
  </acceptance_criteria>
  <done>9 cut-only test files deleted; surviving tests untouched.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update 6 mixed test files</name>
  <files>tests/test_save_system.py, tests/test_input.py, tests/test_input_remap.py, tests/test_fusion.py, tests/test_schema.py, tests/test_tuning.py</files>
  <read_first>
    - tests/conftest.py (full file — verify make_player / make_slime helpers; confirm `**overrides` pattern)
    - tests/test_save_system.py (full file)
    - tests/test_input.py (focus on lines 50, 163)
    - tests/test_input_remap.py (focus on lines 46-103)
    - tests/test_fusion.py (full scan — verify zero ram references per PATTERNS)
    - tests/test_schema.py (full scan — verify no cut-group assertions per PATTERNS)
    - tests/test_tuning.py (focus on PEP-562 access tests around lines 95-105)
    - .planning/phases/31.5-cut-ability-code-strip/31.5-PATTERNS.md § "Test files" — six mixed test detail tables
    - .planning/phases/31.5-cut-ability-code-strip/31.5-CONTEXT.md D-24 (test_save_system update scope)
    - .planning/phases/31.5-cut-ability-code-strip/31.5-RESEARCH.md § Risk Surface 4 (make_player kwargs) + Risk Surface 5 (sympathetic regressions)
  </read_first>
  <action>
    **A. `tests/test_save_system.py`** — drop cut-flag assertions:

    1. In the round-trip assertions (lines ~91-100 area per PATTERNS):
       - DELETE: `assert data["player"]["has_dash"] is False` (and similar for has_shield, has_shield_t2, has_boost)
       - KEEP: `assert data["version"] == 1`, `assert data["player"]["max_hp"] == ...`, `assert data["player"]["has_drill"] is True`

    2. In `_make_save_data` fixture (lines ~179-202 per PATTERNS):
       - DELETE has_dash/has_shield/has_shield_t2/has_boost params from the function signature
       - DELETE the corresponding entries from the returned dict
       - KEEP: max_hp, max_juice, has_drill, collected_iids, event_flags, visited_rooms, save_room_id

    3. `test_restore_sets_abilities` test at lines ~323-331:
       - DELETE all four cut-flag assertions
       - If no surviving flag remains for the test to assert, DELETE the entire test function

    **B. `tests/test_input.py`** — drop dash references:

    1. Line ~163 `test_action_map_has_all_keys`:
       - Update `expected_actions = {"left", "right", "up", "down", "jump", "spit", "dash", "pause", "confirm"}` to remove `"dash"`. Surviving set: `{"left", "right", "up", "down", "jump", "spit", "pause", "confirm"}` (8 actions).

    2. Line ~50 (if any mock `_ACTION_MAP` rebuild):
       - Drop `"dash": [...]` entry if present

    **C. `tests/test_input_remap.py`** — drop dash/ram/V-button tests:

    1. Line ~56 `make_player(...has_dash=False...)` call:
       - Drop `has_dash=False` kwarg. Per Risk Surface 4: `make_player` uses `**overrides` so the kwarg becomes a no-op rather than a crash, but drop for hygiene.

    2. Line ~70 (similar):
       - Drop `has_dash=False` kwarg.

    3. Test methods to DELETE if present (per PATTERNS lines 60-103):
       - `test_dash_button_no_drill_dive`
       - `test_v_still_triggers_ram_when_fused`
       - `test_v_still_triggers_dash_when_unfused`

    KEEP `class TestDrillDiveOnDownSpace` and its `test_drill_dive_on_down_space` method — surviving drill activation test (PATTERNS analog).

    **D. `tests/test_fusion.py`** — verify no edit needed:

    Per PATTERNS verified 2026-04-25: this file has zero `ram` references. Re-grep:
    ```
    grep -nE 'ram|RAM_|charge_shot|CHARGE_|dash|DASH_|boost|BOOST_|bubble_shield|shield_active' tests/test_fusion.py
    ```
    If the grep returns 0 matches, no edit needed. If it returns matches (PATTERNS may be slightly stale), strip them.

    **E. `tests/test_schema.py`** — verify no edit needed:

    Per PATTERNS this file does NOT assert against cut tuning groups. Re-grep:
    ```
    grep -nE '"(dash|slime_ram|charge_shot|boost)"|DashPickup|ShieldPickup|BoostPickup|ShieldT2' tests/test_schema.py
    ```
    If 0 matches, no edit. If matches present (e.g., entity-schema test referencing cut entities), update to drop them.

    **F. `tests/test_tuning.py`** — drop cut-key PEP-562 access:

    1. Around line ~100 (per PATTERNS) `test_pep562_flat_access`:
       - DELETE: `assert tuning.RAM_INVINCIBLE is True`
       - DELETE any other `assert tuning.<cut-key>` lines (DASH_*, CHARGE_*, BOOST_*, HOLD_TAP_THRESHOLD)
       - KEEP: assertions for `tuning.GRAVITY`, `tuning.JUMP_FORCE`, `tuning.MAX_WALK_SPEED`, `tuning.SAVE_FILE`, and the `with pytest.raises(AttributeError)` block

    **G. Sympathetic regression sweep (per RESEARCH Risk 5 + Open Question 5):**

    Read `tests/test_persistence.py` (if present); grep for cut-flag references:
    ```
    grep -nE 'has_(dash|shield|shield_t2|boost)' tests/test_persistence.py
    ```
    If matches present, update accordingly. If absent, no edit needed; D-21's full pytest gate at Task 5 catches anything else.

    **Final hygiene gate (Risk 4):**
    ```
    grep -rnE '\bhas_(dash|shield|shield_t2|boost)\s*=' tests/
    ```
    Expected 0 matches across `tests/` after this task.
  </action>
  <verify>
    <automated>grep -rnE '\bhas_(dash|shield|shield_t2|boost)\s*=' tests/ ; rc=$?; if [ $rc -eq 1 ]; then echo "OK: zero hits"; else echo "FAIL: cut-flag refs still present"; exit 1; fi</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rnE '\bhas_(dash|shield|shield_t2|boost)\s*=' tests/` returns 0 matches (Risk 4 hygiene)
    - `grep -nE '"dash"' tests/test_input.py` returns 0 matches
    - `grep -nE 'tuning\.(DASH|RAM|CHARGE|BOOST)_' tests/test_tuning.py` returns 0 matches
    - `grep -n 'class TestDrillDiveOnDownSpace' tests/test_input_remap.py` returns 1 match (surviving drill test preserved)
    - `grep -n 'has_drill' tests/test_save_system.py` returns 1+ match (surviving flag preserved in fixture + assertions)
    - `python -c "import ast; [ast.parse(open(f).read()) for f in ['tests/test_save_system.py','tests/test_input.py','tests/test_input_remap.py','tests/test_fusion.py','tests/test_schema.py','tests/test_tuning.py']]"` exits 0 (all 6 files are valid Python)
  </acceptance_criteria>
  <done>6 mixed test files updated; cut-flag references removed; surviving fixtures and tests intact.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Run Gate 1 (grep checklist) + Gate 2 (boot smoke) + Gate 4 (pytest)</name>
  <files>.planning/phases/31.5-cut-ability-code-strip/31.5-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/31.5-cut-ability-code-strip/31.5-VALIDATION.md § Phase Goal → Gate Map
    - .planning/phases/31.5-cut-ability-code-strip/31.5-CONTEXT.md D-18, D-21
    - .planning/phases/30-fusion-lifecycle-design-doc/30-VERIFICATION.md (precedent format)
  </read_first>
  <action>
    Run the three automated gates in order. Capture output to be embedded in `31.5-VERIFICATION.md`.

    **Gate 1 — Grep checklist:**
    ```
    bash gates/grep-checklist.sh
    echo "Gate 1 rc=$?"
    ```
    Expected: `Gate 1 PASS: all cut symbols absent from src/`, rc=0.

    If FAIL: read the printed `git grep` output for any surviving symbol; fix in the relevant Plan 01-04 file (the strip is incomplete) before proceeding. This is a hard gate — Gate 2/4 cannot run if symbols remain.

    **Gate 2 — Boot smoke:**
    ```
    timeout 30 python main.py 2>&1 | tee /tmp/31.5-boot.log
    rc=$?
    echo "Gate 2 rc=$rc (expected 124 = clean timeout boot)"
    ```
    Expected: rc=124 (timeout — game booted to title and is running normally) OR rc=0 (clean exit if Pyxel auto-quits).

    If rc != 124 and rc != 0: grep `/tmp/31.5-boot.log` for `error|exception|traceback`; fix the boot crash; re-run.

    **Gate 4 — Full pytest:**
    ```
    python -m pytest 2>&1 | tee /tmp/31.5-pytest.log
    echo "Gate 4 rc=$?"
    ```
    Expected: rc=0 (pytest green).

    If FAIL: read failure traces. Per RESEARCH Risk Surface 5, sympathetic regressions in untouched tests are expected (e.g., `test_persistence.py` round-tripping a save dict that previously contained `has_dash`). Update those tests inline (this task can extend Task 3's scope if needed) and re-run.

    **Per CONTEXT D-21:** Run the FULL pytest suite, not filtered. Catches indirect regressions in untouched tests.

    Per CONTEXT Specifics line 220: pytest runs ONCE in the entire phase, at end of Plan 05. This task is that single run.

    **Author `31.5-VERIFICATION.md`** with Gate 1, 2, 4 results recorded so far (Gate 3 will be added in Task 5):

    ```markdown
    # Phase 31.5: Cut-Ability Code-Strip — Verification

    **Verified:** YYYY-MM-DD
    **Phase commits:** 5 (Plans 01-05 each one commit per CONTEXT D-15)

    ## Gate 1 — Grep Checklist (automated)

    Status: PASS / FAIL
    Command: `bash gates/grep-checklist.sh`
    Result: <paste output>

    ## Gate 2 — Boot Smoke (semi-headless)

    Status: PASS / FAIL
    Command: `timeout 30 python main.py`
    rc: <number>
    Notes: <any deviations>

    ## Gate 3 — Human Smoke Playthrough (manual)

    Status: PENDING (filled in by Task 5)
    Reference: `31.5-SMOKE-CHECKLIST.md` (8 actions)

    ## Gate 4 — Full Pytest (automated)

    Status: PASS / FAIL
    Command: `python -m pytest`
    rc: <number>
    Tests run: <count>
    Failures: <list any>

    ## Cross-Cutting Invariants Audit

    Per RESEARCH § Cross-Cutting Invariant Audit (13 invariants):

    | # | Invariant | Verified | Evidence |
    |---|-----------|----------|----------|
    | 1 | Drill paths | ✅ | grep `apply_diving_physics`/`has_drill` returns matches |
    | 2 | Spit | ✅ | `class Projectile` survives in projectile.py |
    | ... | ... | ... | ... |

    ## Sign-off

    Phase 31.5 complete. Phase 32 unblocked.
    ```

    Fill in real values for Gate 1/2/4; leave Gate 3 PENDING — Task 5 (checkpoint) populates it.
  </action>
  <verify>
    <automated>bash gates/grep-checklist.sh && timeout 30 python main.py; rc=$?; if [ $rc -eq 124 ] || [ $rc -eq 0 ]; then echo "Gate 2 OK rc=$rc"; else echo "Gate 2 FAIL rc=$rc"; exit 1; fi && python -m pytest && echo "All automated gates PASS"</automated>
  </verify>
  <acceptance_criteria>
    - Gate 1: `bash gates/grep-checklist.sh` exits 0 with `Gate 1 PASS: all cut symbols absent from src/`
    - Gate 2: `timeout 30 python main.py` returns rc=124 or rc=0; `/tmp/31.5-boot.log` has no `Traceback`
    - Gate 4: `python -m pytest` exits 0 (full suite green)
    - `31.5-VERIFICATION.md` exists with Gate 1, 2, 4 status filled in (Gate 3 left PENDING)
  </acceptance_criteria>
  <done>3 automated gates passed; verification doc drafted with Gate 3 pending the human smoke.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Gate 3 — Human smoke playthrough + sign-off</name>
  <files>.planning/phases/31.5-cut-ability-code-strip/31.5-SMOKE-CHECKLIST.md, .planning/phases/31.5-cut-ability-code-strip/31.5-VERIFICATION.md</files>
  <what-built>
    Plans 01-04 stripped cut-ability code, schema, presets, input bindings, save fields, pickup entities, and UI panel routing. Plan 05 Tasks 1-4 authored Wave 0 artifacts, deleted 9 cut-only tests, updated 6 mixed tests, and ran 3 automated gates (Gate 1: grep checklist; Gate 2: boot smoke; Gate 4: pytest).

    Gate 3 is the human-driven smoke playthrough per CONTEXT D-19 + D-20. Pyxel MCP automation was deferred (D-19 + Deferred Idea) because designing an input script for Pyxel `play_and_capture` is itself work outside a strip phase.
  </what-built>
  <how-to-verify>
    1. Open `.planning/phases/31.5-cut-ability-code-strip/31.5-SMOKE-CHECKLIST.md`.
    2. Run `python main.py`.
    3. Execute each of the 8 actions in order; tick each checkbox in the checklist:
       1. Boot game — title renders without crash
       2. Walk left/right
       3. Jump (with variable height)
       4. Wall-jump
       5. Kick a block (slime kicks via recall onto target)
       6. Spit while unfused (Z press, slime fires projectile)
       7. Drill into CRACKED_V with full juice (DOWN+SPACE while airborne)
       8. Save + reload round-trip (touch SavePoint, quit, relaunch, load — `has_drill` preserved)
    4. Sign off the checklist (tester, date, PASS/FAIL, notes).
    5. Update `31.5-VERIFICATION.md` Gate 3 section: change `Status: PENDING` to `Status: PASS` (or FAIL with notes).

    **Critical sub-actions** (RESEARCH § Manual-Only Verifications):
    - **Mana shield invariant (Cross-Cutting Invariant #5):** While fused with juice > 0, take damage from any hazard. Juice should drain by `MANA_SHIELD_COST`; HP should remain unchanged. Verifies bubble-shield strip didn't break mana shield.
    - **Save load with stale flags:** If a pre-strip save exists on disk, load it. D-23 keeps `load()` body untouched; stale `has_dash`/`has_shield` keys in the JSON should be ignored downstream because Player no longer reads them. Confirm no crash.

    If any action FAILS: do not proceed to commit. Capture the failure mode in `31.5-VERIFICATION.md` Gate 3 notes; fix the regression in the relevant plan; re-run.
  </how-to-verify>
  <action>This is a checkpoint task — see `<how-to-verify>` for the human action steps. The executor pauses here until the user types "approved" with the checklist signed off, then proceeds to Task 6.</action>
  <verify>
    <automated>grep -E "^\s*-\s*\[x\]" .planning/phases/31.5-cut-ability-code-strip/31.5-SMOKE-CHECKLIST.md | wc -l | grep -E "^\s*8\s*$" && echo "OK: all 8 actions ticked"</automated>
  </verify>
  <done>All 8 smoke-checklist actions ticked; tester signed off; `31.5-VERIFICATION.md` Gate 3 updated to PASS or FAIL with notes.</done>
  <resume-signal>Type "approved" with checklist signed and Gate 3 marked PASS, or describe failures with reproduction steps.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 6: Finalize 31.5-VERIFICATION.md + commit</name>
  <files>.planning/phases/31.5-cut-ability-code-strip/31.5-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/31.5-cut-ability-code-strip/31.5-SMOKE-CHECKLIST.md (post-Task-5; should be signed off)
    - .planning/phases/30-fusion-lifecycle-design-doc/30-VERIFICATION.md (precedent: gate-by-gate format)
  </read_first>
  <action>
    Update `31.5-VERIFICATION.md` after Gate 3 sign-off:

    1. Change Gate 3 status from `PENDING` to `PASS` (or FAIL with notes).
    2. Embed the signed `31.5-SMOKE-CHECKLIST.md` (link or quoted summary).
    3. Fill in the Cross-Cutting Invariants Audit table (13 invariants per RESEARCH; each with grep command + result).
    4. Add a Phase Sign-Off section: "Phase 31.5 complete. Phase 32 unblocked."

    **Commit Plan 05** (per CONTEXT D-15):
    ```
    git add gates/grep-checklist.sh \
            .planning/phases/31.5-cut-ability-code-strip/31.5-SMOKE-CHECKLIST.md \
            .planning/phases/31.5-cut-ability-code-strip/31.5-VERIFICATION.md \
            tests/test_save_system.py tests/test_input.py tests/test_input_remap.py \
            tests/test_fusion.py tests/test_schema.py tests/test_tuning.py
    # The 9 deletions are already staged from Task 2's git rm
    git commit -m "$(cat <<'EOF'
    feat(31.5): test cleanup + Wave 0 gates + verification

    - Author gates/grep-checklist.sh (Gate 1) + 31.5-SMOKE-CHECKLIST.md (Gate 3)
    - Delete 9 cut-only tests: test_bubble_shield, test_charge_recoil,
      test_charge_shot, test_charge_shot_windup, test_dash, test_ram,
      test_slime_boost, test_slime_hold, test_tap_reposition (D-07, D-21)
    - Update 6 mixed tests: drop cut-flag references from test_save_system,
      test_input, test_input_remap, test_tuning; verify test_fusion +
      test_schema need no edit (D-24)
    - Author 31.5-VERIFICATION.md recording all 4 D-18 gates green

    All 4 D-18 gates PASS:
    - Gate 1 (grep): cut symbols absent from src/
    - Gate 2 (boot): rc=124 timeout, no crash
    - Gate 3 (human smoke): 8 actions ticked
    - Gate 4 (pytest): full suite green

    Phase 31.5 complete. Phase 32 fusion refactor unblocked.

    Per CONTEXT D-07, D-15, D-18, D-19, D-20, D-21, D-24.
    EOF
    )"
    ```
  </action>
  <verify>
    <automated>git log --oneline -5 && cat .planning/phases/31.5-cut-ability-code-strip/31.5-VERIFICATION.md | grep -E '^Status:|^## Gate' | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `31.5-VERIFICATION.md` has all 4 gates marked PASS (or noted FAIL with disposition)
    - Cross-Cutting Invariants Audit table populated for all 13 invariants
    - Phase Sign-Off section present
    - `git log --oneline -1` shows the Plan 05 commit
    - `git show --stat HEAD` lists ~17 files (1 gate script + 1 smoke checklist + 1 verification doc + 6 updated tests + 9 deleted tests, accounting for Task 2's stage)
    - All 4 D-18 gates: PASS
  </acceptance_criteria>
  <done>Verification finalized; Phase 31.5 commit on main; Phase 32 unblocked.</done>
</task>

</tasks>

<verification>
After Plan 05 commit:

1. **Gate 1 PASS:** `bash gates/grep-checklist.sh` exits 0
2. **Gate 2 PASS:** `timeout 30 python main.py` returns rc=124
3. **Gate 3 PASS:** `31.5-SMOKE-CHECKLIST.md` signed off (8 actions ticked)
4. **Gate 4 PASS:** `python -m pytest` exits 0
5. `31.5-VERIFICATION.md` exists, all 4 gates PASS
6. 9 cut-only tests deleted; 6 mixed tests updated
7. Wave 0 artifacts (`gates/grep-checklist.sh`, `31.5-SMOKE-CHECKLIST.md`) on disk
8. ONE commit per CONTEXT D-15 (5 commits total for Phase 31.5)
9. ROADMAP Phase 31.5 success criteria 1-4 all satisfied:
   - SC1: player.py + slime.py contain no cut-ability code paths
   - SC2: physics-schema.json + presets.py + 5 presets free of cut groups
   - SC3: dash removed from _ACTION_MAP; save schema no longer reads/writes cut flags; round-trip succeeds
   - SC4: test suite passes; grep returns zero gameplay hits for cut symbols
</verification>

<success_criteria>
- All 4 CONTEXT D-18 gates PASS (Gate 1 grep + Gate 2 boot + Gate 3 human smoke + Gate 4 pytest)
- `31.5-VERIFICATION.md` written with full pass/fail record + Cross-Cutting Invariants Audit
- 9 cut-only tests deleted, 6 mixed tests updated
- Wave 0 artifacts authored before gates ran
- ONE commit per CONTEXT D-15 (Phase 31.5 total: 5 commits)
- Phase 32 hard-gate dependency satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/31.5-cut-ability-code-strip/31.5-05-SUMMARY.md` with:
- Wave 0 artifacts authored (paths)
- 9 cut-only tests deleted
- 6 mixed tests updated (per-file change summary)
- 4 D-18 gates results
- Cross-Cutting Invariants Audit summary (13/13 verified)
- Commit SHA
- Phase 32 unblocked confirmation
</output>
