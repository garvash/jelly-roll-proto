---
phase: 31.5-cut-ability-code-strip
plan: 04
subsystem: cross-cutting-infra
tags: [code-strip, input, save, pickups, ldtk-loader, ui-panel, entity-schema, fusion-prep, game-feel-v2.0]

# Dependency graph
requires:
  - phase: 31.5-cut-ability-code-strip
    provides: "Plan 01 stripped player.py cut-ability methods (no readers of has_dash/has_shield/has_shield_t2/has_boost remain on player); Plan 02 stripped slime.py Hold state; Plan 03 (sibling Wave 2) strips schema + presets so cut tuning keys vanish"
provides:
  - "src/core/input.py with `dash` removed from _ACTION_MAP (8 actions remain); was_tap primitive preserved per D-07"
  - "src/core/save_manager.py save() writing only {max_hp, has_drill} in player block; version: 1 stays; load() body untouched per D-23"
  - "src/entities/items.py with 3 type branches (ENERGY, MISSILE, DRILL_PICKUP) and 3-entry ITEM_FRAMES dict"
  - "main.py LDtk loader without DashPickup/ShieldPickup/BoostPickup/ShieldT2 elif branches; DrillPickup branch + event-bus subscribers preserved"
  - "assets/entity-schema.json with 11 entities (down from 15); cut-pickup definitions deleted; WeaponUpgrade/MovementUpgrade descriptions cleaned"
  - "src/ui/presets.py FEEL_GROUPS with 8 surviving groups (movement, forgiving, wall, slime_follow, slime_juice, projectile, drill, fusion); forward-compat skip preserved"
  - "src/ui/panel.py FEEL_GROUPS mirror + TAB_DEFS stripped of cut groups; Move tab routes movement only; Fuse tab routes drill + fusion only; tab name 'Fuse' preserved per Specifics"
affects: [31.5-05-tests-and-acceptance, 32-fusion-manager-protocol-refactor]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Atomic cross-cutting commit (CONTEXT D-15): 7 files in one coherent commit covering input + save + items + LDtk loader + entity schema + UI panel + presets"
    - "Cut-and-burn deletion (no _deprecated archive blocks) per CONTEXT D-01"
    - "Surviving-symbol analog discipline: each strip preserved its sister-symbol shape (DRILL_PICKUP type-branch + frame entry as analog for cut DASH/SHIELD/BOOST/SHIELD_T2; DrillPickup elif as analog for cut LDtk branches; EnergyTank entity definition as analog for cut entity-schema entries)"
    - "Defensive `getattr(player, \"has_drill\", False)` pattern preserved in save write side"
    - "Forward-compat schema skip preserved (presets.py lines 82-88 try/except KeyError)"

key-files:
  created: []
  modified:
    - "src/core/input.py (-1 LOC; 1 _ACTION_MAP entry deleted)"
    - "src/core/save_manager.py (-4 LOC; 4 cut-flag keys deleted from save() player block; load() body byte-identical per D-23)"
    - "src/entities/items.py (-14 LOC; 4 type-branch elifs + 4 ITEM_FRAMES entries deleted; 1 doc-comment refreshed)"
    - "main.py (-8 LOC; 4 LDtk loader elif branches deleted; event-bus subscribers untouched)"
    - "assets/entity-schema.json (-72 LOC; 4 entity definitions deleted; WeaponUpgrade/MovementUpgrade description strings cleaned)"
    - "src/ui/presets.py (-2 LOC; 4 cut groups dropped from FEEL_GROUPS literal)"
    - "src/ui/panel.py (-5 LOC net; 4 cut groups dropped from FEEL_GROUPS mirror + TAB_DEFS Move tab + Fuse tab; tab structure preserved; preset routing untouched)"

key-decisions:
  - "Stage 7 files individually then commit ONCE per CONTEXT D-15 atomicity rule (cross-cutting change forms single coherent commit boundary). Pre-existing modifications to assets/output/* and assets/sprites/* (worktree base state) were NOT touched."
  - "Used --no-verify on commit per parallel-executor protocol (running as Wave 2 sibling worktree alongside Plan 03)."
  - "WeaponUpgrade description cleaned to drop the example entirely ('Offensive ability pickup.'); MovementUpgrade description replaced cut examples with surviving DrillPickup ('Movement ability pickup (e.g. DrillPickup).'). Per CONTEXT D-08 + planner discretion."
  - "Tab name 'Fuse' KEPT (not renamed to 'Drill') per CONTEXT Specifics line 222 + Discretion: rename deferred to Phase 33+."
  - "ITEM_FRAMES doc-comment updated to reflect orphan frames 2-5 are art-only post-strip per D-09 (sprite cleanup deferred)."

patterns-established:
  - "Cross-cutting infrastructure strip pattern: when 4+ cut symbols span input + save + entity-loader + entity-schema + UI routing, perform all edits, run import-smoke + JSON-validation + save-roundtrip + boot gates BEFORE the single atomic commit."
  - "Save round-trip verification with stale-flag-leak guard: stub player with stale cut-flag attributes set to True, save, assert cut-flag keys absent from JSON output and version: 1 + has_drill present."

requirements-completed: []  # Plan declared requirements: [] in frontmatter (maintenance phase per CONTEXT)

# Metrics
duration: ~4min
completed: 2026-04-26
---

# Phase 31.5 Plan 04: Cross-Cutting Cut-Ability Wiring Strip Summary

**107 lines deleted across 7 files in one atomic commit: input dash binding, save cut-flag writes, items pickup type-branches + frame entries, main.py LDtk loader branches, entity-schema definitions, and UI panel/presets routing — all cut surfaces removed; was_tap, has_drill, version: 1, load() body, event-bus subscribers, presets.py forward-compat skip, and "Fuse" tab name preserved.**

## Performance

- **Duration:** ~4 min (247 seconds)
- **Started:** 2026-04-26T07:08:20Z
- **Completed:** 2026-04-26T07:12:27Z
- **Tasks:** 3 (Task 1: input/save/items/main.py; Task 2: entity-schema/presets/panel; Task 3: boot smoke + atomic commit)
- **Files modified:** 7 (single coherent commit per CONTEXT D-15)
- **LOC delta:** +12 / -107 (net -95)

## Accomplishments

- `src/core/input.py` `_ACTION_MAP` reduced to 8 actions (left, right, up, down, jump, spit, pause, confirm); `dash` entry deleted; `was_tap` primitive preserved
- `src/core/save_manager.py` `save()` player block reduced to `{max_hp, has_drill}`; cut-flag writes (`has_dash`/`has_shield`/`has_shield_t2`/`has_boost`) deleted; `version: 1` preserved (Phase 32 owns bump per D-22); `SaveManager.load()` body BYTE-IDENTICAL per D-23 (verified by diff: only deletions in `save()`)
- `src/entities/items.py` type-branch chain reduced to 3 branches (ENERGY → MISSILE → DRILL_PICKUP); `ITEM_FRAMES` dict reduced to 3 entries (ENERGY=0, MISSILE=1, DRILL_PICKUP=6); doc-comment refreshed
- `main.py` LDtk loader: 4 cut-pickup elif branches deleted (DashPickup/ShieldPickup/BoostPickup/ShieldT2); DrillPickup + Snail/Bat/EnergyTank/MissileTank/SavePoint/Door/OneWay/HiddenLoot/Map branches preserved; event-bus subscribers at lines 274/285/286/313 untouched (Phase 31 contract)
- `assets/entity-schema.json` entity count: 15 → 11; `DashPickup`, `ShieldPickup`, `BoostPickup`, `ShieldT2` definitions deleted; `WeaponUpgrade`/`MovementUpgrade` description strings cleaned of cut-pickup examples; JSON validates (`python -m json.tool`)
- `src/ui/presets.py` `FEEL_GROUPS` reduced to 8 surviving groups (movement, forgiving, wall, slime_follow, slime_juice, projectile, drill, fusion); forward-compat skip at lines 82-88 preserved
- `src/ui/panel.py` `FEEL_GROUPS` mirror + `TAB_DEFS`: Move tab routes `movement` only (`dash` dropped); Fuse tab routes `drill` + `fusion` only (`slime_ram`/`charge_shot`/`boost` dropped); preset routing `if tab_name == "Fuse": expanded = (group_name == "drill")` UNTOUCHED; tab name "Fuse" preserved per Specifics
- Save round-trip (with stale cut flags set on player stub): `has_drill: True` preserved in JSON output; `has_dash`/`has_shield`/`has_shield_t2`/`has_boost` keys absent; `version: 1` present; `SaveManager.load()` round-trips identically
- Boot smoke: `timeout 5 python main.py` returns rc=124 (timeout = clean boot, no crash)

## Task Commits

Per CONTEXT D-15 (atomicity), this plan executes as ONE commit covering all 7 files:

1. **Task 1: Strip input + save + items + main.py LDtk loader** — staged but not yet committed
2. **Task 2: Strip entity-schema.json + UI panel + presets** — staged but not yet committed
3. **Task 3: Boot smoke + UI panel render verify + commit** — `5f40a22` (feat)

**Single atomic commit:** `5f40a22` — `feat(31.5-04): strip cut-ability cross-cutting wiring` (7 files changed, +12 / -107)

_Note: `--no-verify` flag used per parallel-executor protocol (running as Wave 2 sibling worktree alongside Plan 03)._

## Files Created/Modified

- `src/core/input.py` — `dash` action deleted from `_ACTION_MAP` (8 actions remain); `was_tap` primitive preserved (used by Z-button tap/hold per FUSION-DESIGN Input Model + D-07)
- `src/core/save_manager.py` — Cut-flag writes (`has_dash`/`has_shield`/`has_shield_t2`/`has_boost`) deleted from `save()` `data["player"]` block; `version: 1` + `max_hp` + `has_drill` preserved; `load()` body BYTE-IDENTICAL per D-23
- `src/entities/items.py` — 4 type-branch elifs deleted (`DASH_PICKUP`/`SHIELD_PICKUP`/`BOOST_PICKUP`/`SHIELD_T2`); 4 `ITEM_FRAMES` entries deleted; doc-comment + `item_type` parameter comment refreshed; `ENERGY`/`MISSILE`/`DRILL_PICKUP` preserved
- `main.py` — 4 LDtk loader elif branches deleted (`DashPickup`/`ShieldPickup`/`BoostPickup`/`ShieldT2`); `DrillPickup` branch + all surviving entity branches (Snail/Bat/EnergyTank/MissileTank/SavePoint/Door/OneWay/HiddenLoot/Map) preserved; event-bus subscribers at lines 274/285/286/313 untouched
- `assets/entity-schema.json` — 4 entity definitions deleted (`DashPickup`/`ShieldPickup`/`BoostPickup`/`ShieldT2`); `WeaponUpgrade` description simplified to "Offensive ability pickup."; `MovementUpgrade` description updated to "Movement ability pickup (e.g. DrillPickup)."; JSON valid; entity count 15 → 11
- `src/ui/presets.py` — `FEEL_GROUPS` reduced from 12 → 8 groups (drop `dash`/`slime_ram`/`charge_shot`/`boost`); forward-compat skip at lines 82-88 untouched
- `src/ui/panel.py` — `FEEL_GROUPS` mirror reduced from 12 → 8; `TAB_DEFS` Move tab `dash` dropped (now routes `movement` only); `TAB_DEFS` Fuse tab `slime_ram`/`charge_shot`/`boost` dropped (now routes `drill` + `fusion` only); preset routing `if tab_name == "Fuse": expanded = (group_name == "drill")` UNTOUCHED; tab name "Fuse" preserved per Specifics line 222

## Decisions Made

- **Atomic 7-file commit per CONTEXT D-15:** Cross-cutting wiring change committed in ONE commit. Sub-tasks (1, 2, 3) staged incrementally but final commit boundary is single. Rationale: bisect-clean — schema + loader + persistence + UI routing all change consistently in one revision.
- **`--no-verify` per parallel-executor protocol:** This worktree is Wave 2 sibling alongside Plan 03 (which modifies different files: `assets/physics-schema.json` + `assets/presets/*.json`). Pre-commit hooks may exercise tests that fail mid-strip (cut-only tests still on disk per Plan 05 schedule); `--no-verify` is appropriate per `<parallel_execution>` directive.
- **`WeaponUpgrade` description cleanup choice:** Plan said "drop examples or replace with DrillPickup-only (planner discretion)". Chose to DROP the example for `WeaponUpgrade` ("Offensive ability pickup.") because there is no surviving offensive pickup entity in the converter mapping. Chose to REPLACE for `MovementUpgrade` ("Movement ability pickup (e.g. DrillPickup).") because DrillPickup IS the surviving movement-related pickup.
- **`ITEM_FRAMES` doc-comment refresh:** The pre-strip comment said "Frame order: energy=0, missile=1, dash=2, shield=3, boost=4, shield_t2=5". Post-strip comment now reads "Frame order: energy=0, missile=1, drill=6 (frames 2-5 are orphan art per CONTEXT D-09)". This documents that frames 2-5 in `items.png` remain (deferred sprite cleanup per D-09) but no live code path references them.
- **"Fuse" tab name preserved:** Per CONTEXT Specifics line 222 + Discretion. Post-strip the tab routes only `drill` + `fusion` (rename to "Drill" deferred to Phase 33+).

## Deviations from Plan

None — plan executed exactly as written. The 7 file edits matched the plan's `<interfaces>` block precisely; all acceptance criteria from Tasks 1-3 passed on the first verification run; no architectural changes required; no auth gates encountered; no orphan symbols discovered; all cross-cutting invariants from CONTEXT and PATTERNS preserved.

## Issues Encountered

- **PreToolUse hook noise:** Several `READ-BEFORE-EDIT` hook reminders fired on edits to files that had already been read in the current session (input.py, save_manager.py, items.py, entity-schema.json, presets.py, panel.py, main.py). Edits succeeded despite the warnings — the hook is overly conservative. No re-reads were needed.
- **Pre-existing worktree modifications:** The worktree base (commit `490402c`) ships with many `M` and `D` entries for `assets/output/*`, `assets/output.ldtk`, `assets/sprites/*` that are unrelated to this plan. Avoided `git add .` / `git add -A`; instead staged the 7 plan files individually per task_commit_protocol. Pre-existing changes left untouched.
- **Worktree base required hard-reset:** Initial worktree HEAD was `17f312c` (Plan 01-02 wave merge head); plan prompt required base `490402c`. Hard-reset performed before any edits per `<worktree_branch_check>`.
- **No pytest run during execution:** Per CONTEXT D-21, full pytest is reserved for end of Plan 05. Per-task validation in this plan was: module imports (5 modules), JSON schema validation (`python -m json.tool`), save round-trip with stale-flag-leak guard, and boot smoke (`timeout 5 python main.py` rc=124).

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- **Plan 31.5-05 (tests + acceptance) ready (after Wave 2 merge):** All cross-cutting cut-ability wiring removed; the grep-checklist gate (CONTEXT D-18 Gate 1) can be authored and run. The 8 cut-only tests slated for deletion (`test_bubble_shield`, `test_charge_recoil`, `test_charge_shot`, `test_charge_shot_windup`, `test_dash`, `test_ram`, `test_slime_boost`, `test_slime_hold`, `test_tap_reposition`) and 6 mixed tests for update (`test_save_system`, `test_input`, `test_input_remap`, `test_fusion`, `test_schema`, `test_tuning`) can proceed without further blockers.
- **Plan 03 sibling merge (Wave 2):** Plan 03 modifies `assets/physics-schema.json` + 5 preset files — different files from this plan. After orchestrator merges both Wave 2 worktrees, the schema strip closes the dead-reference window for `tuning.DASH_*`/`tuning.RAM_*`/etc. (per CONTEXT D-25).
- **Phase 32 (fusion-manager-protocol-refactor) closer:** Cross-cutting cut-ability surface is now zero in src/, main.py, and assets/entity-schema.json. Remaining work before Phase 32 unblock: Plan 03 schema/preset commit (sibling Wave 2) + Plan 05 test cleanup + 4-gate proof-of-work (CONTEXT D-18).
- **Cross-Cutting Invariants verified preserved:** #6 (save format `version: 1`), #7 (`SaveManager.load()` body untouched), #8 (`presets.py` forward-compat skip), #9 (event-bus subscribers in `Game.__init__`), #11 (`was_tap` primitive in input.py), #13 (CRACKED_H tile rendering — entity-schema `cracked_h` definitions untouched).

## Verification Output

```
$ python -c "from src.core import input as im; assert 'dash' not in im._ACTION_MAP; assert hasattr(im, 'was_tap'); print(sorted(im._ACTION_MAP.keys()))"
['confirm', 'down', 'jump', 'left', 'pause', 'right', 'spit', 'up']
(8 actions, no dash, was_tap preserved)

$ python -m json.tool < assets/entity-schema.json > /dev/null && echo "entity-schema OK"
entity-schema OK

$ python -c "import json; d=json.load(open('assets/entity-schema.json')); ents=d['entities']; cut=('DashPickup','ShieldPickup','BoostPickup','ShieldT2'); [print(f'{e}: ABSENT' if e not in ents else f'{e}: STILL PRESENT') for e in cut]"
DashPickup: ABSENT
ShieldPickup: ABSENT
BoostPickup: ABSENT
ShieldT2: ABSENT
(11 entities remain: PlayerStart, Door, Snail, Bat, Boss, EnergyTank, MissileTank, SavePoint, OneWay, HiddenLoot, Map)

$ python -c "from src.ui.presets import FEEL_GROUPS; assert FEEL_GROUPS == {'movement','forgiving','wall','slime_follow','slime_juice','projectile','drill','fusion'}; assert len(FEEL_GROUPS) == 8; print('FEEL_GROUPS OK count=8')"
FEEL_GROUPS OK count=8

$ python -c "import src.core.input; import src.core.save_manager; import src.entities.items; import src.ui.presets; import src.ui.panel; print('All 5 modules OK')"
All 5 modules OK

$ python -c "<save round-trip with stale cut flags on player stub>"
Saved keys: ['has_drill', 'max_hp']
Save round-trip OK -- has_drill=True preserved; cut-flag keys absent; version=1
SaveManager.load() roundtrip OK -- D-23 body still works

$ git diff -- src/core/save_manager.py | grep -E '^[-+]' | grep -v '^---\|^+++' | head
-                "has_dash": getattr(player, "has_dash", False),
-                "has_shield": getattr(player, "has_shield", False),
-                "has_shield_t2": getattr(player, "has_shield_t2", False),
-                "has_boost": getattr(player, "has_boost", False),
(only deletions in save() body; load() byte-identical per D-23)

$ timeout 5 python main.py; echo "RC=$?"
RC=124  (timeout = clean boot, no crash, UI panel renders both Move and Fuse tabs without crash)

$ git show --stat HEAD | tail -10
 assets/entity-schema.json | 74 ++---------------------------------------------
 main.py                   |  8 -----
 src/core/input.py         |  1 -
 src/core/save_manager.py  |  4 ---
 src/entities/items.py     | 18 ++----------
 src/ui/panel.py           | 10 +++----
 src/ui/presets.py         |  4 +--
 7 files changed, 12 insertions(+), 107 deletions(-)
(exactly 7 files, atomic per CONTEXT D-15)
```

## Self-Check: PASSED

- FOUND: src/core/input.py (modified; _ACTION_MAP has 8 actions, was_tap preserved)
- FOUND: src/core/save_manager.py (modified; player block {max_hp, has_drill} only; load() body byte-identical)
- FOUND: src/entities/items.py (modified; 3 type-branches + 3 ITEM_FRAMES entries)
- FOUND: main.py (modified; LDtk loader stripped of 4 cut-pickup elifs; event-bus subscribers untouched)
- FOUND: assets/entity-schema.json (modified; 11 entities remain; valid JSON)
- FOUND: src/ui/presets.py (modified; FEEL_GROUPS has 8 groups; forward-compat skip preserved)
- FOUND: src/ui/panel.py (modified; FEEL_GROUPS mirror + TAB_DEFS stripped; "Fuse" name preserved; preset routing untouched)
- FOUND commit 5f40a22 (single atomic commit per CONTEXT D-15)
- VERIFIED: `python -c "import src.core.input; import src.core.save_manager; import src.entities.items; import src.ui.presets; import src.ui.panel"` exits 0
- VERIFIED: `python -m json.tool < assets/entity-schema.json` exits 0
- VERIFIED: `timeout 5 python main.py` returns rc=124 (clean boot)
- VERIFIED: Save round-trip preserves has_drill=True; cut-flag keys absent; version=1 stays
- VERIFIED: `SaveManager.load()` round-trips the saved dict identically (D-23 body untouched)
- VERIFIED: All Cross-Cutting Invariants 6, 7, 8, 9, 11, 13 preserved (per Plan 04 Verification section)
- VERIFIED: Commit `5f40a22` includes exactly 7 files (per `git show --stat HEAD`)
- VERIFIED: No file deletions in commit (only content edits; pre-existing worktree-base modifications to assets/output/* and assets/sprites/* untouched)

---
*Phase: 31.5-cut-ability-code-strip*
*Completed: 2026-04-26*
