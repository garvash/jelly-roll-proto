# Phase 31.5: Cut-Ability Code-Strip - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-25
**Phase:** 31.5-cut-ability-code-strip
**Areas discussed:** Strip surface, Plan structure, Verification beyond grep, Save load-side handling

---

## Strip Surface

### Schema cut

| Option | Description | Selected |
|--------|-------------|----------|
| Drop 4 groups + 2 stray keys | Delete entire `dash`, `slime_ram`, `charge_shot`, `boost` groups; drop `BOOST_CRACKED_V_COST` from `gates`; drop `HOLD_TAP_THRESHOLD` from `fusion`. | ✓ |
| Drop 4 groups only, leave stray keys | Risk: dead keys re-emerge in panel/preset round-trips. | |
| Drop 4 groups + retain `_deprecated` block | Conflicts with D-03 (no half-state). | |

**User's choice:** Drop 4 groups + 2 stray keys (Recommended).

### Hold cut

| Option | Description | Selected |
|--------|-------------|----------|
| Strip Hold fully | Remove `is_holding_position`/`hold_x`/`hold_y`/`HOLD_SCAN_RANGE` from slime.py + Hold tap-reposition input branches from player.py + delete test_slime_hold.py. | ✓ |
| Defer Hold | Risk: leaves hold guards in slime.update() with no setter. | |
| Strip Hold but keep was_tap left/right primitive | Scout shows nothing else uses tap-direction; degenerates to A. | |

**User's choice:** Strip Hold fully (Recommended).

### Pickups

| Option | Description | Selected |
|--------|-------------|----------|
| Full removal across code + entity-schema | Delete items.py branches + main.py LDtk loader + entity-schema.json definitions. LDtk has zero placements. | ✓ |
| Code-only strip, leave entity-schema entries | Risk: pml-to-ldtk converter could re-introduce; entity-schema becomes a contract liar. | |
| Degenerate pickups to no-op | Conflicts with D-03 (no half-state). | |

**User's choice:** Full removal across code + entity-schema (Recommended).

### Player.state strip

| Option | Description | Selected |
|--------|-------------|----------|
| Strip in 31.5 | Remove RAMMING/DASHING/BOOSTING/CHARGING_SHOT branches; leave IDLE/RUN/JUMP/FALL/DIVING/WALL_SLIDE. | ✓ |
| Strip activation methods only, leave state names | Risk: misleading dead code; defeats clean-base goal. | |
| Defer to Phase 32 | 32-CONTEXT D-15 calls cut-ability remnants in Phase 32 "bugs"; deferring forces 32 to inherit them. | |

**User's choice:** Strip in 31.5 (Recommended).

### Panel UI

| Option | Description | Selected |
|--------|-------------|----------|
| Drop cut groups from FEEL_GROUPS and tab routing | Update presets.py and panel.py TABS dict; Fuse keeps drill+fusion only. | ✓ |
| Drop from FEEL_GROUPS only, leave tab routing | Tab opens, routes empty; visible bug. | |
| Re-design panel tabs in this phase | Out of scope; defer to Phase 33+. | |

**User's choice:** Drop cut groups from FEEL_GROUPS and tab routing (Recommended).

### Projectile

| Option | Description | Selected |
|--------|-------------|----------|
| Remove charge-shot branches, keep spit projectile path | Surgical removal; keeps spit foundation for Phase 32 daze shot. | ✓ |
| Replace projectile.py with a clean spit-only rewrite | Scope creep; defer to 33 if intertwining is too deep. | |
| Leave projectile.py intact, strip only the caller | D-03 half-state risk — dead code in projectile.py. | |

**User's choice:** Remove charge-shot branches, keep spit projectile path (Recommended).

### Tests cut

| Option | Description | Selected |
|--------|-------------|----------|
| Delete 8 files | test_bubble_shield, test_charge_recoil, test_charge_shot, test_charge_shot_windup, test_dash, test_ram, test_slime_boost, test_slime_hold. | ✓ |
| Delete 7 files, keep test_slime_hold.py | Hold has no surviving infrastructure; degenerates to A. | |
| Mark deleted tests as @pytest.skip | Conflicts with D-03; skipped-test noise. | |

**User's choice:** Delete 8 files (Recommended).

### Tests update

| Option | Description | Selected |
|--------|-------------|----------|
| Update 6 known files | test_save_system, test_input, test_input_remap, test_fusion, test_schema, test_tuning. | ✓ |
| Update only files where pytest fails | Risk: silent erosion via getattr fallback. | |
| Aggressive sweep of any test importing cut symbols | Most thorough; scope creep. | |

**User's choice:** Update 6 known files (Recommended).

---

## Plan Structure

### Plan split

| Option | Description | Selected |
|--------|-------------|----------|
| 5 plans, wave-based | 01-player-strip / 02-slime-strip / 03-schema-presets / 04-input-save-pickups / 05-tests-and-acceptance. | ✓ |
| 3 plans, coarse | code / data / tests. | |
| 1 monolithic plan | Hardest to roll back. | |
| 7+ plans, ultra-granular | Planning overhead high; fragile inter-file dependencies. | |

**User's choice:** 5 plans, wave-based (Recommended).

### Commits

| Option | Description | Selected |
|--------|-------------|----------|
| One commit per plan | 5 phase commits total. Each rollback-safe. | ✓ |
| One commit per file | Plan 03 yields 6+ commits; Plan 05 noisy. | |
| Squash to single commit | Cleanest history; impossible to bisect. | |

**User's choice:** One commit per plan (Recommended).

### Worktrees

| Option | Description | Selected |
|--------|-------------|----------|
| Sequential, no worktrees | MEMORY worktree-regression risk + high file-coupling makes parallel branches unsafe. | ✓ |
| Wave 1 parallel (01+02) | Saves wall time; pulls in worktree-regression risk. | |
| All independent waves parallel | Maximum parallelism; high merge-conflict risk. | |

**User's choice:** Sequential, no worktrees (Recommended).

### Strip order within Plan 01

| Option | Description | Selected |
|--------|-------------|----------|
| Top-down by symbol | ram methods → boost → dash → shield → charge_shot → init flags. | ✓ |
| Bottom-up by call site | Slower; doesn't change final diff. | |
| Author-driven at execute time | Most flexible; least predictable diffs. | |

**User's choice:** Top-down by symbol (Recommended).

---

## Verification Beyond Grep

### Verify bar

| Option | Description | Selected |
|--------|-------------|----------|
| Grep + boot + smoke playthrough + pytest green | Four gates all required. | ✓ |
| Grep + boot only | Risk: silent runtime errors. | |
| Grep + pytest + automated smoke | Requires authoring new test. | |
| Add v1.3 vs post-strip parity test | Belongs in Phase 32. | |

**User's choice:** Grep + boot + smoke playthrough + pytest green (Recommended).

### Smoke format

| Option | Description | Selected |
|--------|-------------|----------|
| Human-driven, documented checklist | 31.5-SMOKE-CHECKLIST.md with 8 ticked actions; matches v2.0 phase precedent. | ✓ |
| Claude-runs-pyxel-via-MCP | Pyxel MCP may not exercise input-driven paths without play-and-capture script. | |
| Both | Belt-and-suspenders; more work. | |

**User's choice:** Human-driven, documented checklist (Recommended).

### Gate file

| Option | Description | Selected |
|--------|-------------|----------|
| 31.5-VERIFICATION.md + 31.5-SMOKE-CHECKLIST.md | gsd-verifier output + sibling human-runnable doc. | ✓ |
| Verification only, smoke checklist inline | One file, slightly longer. | |
| 31.5-EXIT-CRITERIA.md instead | Aliasing breaks downstream discovery. | |

**User's choice:** 31.5-VERIFICATION.md + 31.5-SMOKE-CHECKLIST.md (Recommended).

### Pytest

| Option | Description | Selected |
|--------|-------------|----------|
| Full pytest suite | Catches indirect regressions in untouched tests. | ✓ |
| Filtered: only re-run touched files | Misses indirect breakage. | |
| Full + clean state | Belt-and-suspenders; only useful with cached-import flakiness evidence. | |

**User's choice:** Full pytest suite must pass (Recommended).

---

## Save Load-Side Handling

### Load handling

| Option | Description | Selected |
|--------|-------------|----------|
| Leave load untouched | Phase 32 will reject all v1 saves anyway; gap-window behavior moot. | ✓ |
| Defensive pop in load() | Belt-and-suspenders; gets ripped out by Phase 32. | |
| Phase 31.5 also bumps save_version | Conflicts with D-05; collapses Phase 32 scope. | |

**User's choice:** Leave load untouched — Phase 32 rejects all v1 saves anyway (Recommended).

### Save WRITE

| Option | Description | Selected |
|--------|-------------|----------|
| Drop 4 fields, keep version=1 | Remove has_dash/has_shield/has_shield_t2/has_boost; keep has_drill. | ✓ |
| Drop 4 fields + add `stripped_at` | Invents schema field outside Phase 32 design. | |
| Refactor `has_*` namespace into `unlocked_abilities` list | Out of scope. | |

**User's choice:** Drop 4 fields, keep version=1 (Recommended).

### test_save_system update

| Option | Description | Selected |
|--------|-------------|----------|
| Drop cut-flag assertions, keep round-trip + persistence | Update round-trip to assert `{max_hp, has_drill}` only. | ✓ |
| Add regression test asserting cut-flags NOT in save | Test bloat; harmless. | |
| Defer to Phase 32 | Pytest goes red between phases; unacceptable. | |

**User's choice:** Drop cut-flag assertions, keep round-trip + persistence tests (Recommended).

---

## Emergent Areas

### Slot files (cleanup of stale on-disk presets)

| Option | Description | Selected |
|--------|-------------|----------|
| Rewrite all 5 preset files clean in Plan 03 | Same diff pattern across _v1.3-reference + slot_0..3. | ✓ |
| Leave slot files alone, rely on forward-compat skip | D-03 half-state in on-disk artifact. | |
| Rewrite slots 0-3 only, leave _v1.3-reference | Conflicts with D-03; 32-CONTEXT marks _v1.3 as editable. | |

**User's choice:** Rewrite all 5 preset files to clean state in Plan 03 (Recommended).

### I-frames

| Option | Description | Selected |
|--------|-------------|----------|
| No special action — cleanup is automatic | 3 cut-ability invuln_timer setters live inside deleted methods; exit naturally. | ✓ |
| Audit invuln_timer in test files post-strip | Light belt-and-suspenders; covered by full pytest gate. | |

**User's choice:** No special action — cleanup is automatic (Recommended).

---

## Claude's Discretion

- Exact ordering within each plan beyond D-17's player.py top-down sequence.
- Whether to run interim smoke between plans 01-04 or only at end of Plan 05.
- Whether 31.5-SMOKE-CHECKLIST.md lives at phase-dir root or under a `gates/` subdirectory.
- Specific commit-message wording for each of the 5 plan commits.
- Whether `test_input_remap.py` line 70's `has_dash=False` kwarg gets dropped or left as no-op.
- Whether the `Fuse` panel tab gets renamed (defer to Phase 33+).

## Deferred Ideas

- Sprite frame cleanup in spritesheet PNGs (orphan art).
- CRACKED_H gates dead under single-fusion (level design follow-up).
- Save format `save_version` bump (Phase 32 D-21).
- Save load-side hard-fail rejection (Phase 32 D-24).
- `Fuse` panel tab rename (Phase 33+).
- Pogo bounce code (Phase 32, new code).
- Reintroduction of any cut ability (post-prototype).
- V-button rebinding (post-prototype).
- `tuning.DASH_IFRAMES` dead-reference window (closes at Plan 03; awareness only).
- Test fixture `make_player(has_dash=False)` kwargs (Plan 05 audits conftest.py).
- Aggressive test sweep beyond 6 known files (full pytest gate catches indirect).
- Pyxel MCP automated smoke (Phase 35+ if standardized verification wanted).
