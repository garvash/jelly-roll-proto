---
phase: 31.5-cut-ability-code-strip
plan: 03
type: execute
wave: 2
depends_on: [31.5-01, 31.5-02]
files_modified:
  - assets/physics-schema.json
  - assets/presets/_v1.3-reference.json
  - assets/presets/slot_0.json
  - assets/presets/slot_1.json
  - assets/presets/slot_2.json
  - assets/presets/slot_3.json
autonomous: true
requirements: []
threat_model_severity: low
nyquist_compliant: true

must_haves:
  truths:
    - "physics-schema.json no longer contains `dash`, `slime_ram`, `charge_shot`, `boost` groups"
    - "physics-schema.json `gates` group keeps `DRILL_CRACKED_V_COST`; `BOOST_CRACKED_V_COST` is removed"
    - "physics-schema.json `fusion` group keeps RECALL_SPEED, RECALL_OVERLAP_DIST, MANA_SHIELD_COST, SLIME_DISSIPATE_COOLDOWN, RECALL_TRAIL_COLOR, SPIT_HOLD_THRESHOLD; `HOLD_TAP_THRESHOLD` is removed"
    - "All 5 preset files have 35 surviving keys each (down from 54)"
    - "All 6 JSON files validate via `python -m json.tool`"
    - "`tuning.DASH_SPEED` raises AttributeError post-strip (cut keys absent from `_flat_index`)"
    - "`tuning.DRILL_SPEED > 0` (drill group preserved)"
    - "Game boots room 0 with default preset"
    - "Schema + 5 presets land in ONE atomic commit (RESEARCH Pattern 1, prevents bisect-window key disagreement)"
  artifacts:
    - path: "assets/physics-schema.json"
      provides: "Tuning schema without cut groups"
      contains: "drill, fusion, gates"
    - path: "assets/presets/_v1.3-reference.json"
      provides: "v1.3-baseline preset, cut keys removed"
    - path: "assets/presets/slot_0.json"
      provides: "auto preset, cut keys removed"
    - path: "assets/presets/slot_1.json"
      provides: "v2.0-default preset, cut keys removed"
    - path: "assets/presets/slot_2.json"
      provides: "tight preset, cut keys removed"
    - path: "assets/presets/slot_3.json"
      provides: "floaty preset, cut keys removed"
  key_links:
    - from: "tuning._flat_index"
      to: "physics-schema.json post-strip"
      via: "schema load builds attribute index"
      pattern: "(DASH_|RAM_|CHARGE_|BOOST_)"
---

<objective>
Strip cut-ability tuning groups (`dash`, `slime_ram`, `charge_shot`, `boost`) from `assets/physics-schema.json`, drop the 2 stray cut keys (`BOOST_CRACKED_V_COST` from `gates`, `HOLD_TAP_THRESHOLD` from `fusion`), and rewrite all 5 preset JSON files clean. **All edits land in ONE commit** (atomicity per RESEARCH Pattern 1: schema + presets share a single coherent meaning; splitting introduces a bisect window where `tuning.set_value` raises KeyError mid-game).

Purpose: After Plan 01-02 deleted the code that read these keys, this plan removes the keys themselves so `tuning.X` access on cut symbols raises `AttributeError` and the `_flat_index` walk in `presets.py` no longer emits cut keys.

Output: 6 JSON files (1 schema + 5 presets), all valid, all cut-keys-free; game boots with default preset; one atomic commit per CONTEXT D-15.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/31.5-cut-ability-code-strip/31.5-CONTEXT.md
@.planning/phases/31.5-cut-ability-code-strip/31.5-PATTERNS.md
@.planning/phases/31.5-cut-ability-code-strip/31.5-RESEARCH.md
@.planning/phases/31.5-cut-ability-code-strip/31.5-VALIDATION.md
@.planning/phases/31.5-cut-ability-code-strip/31.5-01-SUMMARY.md
@.planning/phases/31.5-cut-ability-code-strip/31.5-02-SUMMARY.md
@assets/physics-schema.json
@assets/presets/_v1.3-reference.json
@assets/presets/slot_0.json
@assets/presets/slot_1.json
@assets/presets/slot_2.json
@assets/presets/slot_3.json

<interfaces>
<!-- Cut footprint per file (PATTERNS verified live 2026-04-25) -->

physics-schema.json:
- Lines 95-100: `dash` group (DELETE entirely) — DASH_SPEED, DASH_DURATION, DASH_IFRAMES, DASH_COOLDOWN
- Lines 110-115: `slime_ram` group (DELETE entirely) — RAM_SPEED, RAM_DIAGONAL_FACTOR, RAM_BLOCK_COST, RAM_INVINCIBLE
- Lines 116-122: `charge_shot` group (DELETE entirely) — CHARGE_SHOT_SPEED, CHARGE_SHOT_SIZE, CHARGE_SHOT_DAMAGE, CHARGE_RECOIL_FORCE, CHARGE_WINDUP_DURATION
- Lines 123-129: `boost` group (DELETE entirely) — BOOST_FORCE, BOOST_JUICE_COST, BOOST_RECOMMIT_WINDOW, BOOST_DOWNWARD_DAMAGE_W, BOOST_DOWNWARD_DAMAGE_H
- Line 132: `gates.BOOST_CRACKED_V_COST` (DELETE single key — group keeps `DRILL_CRACKED_V_COST`)
- Line 108: `fusion.HOLD_TAP_THRESHOLD` (DELETE single key — group keeps RECALL_SPEED, RECALL_OVERLAP_DIST, MANA_SHIELD_COST, SLIME_DISSIPATE_COOLDOWN, RECALL_TRAIL_COLOR, SPIT_HOLD_THRESHOLD)

Each preset file (`_v1.3-reference.json`, `slot_0.json`, `slot_1.json`, `slot_2.json`, `slot_3.json`):
- DELETE 19 keys from `values` block:
  - DASH_SPEED, DASH_DURATION, DASH_IFRAMES, DASH_COOLDOWN (4 dash)
  - RAM_SPEED, RAM_DIAGONAL_FACTOR, RAM_BLOCK_COST, RAM_INVINCIBLE (4 ram)
  - CHARGE_SHOT_SPEED, CHARGE_SHOT_SIZE, CHARGE_SHOT_DAMAGE, CHARGE_RECOIL_FORCE, CHARGE_WINDUP_DURATION (5 charge-shot)
  - BOOST_FORCE, BOOST_JUICE_COST, BOOST_RECOMMIT_WINDOW, BOOST_DOWNWARD_DAMAGE_W, BOOST_DOWNWARD_DAMAGE_H (5 boost)
  - HOLD_TAP_THRESHOLD (1 hold)
- KEEP 35 surviving keys (all movement, forgiving, wall, slime_follow, slime_juice, projectile, drill, fusion-survivors)
- KEEP JSON metadata wrapper (`version`, `schema_version`, `slot`, `alias`, `timestamp`, `description` if present)

Surviving group analog (post-strip shape — physics-schema.json):
```json
"drill": {
  "DRILL_SPEED": 2.0,
  "DRILL_DRIFT_SPEED": 0.5,
  "DRILL_IMPACT_COST": 20.0,
  "DRILL_ACTIVATION_COST": 5.0,
  "DRILL_BLOCK_REFUND": 15.0
},
"fusion": {
  "RECALL_SPEED": 4.0,
  "RECALL_OVERLAP_DIST": 4,
  "MANA_SHIELD_COST": 20.0,
  "SLIME_DISSIPATE_COOLDOWN": 240,
  "RECALL_TRAIL_COLOR": 11,
  "SPIT_HOLD_THRESHOLD": 16
},
"gates": {
  "DRILL_CRACKED_V_COST": 20.0
}
```
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Asset JSON load → tuning module | Schema and presets are loaded at startup. Malformed JSON crashes the game. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31.5-03 | Tampering | physics-schema.json + 5 preset JSON files | mitigate | Validate every JSON file post-edit with `python -m json.tool` (RESEARCH Risk Surface 2 — trailing-comma bugs are the #1 risk class for hand-edited JSON). |
| T-31.5-04 | Information Disclosure | n/a | accept | No PII or secrets in tuning data. |

**Severity:** low. The only adjacent concern is **input validation (V5)** for the JSON files being rewritten — mitigated by `python -m json.tool` after every edit.
</threat_model>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Strip 4 cut groups + 2 stray keys from physics-schema.json</name>
  <files>assets/physics-schema.json</files>
  <read_first>
    - assets/physics-schema.json (full file — read to confirm live group order and trailing-comma positions)
    - .planning/phases/31.5-cut-ability-code-strip/31.5-PATTERNS.md § "assets/physics-schema.json"
    - .planning/phases/31.5-cut-ability-code-strip/31.5-CONTEXT.md D-01, D-02, D-03
    - .planning/phases/31.5-cut-ability-code-strip/31.5-RESEARCH.md § Risk Surface 2 (trailing-comma bugs)
  </read_first>
  <action>
    Edit `assets/physics-schema.json`:

    1. **Delete `dash` group entirely** (~lines 95-100). Find via grep `"dash":` (with the trailing colon to disambiguate from a "dash" key if any).

    2. **Delete `slime_ram` group entirely** (~lines 110-115).

    3. **Delete `charge_shot` group entirely** (~lines 116-122).

    4. **Delete `boost` group entirely** (~lines 123-129).

    5. **Delete `BOOST_CRACKED_V_COST` key from `gates` group** (~line 132). Result: `gates` keeps only `DRILL_CRACKED_V_COST`. **Trailing-comma rule:** since `BOOST_CRACKED_V_COST` was the LAST key, removing it requires `DRILL_CRACKED_V_COST` to lose its trailing comma. Per RESEARCH Risk Surface 2: this is the #1 JSON failure mode.

    6. **Delete `HOLD_TAP_THRESHOLD` key from `fusion` group** (~line 108). Surviving keys (per PATTERNS): RECALL_SPEED, RECALL_OVERLAP_DIST, MANA_SHIELD_COST, SLIME_DISSIPATE_COOLDOWN, RECALL_TRAIL_COLOR, SPIT_HOLD_THRESHOLD. **Trailing-comma rule:** ensure the LAST surviving key in `fusion` has no trailing comma.

    **Preserve top-level group order** (per PATTERNS): tile/display/sprite/hazards/movement/forgiving/wall/slime_follow/slime_juice/projectile/drill/juice_effects/health/fusion/gates/save/death/save_point.

    **DO NOT TOUCH:**
    - `drill` group — KEEP (DRILL_SPEED, DRILL_DRIFT_SPEED, DRILL_IMPACT_COST, DRILL_ACTIVATION_COST, DRILL_BLOCK_REFUND)
    - All `movement`, `forgiving`, `wall`, `slime_follow`, `slime_juice`, `projectile`, `juice_effects`, `health`, `save`, `death`, `save_point` groups — KEEP

    Surviving-group analog (post-strip):
    ```json
    "drill": {
      "DRILL_SPEED": 2.0,
      "DRILL_DRIFT_SPEED": 0.5,
      "DRILL_IMPACT_COST": 20.0,
      "DRILL_ACTIVATION_COST": 5.0,
      "DRILL_BLOCK_REFUND": 15.0
    },
    ```
  </action>
  <verify>
    <automated>python -m json.tool < assets/physics-schema.json > /dev/null && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `python -m json.tool < assets/physics-schema.json` exits 0 (valid JSON; catches trailing-comma bugs)
    - `grep -nE '^\s*"(dash|slime_ram|charge_shot|boost)":' assets/physics-schema.json` returns 0 matches
    - `grep -n '"BOOST_CRACKED_V_COST"' assets/physics-schema.json` returns 0 matches
    - `grep -n '"HOLD_TAP_THRESHOLD"' assets/physics-schema.json` returns 0 matches
    - `grep -n '"drill":' assets/physics-schema.json` returns 1 match (drill group preserved)
    - `grep -n '"DRILL_CRACKED_V_COST"' assets/physics-schema.json` returns 1 match (surviving gate key)
    - `grep -n '"MANA_SHIELD_COST"\|"SPIT_HOLD_THRESHOLD"' assets/physics-schema.json` returns 2 matches (surviving fusion keys)
  </acceptance_criteria>
  <done>physics-schema.json: 4 cut groups + 2 stray keys removed; all surviving groups preserved; JSON validates.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Rewrite all 5 preset files clean (cut keys removed)</name>
  <files>assets/presets/_v1.3-reference.json, assets/presets/slot_0.json, assets/presets/slot_1.json, assets/presets/slot_2.json, assets/presets/slot_3.json</files>
  <read_first>
    - assets/presets/_v1.3-reference.json (full file — establish surviving-key order)
    - assets/presets/slot_0.json, slot_1.json, slot_2.json, slot_3.json (full files — each may have its own value distribution)
    - .planning/phases/31.5-cut-ability-code-strip/31.5-PATTERNS.md § "assets/presets/*.json" (35 surviving keys enumerated)
    - .planning/phases/31.5-cut-ability-code-strip/31.5-CONTEXT.md D-05 (rewrite all 5 clean; metadata stays)
  </read_first>
  <action>
    For each of the 5 preset files (`_v1.3-reference.json`, `slot_0.json`, `slot_1.json`, `slot_2.json`, `slot_3.json`):

    1. **Delete 19 keys from the `values` block:**
       - DASH_SPEED, DASH_DURATION, DASH_IFRAMES, DASH_COOLDOWN (4 dash keys)
       - RAM_SPEED, RAM_DIAGONAL_FACTOR, RAM_BLOCK_COST, RAM_INVINCIBLE (4 ram keys)
       - CHARGE_SHOT_SPEED, CHARGE_SHOT_SIZE, CHARGE_SHOT_DAMAGE, CHARGE_RECOIL_FORCE, CHARGE_WINDUP_DURATION (5 charge-shot keys)
       - BOOST_FORCE, BOOST_JUICE_COST, BOOST_RECOMMIT_WINDOW, BOOST_DOWNWARD_DAMAGE_W, BOOST_DOWNWARD_DAMAGE_H (5 boost keys)
       - HOLD_TAP_THRESHOLD (1 hold key)

    2. **Preserve 35 surviving keys** (per PATTERNS enumeration):
       - 8 movement: WALK_ACCEL, WALK_FRICTION, MAX_WALK_SPEED, GRAVITY, MAX_FALL_SPEED, JUMP_FORCE, VARIABLE_JUMP_REDUCTION, FALLING_GRAVITY_MULTIPLIER
       - 2 forgiving: COYOTE_TIME, JUMP_BUFFER
       - 3 wall: WALL_SLIDE_FRICTION, WALL_JUMP_X_IMPULSE, WALL_JUMP_Y_FORCE
       - 4 slime_follow: SLIME_FOLLOW_DELAY, SLIME_MAX_DIST, SLIME_REFORM_DIST, SLIME_LERP_FACTOR
       - 4 slime_juice: JUICE_MAX, JUICE_REGEN_RATE, JUICE_MIN_SCALE, SLIME_SPIT_COST
       - 3 projectile: PROJECTILE_SPEED, SPIT_AIM_RANGE, BOSS_ROCK_SPEED
       - 5 drill: DRILL_SPEED, DRILL_DRIFT_SPEED, DRILL_IMPACT_COST, DRILL_ACTIVATION_COST, DRILL_BLOCK_REFUND
       - 6 fusion: RECALL_SPEED, RECALL_OVERLAP_DIST, MANA_SHIELD_COST, SLIME_DISSIPATE_COOLDOWN, RECALL_TRAIL_COLOR, SPIT_HOLD_THRESHOLD

    3. **Preserve JSON metadata wrapper:** `version`, `schema_version`, `slot`, `alias`, `timestamp`, `description` (if present in `_v1.3-reference.json`). Each file retains its distinct `alias`: v1.3-baseline, auto, v2.0-default, tight, floaty.

    4. **Trailing-comma rule:** the LAST surviving key in `values` must have no trailing comma. Run `python -m json.tool < <file>` after each rewrite.

    Per CONTEXT D-05: `_v1.3-reference.json` is editable here — the v1.3 *behavioral* baseline lives in `FUSION-DESIGN.md` Drill-Dive Contract, not in this preset file. The presets.py forward-compat skip at lines 84-88 (`try: tuning.set_value(key, val); except KeyError: pass`) STAYS — backstop for externally-edited presets.

    Surviving-key shape per file: 35 keys total (down from 54).
  </action>
  <verify>
    <automated>for f in assets/presets/_v1.3-reference.json assets/presets/slot_0.json assets/presets/slot_1.json assets/presets/slot_2.json assets/presets/slot_3.json; do python -m json.tool < "$f" > /dev/null && echo "OK: $f" || { echo "FAIL: $f"; exit 1; }; done</automated>
  </verify>
  <acceptance_criteria>
    - All 5 preset files validate via `python -m json.tool` (exit 0)
    - For each file: `grep -cE '"(DASH_|RAM_|CHARGE_|BOOST_|HOLD_TAP_THRESHOLD)' <file>` returns 0 (no cut keys)
    - For each file: `grep -cE '"(WALK_ACCEL|GRAVITY|JUMP_FORCE|DRILL_SPEED|MANA_SHIELD_COST|SPIT_HOLD_THRESHOLD)"' <file>` returns 6 (key surviving keys present)
    - For each file: `python -c "import json; d = json.load(open('<file>')); assert len(d.get('values', {})) == 35, f'Expected 35 values keys, got {len(d.get(\"values\", {}))}'"` exits 0
    - JSON metadata preserved: `python -c "import json; d = json.load(open('<file>')); assert 'version' in d and 'alias' in d"` exits 0
  </acceptance_criteria>
  <done>5 preset files rewritten clean; 35 surviving keys per file; JSON valid; metadata preserved.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Boot smoke + tuning attribute verification + atomic commit</name>
  <files>assets/physics-schema.json, assets/presets/_v1.3-reference.json, assets/presets/slot_0.json, assets/presets/slot_1.json, assets/presets/slot_2.json, assets/presets/slot_3.json</files>
  <read_first>
    - .planning/phases/31.5-cut-ability-code-strip/31.5-VALIDATION.md § Per-Task Verification Map (Plan 03 boundary command)
    - .planning/phases/31.5-cut-ability-code-strip/31.5-RESEARCH.md § Code Examples — Example 3 (Plan 03 atomicity commit)
  </read_first>
  <action>
    Run end-to-end verification BEFORE committing:

    1. **JSON validation gate** (all 6 files):
       ```
       for f in assets/physics-schema.json assets/presets/*.json; do
         python -m json.tool < "$f" > /dev/null && echo "OK: $f" || { echo "FAIL: $f"; exit 1; }
       done
       ```

    2. **Tuning attribute gate** (cut keys absent, drill present):
       ```
       python -c "from src.core import tuning; assert not hasattr(tuning, 'DASH_SPEED'), 'DASH_SPEED still in tuning'; assert not hasattr(tuning, 'RAM_SPEED'), 'RAM_SPEED still in tuning'; assert not hasattr(tuning, 'CHARGE_SHOT_DAMAGE'), 'CHARGE_SHOT_DAMAGE still in tuning'; assert not hasattr(tuning, 'BOOST_FORCE'), 'BOOST_FORCE still in tuning'; assert not hasattr(tuning, 'HOLD_TAP_THRESHOLD'), 'HOLD_TAP_THRESHOLD still in tuning'; assert not hasattr(tuning, 'BOOST_CRACKED_V_COST'), 'BOOST_CRACKED_V_COST still in tuning'; assert tuning.DRILL_SPEED > 0, 'DRILL_SPEED must remain'; assert tuning.MANA_SHIELD_COST > 0, 'MANA_SHIELD_COST must remain'; print('OK')"
       ```

    3. **Boot smoke gate** (game loads default preset, room 0 renders):
       ```
       timeout 30 python main.py
       ```
       Expected rc=124 (timeout = clean boot to title; not a crash).

    4. **Atomic commit** (per CONTEXT D-15 + RESEARCH Pattern 1):
       ```
       git add assets/physics-schema.json \
               assets/presets/_v1.3-reference.json \
               assets/presets/slot_0.json \
               assets/presets/slot_1.json \
               assets/presets/slot_2.json \
               assets/presets/slot_3.json
       git commit -m "$(cat <<'EOF'
       feat(31.5): strip cut-ability tuning groups + preset values

       - Delete dash, slime_ram, charge_shot, boost groups from physics-schema.json
       - Drop BOOST_CRACKED_V_COST from gates (D-02)
       - Drop HOLD_TAP_THRESHOLD from fusion (D-03)
       - Rewrite all 5 preset files: 35 keys per preset (down from 54)

       Per CONTEXT D-05, D-15. One coherent data-shape change in one commit
       to avoid bisect-window key disagreement (RESEARCH Pattern 1).
       EOF
       )"
       ```

    **Critical: ONE commit.** Do NOT split into "schema commit + preset commits" — the bisect window where schema is updated but presets still contain cut keys would cause `tuning.set_value` to either raise KeyError mid-game or silently swallow it via the forward-compat skip at presets.py:84-88, depending on the order. Atomicity prevents this.
  </action>
  <verify>
    <automated>python -c "from src.core import tuning; assert not hasattr(tuning, 'DASH_SPEED'); assert not hasattr(tuning, 'BOOST_CRACKED_V_COST'); assert not hasattr(tuning, 'HOLD_TAP_THRESHOLD'); assert tuning.DRILL_SPEED > 0; assert tuning.MANA_SHIELD_COST > 0; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "from src.core import tuning; ..."` (the assertions above) exits 0
    - `timeout 30 python main.py` returns rc=124 (boot to title without crash)
    - `git log --oneline -1` shows ONE commit covering all 6 JSON files (not 6 separate commits)
    - `git show --stat HEAD` lists exactly 6 files: physics-schema.json + 5 preset files
    - JSON validation gate (all 6 files) returns OK on every line
  </acceptance_criteria>
  <done>Schema + 5 presets cleanly stripped; tuning attributes confirm cut keys absent + drill keys present; game boots; ONE atomic commit per CONTEXT D-15.</done>
</task>

</tasks>

<verification>
After Plan 03 commit:

1. JSON validation: all 6 files pass `python -m json.tool`
2. Tuning attribute access: `hasattr(tuning, 'DASH_SPEED')` is False; `tuning.DRILL_SPEED > 0` is True; `tuning.MANA_SHIELD_COST > 0` is True
3. `timeout 30 python main.py` returns rc=124
4. Single atomic commit per RESEARCH Pattern 1 (Plan 03 atomicity)
5. PATTERNS Cross-Cutting Invariants:
   - #6 (Save format `version: 1`) — N/A this plan
   - Drill group + DRILL_CRACKED_V_COST (#1) — preserved
   - Mana-shield, recall-related fusion keys (#5, #4) — preserved
</verification>

<success_criteria>
- All 6 JSON files validate
- Cut keys absent from `tuning` module
- Drill + mana-shield + spit keys present in `tuning` module
- Game boots with default preset
- ONE atomic commit per CONTEXT D-15
</success_criteria>

<output>
After completion, create `.planning/phases/31.5-cut-ability-code-strip/31.5-03-SUMMARY.md` with:
- Schema groups removed
- Stray keys removed (BOOST_CRACKED_V_COST, HOLD_TAP_THRESHOLD)
- Preset key count: 35 per file (down from 54)
- JSON validation result (all 6 OK)
- Tuning attribute gate result
- Boot smoke result
- Commit SHA (single)
</output>
