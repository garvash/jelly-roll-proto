---
phase: 31.5-cut-ability-code-strip
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/entities/player.py
  - src/entities/projectile.py
autonomous: true
requirements: []
threat_model_severity: low
nyquist_compliant: true

must_haves:
  truths:
    - "src/entities/player.py imports cleanly after each sub-step (no half-stripped NameError)"
    - "Game boots room 0 after Plan 01 commit"
    - "No `RAMMING`, `DASHING`, `BOOSTING`, `CHARGING_SHOT` state strings remain in src/entities/player.py"
    - "No `start_ram`, `apply_ram_physics`, `end_ram`, `start_boost`, `update_boost`, `end_boost`, `start_dash`, `apply_dash_physics`, `update_shield`, `update_charge_shot`, `_tick_charge_shot_windup`, `fire_charge_shot`, `draw_shield` symbols remain in src/entities/player.py"
    - "No `ChargeProjectile` class remains in src/entities/projectile.py"
    - "Drill (`apply_diving_physics`, `start_dive` activation, drill block-break, drill exit) is preserved unchanged"
    - "Mana-shield damage path at player.py:188-204 is preserved (separate from bubble shield)"
    - "Mid-drill jump-cancel block at player.py:466-471 is preserved (Phase 32 deletes it, NOT 31.5)"
    - "fuse / unfuse / fuse_start / fuse_end remain intact (Phase 32 owns refactor)"
    - "Spit projectile (`Projectile` class in projectile.py:5-49) is preserved"
  artifacts:
    - path: "src/entities/player.py"
      provides: "Player entity with cut abilities removed; surviving state set IDLE/RUN/JUMP/FALL/DIVING/WALL_SLIDE"
      contains: "apply_diving_physics, fuse, unfuse, has_drill"
    - path: "src/entities/projectile.py"
      provides: "Surviving Projectile class only"
      contains: "class Projectile"
  key_links:
    - from: "src/entities/player.py"
      to: "tuning.DRILL_SPEED / DRILL_DRIFT_SPEED"
      via: "use-site reads inside apply_diving_physics"
      pattern: "tuning\\.DRILL_"
    - from: "src/entities/player.py state dispatch"
      to: "DIVING state branch only"
      via: "if self.state == 'DIVING'"
      pattern: "self\\.state == \"DIVING\""
---

<objective>
Strip cut abilities (ram, boost, dash, bubble shield, charge shot) from `src/entities/player.py` and the `ChargeProjectile` class from `src/entities/projectile.py`, in the strict top-down sub-step order locked by CONTEXT D-17. Preserve drill, spit, fuse/unfuse, mana-shield damage path, and the mid-drill jump-cancel block (Phase 32's job).

Purpose: Give Phase 32's fusion refactor a clean base. Phase 31.5 enables FUS-04/05/07 by shrinking the surface area.

Output: `src/entities/player.py` and `src/entities/projectile.py` with cut symbols removed; game boots room 0; surviving state set is `IDLE / RUN / JUMP / FALL / DIVING / WALL_SLIDE`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/FUSION-DESIGN.md
@.planning/phases/31.5-cut-ability-code-strip/31.5-CONTEXT.md
@.planning/phases/31.5-cut-ability-code-strip/31.5-PATTERNS.md
@.planning/phases/31.5-cut-ability-code-strip/31.5-RESEARCH.md
@.planning/phases/31.5-cut-ability-code-strip/31.5-VALIDATION.md
@src/entities/player.py
@src/entities/projectile.py

<interfaces>
<!-- Surviving symbols Plan 01 must NOT touch (PATTERNS Cross-Cutting Invariants) -->

Drill (KEEP):
- player.py:451-457 — drill activation `if input_manager.btn("down") and self.has_drill ...`
- player.py:661-674 — `apply_diving_physics(self, slime)` method
- player.py:161-163 — drill state-dispatch elif `if self.state == "DIVING": self.apply_diving_physics(slime); self.move_and_collide(slime)`
- player.py:770-790 — drill block-break inside `move_and_collide`
- player.py:800-806 — drill exit conditions + dissipate handoff
- player.py:466-471 — **mid-drill jump-cancel block (Phase 32 deletes this, NOT 31.5)**

Mana shield (KEEP):
- player.py:188-204 — `if self.is_fused and slime and slime.juice > 0: slime.consume(tuning.MANA_SHIELD_COST)` damage path

Fusion lifecycle (KEEP):
- player.py:91-99 — `def fuse(self, slime)` + `event_bus.emit("fuse_start", ...)`
- player.py:101-112 — `def unfuse(self, slime, dissipate=False)` + `event_bus.emit("fuse_end", ...)`

Spit (KEEP):
- player.py:344-414 — spit input branch
- projectile.py:5-49 — `class Projectile`

Surviving state-tuple after sub-step 6:
```python
def update_state(self):
    if self.state in ("DIVING",):  # was: ("DIVING", "DASHING", "RAMMING", "BOOSTING", "CHARGING_SHOT")
        return
    ...
```
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (n/a) | Single-player game prototype; no network surface, no auth, no untrusted input, no PII, no secrets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31.5-01 | Tampering | n/a | accept | Internal code-strip; ASVS V2-V6 do not apply. RESEARCH § Security Domain confirms low severity. |

**Severity:** low. Phase 31.5 has no security surface; this block is recorded for completeness per `security_enforcement` policy.
</threat_model>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Sub-step 1 — Strip Ram (methods + dispatch + V-fused input + block-break)</name>
  <files>src/entities/player.py</files>
  <read_first>
    - src/entities/player.py (lines 110-180, 430-440, 715-745) — read the file BEFORE editing to confirm live line ranges
    - .planning/phases/31.5-cut-ability-code-strip/31.5-PATTERNS.md § "src/entities/player.py" sub-step 1/1a/1b/1c table
    - .planning/phases/31.5-cut-ability-code-strip/31.5-CONTEXT.md D-17 (sub-step ordering) + D-10/D-11 (state-name strip)
    - .planning/phases/31.5-cut-ability-code-strip/31.5-RESEARCH.md § Risk Surface 8 (Plan 01 do-not-touch line 466-471)
  </read_first>
  <action>
    Delete from `src/entities/player.py`, in this order:

    1. **Ram methods (lines 114-143):** Delete `start_ram` (~lines 114-128), `apply_ram_physics` (~lines 130-138), `end_ram` (~lines 140-143). All three methods are removed entirely. Their `tuning.RAM_*` and `tuning.DASH_IFRAMES` reads exit with the methods (PATTERNS Shared Patterns: tuning at use-site).

    2. **Ram dispatch elif (lines 164-166):** Delete the elif block:
       ```
       elif self.state == "RAMMING":
           self.apply_ram_physics()
           self.move_and_collide(slime)
       ```

    3. **Ram input branch — V fused (lines 433-437):** Delete the `if input_manager.btnp("dash") ... if self.is_fused: self.start_ram(slime)` block. Note: Plan 04 strips `"dash"` from `_ACTION_MAP`, but the input branch in player.py goes here.

    4. **Ram block-break in `move_and_collide` (lines 717-740):** Delete the `if self.state == "RAMMING" and slime: ... INTGRID_CRACKED_H ... self.end_ram(slime)` block.

    **DO NOT TOUCH** (per PATTERNS Cross-Cutting Invariants + Risk Surface 8):
    - Lines 161-163 (drill state-dispatch elif `if self.state == "DIVING": ...`) — KEEP
    - Lines 451-457 (drill activation `if input_manager.btn("down") and self.has_drill ...`) — KEEP
    - Lines 466-471 (mid-drill jump-cancel block) — KEEP (Phase 32 deletes this, NOT 31.5)
    - Lines 770-790 (drill block-break inside `move_and_collide`) — KEEP
    - Lines 800-806 (drill exit conditions) — KEEP
    - Lines 188-204 (mana-shield damage path) — KEEP

    Re-verify line numbers against the live file before each cut — line numbers shift as deletions progress within this task. Use grep on each symbol name (`def start_ram`, `RAMMING`, `INTGRID_CRACKED_H`) to locate exact ranges.

    Surviving analog (the shape Plan 01 must preserve): `apply_diving_physics` at player.py:661-674 + drill activation at player.py:451-457 + drill state-dispatch elif at player.py:161-163.
  </action>
  <verify>
    <automated>python -c "import src.entities.player; import src.entities.slime"</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import src.entities.player"` exits 0 (no NameError, AttributeError, or SyntaxError)
    - `grep -nE 'def (start_ram|apply_ram_physics|end_ram)\b' src/entities/player.py` returns 0 matches
    - `grep -n '"RAMMING"' src/entities/player.py` returns 0 matches
    - `grep -n 'INTGRID_CRACKED_H' src/entities/player.py` returns 0 matches
    - `grep -n 'def apply_diving_physics' src/entities/player.py` returns 1 match (drill preserved)
    - `grep -nC1 'self.state == "DIVING":' src/entities/player.py` returns the cancel block near line 466 area (Risk 8 invariant — the mid-drill cancel block is STILL PRESENT)
  </acceptance_criteria>
  <done>Ram methods, dispatch, V-fused input branch, and CRACKED_H block-break all removed; drill paths intact; module imports cleanly.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Sub-step 2 — Strip Boost (methods + dispatch + jump-buffer guards + SPACE input + CRACKED_V branch)</name>
  <files>src/entities/player.py</files>
  <read_first>
    - src/entities/player.py (re-read post sub-step 1 to confirm shifted line numbers; sections of interest: 170-180, 250-260, 455-470, 520-530, 550-610, 580-605, 805-830)
    - .planning/phases/31.5-cut-ability-code-strip/31.5-PATTERNS.md § "src/entities/player.py" sub-steps 2/2a/2b/2c/2d
    - .planning/phases/31.5-cut-ability-code-strip/31.5-CONTEXT.md D-17 step 2
    - .planning/phases/31.5-cut-ability-code-strip/31.5-RESEARCH.md § Sequence Verification — Sub-step 2
  </read_first>
  <action>
    Delete from `src/entities/player.py` (line numbers from PATTERNS — re-verify before each cut):

    1. **Boost methods (lines 552-606):** Delete `start_boost`, `update_boost`, `end_boost` methods entirely.

    2. **Boost dispatch elif (lines 170-173):** Delete `elif self.state == "BOOSTING": ...` block.

    3. **Boost jump-buffer guards (lines 255, 525, 561, 584, 600):** Each is `if self.state != "BOOSTING": ...`. Remove the guard so the gated body runs unconditionally on grounded — that's the correct post-strip behavior per RESEARCH Sequence Verification Sub-step 2. Re-verify each line via grep `if self.state != "BOOSTING"` before editing.

    4. **Boost SPACE input branch (lines 459-463):** Delete `elif (self.is_fused and not self.is_grounded and self.has_boost ... self.start_boost(slime)` block.

    5. **Boost CRACKED_V branch in `move_and_collide` (lines 810-824):** Delete the `if self.state == "BOOSTING" and slime: ... BOOST_CRACKED_V_COST` block.

    **DO NOT TOUCH:**
    - Lines 466-471 mid-drill jump-cancel — KEEP
    - Drill block-break inside `move_and_collide` (still around line 770-790 area) — KEEP
    - Drill state-dispatch elif at line 161-163 — KEEP
    - Mana-shield damage path lines 188-204 — KEEP

    Surviving analog: drill state-dispatch elif (1 if-block) at line 161-163 — post-Sub-step 6 the dispatch becomes a 2-branch if/else (DIVING + default).
  </action>
  <verify>
    <automated>python -c "import src.entities.player; import src.entities.slime"</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import src.entities.player"` exits 0
    - `grep -nE 'def (start_boost|update_boost|end_boost)\b' src/entities/player.py` returns 0 matches
    - `grep -n '"BOOSTING"' src/entities/player.py` returns 0 matches
    - `grep -n 'self.has_boost' src/entities/player.py` returns 0 matches outside `__init__` (the flag init is stripped in Sub-step 6)
    - `grep -n 'BOOST_CRACKED_V_COST\|BOOST_FORCE\|BOOST_JUICE_COST' src/entities/player.py` returns 0 matches
    - `grep -n 'def apply_diving_physics' src/entities/player.py` returns 1 match (drill preserved)
  </acceptance_criteria>
  <done>Boost methods, dispatch, jump-buffer guards (5 sites), SPACE input branch, and BOOST CRACKED_V branch all removed; drill + mana-shield + mid-drill cancel preserved.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Sub-step 3 — Strip Dash (methods + dispatch + V-unfused input + timers + air-used)</name>
  <files>src/entities/player.py</files>
  <read_first>
    - src/entities/player.py (sections 165-170, 270-280, 435-445, 605-625, 670-685, 790-800)
    - .planning/phases/31.5-cut-ability-code-strip/31.5-PATTERNS.md § "src/entities/player.py" sub-steps 3/3a/3b/3c/3d
    - .planning/phases/31.5-cut-ability-code-strip/31.5-CONTEXT.md D-17 step 3
  </read_first>
  <action>
    Delete from `src/entities/player.py` (re-verify lines via grep before editing):

    1. **Dash methods (lines 607-616, 676-679):** Delete `start_dash` (~lines 607-616) and `apply_dash_physics` (~lines 676-679).

    2. **Dash dispatch elif (lines 167-169):** Delete `elif self.state == "DASHING": ...` block.

    3. **Dash input branch — V unfused (lines 438-443):** Delete `elif self.has_dash and self.dash_cooldown <= 0: ... self.start_dash()` block (this is the unfused V branch; Plan 04 will also strip the `"dash"` action key from `_ACTION_MAP`).

    4. **Dash timers (lines 271-276):** Delete `self.dash_cooldown` / `self.dash_timer` decrement loops. Locate via grep `self.dash_cooldown` and `self.dash_timer`.

    5. **Dash air-used reset on landing (line 796):** Delete `self.dash_air_used = False  # Reset air dash on landing`.

    **DO NOT TOUCH** (verify these references still exist after this task):
    - Lines 161-163 drill dispatch — KEEP
    - Lines 466-471 mid-drill jump-cancel — KEEP
    - Mana shield damage at 188-204 — KEEP

    Note: `tuning.DASH_IFRAMES` reads inside `start_dash` exit with the deleted method (CONTEXT D-25). Schema strip in Plan 03 closes the dead-reference window.
  </action>
  <verify>
    <automated>python -c "import src.entities.player; import src.entities.slime"</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import src.entities.player"` exits 0
    - `grep -nE 'def (start_dash|apply_dash_physics)\b' src/entities/player.py` returns 0 matches
    - `grep -n '"DASHING"' src/entities/player.py` returns 0 matches
    - `grep -nE 'self\.(dash_timer|dash_cooldown|dash_dx|dash_air_used)' src/entities/player.py` returns 0 matches outside `__init__` (init flag strip is Sub-step 6)
    - `grep -n 'def apply_diving_physics' src/entities/player.py` returns 1 match
  </acceptance_criteria>
  <done>Dash methods, dispatch, V-unfused input branch, timer decrements, and air-used reset all removed.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Sub-step 4 — Strip Bubble Shield (method + call site + draw + flash timer render branches)</name>
  <files>src/entities/player.py</files>
  <read_first>
    - src/entities/player.py (sections 155-165, 188-205, 275-325, 890-920)
    - .planning/phases/31.5-cut-ability-code-strip/31.5-PATTERNS.md § "src/entities/player.py" sub-steps 4/4a/4b/4c
    - .planning/phases/31.5-cut-ability-code-strip/31.5-CONTEXT.md D-04 (no `bubble_shield` schema group; bubble shield is code-only)
    - .planning/phases/31.5-cut-ability-code-strip/31.5-RESEARCH.md § Risk Surface 6 (draw_shield orphan render)
  </read_first>
  <action>
    Delete from `src/entities/player.py`:

    1. **`update_shield(slime)` method (lines 278-322):** Delete the entire method.

    2. **Shield call site (line 160):** Delete `self.update_shield(slime)` from the update loop.

    3. **Shield draw / flash timer render branches (lines 894-915):** Delete `draw_shield()` method AND any `shield_flash_timer` / `shield_active` render branches. **CRITICAL per Risk 6** — these are at the BOTTOM of the file and easy to miss; locate via grep `draw_shield\|shield_flash_timer\|shield_active`.

    **DO NOT TOUCH (Cross-Cutting Invariant #5 + PATTERNS Sub-step 4b):**
    - Lines 188-204 (mana-shield damage path: `if self.is_fused and slime and slime.juice > 0: slime.consume(tuning.MANA_SHIELD_COST)`) — KEEP. This is the *fused damage drains juice* primitive; distinct from `update_shield()` (bubble-shield-zone-entry). Bubble-shield strip MUST NOT touch this branch.

    Surviving analog: mana-shield damage path at lines 188-204 demonstrates the post-strip damage path that survives.
  </action>
  <verify>
    <automated>python -c "import src.entities.player; import src.entities.slime"</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import src.entities.player"` exits 0
    - `grep -nE 'def update_shield\b|def draw_shield\b' src/entities/player.py` returns 0 matches
    - `grep -nE '(self\.shield_flash_timer|self\.shield_active|self\.shield_cooldown)' src/entities/player.py` returns 0 matches outside `__init__` (init flag strip is Sub-step 6)
    - `grep -n 'MANA_SHIELD_COST' src/entities/player.py` returns 1+ match (mana-shield damage path preserved at lines 188-204)
    - `grep -n 'self.update_shield' src/entities/player.py` returns 0 matches
  </acceptance_criteria>
  <done>`update_shield`, its call site, `draw_shield`, and shield_flash_timer render branches removed; mana-shield damage path at 188-204 untouched.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Sub-step 5 — Strip Charge Shot (methods + CHARGING_SHOT dispatch + Z-release + was_tap Hold + import + ChargeProjectile class)</name>
  <files>src/entities/player.py, src/entities/projectile.py</files>
  <read_first>
    - src/entities/player.py (sections 170-180, 325-345, 415-425, 618-660, 830-850)
    - src/entities/projectile.py (lines 5-103) — verify `Projectile` class and `ChargeProjectile` class
    - .planning/phases/31.5-cut-ability-code-strip/31.5-PATTERNS.md § "src/entities/projectile.py" + § "src/entities/player.py" sub-steps 5/5a/5b/5c/5d
    - .planning/phases/31.5-cut-ability-code-strip/31.5-CONTEXT.md D-12 (projectile.py charge-shot prune)
    - .planning/phases/31.5-cut-ability-code-strip/31.5-RESEARCH.md § Risk Surface 7 (line 837 tuple guard) + Sequence Verification Sub-step 5 (import line at player.py:621)
  </read_first>
  <action>
    Delete from `src/entities/player.py`:

    1. **Charge-shot methods (lines 618-659):** Delete `fire_charge_shot`, `update_charge_shot`, `_tick_charge_shot_windup`.

    2. **CHARGING_SHOT dispatch + early-return guards (lines 174-177, 328-329, 651, AND line 837 tuple):**
       - Delete elif at lines 174-177 (`elif self.state == "CHARGING_SHOT": ...`).
       - Delete early-return at lines 328-329 (`if self.state == "CHARGING_SHOT": ...`).
       - Delete reference at line 651 (inside deleted method, exits with it).
       - **Risk 7:** Update the tuple guard at line 837 from `("DIVING", "DASHING", "RAMMING", "BOOSTING", "CHARGING_SHOT")` to `("DIVING",)`. Single-element tuple — keep DIVING (only surviving physics-managed state).

    3. **Charge-shot release branch (lines 339-342):** Delete `if self.is_fused and input_manager.btnr("spit") ... self.fire_charge_shot(slime)`.

    4. **`was_tap` Hold input branches (lines 333-336):** Delete `was_tap("left", HOLD_TAP_THRESHOLD)` / `was_tap("right", HOLD_TAP_THRESHOLD)` branches. Per CONTEXT D-06 these are the Directional Hold input. The `was_tap` primitive in `src/core/input.py` STAYS (used by surviving spit tap/hold).

    5. **Import line at player.py:621 — `from src.entities.projectile import ChargeProjectile`** (RESEARCH Sequence Verification Sub-step 5): Delete this import line. Locate via grep `from src.entities.projectile import ChargeProjectile`.

    **From `src/entities/projectile.py`:**

    6. **Delete entire `ChargeProjectile` class (lines 52-103).** Per PATTERNS, this includes the constructor at 56-68, `update` at 70-90, `_on_impact` at 92-95, `draw` at 97-103.

    **DO NOT TOUCH:**
    - `Projectile` class in projectile.py:5-49 — KEEP (foundation of Phase 32 daze shot)
    - Spit input branch at player.py:344-414 — KEEP
    - `was_tap` primitive in src/core/input.py — KEEP (Plan 04 only strips `"dash"` from `_ACTION_MAP`)

    Surviving analog: `Projectile` class at projectile.py:5-49 is the entire content of projectile.py post-strip.
  </action>
  <verify>
    <automated>python -c "import src.entities.projectile; import src.entities.player; import src.entities.slime"</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import src.entities.projectile; import src.entities.player"` exits 0
    - `grep -nE 'def (fire_charge_shot|update_charge_shot|_tick_charge_shot_windup)\b' src/entities/player.py` returns 0 matches
    - `grep -n '"CHARGING_SHOT"' src/entities/player.py` returns 0 matches
    - `grep -n 'class ChargeProjectile' src/entities/projectile.py` returns 0 matches
    - `grep -n 'class Projectile' src/entities/projectile.py` returns 1 match (`class Projectile:` survives)
    - `grep -n 'from src.entities.projectile import ChargeProjectile' src/entities/player.py` returns 0 matches
    - `grep -n 'HOLD_TAP_THRESHOLD' src/entities/player.py` returns 0 matches
    - `grep -nE 'self\.state in \(' src/entities/player.py | grep -E 'RAMMING|DASHING|BOOSTING|CHARGING_SHOT'` returns 0 matches (line 837 tuple updated to `("DIVING",)`)
  </acceptance_criteria>
  <done>Charge-shot methods, CHARGING_SHOT dispatch + line 837 tuple guard, Z-release branch, was_tap Hold branches, ChargeProjectile import, and ChargeProjectile class all removed; spit + Projectile + was_tap primitive intact.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 6: Sub-step 6 — Strip cut-ability flag init + god-mode override + final state-tuple cleanup</name>
  <files>src/entities/player.py</files>
  <read_first>
    - src/entities/player.py (sections 45-90, 150-160) — `__init__` block + god-mode override
    - .planning/phases/31.5-cut-ability-code-strip/31.5-PATTERNS.md § "src/entities/player.py" sub-steps 6/6a/6b/6c
    - .planning/phases/31.5-cut-ability-code-strip/31.5-CONTEXT.md D-10/D-11 (state-name strip; final state set IDLE/RUN/JUMP/FALL/DIVING/WALL_SLIDE)
    - .planning/phases/31.5-cut-ability-code-strip/31.5-RESEARCH.md § Risk Surface 7 + 8
  </read_first>
  <action>
    Delete from `src/entities/player.py`:

    1. **Cut-ability flag init (lines 50, 53-55, 65-69, 75-76, 79, 82):** Locate via grep on each name, then delete the init lines for: `has_dash`, `has_shield`, `has_shield_t2`, `has_boost`, `shield_active`, `shield_cooldown`, `hazard_hp_timer`, `boost_recommit_timer`, `dash_timer`, `dash_cooldown`, `dash_dx`, `dash_air_used`, `ram_dx`, `ram_dy`, `charge_windup_timer`, `shield_flash_timer`.

    2. **God-mode override (lines 152-155):** Delete the `if debug.god_abilities: self.has_dash = True; self.has_shield = True; self.has_shield_t2 = True; self.has_boost = True` lines. If the surviving god-mode override only sets `has_drill = True` (or similar surviving flags), keep that line; just drop the cut-flag setters.

    3. **State-tuple guards at lines 174, 339, 345, 415, 434, 447, 466, 837 (verify):** Strip cut state names (`RAMMING`, `DASHING`, `BOOSTING`, `CHARGING_SHOT`) from any remaining tuples. Per Risk 7, line 837 was already updated in Sub-step 5; verify here that no other tuple guards still include the cut names. **KEEP `"DIVING"` everywhere** — drill is the surviving physics-managed state.

    **DO NOT TOUCH:**
    - `has_drill` init — KEEP (drill survives)
    - `invuln_timer` init at line 46 — KEEP (damage system; per CONTEXT D-25)
    - All damage-system invuln_timer setters at lines 185, 191, 208, 265-266 — KEEP
    - Mid-drill jump-cancel block at 466-471 — KEEP
    - `fuse` / `unfuse` methods at 91-112 — KEEP

    Surviving analog: `has_drill = False` line in `__init__` is the post-strip flag-init template.
  </action>
  <verify>
    <automated>python -c "from src.entities.player import Player; from unittest.mock import MagicMock; p = Player(0, 0, MagicMock(), MagicMock()); assert hasattr(p, 'has_drill'); assert not hasattr(p, 'has_dash'); assert not hasattr(p, 'has_shield'); assert not hasattr(p, 'has_boost'); print('OK', p.state)"</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import src.entities.player"` exits 0
    - `grep -nE 'self\.(has_dash|has_shield|has_shield_t2|has_boost|shield_active|shield_cooldown|hazard_hp_timer|boost_recommit_timer|dash_timer|dash_cooldown|dash_dx|dash_air_used|ram_dx|ram_dy|charge_windup_timer|shield_flash_timer)\s*=' src/entities/player.py` returns 0 matches
    - `grep -nE 'RAMMING|DASHING|BOOSTING|CHARGING_SHOT' src/entities/player.py` returns 0 matches
    - `grep -n 'self.has_drill' src/entities/player.py` returns 1+ match (drill flag preserved)
    - `grep -nC1 'self.state == "DIVING":' src/entities/player.py` shows the cancel block at ~466 area STILL PRESENT (Risk 8)
    - `grep -n 'MANA_SHIELD_COST' src/entities/player.py` returns 1+ match (mana-shield path preserved)
    - Game boot smoke: `timeout 30 python main.py` returns rc=124 (timeout, not crash) — title screen reachable, no AttributeError on missing flags
  </acceptance_criteria>
  <done>All cut-ability flag inits, god-mode cut-setters, and remaining cut state-name tuple references removed; drill flag preserved; game boots room 0 cleanly. Plan 01 ready for commit.</done>
</task>

</tasks>

<verification>
After all 6 sub-step tasks complete:

1. `python -c "import src.entities.player; import src.entities.projectile; import src.entities.slime"` exits 0
2. `timeout 30 python main.py` returns rc=124 (game booted to title without crash)
3. Risk Surface verification commands all pass:
   - R6: `grep -nE '(draw_shield|shield_flash_timer|shield_active)' src/entities/player.py` returns 0 (`shield_active` allowed only inside the deleted-section absence; verify no init survives)
   - R7: `grep -nE 'RAMMING|DASHING|BOOSTING|CHARGING_SHOT' src/entities/player.py` returns 0
   - R8: `grep -nC2 'self.state == "DIVING"' src/entities/player.py` shows cancel block at ~466 STILL PRESENT
4. Cross-Cutting Invariant audit (PATTERNS):
   - Drill (#1): `grep -n 'apply_diving_physics' src/entities/player.py` returns 1+ match
   - Spit (#2): `grep -n 'class Projectile' src/entities/projectile.py` returns 1 match
   - Fusion (#3): `grep -n 'def fuse\|def unfuse' src/entities/player.py` returns 2 matches
   - Mana shield (#5): `grep -n 'MANA_SHIELD_COST' src/entities/player.py` returns 1+ match
   - Mid-drill cancel (#12): `grep -nC2 'self.state == "DIVING"' src/entities/player.py` shows the 466-471 cancel block intact
</verification>

<success_criteria>
- `python -c "import src.entities.player; import src.entities.projectile; import src.entities.slime"` exits 0
- `timeout 30 python main.py` returns rc=124
- All sub-step `<acceptance_criteria>` greps pass
- All Cross-Cutting Invariants verified (drill, spit, fuse/unfuse, recall, mana shield, save format, presets forward-compat, event subscribers, was_tap primitive, mid-drill cancel)
- ONE commit covers Plan 01 (per CONTEXT D-15)
</success_criteria>

<output>
After completion, create `.planning/phases/31.5-cut-ability-code-strip/31.5-01-SUMMARY.md` with:
- Sub-steps completed (1-6)
- Files modified: `src/entities/player.py`, `src/entities/projectile.py`
- Symbols removed (full enumeration)
- Cross-cutting invariants verified
- Boot-smoke result
- Commit SHA

Commit message format:
```
feat(31.5): strip cut-ability code from player.py + projectile.py

- Remove ram methods + RAMMING dispatch + V-fused input + CRACKED_H block-break
- Remove boost methods + BOOSTING dispatch + jump-buffer guards + SPACE input + CRACKED_V branch
- Remove dash methods + DASHING dispatch + V-unfused input + dash timers + air-used reset
- Remove update_shield + draw_shield + shield_flash_timer render branches
- Remove charge-shot methods + CHARGING_SHOT dispatch + Z-release + was_tap Hold input branches
- Update line 837 state-tuple guard to ("DIVING",) only
- Strip ChargeProjectile class + import line from player.py:621
- Strip cut-ability flag init + god-mode cut-setters

Surviving: drill, spit (Projectile), fuse/unfuse, mana-shield damage path,
mid-drill jump-cancel block (Phase 32 owns that deletion).

Per CONTEXT D-14, D-15, D-17. Boot smoke: rc=124 (timeout = clean boot).
```
</output>
