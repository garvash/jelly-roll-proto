---
phase: 31.5-cut-ability-code-strip
verified: 2026-04-26
status: complete
verdict: PASS
score: 13/13 invariants verified; all 4 D-18 gates PASS; both phase-verifier-flagged dead-code survivors hotfixed (commit e88a638)
re_verification:
  previous_status: gaps_found (CONDITIONAL_PASS)
  previous_score: "12/13 invariants; 2 leftover items outside D-18 grep scope"
  gaps_closed:
    - "All 4 D-18 gates confirmed PASS via independent re-run (Gate 1 grep, Gate 2 boot smoke, Gate 4 pytest 10/396 = identical to deferred-items.md baseline)"
    - "main.py:497-499 dead DASH_PICKUP tile-marker fallback DELETED in hotfix commit e88a638"
    - "assets/physics-schema.json SHIELD_T2_DRAIN_REDUCTION orphan key DELETED in hotfix commit e88a638 (not present in any preset — schema-only orphan)"
  gaps_remaining: []
gaps:
  - truth: "Cut symbols absent from EVERY production location, not just src/"
    status: partial
    reason: "CONTEXT D-18 Gate 1 scoped grep to src/ only. Two cut-ability artifacts survive outside src/: a dead legacy tile-marker branch in main.py and an orphan schema key in physics-schema.json."
    artifacts:
      - path: "main.py"
        issue: "Line 498: self.items.append(Item(tx * TILE_SIZE, ty * TILE_SIZE, \"DASH_PICKUP\")) inside the legacy tile-marker fallback (not the LDtk loader at lines 442-455 which IS clean). items.py has no DASH_PICKUP collect branch — collect() is silent no-op; ITEM_FRAMES.get() returns 0 (renders as ENERGY frame). Dead in practice (no LDtk Level has tile (3,0) per CONTEXT D-09) but the literal string survives."
      - path: "assets/physics-schema.json"
        issue: "Line 38: \"SHIELD_T2_DRAIN_REDUCTION\": 0.25 in hazards group. Sole consumer was player.py update_shield, which Plan 01 deleted. No live reference in src/. CONTEXT D-04 declared bubble shield 'code-only,' so the schema key was implicitly out-of-scope but not explicitly enumerated for deletion."
    missing:
      - "Delete main.py:497-499 (the tile == (3, 0) Dash Pickup Marker branch)"
      - "Delete \"SHIELD_T2_DRAIN_REDUCTION\": 0.25, line from assets/physics-schema.json hazards group"
      - "Re-verify Gate 4 pytest after both deletions (no regressions expected — both are dead code paths)"
deferred:
  - truth: "Stale RAMMING/DASHING/BOOSTING/CHARGING_SHOT defensive-tuple assertions in tests/test_anim.py and tests/test_anim_hitbox.py"
    addressed_in: "Phase 32 (during fusion-manager refactor) or hygiene-only cleanup phase"
    evidence: "Documented in deferred-items.md § 'Stale assertions in animation defensive tests (low priority)' — tests still PASS; setattr lets them set arbitrary state strings; functionally correct as defensive coverage"
  - truth: "10 pre-existing pytest failures (Group A test_tuning v1.3-baseline mismatch, Group B test_phase22 archive expectations, Group C test_sprite_assets MagicMock + test_ldtk_migration tileset uid)"
    addressed_in: "Future Phase 29 cleanup or test-baseline-refresh maintenance phase"
    evidence: "Confirmed pre-existing on base commit c3d276f via Plan 05 baseline diff; documented in deferred-items.md; per SCOPE BOUNDARY out of scope for a strip phase"
  - truth: "CRACKED_H tile dead-opener (no fusion can break horizontal cracked tiles after ram strip)"
    addressed_in: "Phase 32+ or pml-to-ldtk converter level-design follow-up"
    evidence: "CONTEXT § Deferred Ideas + 32-CONTEXT § Deferred. CRACKED_H tiles render fine (verified in src/level/map.py:282) — just become permanently solid"
  - truth: "Save format save_version bump v1 → v2"
    addressed_in: "Phase 32 (D-21 of 32-CONTEXT)"
    evidence: "CONTEXT D-22/D-23: 31.5 keeps version: 1, Phase 32 owns the bump. Verified version: 1 still present in save_manager.py:31."
  - truth: "Mid-drill jump-cancel block deletion (player.py:298-302)"
    addressed_in: "Phase 32 (when apply_diving_physics migrates to src/fusion/drill_dive.py per 32-CONTEXT D-10)"
    evidence: "CONTEXT § Cross-Cutting Invariants Audit #12 + Plan 01 SUMMARY: block preserved unchanged at lines 298-302. Phase 32 owns deletion."
  - truth: "V-button rebinding (V is unbound in v2.0 after dash strip)"
    addressed_in: "Post-prototype (Godot/Unity transition)"
    evidence: "CONTEXT § Deferred Ideas. V binding removed from _ACTION_MAP per Plan 04; reuse out of scope."
  - truth: "Pogo bounce code path (new code on DOWN+SPACE input)"
    addressed_in: "Phase 32 (32-CONTEXT D-16/D-17/D-18)"
    evidence: "Sibling code path Phase 32 adds; not a strip target for 31.5."
---

# Phase 31.5: Cut-Ability Code-Strip — Independent Goal Verification

**Phase Goal:** Purge all cut-ability code, schema, presets, input bindings,
save flags, pickups, UI tab routing, and tests from the prototype so Phase 32's
`src/fusion/` refactor starts from a clean base. Hard gate before Phase 32 per
`FUSION-DESIGN.md` § Cut Abilities and `32-CONTEXT.md` D-01.

**Verifier:** gsd-verifier (independent re-run of all 4 D-18 gates + invariant audit + out-of-`src/` sweep)
**Verified:** 2026-04-26
**Verdict:** **CONDITIONAL_PASS** — phase goal substantively achieved, all 4 D-18 gates confirmed PASS, but 2 leftover cut-ability artifacts survive OUTSIDE the D-18 grep scope (`src/`). Both are dead in practice; neither blocks Phase 32; both should be cleaned up before or during Phase 32 to honor the FUSION-DESIGN.md "clean base" intent.

This report is **separate from** `31.5-VERIFICATION.md` (the D-18 four-gate sign-off record). This report verifies the **stated phase goal** rather than the four-gate procedural contract.

---

## Verification Methodology

Goal-backward verification: starting from "what must be TRUE for the phase goal to be achieved," verify each truth against the live codebase rather than trusting SUMMARY claims. Re-ran all 4 D-18 gates independently; spot-checked all 13 RESEARCH cross-cutting invariants; swept for cut-ability survivors OUTSIDE the D-18 grep scope (which is `src/` only).

---

## Section A — Phase Goal Achievement (the strip itself)

### A.1 D-18 Gate Re-Verification

| Gate | Spec | Re-run Result | Status |
|------|------|---------------|--------|
| **Gate 1** | `bash gates/grep-checklist.sh` exits 0; 16 cut symbols absent from `src/` + targeted file checks | Re-ran live; output: `Gate 1 PASS: all cut symbols absent from src/`; exit 0 | PASS |
| **Gate 2** | `python main.py` boots to title without crash | Re-ran `timeout 5 python main.py`; exit 124 (clean timeout = booted and ran 5s without raising) | PASS |
| **Gate 3** | Human smoke playthrough (8 actions) | Signed off PASS by user 2026-04-26 (per `31.5-VERIFICATION.md` Gate 3 record); not re-run by this verifier (out-of-scope per CONTEXT D-19) | PASS (delegated) |
| **Gate 4** | `python -m pytest` exits 0 across surviving tests | Re-ran live: `10 failed, 396 passed in 25.84s`. The 10 failures are the EXACT 10 documented in `deferred-items.md` (group A 5 + group B 3 + group C 2). Strip-caused failures: 0. | PASS (with documented 10 pre-existing failures deferred per SCOPE BOUNDARY) |

### A.2 Cut-Symbol Absence — Code (src/ scope)

All 16 CONTEXT D-18 Gate 1 symbols verified absent from `src/`. The script also enforces:
- `"dash"` key gone from `src/core/input.py` `_ACTION_MAP` (verified: 8 actions present, no dash)
- Cut tuning groups gone from `assets/physics-schema.json` (verified: no `dash`/`slime_ram`/`charge_shot`/`boost` group names)

### A.3 Cut-Symbol Sweep — Outside the D-18 grep scope

This sweep was NOT specified by CONTEXT D-18 but is implied by the phase goal ("purge all cut-ability code, schema, presets, input bindings, save flags, pickups, UI tab routing, and tests"). Two survivors found:

| # | Location | Survivor | Reachability | Severity |
|---|----------|----------|--------------|----------|
| 1 | `main.py:497-499` | `elif tile == (3, 0): # Dash Pickup Marker` legacy tile-marker fallback creating `Item(..., "DASH_PICKUP")` | DEAD in practice — no LDtk Level has tile (3,0) per CONTEXT D-09. `items.py` has no `"DASH_PICKUP"` collect branch — collect() is silent no-op; `ITEM_FRAMES.get(self.item_type, 0)` defaults to ENERGY frame. | Low (dead branch) but blocks the "clean base" invariant |
| 2 | `assets/physics-schema.json:38` | `"SHIELD_T2_DRAIN_REDUCTION": 0.25` in `hazards` tuning group | DEAD — sole consumer was `player.py update_shield` deleted in Plan 01. No reference in `src/`. CONTEXT D-04 declared bubble shield "code-only" but this hazard-group key was not explicitly enumerated for deletion. | Low (orphan schema key) but contradicts CONTEXT D-04 intent |

**Why the D-18 grep didn't catch them:**
- `main.py` lives outside `src/`. CONTEXT D-18 Gate 1 scopes the symbol scan to `src/` only. The CONTEXT does enumerate `main.py:442-455` as a touch point, but only the LDtk loader portion was stripped (verified clean) — the legacy tile-marker fallback at lines 488-505 was overlooked.
- `SHIELD_T2_DRAIN_REDUCTION` is not on the 16-symbol Gate 1 list. The list catches `SHIELD_T2` (which would word-boundary-match `SHIELD_T2_DRAIN_REDUCTION`) IF the scan included `assets/`, but CONTEXT D-18 Gate 1 only scans `src/`.

**Auto-suggestion for fix (estimated <5 min, atomic single-commit):**
```bash
# main.py — delete legacy DASH_PICKUP marker branch
# Delete lines 497-499 of main.py (the elif tile == (3, 0) block)

# physics-schema.json — delete orphan hazards key
# Delete line 38 ("SHIELD_T2_DRAIN_REDUCTION": 0.25,) from assets/physics-schema.json
```
Both are dead code paths; neither requires test updates; pytest re-run after the deletions should remain at 10 failures / 396 passed (no new regressions).

---

## Section B — Surviving-System Invariants Preserved

Spot-checked all 13 RESEARCH cross-cutting invariants against the live codebase:

| # | Invariant | Live evidence | Status |
|---|-----------|---------------|--------|
| 1 | Drill paths (`apply_diving_physics`, `has_drill`, `DRILL_SPEED`, `start_dive`) | `src/entities/player.py:47` (`has_drill = False`), `:92` (`has_drill = True`), `:98` (`apply_diving_physics(slime)`), `:286` (`btn("down") and self.has_drill`), `:293` (`tuning.DRILL_SPEED`), `:385` (`def apply_diving_physics`), `:386` (`tuning.DRILL_SPEED`) | PASS |
| 2 | Spit code path (`def spit` in slime, `class Projectile` in projectile) | `src/entities/slime.py:225` (`def spit`), `src/entities/projectile.py:4` (`class Projectile`) | PASS |
| 3 | Fuse/unfuse lifecycle | `src/entities/player.py:59` (`def fuse`), `:73` (`def unfuse`) | PASS |
| 4 | Recall/dissipate code path | `src/entities/slime.py:46` (`def recall`), `:54` (`def update_recall`), `:79` (`def dissipate`) | PASS |
| 5 | Mana shield damage path | `src/entities/player.py:112` (`slime.consume(tuning.MANA_SHIELD_COST)`) | PASS |
| 6 | Save format `version: 1` | `src/core/save_manager.py:31` (`"version": 1`) | PASS (D-22 — Phase 32 owns bump to 2) |
| 7 | `SaveManager.load()` body untouched (D-23) | `src/core/save_manager.py:51-58` is the canonical raw-JSON load (no field filtering), exactly as D-23 mandated | PASS |
| 8 | `presets.py` forward-compat skip | `src/ui/presets.py:84-88` try/except KeyError preserved | PASS |
| 9 | Event subscribers in `Game.__init__` (Phase 31 contract) | `main.py` has 4 `event_bus.subscribe(...)` calls at lines 274 (`drill_block_break`), 285 (`land`), 286 (`jump_start`), 313 (`fuse_start`) — all 4 Phase 31 events present | PASS |
| 10 | `slime.reposition` removed (planner discretion per Plan 02) | `grep -n "def reposition" src/entities/slime.py` → 0 matches | PASS |
| 11 | `was_tap` primitive in input.py | `src/core/input.py:56` (`def was_tap(action, threshold)`) | PASS |
| 12 | Mid-drill jump-cancel block preserved (Phase 32's job) | `src/entities/player.py:298-302` (`# Drill Dive Cancellation` block with `if self.state == "DIVING": if input_manager.btnp("jump"): self.state = "FALLING"; self.unfuse(slime)`) | PASS |
| 13 | CRACKED_H tile rendering preserved (deferred level-design) | `assets/entity-schema.json:56` (`"name": "cracked_h"`); `src/level/map.py:282` (`return tile in (11, 12)  # cracked_h and cracked_v`) | PASS |

### B.1 Player.state values

CONTEXT D-11 expected post-strip set: `IDLE / RUN / JUMP / FALL / DIVING / WALL_SLIDE` (informal naming).
Live `self.state = "..."` assignments in `src/entities/player.py`:
- `IDLE` (lines 27, 497, 526)
- `RUNNING` (line 524)
- `JUMPING` (line 520)
- `FALLING` (lines 301, 397, 522)
- `WALL_SLIDING` (line 517)
- `DIVING` (line 291)
- `DEAD` (line 154 — pre-existing combat state, not a cut state)

7 active states. No `RAMMING`, `DASHING`, `BOOSTING`, `CHARGING_SHOT` assignments. Matches Plan 01 SUMMARY's "Surviving Player.state set." (CONTEXT used short names; the codebase uses long names — same states, just informal vs formal naming.)

### B.2 Schema integrity

`assets/physics-schema.json` `tuning` block has 16 groups: `tile, display, sprite, hazards, movement, forgiving, wall, slime_follow, slime_juice, projectile, drill, juice_effects, health, fusion, gates, save, death, save_point`. Cut groups (`dash`, `slime_ram`, `charge_shot`, `boost`) absent. Survivor key cleanups: `gates` retains only `DRILL_CRACKED_V_COST` (no `BOOST_CRACKED_V_COST`); `fusion` retains `RECALL_SPEED, RECALL_OVERLAP_DIST, MANA_SHIELD_COST, SLIME_DISSIPATE_COOLDOWN, RECALL_TRAIL_COLOR, SPIT_HOLD_THRESHOLD` (no `HOLD_TAP_THRESHOLD`).

### B.3 Preset integrity

All 5 preset files have **exactly 35 keys** in `values` block:
- `_v1.3-reference.json` — 35 keys
- `slot_0.json` — 35 keys
- `slot_1.json` — 35 keys
- `slot_2.json` — 35 keys
- `slot_3.json` — 35 keys

Verified zero cut keys (`DASH_*`, `RAM_*`, `CHARGE_*`, `BOOST_*`, `HOLD_TAP*`) in any preset.

### B.4 Input layer

`src/core/input.py` `_ACTION_MAP` has 8 actions: `left, right, up, down, jump, spit, pause, confirm`. No `dash` key. `was_tap` primitive at line 56 preserved.

### B.5 UI layer

`src/ui/presets.py:15-19` `FEEL_GROUPS` has 8 groups: `movement, forgiving, wall, slime_follow, slime_juice, projectile, drill, fusion`. No cut groups.

`src/ui/panel.py:74-78` `FEEL_GROUPS` mirrors (same 8). `TAB_DEFS` (line 92) has 5 tabs: Move / Jump / Slime / Fuse / Anim. The `Fuse` tab routes only `drill: None, fusion: None` (CONTEXT D-13 clean). The contentious "Fuse" tab name was kept per CONTEXT § Specifics (defer rename to Phase 33+).

### B.6 Items + entity schema

`src/entities/items.py` clean — only `ENERGY, MISSILE, DRILL_PICKUP` types and frames. `assets/entity-schema.json` clean — no `DashPickup, ShieldPickup, BoostPickup` definitions (verified by grep).

### B.7 Test deletions

All 9 cut-only test files DELETED:
- `test_bubble_shield.py`, `test_charge_recoil.py`, `test_charge_shot.py`, `test_charge_shot_windup.py`, `test_dash.py`, `test_ram.py`, `test_slime_boost.py`, `test_slime_hold.py`, `test_tap_reposition.py`

Plus `test_drill_retcon.py` deleted (10th file — Phase 24 retcon obsolete per Plan 05 SUMMARY).

---

## Section C — Leftover Concerns Phase 32 Should Know About

These are NOT phase-31.5 failures per the CONTEXT-D-18 framework (the phase signed off legitimately on its stated four-gate contract). They are leftover concerns the strip did not fully eliminate that **Phase 32 should resolve early in its refactor** to honor the FUSION-DESIGN.md "clean base" intent.

### C.1 main.py legacy tile-marker fallback (BLOCKING for "clean base" invariant)

`main.py:497-499` creates `Item("DASH_PICKUP")` from a (3,0) tile marker that no live LDtk level uses. Phase 32 will be touching `main.py` extensively (`src/fusion/` requires `Game.__init__` registration of FusionManager). Recommend deleting these 3 lines as a hygiene step in Phase 32's first plan, OR adding a separate hotfix commit before Phase 32 begins.

**Suggested fix:** Delete lines 497-499 (the `elif tile == (3, 0): # Dash Pickup Marker` branch). Adjacent legacy markers (Snail/Bat/EnergyTank/MissileTank) at lines 491-505 should also be reviewed — any whose LDtk-entity path supersedes them is dead code, but OUT OF SCOPE for this phase verification.

### C.2 SHIELD_T2_DRAIN_REDUCTION orphan schema key (BLOCKING for "clean base" invariant)

`assets/physics-schema.json:38` retains `"SHIELD_T2_DRAIN_REDUCTION": 0.25`. Sole consumer was `player.py update_shield`, deleted in Plan 01. This is dead schema. CONTEXT D-04 declared bubble shield "code-only," which made this key implicitly out-of-scope, but didn't enumerate it for deletion. Phase 32 will be modifying the schema (FusionAbility tuning groups per 32-CONTEXT D-15 area). Recommend deleting this key in the same atomic commit that adds Phase 32 schema groups.

**Suggested fix:** Delete line 38 from `assets/physics-schema.json`.

### C.3 Stale animation defensive-tuple assertions (LOW PRIORITY, documented)

`tests/test_anim.py:187-188, 304-305` and `tests/test_anim_hitbox.py:38` have RAMMING/DASHING/BOOSTING/CHARGING_SHOT strings in defensive fallback tuples. Tests still PASS (Python setattr accepts arbitrary strings; the fallback rule applies). Documented in `deferred-items.md`. No action required for Phase 32 unblock; address in animation-suite hygiene cleanup.

### C.4 Drill activates without prior fusion state (DOCUMENTED, deferred to Phase 32)

Per Gate 3 sign-off note in `31.5-VERIFICATION.md`: tester confirmed drill activates without prior fusion state — matches the v1.3 contract Phase 31.5 explicitly preserved unchanged. **Phase 32 will tighten the drill-entry gate to require FUSED-at-100%-juice per FUSION-DESIGN.md § Drill-Dive Contract.** This is intentional — a strip phase must not introduce new gameplay constraints. Phase 32 owns this consolidation per its CONTEXT D-10.

### C.5 10 pre-existing pytest failures (DOCUMENTED, deferred)

Verified independently — `python -m pytest` produces exactly the 10 failures cataloged in `deferred-items.md`. All Phase 22/29-era baseline mismatches; none caused by 31.5. Address in dedicated test-baseline-refresh task.

---

## Section D — Confidence Calibration

**Why the verdict is CONDITIONAL_PASS rather than PASS:**

The CONTEXT D-18 four-gate framework is the procedural contract the phase committed to, and all four gates pass cleanly when executed. By that contract, the phase is COMPLETE and Phase 32 IS unblocked.

The phase **goal** ("purge all cut-ability code, schema, presets, input bindings, save flags, pickups, UI tab routing, and tests") is broader than the D-18 grep scope. Two cut-ability artifacts survived outside `src/`:
1. A dead legacy tile-marker fallback in `main.py:497-499`
2. An orphan tuning key `SHIELD_T2_DRAIN_REDUCTION` in `assets/physics-schema.json:38`

Both are dead code paths with no observable runtime effect. Both are easy fixes (~5 min combined). Neither blocks Phase 32 mechanically.

**Why not FAIL:** The phase achieves its functional intent — Phase 32 can begin its refactor on a substantively clean base. Tests pass, game boots, all surviving systems work, all 13 cross-cutting invariants verified.

**Why not unconditional PASS:** The "clean base" hard-gate language in CONTEXT and FUSION-DESIGN.md implies completeness. Two surviving cut-ability artifacts in production files contradict that completeness, even if dead in practice. Honest verification flags them rather than rubber-stamping.

**Recommended path forward:**
- **Option A (preferred):** Apply the two C.1/C.2 fixes as a hotfix commit on `main` before kicking off Phase 32. ~5 minutes; promotes verdict to PASS; leaves Phase 32 with a truly clean base.
- **Option B (acceptable):** Add C.1/C.2 fixes to Phase 32's first plan. Documents the carryover; ensures cleanup happens before any new code lands.
- **Option C (override):** Accept the two survivors as not-actually-blocking (they are dead). Add an `overrides:` block to this verification frontmatter explicitly accepting the deviation. Phase 32 proceeds; cleanup happens whenever convenient.

---

## Sign-Off

**D-18 Four-Gate Framework:** PASS (signed off in `31.5-VERIFICATION.md` by user 2026-04-26)
**Phase Goal Achievement:** SUBSTANTIVE PASS with 2 minor leftover artifacts outside the D-18 grep scope
**Surviving-System Invariants:** ALL 13 PRESERVED
**Phase 32 Unblock:** CLEARED (subject to Option A/B/C handling above)

**Verdict: CONDITIONAL_PASS**

---

*Verified: 2026-04-26*
*Verifier: gsd-verifier (independent re-run + out-of-scope sweep)*
*Companion document: `31.5-VERIFICATION.md` (D-18 four-gate sign-off record)*
