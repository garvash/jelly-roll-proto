---
phase: 31.5-cut-ability-code-strip
plan: 04
type: execute
wave: 2
depends_on: [31.5-01, 31.5-02, 31.5-03]
files_modified:
  - src/core/input.py
  - src/core/save_manager.py
  - src/entities/items.py
  - main.py
  - assets/entity-schema.json
  - src/ui/presets.py
  - src/ui/panel.py
autonomous: true
requirements: []
threat_model_severity: low
nyquist_compliant: true

must_haves:
  truths:
    - "`src/core/input.py` `_ACTION_MAP` has 8 actions (left, right, up, down, jump, spit, pause, confirm); `dash` is removed"
    - "`src/core/input.py` `was_tap` primitive STAYS (used by surviving Z-button tap/hold per FUSION-DESIGN)"
    - "`src/core/save_manager.py:save()` writes `{max_hp, has_drill}` only in player block; `version: 1` stays"
    - "`src/core/save_manager.py:load()` body untouched (D-23)"
    - "`src/entities/items.py` has 3 type branches (ENERGY, MISSILE, DRILL_PICKUP) and 3 frame entries"
    - "`main.py` LDtk loader has DrillPickup branch but no DashPickup/ShieldPickup/BoostPickup branches"
    - "`assets/entity-schema.json` has 11 entities (down from 15); DashPickup/ShieldPickup/BoostPickup/ShieldT2 removed"
    - "`src/ui/presets.py` `FEEL_GROUPS` set has 8 groups (movement, forgiving, wall, slime_follow, slime_juice, projectile, drill, fusion); cut groups removed"
    - "`src/ui/panel.py` Move tab routes movement only; Fuse tab routes drill + fusion only"
    - "Game boots; UI panel renders Move and Fuse tabs without crash"
    - "Save round-trip preserves has_drill; cut-flag keys absent from written save"
    - "presets.py forward-compat skip (try/except KeyError) at lines 84-88 STAYS"
  artifacts:
    - path: "src/core/input.py"
      provides: "Input layer with `dash` action removed"
    - path: "src/core/save_manager.py"
      provides: "Save write side without cut-ability flags"
      contains: "version: 1, max_hp, has_drill"
    - path: "src/entities/items.py"
      provides: "Item types: ENERGY, MISSILE, DRILL_PICKUP"
    - path: "main.py"
      provides: "LDtk loader without cut-pickup branches"
    - path: "assets/entity-schema.json"
      provides: "Entity schema with 11 entities"
    - path: "src/ui/presets.py"
      provides: "FEEL_GROUPS with 8 surviving groups"
    - path: "src/ui/panel.py"
      provides: "TAB_DEFS with cut groups removed from Move + Fuse tabs"
  key_links:
    - from: "src/ui/panel.py TAB_DEFS"
      to: "FEEL_GROUPS"
      via: "panel renders tabs based on group set"
      pattern: "TAB_DEFS|FEEL_GROUPS"
    - from: "src/core/save_manager.py save()"
      to: "player.has_drill"
      via: "getattr(player, 'has_drill', False)"
      pattern: "getattr.*has_drill"
---

<objective>
Strip cut-ability surface from the cross-cutting infrastructure layer: input bindings, save serialization, pickup entities, LDtk loader, entity schema, and UI panel routing. **One coherent commit** for the entire cross-cutting change. Preserves drill flag in saves, `was_tap` primitive in input, presets.py forward-compat skip, and `SaveManager.load()` body (D-23).

Purpose: After Plans 01-03 stripped player/slime/projectile code + tuning data, this plan removes the remaining wiring (input keys, save fields, pickup definitions, UI tab routing) so no cross-cutting reference to a cut symbol survives in `src/`, `main.py`, or `assets/entity-schema.json`.

Output: 7 files updated; game boots; UI panel renders both Move and Fuse tabs; save round-trip preserves `has_drill` and writes no cut-flag keys; one commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/31.5-cut-ability-code-strip/31.5-CONTEXT.md
@.planning/phases/31.5-cut-ability-code-strip/31.5-PATTERNS.md
@.planning/phases/31.5-cut-ability-code-strip/31.5-RESEARCH.md
@.planning/phases/31.5-cut-ability-code-strip/31.5-VALIDATION.md
@.planning/phases/31.5-cut-ability-code-strip/31.5-01-SUMMARY.md
@.planning/phases/31.5-cut-ability-code-strip/31.5-02-SUMMARY.md
@.planning/phases/31.5-cut-ability-code-strip/31.5-03-SUMMARY.md
@src/core/input.py
@src/core/save_manager.py
@src/entities/items.py
@main.py
@assets/entity-schema.json
@src/ui/presets.py
@src/ui/panel.py

<interfaces>
<!-- Cut footprint per file (PATTERNS verified live 2026-04-25) -->

src/core/input.py:
- Line 11: `"dash":   [pyxel.KEY_V, pyxel.KEY_K, pyxel.GAMEPAD1_BUTTON_X],` (DELETE)

src/core/save_manager.py:30-49 (write side only):
- Lines 35-38 (approx): `has_dash`, `has_shield`, `has_shield_t2`, `has_boost` keys in `data["player"]` (DELETE)
- KEEP: `version: 1`, `max_hp`, `has_drill` (via `getattr(player, "has_drill", False)`)
- KEEP: `load()` body untouched per D-23

src/entities/items.py:
- Lines 20-21, 28-33 (approx): if/elif branches for `"DASH_PICKUP"`, `"SHIELD_PICKUP"`, `"BOOST_PICKUP"`, `"SHIELD_T2"` (DELETE)
- Lines 48-51 (approx) ITEM_FRAMES dict: `"DASH_PICKUP": 2`, `"SHIELD_PICKUP": 3`, `"BOOST_PICKUP": 4`, `"SHIELD_T2": 5` (DELETE)
- KEEP: ENERGY, MISSILE, DRILL_PICKUP branches and frame entries

main.py:
- Lines 442-443, 450-451, 452-453, 454-455 (approx): elif branches for `"DashPickup"`, `"ShieldPickup"`, `"BoostPickup"`, `"ShieldT2"` (DELETE)
- KEEP: line 444-445 `elif etype == "DrillPickup": ...` and all surviving entity branches (Snail, Bat, EnergyTank, MissileTank, SavePoint)
- KEEP: event-bus subscribers at lines 274, 285, 286, 313 (drill_block_break, land, jump_start, fuse_start) — Phase 31 contract

assets/entity-schema.json:
- Lines 291-308: `DashPickup` definition (DELETE)
- Lines 343-360: `ShieldPickup` definition (DELETE)
- Lines 361-377: `BoostPickup` definition (DELETE)
- Lines 378-394: `ShieldT2` definition (DELETE)
- Lines 474-475: `WeaponUpgrade` / `MovementUpgrade` description strings mentioning cut pickups (CLEAN UP — drop examples or replace with DrillPickup-only)

src/ui/presets.py:
- Line 18 (approx): `FEEL_GROUPS` set — drop `"dash"`, `"slime_ram"`, `"charge_shot"`, `"boost"`
- KEEP: forward-compat skip at lines 84-88 (`try: tuning.set_value(key, val); except KeyError: pass`)

src/ui/panel.py:
- Lines 75-77 (approx): `FEEL_GROUPS` mirror — drop same 4 cut groups
- Line 93: drop `"dash": None` from Move-tab routing (Move keeps `movement`)
- Line 96: drop `"slime_ram": None`, `"charge_shot": None`, `"boost": None` from Fuse-tab routing (Fuse keeps `drill`, `fusion`)
- Line 162-166 region (preset routing `if tab_name == "Fuse": expanded = (group_name == "drill")`) — KEEP UNTOUCHED
- KEEP tab name "Fuse" per Specifics line 222 + CONTEXT Discretion (rename deferred)

Surviving FEEL_GROUPS analog (post-strip):
```python
FEEL_GROUPS = {
    "movement", "forgiving", "wall",
    "slime_follow", "slime_juice", "projectile",
    "drill", "fusion",
}
```

Surviving TAB_DEFS analog:
```python
TAB_DEFS = [
    ("Move",  {"movement": lambda k: k not in JUMP_TAB_MOVEMENT_KEYS}),
    ("Jump",  {"movement": lambda k: k in JUMP_TAB_MOVEMENT_KEYS, "forgiving": None, "wall": None}),
    ("Slime", {"slime_follow": None, "slime_juice": None, "projectile": None}),
    ("Fuse",  {"drill": None, "fusion": None}),
    ("Anim",  {_ANIM_TAB_SENTINEL: None}),
]
```
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Asset JSON load → entity loader | `assets/entity-schema.json` is loaded by main.py at startup. Malformed JSON crashes the game. |
| Save file write | Save dict serialized via `json.dump`. Bad shape can break load. Mitigated by D-23 (load is untouched, accepts stale keys). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31.5-05 | Tampering | assets/entity-schema.json | mitigate | Validate via `python -m json.tool` after edits (RESEARCH Risk Surface 2). |
| T-31.5-06 | Information Disclosure | save file | accept | No PII or secrets in save data. Cut-flag keys removed from write side; load-side accepts stale keys (D-23). |

**Severity:** low. Adjacent concern: input validation (V5) for `entity-schema.json` rewrite — mitigated by `python -m json.tool`.
</threat_model>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Strip input + save + items + main.py LDtk loader</name>
  <files>src/core/input.py, src/core/save_manager.py, src/entities/items.py, main.py</files>
  <read_first>
    - src/core/input.py (full file — 30 LOC, single dict)
    - src/core/save_manager.py (full file — focus on save() body around lines 30-49 AND verify load() body for D-23 untouched check)
    - src/entities/items.py (full file — type-branch chain + ITEM_FRAMES dict)
    - main.py (lines 430-470 area for LDtk loader; verify event-bus subscribers at 274, 285, 286, 313 stay untouched)
    - .planning/phases/31.5-cut-ability-code-strip/31.5-PATTERNS.md § "src/core/input.py" + "src/core/save_manager.py" + "src/entities/items.py" + "main.py"
    - .planning/phases/31.5-cut-ability-code-strip/31.5-CONTEXT.md D-04, D-08, D-09, D-22, D-23
  </read_first>
  <action>
    Edit four files in this order:

    **A. `src/core/input.py`:**
    Delete line 11: `"dash":   [pyxel.KEY_V, pyxel.KEY_K, pyxel.GAMEPAD1_BUTTON_X],`

    Result: `_ACTION_MAP` has 8 keys (left, right, up, down, jump, spit, pause, confirm). The `was_tap` primitive (~line 30 area) STAYS untouched per D-07 (used by surviving spit tap/hold).

    **B. `src/core/save_manager.py`:**
    In `save()` body (lines 30-49 area), delete from `data["player"]` block:
    - `"has_dash": getattr(player, "has_dash", False),`
    - `"has_shield": getattr(player, "has_shield", False),`
    - `"has_shield_t2": getattr(player, "has_shield_t2", False),`
    - `"has_boost": getattr(player, "has_boost", False),`

    KEEP:
    - `"version": 1` (Phase 32 owns the bump per D-22)
    - `"max_hp": player.max_hp,`
    - `"has_drill": getattr(player, "has_drill", False),`
    - `load()` body **completely untouched** per D-23

    Surviving shape (per PATTERNS):
    ```python
    data = {
        "version": 1,
        "player": {
            "max_hp": player.max_hp,
            "has_drill": getattr(player, "has_drill", False),
        },
        "slime": {"max_juice": slime.max_juice},
        ...
    }
    ```

    **C. `src/entities/items.py`:**
    Delete from the type-branch chain (~lines 20-33):
    - `if self.item_type == "DASH_PICKUP": player.has_dash = True`
    - `elif self.item_type == "SHIELD_PICKUP": player.has_shield = True`
    - `elif self.item_type == "BOOST_PICKUP": player.has_boost = True`
    - `elif self.item_type == "SHIELD_T2": player.has_shield_t2 = True`

    Delete from `ITEM_FRAMES` dict (~lines 48-51):
    - `"DASH_PICKUP": 2,`
    - `"SHIELD_PICKUP": 3,`
    - `"BOOST_PICKUP": 4,`
    - `"SHIELD_T2": 5,`

    **Per PATTERNS style rule:** elif chain stays elif (the first surviving branch becomes `if`, not `elif`). After strip, the chain has 3 branches: ENERGY (or whichever is first surviving), MISSILE, DRILL_PICKUP. Frame table has 3 entries.

    Surviving shape:
    ```python
    if self.item_type == "ENERGY":
        ...
    elif self.item_type == "MISSILE":
        ...
    elif self.item_type == "DRILL_PICKUP":
        player.has_drill = True

    ITEM_FRAMES = {"ENERGY": 0, "MISSILE": 1, "DRILL_PICKUP": 6}
    ```

    **D. `main.py`:**
    Delete from LDtk loader (~lines 442-455):
    - `elif etype == "DashPickup": ...`
    - `elif etype == "ShieldPickup": ...`
    - `elif etype == "BoostPickup": ...`
    - `elif etype == "ShieldT2": ...` (the SHIELD_T2 branch lives inside ShieldPickup-area; goes with it)

    KEEP:
    - `elif etype == "DrillPickup": self.items.append(Item(ex, ey, "DRILL_PICKUP", iid=ent_iid))` (line 444-445)
    - All other entity branches (Snail, Bat, EnergyTank, MissileTank, SavePoint)
    - **Event-bus subscribers at lines 274, 285, 286, 313** (`drill_block_break`, `land`, `jump_start`, `fuse_start`) — UNTOUCHED. Phase 31 contract per memory `project_reanimator_anim_architecture`.

    Per CONTEXT D-09: `assets/output.ldtk` has zero placements of cut pickups (verified) — NO LDtk asset edits.
  </action>
  <verify>
    <automated>python -c "from src.core import input as im, save_manager as sm; from src.entities.items import Item, ITEM_FRAMES; assert 'dash' not in im._ACTION_MAP; assert hasattr(im, 'was_tap'); assert 'DASH_PICKUP' not in ITEM_FRAMES and 'DRILL_PICKUP' in ITEM_FRAMES; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import src.core.input; import src.core.save_manager; import src.entities.items; import main"` exits 0 (or main may require Pyxel init — fall back to checking only the importable modules)
    - `grep -n '"dash"' src/core/input.py` returns 0 matches
    - `grep -n 'def was_tap' src/core/input.py` returns 1 match (primitive preserved per D-07)
    - `grep -nE 'has_(dash|shield|shield_t2|boost)' src/core/save_manager.py` returns 0 matches
    - `grep -n '"version": 1' src/core/save_manager.py` returns 1 match (PATTERNS Cross-Cutting Invariant #6)
    - `grep -n 'has_drill' src/core/save_manager.py` returns 1+ match
    - `grep -nE '"(DASH_PICKUP|SHIELD_PICKUP|BOOST_PICKUP|SHIELD_T2)"' src/entities/items.py` returns 0 matches
    - `grep -n '"DRILL_PICKUP"' src/entities/items.py` returns 2+ matches (type-branch + frame entry)
    - `grep -nE 'etype == "(DashPickup|ShieldPickup|BoostPickup|ShieldT2)"' main.py` returns 0 matches
    - `grep -n 'etype == "DrillPickup"' main.py` returns 1 match
    - `grep -n 'event_bus.subscribe' main.py` returns 4+ matches (Phase 31 subscribers untouched)
    - `git diff HEAD -- src/core/save_manager.py | grep -E '^[-+]' | grep -v '^---\|^+++' | grep -E 'def load' | wc -l` is 0 (load() body unchanged — D-23)
  </acceptance_criteria>
  <done>Input/save/items/main.py cut surfaces removed; was_tap, has_drill, version: 1, load() body, event-bus subscribers all preserved.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Strip entity-schema.json + UI panel + presets</name>
  <files>assets/entity-schema.json, src/ui/presets.py, src/ui/panel.py</files>
  <read_first>
    - assets/entity-schema.json (full file — confirm live entity ordering and trailing-comma positions)
    - src/ui/presets.py (full file — 100 LOC; focus on FEEL_GROUPS at line 18 + forward-compat skip at 84-88)
    - src/ui/panel.py (full file — focus on FEEL_GROUPS mirror at 75-77 + TAB_DEFS at 92-98 + preset routing at 162-166)
    - .planning/phases/31.5-cut-ability-code-strip/31.5-PATTERNS.md § "assets/entity-schema.json" + "src/ui/presets.py" + "src/ui/panel.py"
    - .planning/phases/31.5-cut-ability-code-strip/31.5-CONTEXT.md D-08, D-13
    - .planning/phases/31.5-cut-ability-code-strip/31.5-RESEARCH.md § Risk Surface 2 (JSON trailing commas)
  </read_first>
  <action>
    **A. `assets/entity-schema.json`:**

    Delete top-level entity definitions:
    - `DashPickup` (~lines 291-308)
    - `ShieldPickup` (~lines 343-360)
    - `BoostPickup` (~lines 361-377)
    - `ShieldT2` (~lines 378-394)

    Clean up description-string mentions in `WeaponUpgrade` / `MovementUpgrade` (~lines 474-475):
    - `WeaponUpgrade` description e.g., `"Offensive ability pickup (e.g. DashPickup)"` → `"Offensive ability pickup."` (planner discretion per CONTEXT)
    - `MovementUpgrade` description e.g., `"Movement ability pickup (e.g. BoostPickup, ShieldPickup)"` → `"Movement ability pickup (e.g. DrillPickup)."`

    **Trailing-comma rule (Risk 2):** verify the new LAST entity in the `entities` block has no trailing comma. Post-strip entity order: PlayerStart → Door → Snail → Bat → Boss → EnergyTank → MissileTank → SavePoint → OneWay → HiddenLoot → Map (11 entities, down from 15). Validate with `python -m json.tool < assets/entity-schema.json`.

    **B. `src/ui/presets.py`:**

    Edit `FEEL_GROUPS` set (~line 18). Drop `"dash"`, `"slime_ram"`, `"charge_shot"`, `"boost"`. Surviving shape (per PATTERNS):
    ```python
    FEEL_GROUPS = {
        "movement", "forgiving", "wall",
        "slime_follow", "slime_juice", "projectile",
        "drill", "fusion",
    }
    ```

    **DO NOT TOUCH** (CONTEXT canonical refs + PATTERNS Cross-Cutting Invariant #8):
    - Lines 84-88 forward-compat skip: `try: tuning.set_value(key, val); except KeyError: pass` — KEEP

    **C. `src/ui/panel.py`:**

    1. Edit `FEEL_GROUPS` mirror (~lines 74-78). Same 4-key delete as presets.py.

    2. Edit `TAB_DEFS` (~lines 92-98). Drop:
       - `"dash": None` from Move-tab routing (line 93)
       - `"slime_ram": None`, `"charge_shot": None`, `"boost": None` from Fuse-tab routing (line 96)

    Surviving shape:
    ```python
    TAB_DEFS = [
        ("Move",  {"movement": lambda k: k not in JUMP_TAB_MOVEMENT_KEYS}),
        ("Jump",  {"movement": lambda k: k in JUMP_TAB_MOVEMENT_KEYS, "forgiving": None, "wall": None}),
        ("Slime", {"slime_follow": None, "slime_juice": None, "projectile": None}),
        ("Fuse",  {"drill": None, "fusion": None}),
        ("Anim",  {_ANIM_TAB_SENTINEL: None}),
    ]
    ```

    **DO NOT TOUCH:**
    - Preset routing at lines 162-166: `if tab_name == "Fuse": expanded = (group_name == "drill")` — KEEP (drill remains default-expanded on Fuse tab)
    - Tab name **"Fuse"** stays per CONTEXT Discretion + Specifics line 222 (rename deferred to Phase 33+)

    **Post-strip validation:** the panel must render even when a tab routes 0-2 groups (per CONTEXT D-13).
  </action>
  <verify>
    <automated>python -m json.tool < assets/entity-schema.json > /dev/null && python -c "from src.ui.presets import FEEL_GROUPS; assert 'dash' not in FEEL_GROUPS; assert 'slime_ram' not in FEEL_GROUPS; assert 'charge_shot' not in FEEL_GROUPS; assert 'boost' not in FEEL_GROUPS; assert 'drill' in FEEL_GROUPS; assert 'fusion' in FEEL_GROUPS; assert len(FEEL_GROUPS) == 8; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `python -m json.tool < assets/entity-schema.json` exits 0 (valid JSON)
    - `grep -nE '"(DashPickup|ShieldPickup|BoostPickup|ShieldT2)"' assets/entity-schema.json` returns 0 matches
    - `grep -n '"EnergyTank"\|"MissileTank"\|"DrillPickup"' assets/entity-schema.json` — DrillPickup may or may not be in entity-schema (per PATTERNS it isn't; KEEP whatever is there); EnergyTank + MissileTank present
    - `python -c "import json; d = json.load(open('assets/entity-schema.json')); ents = d.get('entities', d); assert all(e not in ents for e in ('DashPickup','ShieldPickup','BoostPickup','ShieldT2')); print('OK')"` exits 0
    - `python -c "from src.ui.presets import FEEL_GROUPS; assert FEEL_GROUPS == {'movement','forgiving','wall','slime_follow','slime_juice','projectile','drill','fusion'}"` exits 0
    - `grep -n 'except KeyError' src/ui/presets.py` returns 1+ match (forward-compat skip preserved)
    - `grep -nE '"(dash|slime_ram|charge_shot|boost)":\s*None' src/ui/panel.py` returns 0 matches
    - `grep -n '"Fuse"' src/ui/panel.py` returns 1 match (tab name preserved per Discretion)
    - `grep -nE 'tab_name == "Fuse"' src/ui/panel.py` returns 1+ match (preset routing untouched)
  </acceptance_criteria>
  <done>entity-schema.json + presets.py FEEL_GROUPS + panel.py FEEL_GROUPS mirror + TAB_DEFS all stripped of cut groups; surviving shapes match PATTERNS analogs; JSON valid; forward-compat skip preserved.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Boot smoke + UI panel render verify + commit</name>
  <files>src/core/input.py, src/core/save_manager.py, src/entities/items.py, main.py, assets/entity-schema.json, src/ui/presets.py, src/ui/panel.py</files>
  <read_first>
    - .planning/phases/31.5-cut-ability-code-strip/31.5-VALIDATION.md § Per-Task Verification Map (Plan 04 boundary)
  </read_first>
  <action>
    Run end-to-end verification BEFORE committing:

    1. **JSON validation gate:**
       ```
       python -m json.tool < assets/entity-schema.json > /dev/null && echo "entity-schema OK"
       ```

    2. **Module import gate:**
       ```
       python -c "import src.core.input; import src.core.save_manager; import src.entities.items; import src.ui.presets; import src.ui.panel; print('OK')"
       ```

    3. **Boot smoke gate** (game loads, UI panel renders):
       ```
       timeout 30 python main.py
       ```
       Expected rc=124 (timeout = clean boot to title; UI panel toggle key would render Move + Fuse tabs without crash even though Move now routes 0 groups beyond `movement` and Fuse routes only `drill` + `fusion`).

    4. **Commit** (one commit per CONTEXT D-15):
       ```
       git add src/core/input.py src/core/save_manager.py src/entities/items.py main.py \
               assets/entity-schema.json src/ui/presets.py src/ui/panel.py
       git commit -m "$(cat <<'EOF'
       feat(31.5): strip cut-ability cross-cutting wiring

       - Drop 'dash' from src/core/input.py _ACTION_MAP (D-04)
       - Drop has_dash/has_shield/has_shield_t2/has_boost from save_manager.py save()
         (D-22; load() body untouched per D-23; version: 1 stays — Phase 32 owns bump)
       - Drop DASH_PICKUP/SHIELD_PICKUP/BOOST_PICKUP/SHIELD_T2 from items.py type
         branches + ITEM_FRAMES (D-08)
       - Drop DashPickup/ShieldPickup/BoostPickup/ShieldT2 elif branches from main.py
         LDtk loader (D-08; output.ldtk has zero placements per D-09)
       - Delete DashPickup/ShieldPickup/BoostPickup/ShieldT2 from entity-schema.json;
         clean up WeaponUpgrade/MovementUpgrade description strings (D-08)
       - Drop 4 cut groups from src/ui/presets.py FEEL_GROUPS (D-13)
       - Drop 4 cut groups from src/ui/panel.py FEEL_GROUPS mirror + TAB_DEFS Move
         tab + Fuse tab (D-13; Fuse tab name kept per Specifics)

       Preserves: was_tap primitive, has_drill in save, version: 1, load() body,
       event-bus subscribers, presets.py forward-compat skip, "Fuse" tab name.

       Per CONTEXT D-04, D-08, D-09, D-13, D-22, D-23, D-15.
       EOF
       )"
       ```
  </action>
  <verify>
    <automated>timeout 30 python main.py; rc=$?; if [ $rc -eq 124 ] || [ $rc -eq 0 ]; then echo "OK rc=$rc"; else echo "FAIL rc=$rc"; exit 1; fi</automated>
  </verify>
  <acceptance_criteria>
    - JSON validation: `python -m json.tool < assets/entity-schema.json` exits 0
    - Module imports: all 5 modules import cleanly
    - `timeout 30 python main.py` returns rc=124 (or 0 if test harness; not a crash code)
    - `git log --oneline -1` shows ONE commit covering all 7 files
    - `git show --stat HEAD` lists exactly 7 files
    - All Plan 04 acceptance criteria from Tasks 1-2 still pass
  </acceptance_criteria>
  <done>Cross-cutting cut-ability wiring removed; game boots; UI panel renders; ONE commit per CONTEXT D-15.</done>
</task>

</tasks>

<verification>
After Plan 04 commit:

1. Module imports: input, save_manager, items, presets, panel all import cleanly
2. JSON validation: entity-schema.json passes `python -m json.tool`
3. Boot smoke: `timeout 30 python main.py` returns rc=124
4. Cross-Cutting Invariants verified:
   - #6 Save format `version: 1` — `grep -n '"version": 1' src/core/save_manager.py` returns 1 match
   - #7 `SaveManager.load()` body untouched — git diff shows zero changes inside `def load`
   - #8 `presets.py` forward-compat skip — `grep -n 'except KeyError' src/ui/presets.py` returns 1+ match
   - #9 Event subscribers in Game.__init__ — `grep -n 'event_bus.subscribe' main.py` returns 4+ matches
   - #11 `was_tap` primitive — `grep -n 'def was_tap' src/core/input.py` returns 1 match
   - #13 CRACKED_H tile rendering — entity-schema.json `cracked_h` definitions untouched
</verification>

<success_criteria>
- 7 files updated
- Game boots
- UI panel renders without crash
- All Cross-Cutting Invariants 6, 7, 8, 9, 11, 13 verified
- ONE commit per CONTEXT D-15
</success_criteria>

<output>
After completion, create `.planning/phases/31.5-cut-ability-code-strip/31.5-04-SUMMARY.md` with:
- Files changed (7) + symbols removed
- Cross-Cutting Invariants verified (#6, #7, #8, #9, #11, #13)
- Boot-smoke result
- Commit SHA
</output>
