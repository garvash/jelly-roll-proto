---
phase: 31.5-cut-ability-code-strip
plan: 02
type: execute
wave: 1
depends_on: [31.5-01]
files_modified:
  - src/entities/slime.py
autonomous: true
requirements: []
threat_model_severity: low
nyquist_compliant: true

must_haves:
  truths:
    - "src/entities/slime.py imports cleanly after strip"
    - "Slime Hold state (`is_holding_position`, `hold_x`, `hold_y`, `HOLD_SCAN_RANGE`) is fully removed"
    - "Hold-mode gravity-and-collision branch in `update` is removed"
    - "All `is_holding_position = False` reset call sites (recall, reform, init) are removed"
    - "Recall / update_recall / dissipate / spit method bodies are otherwise intact"
    - "Game boots and slime follows player normally (no hold-mode lockout)"
    - "Orphan `slime.reposition()` method is deleted (zero callers post Plan 01; RESEARCH Risk 3 + Open Question #1 recommend DELETE)"
  artifacts:
    - path: "src/entities/slime.py"
      provides: "Slime entity with Hold state stripped; recall/dissipate/spit preserved"
      contains: "def recall, def update_recall, def dissipate, def spit"
  key_links:
    - from: "src/entities/slime.py update loop"
      to: "non-Hold gravity-and-collision branch"
      via: "removal of `if self.is_holding_position:` branch"
      pattern: "is_holding_position"
---

<objective>
Strip Directional Hold mode from `src/entities/slime.py` per CONTEXT D-06: remove `is_holding_position`, `hold_x`/`hold_y`, `HOLD_SCAN_RANGE`, the hold-gravity branch in `update`, and all reset call sites. Delete the orphan `slime.reposition()` method per RESEARCH Risk Surface 3 (zero callers post Plan 01). Preserve `recall`, `update_recall`, `dissipate`, `spit`, and reform behavior intact.

Purpose: Eliminate Hold's surface area before Phase 32's slime/fusion refactor. Hold's input call sites at player.py:333-339 are already deleted in Plan 01 Sub-step 5; this plan removes the slime-side state.

Output: `src/entities/slime.py` with cut Hold state and `reposition()` method removed; game boots; slime follows player normally; recall/dissipate/spit unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/31.5-cut-ability-code-strip/31.5-CONTEXT.md
@.planning/phases/31.5-cut-ability-code-strip/31.5-PATTERNS.md
@.planning/phases/31.5-cut-ability-code-strip/31.5-RESEARCH.md
@.planning/phases/31.5-cut-ability-code-strip/31.5-VALIDATION.md
@.planning/phases/31.5-cut-ability-code-strip/31.5-01-SUMMARY.md
@src/entities/slime.py

<interfaces>
<!-- Surviving slime methods (KEEP) -->

```python
# slime.py:48-55 (KEEP — recall body intact except for line 54 hold-reset)
def recall(self, player_x, player_y):
    if self.is_dissipated:
        return
    self.is_recalling = True
    self.is_punted = False
    # self.is_holding_position = False   <-- DELETE THIS LINE per D-06
    self.history.clear()

# slime.py:57-80 (KEEP — update_recall untouched)
def update_recall(self, player_x, player_y):
    ...

# slime.py:82-89 (KEEP — dissipate untouched)
def dissipate(self):
    ...

# slime.py:277-284 (KEEP — spit untouched, foundation of Phase 32 daze shot)
def spit(self):
    ...
```

<!-- Cut footprint (PATTERNS verified live 2026-04-25) -->

| Cut Item | Lines | Code Pattern |
|----------|-------|--------------|
| `is_holding_position` init | 39 | `self.is_holding_position = False` |
| `hold_x` / `hold_y` init | 37-38 | `self.hold_x = None; self.hold_y = None` |
| Hold reset in `recall` | 54 | `self.is_holding_position = False  # Cancel hold` |
| `HOLD_SCAN_RANGE` const | 107-108 | `HOLD_SCAN_RANGE = 4` (local-scope const inside `reposition`) |
| Scan loop inside `reposition` | 107-130 | `for i in range(HOLD_SCAN_RANGE): ...` |
| Hold-mode gravity-and-collision branch | 169-179 | `if self.is_holding_position: ...` block in `update` |
| Hold reset in `reform` | 306 | `self.is_holding_position = False` |
| Orphan `reposition` method | 103-137 | entire method (RESEARCH Risk 3: zero callers post Plan 01) |
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (n/a) | Single-player game prototype; no network surface, no auth, no untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31.5-02 | Tampering | n/a | accept | Internal code-strip; ASVS V2-V6 do not apply. |

**Severity:** low.
</threat_model>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Pre-flight — verify `slime.reposition()` has zero callers</name>
  <files>src/entities/slime.py</files>
  <read_first>
    - src/entities/slime.py (full file — confirm method definition + scan for any caller)
    - .planning/phases/31.5-cut-ability-code-strip/31.5-RESEARCH.md § Risk Surface 3 (orphan reposition recommendation)
    - .planning/phases/31.5-cut-ability-code-strip/31.5-PATTERNS.md § "src/entities/slime.py" + Cross-Cutting Invariants #10
  </read_first>
  <action>
    Run a final caller-check across the live tree before deleting `reposition`:

    ```
    git grep -nE '\.reposition\b|\breposition\(' -- 'src/' 'main.py' 'tests/'
    ```

    Expected results post Plan 01:
    - `src/entities/slime.py` — definition site only (line ~103)
    - Zero call sites in `src/`, `main.py`, `tests/` (the call sites at player.py:333-339 were deleted in Plan 01 Sub-step 5; the test that exercised it, `test_tap_reposition.py`, is deleted in Plan 05)

    If the grep returns ANY caller other than the definition itself, escalate as an open question — do not delete blind. Per RESEARCH Risk 3 caveat: "If the planner finds a non-obvious caller during Plan 02 task design, escalate as an open question rather than deleting blind."

    Record the grep output in the task summary so Plan 02 commit message can cite "verified zero callers."

    No file edits in this task — pure verification gate.
  </action>
  <verify>
    <automated>git grep -nE '\.reposition\b|\breposition\(' -- 'src/' 'main.py' 'tests/' | grep -v 'def reposition' | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -nE '\.reposition\b|\breposition\(' -- 'src/' 'main.py' 'tests/' | grep -v 'def reposition'` returns 0 lines (zero callers)
    - `grep -n 'def reposition' src/entities/slime.py` returns exactly 1 match (definition still present pre-delete)
  </acceptance_criteria>
  <done>Caller-check complete; `reposition` confirmed orphan; safe to delete in Task 2.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Strip Hold state + reposition method from slime.py</name>
  <files>src/entities/slime.py</files>
  <read_first>
    - src/entities/slime.py (full file — read again to confirm live line numbers; sections 30-45, 48-55, 100-140, 165-185, 300-310)
    - .planning/phases/31.5-cut-ability-code-strip/31.5-PATTERNS.md § "src/entities/slime.py" + Cross-Cutting Invariants #4 + #10
    - .planning/phases/31.5-cut-ability-code-strip/31.5-CONTEXT.md D-06 (full Hold strip enumeration)
  </read_first>
  <action>
    Delete from `src/entities/slime.py`:

    1. **Init flags (lines 37-39):** Delete `self.hold_x = None`, `self.hold_y = None`, `self.is_holding_position = False` (3 lines).

    2. **Hold reset in `recall` (line 54):** Delete the line `self.is_holding_position = False  # Cancel hold`. **Do not touch the rest of the `recall` body** (lines 48-53, 55) — `is_dissipated` check, `is_recalling = True`, `is_punted = False`, `history.clear()` all KEEP.

    3. **Hold-mode gravity-and-collision branch in `update` (lines 169-179):** Delete the `if self.is_holding_position:` block entirely. The non-hold path becomes the unconditional gravity/collision flow.

    4. **Hold reset in `reform` (line 306):** Delete `self.is_holding_position = False`.

    5. **Orphan `reposition` method (lines 103-137):** Delete the entire method body, including the local-scope `HOLD_SCAN_RANGE = 4` constant and the `for i in range(HOLD_SCAN_RANGE):` scan loop. Per RESEARCH Risk 3 + Task 1 verification, this method has zero callers post Plan 01.

    **DO NOT TOUCH (PATTERNS Cross-Cutting Invariants #4):**
    - `recall()` body except line 54 — KEEP all other lines (the docstring, `is_dissipated` early return, `is_recalling = True`, `is_punted = False`, `history.clear()`)
    - `update_recall()` (lines 57-80) — KEEP entire method
    - `dissipate()` (lines 82-89) — KEEP entire method
    - `spit()` method — KEEP (foundation of Phase 32 daze shot)
    - `reform()` body except the deleted `is_holding_position = False` reset line — KEEP all other lines
    - `update()` body except the Hold branch — KEEP fused / dissipated / recalling / punted branches

    Surviving analog: `recall` / `update_recall` / `dissipate` (lines 48-89) demonstrate the post-strip method-shape — flag init at top of `__init__`, reset call sites only inside lifecycle methods, no orphan local-scope constants.
  </action>
  <verify>
    <automated>python -c "from src.entities.slime import Slime; from unittest.mock import MagicMock; s = Slime(0, 0, MagicMock()); assert not hasattr(s, 'is_holding_position'); assert not hasattr(s, 'hold_x'); assert not hasattr(s, 'hold_y'); assert hasattr(s, 'is_recalling'); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import src.entities.slime"` exits 0
    - `grep -nE '\bis_holding_position\b' src/entities/slime.py` returns 0 matches
    - `grep -nE '\bhold_x\b|\bhold_y\b' src/entities/slime.py` returns 0 matches
    - `grep -n 'HOLD_SCAN_RANGE' src/entities/slime.py` returns 0 matches
    - `grep -nE '^\s*def reposition\b' src/entities/slime.py` returns 0 matches (orphan method deleted)
    - `grep -nE '^\s*def (recall|update_recall|dissipate|spit|reform)\b' src/entities/slime.py` returns 5 matches (surviving methods intact)
    - `timeout 30 python main.py` returns rc=124 (game boots to title without crash)
  </acceptance_criteria>
  <done>All Hold state stripped; orphan `reposition` deleted; recall/dissipate/spit/reform intact; game boots room 0 cleanly. Plan 02 ready for commit.</done>
</task>

</tasks>

<verification>
After Plan 02 commit:

1. `python -c "import src.entities.slime; from src.entities.slime import Slime"` exits 0
2. `timeout 30 python main.py` returns rc=124
3. Risk Surface 3 verification: `grep -nE '^\s*def reposition\b' src/entities/slime.py` returns 0 matches
4. Cross-Cutting Invariant #4 (recall preserved): `grep -nE 'def recall\b|def update_recall\b|def dissipate\b' src/entities/slime.py` returns 3 matches
5. Cross-Cutting Invariant #2 (spit preserved): `grep -n 'def spit\b' src/entities/slime.py` returns 1 match
</verification>

<success_criteria>
- `python -c "import src.entities.slime"` exits 0
- `timeout 30 python main.py` returns rc=124
- Hold state symbols (`is_holding_position`, `hold_x`, `hold_y`, `HOLD_SCAN_RANGE`) absent from slime.py
- `reposition` method deleted (zero callers verified in Task 1)
- Surviving slime lifecycle methods (recall, update_recall, dissipate, spit, reform) preserved
- ONE commit per CONTEXT D-15
</success_criteria>

<output>
After completion, create `.planning/phases/31.5-cut-ability-code-strip/31.5-02-SUMMARY.md` with:
- Hold state symbols removed
- `reposition` deletion confirmed (zero-callers grep)
- Surviving methods verified
- Boot-smoke result
- Commit SHA

Commit message format:
```
feat(31.5): strip Slime Hold state + orphan reposition method

- Delete is_holding_position / hold_x / hold_y init
- Delete is_holding_position reset in recall (line 54)
- Delete hold-mode gravity-and-collision branch in update (lines 169-179)
- Delete is_holding_position reset in reform (line 306)
- Delete orphan reposition() method (zero callers verified post Plan 01)
- Delete HOLD_SCAN_RANGE local constant + scan loop

Surviving: recall, update_recall, dissipate, spit, reform bodies untouched
beyond the listed deletions.

Per CONTEXT D-06, D-07, D-15. RESEARCH Risk 3 + Open Question 1 (recommend
DELETE reposition) addressed via Task 1 caller-check.
```
</output>
