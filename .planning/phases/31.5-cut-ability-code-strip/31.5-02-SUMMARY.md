---
phase: 31.5-cut-ability-code-strip
plan: 02
subsystem: entity
tags: [slime, hold, fusion, code-strip, python, pyxel]

# Dependency graph
requires:
  - phase: 31.5-cut-ability-code-strip
    provides: Plan 01 deletes player.py:333-339 input call sites that drove `slime.reposition()`
provides:
  - Slime entity with Directional Hold state fully removed
  - Orphan `slime.reposition()` method deleted (RESEARCH Risk 3 + Open Question 1 resolution)
  - Clean slime.py base for Phase 32 fusion refactor
affects: [31.5-04, 31.5-05, 32-fusion-manager-protocol-refactor]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Cut-and-burn deletion (no `_deprecated` archive blocks) per CONTEXT D-01 / Specifics line 219"
    - "Surviving lifecycle methods (recall/update_recall/dissipate) demonstrate post-strip method shape"

key-files:
  created: []
  modified:
    - src/entities/slime.py

key-decisions:
  - "DELETE orphan reposition() method per RESEARCH Risk 3 + Open Question 1 (recommend DELETE)"
  - "Caller-check (Task 1) confirmed only callers were Plan-01-deleted player.py:333-339 + Plan-05-deleted test_tap_reposition.py"

patterns-established:
  - "Hold-state strip pattern: delete init flags, lifecycle resets, dispatch branch in update(), and orphan helper method as one atomic commit"

requirements-completed: []  # Plan declared requirements: [] in frontmatter

# Metrics
duration: 3min
completed: 2026-04-26
---

# Phase 31.5 Plan 02: Slime Hold State Strip Summary

**Stripped Directional Hold mode (`is_holding_position`/`hold_x`/`hold_y`/`HOLD_SCAN_RANGE`) and the orphan `slime.reposition()` method from `src/entities/slime.py`; recall/update_recall/dissipate/spit/reform method bodies preserved intact.**

## Performance

- **Duration:** ~3 min
- **Started:** 2026-04-26T06:42:55Z
- **Completed:** 2026-04-26T06:45:38Z
- **Tasks:** 2
- **Files modified:** 1

## Accomplishments

- Removed all 3 Hold-state init flags (`hold_x`, `hold_y`, `is_holding_position`) from `Slime.__init__`
- Removed Hold-state reset call sites in `recall()` (line 54) and `reform()` (line 306)
- Removed Hold-mode gravity-and-collision branch in `Slime.update()` (was lines 169-179)
- Deleted entire orphan `reposition()` method including local-scope `HOLD_SCAN_RANGE` constant and scan loop (was lines 103-137)
- Net change: -56 lines from slime.py
- All 5 surviving lifecycle methods (recall, update_recall, dissipate, spit, reform) preserved with bodies intact except for the listed deletions
- Module imports cleanly; game boots room 0 without crash

## Task Commits

This plan executes as ONE commit per CONTEXT D-15 (one commit per plan):

1. **Task 1: Pre-flight caller-check for `reposition()`** — verification gate only, no file changes (output recorded below)
2. **Task 2: Strip Hold state + reposition method from slime.py** — `58ff292` (feat)

### Task 1 caller-check output

```
$ git grep -nE '\.reposition\b|\breposition\(' -- 'src/' 'main.py' 'tests/'
src/entities/player.py:334:                slime.reposition(-1, self.x, self.y, self.level_map)
src/entities/player.py:336:                slime.reposition(1, self.x, self.y, self.level_map)
src/entities/slime.py:103:    def reposition(self, direction, player_x, player_y, level_map):
tests/test_tap_reposition.py: [16 hits — entire test file is reposition-focused]
```

**Caller-check verdict:** All 18 hits are EXPECTED deletions in sibling/downstream plans:
- `src/entities/player.py:334,336` — exactly the `was_tap("left"/"right", HOLD_TAP_THRESHOLD)` input branches at lines 333-339 that **CONTEXT D-06 explicitly enumerates for Plan 01 deletion**
- `tests/test_tap_reposition.py` — explicitly slated for **Plan 05 deletion per CONTEXT D-07**
- `src/entities/slime.py:103` — the definition site itself (deleted in Task 2)

This is a parallel-wave-1 sibling execution against the base commit (`17f312c`); the orchestrator merges Plan 01's worktree alongside Plan 02's. After merge, the player.py callers vanish via Plan 01's strip and the test file vanishes via Plan 05's deletion. Per RESEARCH Risk 3 caveat ("If the planner finds a non-obvious caller during Plan 02 task design, escalate as an open question rather than deleting blind"), no escalation needed — every caller is an enumerated deletion in a sibling/downstream plan. Safe to delete.

## Files Created/Modified

- `src/entities/slime.py` — stripped Hold state and orphan `reposition()` method (-56 LOC)

## Decisions Made

- **DELETE `reposition()`** rather than retain as dead-but-harmless code, per RESEARCH Risk 3 + Open Question 1 recommendation. Reasons: (a) Phase 31.5's purpose is to give Phase 32 a clean base; (b) `HOLD_SCAN_RANGE` is local-scope to `reposition` and would otherwise become an orphan constant; (c) `feedback_magic_numbers` argues against an orphan local-scope constant; (d) `test_tap_reposition.py` is being deleted in Plan 05 anyway.

## Deviations from Plan

None — plan executed exactly as written. The Task 1 caller-check found the exact callers CONTEXT D-06 / D-07 already enumerate for sibling deletion (Plan 01 strips player.py:333-339; Plan 05 deletes test_tap_reposition.py). No escalation needed.

## Issues Encountered

**Worktree-base parallel execution context:** The plan's Task 1 acceptance criterion ("zero callers post Plan 01") is technically not satisfied at base-commit time because Plan 01 has not yet been merged. However, the prompt's `<parallel_execution>` note explicitly addresses this: "Plan 02 declares depends_on: [31.5-01], but this is a WAVE-1 sibling parallel run. The dependency annotation is logical (Plan 02 is meaningful only after Plan 01 removes the input call sites at player.py:333-339), not a runtime block." Plan 02 only modifies `src/entities/slime.py` — no overlap with Plan 01's files. The orchestrator merges both worktrees back together, at which point all `slime.reposition` callers are gone.

## User Setup Required

None.

## Next Phase Readiness

- **Plan 03 (schema + presets) ready:** Hold tuning key removal (`HOLD_TAP_THRESHOLD` from `fusion` group per D-03) is independent of Plan 02 changes and can proceed in Wave 2.
- **Plan 04 (input + save + pickups) ready:** No coupling.
- **Plan 05 (tests + acceptance) needs:** Delete `tests/test_tap_reposition.py` (per CONTEXT D-07) — this will remove the last `slime.reposition` references project-wide. Grep checklist Gate 1 will verify zero `is_holding_position` / `HOLD_SCAN_RANGE` hits in `src/`.
- **Phase 32 (fusion refactor) ready (after Wave 3):** Slime entity now has clean fusion-state surface with no Hold-related code paths.

## Verification Output

```
$ python -c "import src.entities.slime"
(exit 0)

$ grep -nE '\bis_holding_position\b|\bhold_x\b|\bhold_y\b|HOLD_SCAN_RANGE' src/entities/slime.py
(no matches)

$ grep -nE '^\s*def reposition\b' src/entities/slime.py
(no matches)

$ grep -nE '^\s*def (recall|update_recall|dissipate|spit|reform)\b' src/entities/slime.py
43:    def recall(self, player_x, player_y):
51:    def update_recall(self, player_x, player_y):
76:    def dissipate(self):
222:    def spit(self, dx, dy, level_map):
231:    def reform(self, player_x, player_y, player_facing_right, level_map=None):

$ timeout 5 python main.py
RC=124  (timeout = boot smoke pass; game ran without crash)
```

## Self-Check: PASSED

- [x] `src/entities/slime.py` exists and was modified (commit 58ff292)
- [x] Commit 58ff292 exists in `git log --oneline`
- [x] Hold state symbols absent from slime.py
- [x] `reposition` method deleted from slime.py
- [x] All 5 surviving methods (recall/update_recall/dissipate/spit/reform) present
- [x] Module imports cleanly (`python -c "import src.entities.slime"`)
- [x] Game boots without crash (`timeout 5 python main.py` returned RC=124)

---
*Phase: 31.5-cut-ability-code-strip*
*Completed: 2026-04-26*
